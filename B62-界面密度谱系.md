# B62 · 界面密度谱系

> 同一个功能,在 Notion 里是宽松的白色大块,在 Bloomberg Terminal 里是密密麻麻的数据墙。
> 两者都是顶级产品——区别不在质量,在**密度档位**。
> 密度不是"紧不紧"的单一维度,而是**信息密度 × 视觉密度 × 交互密度**的三维空间。
> 这份文档拆的是:如何选择、设计和切换密度——让同一套系统服务新手和专家。

---

## 一、密度术语表

| 中文术语 | 英文术语 | 一句话含义 |
|---|---|---|
| 界面密度 | Interface Density | 单位面积内信息/元素的数量 |
| 信息密度 | Information Density | 可见区域内传达的信息量 |
| 视觉密度 | Visual Density | 元素间距/大小/装饰的紧凑程度 |
| 交互密度 | Interaction Density | 可交互元素的集中程度 |
| 密度档位 | Density Level | 预定义的紧凑/标准/宽松档 |
| 紧凑模式 | Compact Mode | 高密度/小元素/窄间距的视图 |
| 宽松模式 | Comfortable Mode | 低密度/大元素/宽间距的视图 |
| 默认模式 | Default / Regular Mode | 中等密度的标准视图 |
| 自适应密度 | Adaptive Density | 根据视口/内容/用户自动调整密度 |
| 密度令牌 | Density Tokens | 控制密度的 CSS 变量系统 |
| 数据墨水比 | Data-ink Ratio | Tufte 提出的信息/装饰比例概念 |
| 呼吸感 | Breathing Room | 元素之间的空白充裕度 |
| 认知负载 | Cognitive Load | 用户处理界面信息所需的心理资源 |
| 扫读效率 | Scan Efficiency | 快速浏览获取信息的速度 |
| 信息气味 | Information Scent | 用户判断"这里有我要的"的线索强度 |
| 版面率 | Layout Rate | 内容区占总面积的比例 |
| 留白率 | White Space Ratio | 空白区占总面积的比例 |
| 折叠密度 | Collapsed Density | 折叠状态的紧凑展示 |
| 展开密度 | Expanded Density | 展开后的详细展示 |
| 渐进披露 | Progressive Disclosure | 按需逐步展示更多信息 |

---

## 二、密度的三个维度

### 2.1 信息密度

**单位面积展示多少信息**:

```
低: 一屏一个大数字 + 一行说明(Dashboard 卡)
中: 一屏 10-20 条信息(列表/Feed)
高: 一屏 50-100+ 数据点(交易终端/IDE)
```

### 2.2 视觉密度

**元素占多大空间、间距多紧**:

```
低: 大 padding, 大圆角, 大字号, 多留白
中: 标准 padding, 标准字号, 适度留白
高: 小 padding, 小字号, 极少留白, 细线分隔
```

### 2.3 交互密度

**一屏内有多少可交互元素**:

```
低: 一屏只有 1-3 个交互点(落地页)
中: 一屏 5-10 个交互点(设置页)
高: 一屏 20+ 个交互点(IDE/交易终端)
```

---

## 三、密度谱系:从极简到极密

### 3.1 五级密度档

| 档位 | 适用 | 行高 | 元素高度 | Gap | Padding | 字号 |
|---|---|---|---|---|---|---|
| **L1 极宽** | 品牌/落地页/Hero | 2.0 | 56px | 32-48px | 24-40px | 18-24px |
| **L2 宽松** | 消费级 App/阅读 | 1.7 | 48px | 20-28px | 16-24px | 16px |
| **L3 标准** | SaaS/后台 | 1.5 | 40px | 12-16px | 12-16px | 14px |
| **L4 紧凑** | 工具/数据表格 | 1.4 | 32px | 8-12px | 8-12px | 13px |
| **L5 极密** | IDE/终端/金融 | 1.25 | 24-28px | 4-8px | 4-8px | 12px |

### 3.2 密度 Token 系统

```css
:root {
  /* 密度档位变量(切换一个值,全站跟着变) */
  --density: 0;  /* -2 极密 / -1 紧凑 / 0 标准 / 1 宽松 / 2 极宽 */

  /* 基于密度的自适应值 */
  --space-unit: calc(4px + var(--density) * 2px);
  --space-xs:  calc(var(--space-unit) * 1);   /* 4/6/8/10/12 */
  --space-sm:  calc(var(--space-unit) * 2);   /* 8/12/16/20/24 */
  --space-md:  calc(var(--space-unit) * 3);   /* 12/18/24/30/36 */
  --space-lg:  calc(var(--space-unit) * 4);   /* 16/24/32/40/48 */
  --space-xl:  calc(var(--space-unit) * 6);   /* 24/36/48/60/72 */

  --text-size: calc(14px + var(--density) * 1px);  /* 12/13/14/15/16 */
  --element-h: calc(36px + var(--density) * 4px);  /* 28/32/36/40/44 */
  --radius:    calc(6px + var(--density) * 2px);   /* 2/4/6/8/10 */
  --line-h:    calc(1.4 + var(--density) * 0.1);   /* 1.2/1.3/1.4/1.5/1.6 */
}

/* 用户切换档位 */
[data-density="compact"]    { --density: -1; }
[data-density="default"]    { --density: 0;  }
[data-density="comfortable"]{ --density: 1;  }
```

### 3.3 组件消费密度 Token

```css
.btn {
  height: var(--element-h);
  padding-inline: var(--space-md);
  font-size: var(--text-size);
  border-radius: var(--radius);
}

.table td {
  padding: var(--space-xs) var(--space-sm);
  font-size: var(--text-size);
  line-height: var(--line-h);
}

.card {
  padding: var(--space-lg);
  gap: var(--space-sm);
  border-radius: calc(var(--radius) * 2);
}
```

---

## 四、产品类型与推荐密度

### 4.1 产品-密度匹配表

| 产品类型 | 推荐档位 | 原因 |
|---|---|---|
| 品牌官网/落地页 | L1-L2 极宽/宽松 | 核心是传达,不是效率 |
| 博客/文档/阅读 | L2 宽松 | 长时间阅读需要呼吸 |
| 消费级 SaaS | L2-L3 | 平衡美感和功能 |
| 后台管理系统 | L3-L4 | 信息多,需要扫读效率 |
| 邮件客户端 | L3-L4 | 列表密集但需可读 |
| IDE/代码编辑器 | L5 极密 | 最大化代码可见行数 |
| 金融交易终端 | L5 极密 | 实时数据多,快速决策 |
| 移动端 App | L2-L3 | 触控目标需要 ≥ 44px |
| 仪表盘 Dashboard | L3 (卡片内 L4) | 卡片外宽松,卡片内紧凑 |

### 4.2 同一产品的多密度场景

```
Figma 示例:
- 画布: L1 极宽(设计需要留白)
- 属性面板: L4 紧凑(工具需要效率)
- 图层面板: L5 极密(树形结构需要看全)
- 弹窗设置: L3 标准(偶尔使用)
```

```
Gmail 示例:
- 默认视图: L3 标准
- 紧凑视图: L4(邮件列表行高降到 28px)
- 舒适视图: L2(阅读体验)
用户可自由切换。
```

---

## 五、密度切换的 UI 设计

### 5.1 显式切换(用户手动)

```html
<div class="density-switcher" role="radiogroup" aria-label="显示密度">
  <button role="radio" aria-checked="false" data-density="compact">
    <svg><!-- 紧凑图标:三条线靠近 --></svg>
    紧凑
  </button>
  <button role="radio" aria-checked="true" data-density="default">
    <svg><!-- 标准图标:三条线标准间距 --></svg>
    默认
  </button>
  <button role="radio" aria-checked="false" data-density="comfortable">
    <svg><!-- 宽松图标:三条线距离远 --></svg>
    宽松
  </button>
</div>
```

```css
/* 图标暗示密度 */
.density-icon-compact   line { stroke-dashoffset: -2; }
.density-icon-default   line { stroke-dashoffset: 0; }
.density-icon-comfy     line { stroke-dashoffset: 2; }
```

### 5.2 隐式自适应(系统自动)

```css
/* 窄屏自动放大间距和触控目标 */
@media (width < 768px) {
  :root { --density: 0.5; }
  .btn { min-height: 44px; }
}

/* 大屏可以更紧凑 */
@media (width > 1920px) {
  :root { --density: -0.5; }
}

/* 触控设备自动加大 */
@media (pointer: coarse) {
  :root { --density: max(var(--density), 0); }
}
```

### 5.3 混合策略(局部密度覆盖)

```css
/* 全局用标准密度,但表格/面板局部覆盖为紧凑 */
.data-table   { --density: -1; }
.sidebar-tree { --density: -2; }
.hero-section { --density: 2;  }

/* 组件级覆盖,与全局叠加 */
```

---

## 六、高密度设计的精工

### 6.1 高密度下保持可读性

```css
/* 紧凑不等于看不清 */
.compact-list {
  --density: -1;
  font-size: 13px;

  /* 关键:字距微加宽(小字更需要呼吸) */
  letter-spacing: 0.01em;

  /* 行高不能太压(最低 1.3) */
  line-height: 1.35;

  /* 颜色对比不能因紧凑而降低 */
  color: var(--text-primary);  /* 不能用 secondary! */
}
```

### 6.2 高密度下的层次区分

当间距很小时,靠间距区分层次变难。替代方案:

```css
/* 用背景色差分组(不是间距) */
.compact-group:nth-child(odd)  { background: var(--surface); }
.compact-group:nth-child(even) { background: oklch(from var(--surface) calc(l - 2%) c h); }

/* 用细线分隔(0.5px 极细) */
.compact-row + .compact-row {
  border-top: 0.5px solid oklch(0% 0 0 / 0.06);
}

/* 用缩进表示层级 */
.tree-item { padding-left: calc(var(--level) * 12px); }
```

### 6.3 高密度下的焦点引导

```css
/* 当所有元素都很密时,hover 高亮变得极重要 */
.compact-row:hover {
  background: oklch(0% 0 0 / 0.03);
}

/* 选中行更明显 */
.compact-row.selected {
  background: var(--brand-alpha-10);
  border-left: 2px solid var(--brand);
}
```

---

## 七、低密度设计的精工

### 7.1 低密度不等于"空"

```
❌ 低密度的错误做法:
- 所有东西都居中,大量空白,但空白无意义
- 字小+间距大 = 不是"高端",是"放不下内容"

✅ 低密度的正确做法:
- 空白有节奏(不是均匀铺开)
- 大字体承担内容(不是小字配大空白)
- 留白本身是设计元素(有方向、有呼吸)
```

### 7.2 极宽模式的正确姿势

```css
.hero-luxury {
  padding-block: clamp(120px, 20vh, 240px);
  max-width: 800px;
  margin-inline: auto;
}

.hero-luxury h1 {
  font-size: clamp(48px, 8vw, 96px);
  font-weight: 300;  /* 极细标题 + 极大尺寸 = 高端 */
  line-height: 1.0;
  letter-spacing: -0.03em;
  margin-bottom: 48px;
}

.hero-luxury p {
  font-size: 20px;
  line-height: 1.8;  /* 宽行高 = 呼吸感 */
  max-width: 60ch;   /* 限宽保证阅读舒适 */
  color: var(--text-secondary);
}
```

---

## 八、响应式密度

### 8.1 视口与密度的映射

```css
/* 移动端不用紧凑(手指需要大目标) */
@media (width < 640px) {
  :root {
    --density: 0;
    --element-h: 44px;  /* iOS HIG 最小触控 */
    --space-sm: 16px;
  }
}

/* 平板:标准 */
@media (640px <= width < 1024px) {
  :root { --density: 0; }
}

/* 桌面:标准或紧凑 */
@media (width >= 1024px) {
  :root { --density: var(--user-density, 0); }
}

/* 超大屏(4K):可以更宽松 */
@media (width >= 2560px) {
  :root { --density: max(var(--user-density, 0), 1); }
}
```

### 8.2 容器查询驱动密度

```css
.card-grid { container-type: inline-size; }

/* 容器宽时:宽松模式 */
@container (width > 800px) {
  .card { --density: 1; padding: 32px; }
}

/* 容器窄时:紧凑模式 */
@container (width < 400px) {
  .card { --density: -1; padding: 12px; }
}
```

---

## 九、表格/列表的密度工程

### 9.1 表格三档密度

```css
/* 标准 */
.table[data-density="default"] td {
  padding: 12px 16px;
  font-size: 14px;
  line-height: 1.4;
}

/* 紧凑 */
.table[data-density="compact"] td {
  padding: 6px 12px;
  font-size: 13px;
  line-height: 1.3;
}

/* 宽松 */
.table[data-density="comfortable"] td {
  padding: 16px 20px;
  font-size: 15px;
  line-height: 1.5;
}
```

### 9.2 列表密度

```css
/* 邮件列表:紧凑模式一屏看更多 */
.mail-list.compact .mail-item {
  padding: 8px 16px;
  min-height: 36px;
}
.mail-list.compact .mail-item .preview { display: none; }

/* 宽松模式:带预览 */
.mail-list.comfortable .mail-item {
  padding: 16px 20px;
  min-height: 72px;
}
.mail-list.comfortable .mail-item .preview {
  display: block;
  color: var(--text-tertiary);
  margin-top: 4px;
}
```

---

## 十、Prompt 模板

```
【界面密度谱系】

产品类型: {落地页 / 文档 / SaaS / 后台 / IDE / 金融终端}
默认密度档位: {L1极宽 / L2宽松 / L3标准 / L4紧凑 / L5极密}

═══ 密度 Token 系统 ═══
--density: {-2极密 / -1紧凑 / 0标准 / 1宽松 / 2极宽}
间距公式: --space-unit = 4px + density × 2px
字号: 12 / 13 / 14 / 15 / 16(随密度递增)
元素高度: 28 / 32 / 36 / 40 / 44(随密度递增)
行高: 1.2 / 1.3 / 1.4 / 1.5 / 1.6
圆角: 2 / 4 / 6 / 8 / 10

═══ 密度切换 ═══
支持用户手动切换: 紧凑 / 默认 / 宽松 三档
自适应规则:
- 移动端/触控: density ≥ 0(保证 44px 触控目标)
- 大屏桌面: 使用用户偏好或默认
- 超大屏 4K: density ≥ 1

═══ 局部密度覆盖 ═══
- 表格/面板: --density: -1(比全局更紧凑)
- Hero/品牌区: --density: 2(极宽)
- 侧栏树: --density: -2(极密)

═══ 高密度注意事项 ═══
- 字距微加宽(letter-spacing: 0.01em)
- 行高不低于 1.3
- 用背景色差/细线分组(不靠间距)
- hover 高亮必须明显
- 不降低色彩对比度

═══ 低密度注意事项 ═══
- 空白有节奏,不是均匀铺开
- 大字体承担内容(不是小字+大空白)
- 正文限宽 60ch
- 留白本身是设计元素

═══ 响应式密度 ═══
< 640px: density 0, 最小 44px
640-1024px: density 0
≥ 1024px: 用户偏好
≥ 2560px: density ≥ 1
触控设备(pointer: coarse): density ≥ 0
```

---

## 十一、踩坑指南

### 坑1:紧凑模式触控目标太小
**症状**:移动端用紧凑模式,手指点不准。
**解法**:触控设备 density ≥ 0,最小点击区 44×44px。

### 坑2:高密度降低了对比度
**症状**:紧凑时文字用了 secondary 色,更看不清。
**解法**:密度变化不能降低色彩对比。紧凑模式文字仍用 primary。

### 坑3:低密度 = 空白浪费
**症状**:内容很少,页面只有巨大空白,不是"高端"是"没内容"。
**解法**:空白要有节奏/方向,大字+大间距而非小字+大空白。

### 坑4:密度切换时 CLS
**症状**:用户切紧凑模式,页面跳动重排。
**解法**:transition 加在 height/padding/gap 上,切换平滑过渡。

### 坑5:表格紧凑行高太压
**症状**:每行 24px 但文字挤在一起。
**解法**:行高最低 1.25,padding 最少 4px。

### 坑6:所有地方用同一密度
**症状**:Hero 区也很紧凑,没有品牌呼吸感。
**解法**:局部覆盖 `--density`,品牌区极宽,数据区紧凑。

### 坑7:密度偏好没持久化
**症状**:用户选了紧凑,刷新页面又变回默认。
**解法**:`localStorage.setItem('density', value)`。

---

## 十二、谁在用(密度设计典范)

| 产品 | 密度策略 | 值得学的 |
|---|---|---|
| **Gmail** | 三档手动切换 | 紧凑/默认/舒适,经典三档 UI |
| **VS Code** | L5 极密 + 可调 | 最大化代码行数,但字号可调 |
| **Notion** | L2 宽松 | 消费级产品的呼吸感 |
| **Linear** | L3 中偏紧 | 信息多但精致不拥挤 |
| **Bloomberg** | L5 极密 | 交易终端,每像素都是信息 |
| **Figma** | 分区密度 | 画布宽松/面板紧凑/树极密 |
| **Apple 设置** | L2 | 触控友好的大行高列表 |
| **Slack** | L3 带列表 L4 | 消息区标准,频道列表紧凑 |
| **Superhuman** | L4 + 快捷键 | 紧凑但靠键盘而非鼠标操作 |

---

## 检验清单

- [ ] 定义了密度 Token(--density 变量驱动全局)
- [ ] 按钮/输入框/卡片都响应密度 Token
- [ ] 提供至少两档用户可切换的密度(如紧凑/标准)
- [ ] 移动端/触控设备密度不低于 0(44px 最小触控)
- [ ] 高密度下字距微加宽(0.01em),行高 ≥ 1.3
- [ ] 高密度下用色差/细线分组而非间距
- [ ] 高密度下 hover 高亮明显
- [ ] 低密度下空白有节奏(不是无意义铺开)
- [ ] 低密度下正文限宽 60ch
- [ ] 局部可覆盖密度(表格更紧凑/Hero 更宽)
- [ ] 密度切换有 transition 过渡(不跳变)
- [ ] 密度偏好持久化到 localStorage
- [ ] 不因密度变化降低 WCAG 对比度
