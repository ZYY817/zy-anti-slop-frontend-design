# T1 · AI Frontend Capability Stress Test

> **定位**：第二阶段实战压测基准。它不负责继续扩充知识，而是检验现有知识是否真的改变了前端输出，并把失败证据反推回知识库。
>
> **评分原则**：没有可运行页面、截图、交互记录或代码证据，不允许填写分数，只能标记为 `Not Run`。

---

## 一、三阶段对照

每个 Benchmark 至少跑三次，保持产品需求、技术栈、模型和输出格式一致：

| 版本 | 输入 | 目的 |
|---|---|---|
| V0 · Native | 只给产品简报，不加载本知识库 | 测量 AI 默认能力和默认模板倾向 |
| V1 · Knowledge | 使用 M2 → Z2 → M1/Z1，加载相关知识模块 | 测量知识检索和组合的增益 |
| V2 · Iterated | V1 结果再经过 Z3，实际修复 P0/P1，再生成/迭代 | 测量审计和闭环的增益 |

### 实验控制

- 固定：产品简报、页面范围、技术栈、视口、数据集、输出文件结构。
- 变量：是否加载知识库、加载哪些模块、是否运行 Z3。
- 记录：Prompt、检索模块、模型、时间、截图、代码版本、分数、失败证据。
- 不允许用 V1 的截图、代码或修复意见污染 V0。
- 不允许只比较文案描述，必须比较运行结果。

---

## 二、十个 Benchmark

每个 Benchmark 都有不同能力目标；不要把所有项目都做成“高级、极简、科技感”。

### B01 · Premium Editorial Brand

**场景**：一家做可持续建筑材料的高端品牌官网首页。

**目标**：Editorial Composition、强品牌感、摄影/材质叙事、克制转化。

**必须测试**：非对称构图、巨型标题、长标题、案例图片序列、章节节奏、首尾呼应、移动端视觉重排。

**重点模块**：B54、B77-B80、B22-B24、B32、B61、G1、C6、Z3。

**失败信号**：通用 SaaS Hero、机械三列卡片、装饰先于品牌命题、每屏同样强度。

### B02 · AI SaaS Workspace

**场景**：团队使用 AI 生成研究报告，包含项目、来源、草稿和协作。

**目标**：AI-native 工作区、流式生成、引用、编辑、版本和可信感。

**必须测试**：生成中、停止、重试、引用展开、草稿保存、权限、长输出和多轮编辑。

**重点模块**：G6、B50、C2、C6、D4、F4、Z2、Z3。

**失败信号**：只有聊天气泡，没有任务模型、来源、控制权和失败恢复。

### B03 · Dense Developer Tool

**场景**：部署平台的项目详情与日志控制台。

**目标**：高信息密度、快速扫描、键盘操作、状态明确、工程可信。

**必须测试**：长日志、过滤、命令面板、表格、折叠、实时状态、键盘路径、移动端最低可用性。

**重点模块**：G2、B26、B29、B53、B62、B73、B76、F3-F4。

**失败信号**：把后台做成营销页、过度留白、重要状态靠颜色、没有焦点和快捷键。

### B04 · Data Operations Dashboard

**场景**：运营团队监控多地区订单、收入、库存和异常。

**目标**：数据叙事、比较效率、异常优先、可解释图表。

**必须测试**：0/1/大量数据、极端数字、时间范围、筛选、表格/图表联动、导出、错误数据。

**重点模块**：B26、B29、B56-B57、B62、G2、C1、C6、Z3。

**失败信号**：图表为了装饰、没有单位和时间范围、KPI 互相矛盾、颜色数量过多。

### B05 · Developer Tool Onboarding

**场景**：开发者第一次接入一个 API 监控平台。

**目标**：首次使用引导、技术内容、渐进披露、从空到成功。

**必须测试**：空状态、安装代码、密钥权限、复制反馈、步骤进度、错误恢复、文档跳转。

**重点模块**：G2、C1-C5、B14、B52、B73、C6、F4。

**失败信号**：只展示空面板、引导不可跳过、错误不可恢复、代码片段不可复制。

### B06 · E-commerce Product Story

**场景**：一款高端户外照明产品的商品详情和结算路径。

**目标**：产品叙事、信任、比较、变体选择、转化而不廉价。

**必须测试**：图片 art direction、规格、库存、配送、评价、优惠、购物车、结算错误、移动端购买。

**重点模块**：G4、B32、B54、B78、B51、B69、C5-C6、F3。

**失败信号**：图片墙和 CTA 没有产品逻辑、虚假稀缺、价格和配送信息隐藏。

### B07 · Content Platform Reader

**场景**：面向研究人员的长文阅读、收藏、注释和相关推荐。

**目标**：长内容舒适度、编辑版式、阅读路径、内容发现。

**必须测试**：长标题、长段落、引用、代码块、图片缺失、目录、进度、评论、返回位置、多语言。

**重点模块**：G5、C1、C5-C6、B1、B33、B60、B31-B32、Z3。

**失败信号**：正文过宽、标题像营销文案、长内容破版、图片和代码没有语义。

### B08 · Mobile Field App

**场景**：维修人员在户外用手机处理工单和拍照上传。

**目标**：拇指可达、弱网、单手操作、快速反馈和安全恢复。

**必须测试**：安全区、键盘、拍照、上传进度、离线、重试、底部操作、长表单、横竖屏。

**重点模块**：G3、B30、B45、B73-B76、C2-C4、C6、F3。

**失败信号**：桌面端缩小、操作藏在顶部、触控目标过小、离线丢数据。

### B09 · Creative Experimental Site

**场景**：一个声音艺术节的互动活动官网。

**目标**：独特视觉语言、动态构图、声音/动效叙事、实验性但可用。

**必须测试**：视觉母题、非标准布局、滚动编排、动效降级、无声音体验、导航可发现性、低性能设备。

**重点模块**：B77-B80、B23-B24、B27、B35-B37、V5-V14、B44、B48、F3。

**失败信号**：效果堆叠、动效没有叙事、无法回到内容、只在大屏成立。

### B10 · AI Agent Operations

**场景**：Agent 代表团队执行跨系统采购任务。

**目标**：计划、工具调用、权限、批准、进度、回滚和责任边界。

**必须测试**：任务计划、执行步骤、工具输入输出、等待、人工批准、部分成功、失败恢复、审计记录、撤销。

**重点模块**：G6、B50、C2、C3、C4、D4、C6、Z3。

**失败信号**：把 Agent 做成聊天机器人、隐藏自动行动、没有停止/批准/回滚、无法解释结果来源。

---

## 三、每个 Benchmark 的执行卡

复制下面模板，每个 Benchmark 单独保存一份运行记录：

```markdown
# {Benchmark ID} · {Version}

## Run metadata
- Date:
- Model / Agent:
- Framework:
- Viewports:
- Dataset:
- Prompt hash or filename:
- Retrieved modules:

## Evidence
- Running URL or artifact:
- Desktop screenshot:
- Mobile screenshot:
- Interaction recording:
- Console / accessibility / performance result:

## Scores
| Dimension | 0-5 | Evidence |
|---|---:|---|
| Visual | | |
| UX | | |
| Content | | |
| Motion | | |
| Responsive | | |
| Accessibility | | |
| Engineering | | |
| Originality | | |
| Product Completeness | | |

## AI Genericness Test
- [ ] 圆角卡片是否成为默认容器
- [ ] 是否滥用 Bento / 渐变 / 玻璃 / 紫色
- [ ] 是否有模板 Hero / CTA / 三列卡片
- [ ] 是否有无意义装饰
- [ ] 是否有产品签名

## Ceiling level
- [ ] L1：优秀商业产品
- [ ] L2：高级设计团队
- [ ] L3：顶级视觉设计
- [ ] L4：实验性 / 创新型

## Failure analysis
Output Failure → Design Failure → Missing Capability → Missing Knowledge → Upgrade
```

---

## 四、统一评分维度

每项 0-5 分，必须附证据：

| 维度 | 检查 |
|---|---|
| Visual | Composition、Typography、Color、Spacing、Hierarchy、Density、Material、Detail、Rhythm |
| UX | IA、Interaction、Affordance、Feedback、Flow、Cognitive Load |
| Content | 内容层级、真实数据、文案、密度、状态内容 |
| Motion | Timing、Easing、Choreography、Purpose、Restraint |
| Responsive | Desktop、Tablet、Mobile、Recomposition |
| Accessibility | Semantic HTML、Keyboard、Focus、Contrast、Screen Reader |
| Engineering | Components、Reuse、Maintainability、Performance、Browser correctness |
| Originality | AI 味、模板感、品牌差异、视觉签名 |
| Product Completeness | Loading、Empty、Error、Success、Disabled、Permission、Edge Cases |

建议权重：Visual 18%、UX 18%、Content 12%、Motion 8%、Responsive 10%、Accessibility 10%、Engineering 10%、Originality 8%、Product Completeness 6%。

**硬门槛**：任何 P0 未修复，或 Accessibility / UX / Product Completeness 低于 3 分，总体不得标记为通过。

---

## 五、Prompt Effectiveness Test

对高价值 Prompt 进行 A/B 对照，不以“写得更详细”作为有效性证据：

```text
Before = 不加载该 Prompt 的 V1
After  = 只加入该 Prompt 的 V1
Delta  = After - Before
```

记录：

| Prompt | 预期改善 | 实际改善 | 副作用 | 决策 |
|---|---|---|---|---|
| | | | | 保留 / 压缩 / 降级 / 删除 |

若视觉词加入后只增加装饰，没有改善产品目标或原创性，该 Prompt 不算有效。

---

## 六、Prompt Interaction Test

测试单个 Prompt 和组合 Prompt：

```text
A
A + B
A + B + C
```

记录：

```text
Prompt A:
Prompt B:
Conflict:
Priority:
Resolution:
Observed synergy:
Observed side effect:
```

必须重点测试的冲突：

- Minimalism + High Information Density
- Editorial + Dense Data
- Experimental Motion + Accessibility
- Glass / Material + Performance
- Strong Brand Expression + Usability
- Personalization + Consistency

---

## 七、Visual Emergence Test

某些规则单独效果普通，组合后可能形成新的 Design Language。测试：

```text
Editorial
+ Asymmetry
+ Typography Contrast
+ Visual Rhythm
+ Motion
```

若组合产生稳定、可解释、可迁移的视觉语言，记录为 Recipe，而不是新增一堆关键词：

```yaml
recipe_name:
design_intent:
works_when:
ingredients:
dominant_rules:
supporting_rules:
anti_patterns:
responsive_recomposition:
validation:
```

---

## 八、Cross-Domain Transfer Test

选择一个抽象原则，迁移到至少五类产品：

```text
Visual Rhythm
→ Landing Page
→ Dashboard
→ AI Agent
→ E-commerce
→ Mobile App
```

如果每次都只得到同一套视觉表面，说明 Prompt 只是词库，不具备抽象能力。升级方向应是补充“适用条件和上下文变体”，不是继续增加同义词。

---

## 九、Knowledge ROI

不要按文件长度判断价值。每个知识模块按下式评估：

```text
Knowledge ROI
= 使用频率 × 实际影响 × 可复用性 × 生成质量提升
```

每轮测试更新一次：

```markdown
## Top 20 High-ROI Knowledge
| Module | Used | Impact | Reuse | Quality delta | Decision |
|---|---:|---:|---:|---:|---|

## Bottom 20 Low-ROI Knowledge
| Module | Why low value | Action |
|---|---|---|
```

低 ROI 处理顺序：合并 → 压缩 → 降级为参考 → 删除。没有实测证据时不删除。

---

## 十、Capability Report

```markdown
# AI Frontend Capability Report

## 1. Overall Score
## 2. Visual Score
## 3. UX Score
## 4. Content Score
## 5. Motion Score
## 6. Responsive Score
## 7. Accessibility Score
## 8. Engineering Score
## 9. Originality Score
## 10. Product Completeness Score

## V0 → V1 → V2 Delta
| Dimension | V0 | V1 | V2 | Knowledge gain | Iteration gain |
|---|---:|---:|---:|---:|---:|

## Top 20 Weaknesses
| Problem | Evidence | Root Cause | Missing Knowledge | Recommended Upgrade | Expected Impact |
|---|---|---|---|---|---|

## Top 20 High-Impact Upgrades
| Priority | Upgrade | Existing file to update | Expected impact | Validation |
|---|---|---|---|---|

## Visual Capability Ceiling
- L1:
- L2:
- L3:
- L4:

## Next phases
### Phase 1
### Phase 2
### Phase 3
### Phase 4
```

---

## 十一、阶段门

### Phase 1 · 建立基线

完成 10 个 Benchmark 的 V0，并保留运行证据。

### Phase 2 · 知识增益

完成 V1，记录 Z2 实际调用的模块和 Prompt Delta。

### Phase 3 · 迭代增益

对 V1 运行 Z3，修复 P0/P1，完成 V2。

### Phase 4 · 反向升级

只根据失败证据升级已有文档；如果问题属于既有主题，禁止新建同义文件。

**阶段完成条件**：至少一个 Benchmark 的 V0 → V1 → V2 在可验证维度上有明确提升；否则不允许宣称知识库升级有效。

---

## 十二、当前执行状态

本文件先完成 Benchmark 规格和执行协议。由于当前项目没有可运行的前端代码，本轮不填虚构分数。下一次执行必须从 B01、B03、B10 三个差异最大的 Benchmark 开始，优先验证：

1. 视觉叙事是否真的超过模板化营销页；
2. 高密度产品是否仍具备可用性和工程完整度；
3. Agent UX 是否具备计划、权限、批准、失败恢复和可追溯性。
