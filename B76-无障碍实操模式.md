# B76 · 无障碍实操模式

> C4 讲 WCAG **标准与原则**。本文件是**可直接复制的 ARIA 模式代码**——
> 针对 6 种最常出问题的组件，给出完整的 HTML 结构 + ARIA 属性 + 键盘交互。
> 基于 W3C ARIA Authoring Practices Guide (APG) 2026 版本。

---

## 使用方式

找到对应组件 → 复制 HTML 结构 → 接入 JS 键盘逻辑 → 对照检验清单。

---

## 一、Dialog / Modal

### HTML 结构
```html
<dialog id="confirm-dialog" aria-labelledby="dialog-title" aria-describedby="dialog-desc">
  <h2 id="dialog-title">确认删除</h2>
  <p id="dialog-desc">删除后不可恢复，确定继续？</p>
  <div class="dialog-actions">
    <button data-action="cancel">取消</button>
    <button data-action="confirm" autofocus>确认删除</button>
  </div>
</dialog>
```

### ARIA 要点
- 用原生 `<dialog>` 元素（自带 `role="dialog"`、focus trap、backdrop）
- `aria-labelledby` → 标题元素的 id
- `aria-describedby` → 描述内容的 id
- `autofocus` 放在第一个可操作按钮上

### 键盘交互
| 按键 | 行为 |
|---|---|
| `Tab` / `Shift+Tab` | 在 dialog 内循环（焦点陷阱） |
| `Escape` | 关闭 dialog（等同"取消"） |
| `Enter` | 激活当前聚焦按钮 |

### JS 要点
```js
// 打开
dialog.showModal();  // 原生焦点陷阱 + backdrop

// 关闭后把焦点还给触发元素
dialog.addEventListener('close', () => {
  triggerButton.focus();
});
```

### 常见错误
- ❌ 用 `div` + `role="dialog"` 但没实现焦点陷阱 → 用 `<dialog>`
- ❌ 关闭后焦点丢失到 `<body>` → 归还到触发按钮
- ❌ 背景可滚动 → `<dialog>` 原生阻止，或 `body { overflow: hidden }`
- ❌ 没有 `aria-labelledby` → 屏幕阅读器无法报告标题

---

## 二、Tabs

### HTML 结构
```html
<div class="tabs">
  <div role="tablist" aria-label="产品信息">
    <button role="tab" id="tab-1" aria-selected="true" aria-controls="panel-1" tabindex="0">
      概述
    </button>
    <button role="tab" id="tab-2" aria-selected="false" aria-controls="panel-2" tabindex="-1">
      规格
    </button>
    <button role="tab" id="tab-3" aria-selected="false" aria-controls="panel-3" tabindex="-1">
      评价
    </button>
  </div>

  <div role="tabpanel" id="panel-1" aria-labelledby="tab-1" tabindex="0">
    概述内容...
  </div>
  <div role="tabpanel" id="panel-2" aria-labelledby="tab-2" tabindex="0" hidden>
    规格内容...
  </div>
  <div role="tabpanel" id="panel-3" aria-labelledby="tab-3" tabindex="0" hidden>
    评价内容...
  </div>
</div>
```

### ARIA 要点
- `tablist` 包裹所有 tab
- 当前选中 tab：`aria-selected="true"` + `tabindex="0"`
- 未选中 tab：`aria-selected="false"` + `tabindex="-1"`
- 每个 `tab` 有 `aria-controls` 指向对应 panel
- 每个 `tabpanel` 有 `aria-labelledby` 指向对应 tab

### 键盘交互
| 按键 | 行为 |
|---|---|
| `←` / `→` | 在 tab 间移动焦点（自动激活或按 Enter 激活） |
| `Home` | 移到第一个 tab |
| `End` | 移到最后一个 tab |
| `Tab` | 从 tablist 跳入当前 panel |

### 两种激活模式
- **自动激活**（推荐）：箭头移到即切换内容
- **手动激活**：箭头只移焦点，按 Enter/Space 才切换

---

## 三、Disclosure（展开/折叠）

### HTML 结构（原生 details）
```html
<details>
  <summary>高级设置</summary>
  <div class="details-content">
    ...折叠内容
  </div>
</details>
```
原生 `<details>/<summary>` 天生带 `aria-expanded`、键盘支持、屏幕阅读器播报。**能用原生就用原生。**

### 自定义结构
```html
<button aria-expanded="false" aria-controls="advanced-panel" class="disclosure-trigger">
  高级设置
  <svg aria-hidden="true" class="chevron">...</svg>
</button>
<div id="advanced-panel" class="disclosure-content" hidden>
  ...折叠内容
</div>
```

### 键盘交互
| 按键 | 行为 |
|---|---|
| `Enter` / `Space` | 切换展开/折叠 |

### JS 要点
```js
trigger.addEventListener('click', () => {
  const expanded = trigger.getAttribute('aria-expanded') === 'true';
  trigger.setAttribute('aria-expanded', !expanded);
  panel.hidden = expanded;
});
```

### 手风琴（多个 disclosure）
- 可以多个独立（都能同时开）
- 或互斥（开一个关其他）——不是必须互斥，APG 不要求
- 每个折叠项独立标注 `aria-expanded`

---

## 四、Combobox（自动补全输入框）

### HTML 结构
```html
<div class="combobox-wrap">
  <label for="city-input">城市</label>
  <input
    id="city-input"
    role="combobox"
    aria-expanded="false"
    aria-autocomplete="list"
    aria-controls="city-listbox"
    aria-activedescendant=""
    autocomplete="off"
  >
  <ul id="city-listbox" role="listbox" hidden>
    <li id="opt-1" role="option">北京</li>
    <li id="opt-2" role="option">上海</li>
    <li id="opt-3" role="option">广州</li>
  </ul>
</div>
```

### ARIA 要点
- input：`role="combobox"` + `aria-expanded` + `aria-controls` 指向 listbox
- `aria-activedescendant`：当前高亮选项的 id（视觉聚焦不移真焦点）
- 展开时 `aria-expanded="true"`
- 每个选项 `role="option"`，选中时加 `aria-selected="true"`

### 键盘交互
| 按键 | 行为 |
|---|---|
| `↓` | 打开列表 / 移到下一项 |
| `↑` | 移到上一项 |
| `Enter` | 选中当前项并关闭 |
| `Escape` | 关闭列表，清除高亮 |
| 输入字符 | 过滤列表 |
| `Home` / `End` | 光标移到输入框首/尾 |

### 常见错误
- ❌ 用 `<div>` 做输入 → 必须是 `<input>`
- ❌ 箭头键移真焦点到列表项 → 用 `aria-activedescendant` 虚拟焦点
- ❌ 没有 `aria-expanded` → 屏幕阅读器不知道列表是否打开
- ❌ 选中后没更新 input value

---

## 五、Listbox（选择列表）

### HTML 结构（单选）
```html
<label id="color-label">选择颜色</label>
<ul role="listbox" aria-labelledby="color-label" tabindex="0">
  <li role="option" id="red" aria-selected="true">红色</li>
  <li role="option" id="blue" aria-selected="false">蓝色</li>
  <li role="option" id="green" aria-selected="false">绿色</li>
</ul>
```

### 多选
```html
<ul role="listbox" aria-labelledby="..." aria-multiselectable="true" tabindex="0">
  <li role="option" aria-selected="false">选项 A</li>
  <li role="option" aria-selected="true">选项 B</li>
  ...
</ul>
```

### 键盘交互
| 按键 | 行为 |
|---|---|
| `↓` / `↑` | 移焦点 |
| `Space` | 选中/取消（多选时） |
| `Home` / `End` | 首项/末项 |
| `Shift + ↓/↑` | 多选扩展 |
| `Ctrl + A` | 全选（多选时） |
| 字符键 | 跳到以该字符开头的项（type-ahead） |

---

## 六、Menu / Menu Button

### HTML 结构
```html
<button
  aria-haspopup="true"
  aria-expanded="false"
  aria-controls="action-menu"
>
  操作 ▾
</button>
<ul id="action-menu" role="menu" hidden>
  <li role="menuitem" tabindex="-1">编辑</li>
  <li role="menuitem" tabindex="-1">复制</li>
  <li role="separator"></li>
  <li role="menuitem" tabindex="-1">删除</li>
</ul>
```

### ARIA 要点
- 触发按钮：`aria-haspopup="true"` + `aria-expanded`
- 菜单容器：`role="menu"`
- 菜单项：`role="menuitem"`
- 分隔线：`role="separator"`
- 禁用项：`aria-disabled="true"`

### 键盘交互
| 按键 | 行为 |
|---|---|
| `↓` | 在 menuitem 间移动焦点 |
| `↑` | 在 menuitem 间反向移动 |
| `Enter` / `Space` | 激活当前项 |
| `Escape` | 关闭菜单，焦点回按钮 |
| `Home` / `End` | 首项/末项 |
| 字符键 | 跳到匹配项 |

### 焦点管理
- 菜单打开时：焦点移入第一个 `menuitem`
- 菜单项用 `tabindex="-1"`，由 JS 在箭头键时设 `tabindex="0"` + `focus()`（roving tabindex）
- 或用 `aria-activedescendant` 在菜单容器上管理虚拟焦点

### 关闭后焦点
```js
menu.addEventListener('close', () => {
  menuButton.focus();  // 永远归还焦点
});
```

---

## 七、通用规则（适用所有组件）

### Focus 管理三原则
1. **焦点可见**：`:focus-visible` 样式明显（2px ring），永远不 `outline: none`
2. **焦点可预测**：打开面板→焦点进去；关闭面板→焦点回来
3. **焦点不丢失**：DOM 删除后焦点归入合理的下一个元素

### 通用 ARIA 注意
- **能用原生 HTML 就用原生**（`<button>` / `<dialog>` / `<details>` / `<select>`）
- ARIA 不提供行为，只提供语义——键盘交互必须自己写
- `aria-label` / `aria-labelledby` 二选一，不要空 label
- 动态内容变化用 `aria-live`（polite 普通 / assertive 紧急）
- 加载状态用 `aria-busy="true"`

### 测试方法
1. **键盘测试**：Tab 遍历、Enter/Space 操作、Escape 关闭、箭头键导航
2. **屏幕阅读器测试**：VoiceOver(Mac) / NVDA(Win) / TalkBack(Android)
3. **辅助功能树检查**：Chrome DevTools → Accessibility 面板
4. **自动化**：axe-core / Lighthouse accessibility audit

---

## 八、Prompt 模板

```
【无障碍实操】
- Dialog：用原生 <dialog>，aria-labelledby 指向标题，
  Escape 关闭，关闭后焦点归还触发按钮
- Tabs：tablist + tab + tabpanel 完整结构，
  aria-selected 指示选中，箭头键切换，Tab 跳入 panel
- Disclosure：优先 <details>/<summary>，
  自定义时 aria-expanded + aria-controls + hidden 切换
- Combobox：input role="combobox" + aria-activedescendant 虚拟焦点，
  箭头导航 + Escape 关闭 + Enter 选中
- Menu：aria-haspopup + role="menu" + role="menuitem"，
  箭头导航 + Escape 关闭并归焦
- 通用：:focus-visible 永远可见（2px ring），
  焦点可预测（进/出都有归属），aria-live 播报动态内容
```

---

## 九、检验清单

- [ ] 所有自定义组件有正确的 ARIA role + states
- [ ] Dialog 有焦点陷阱，关闭后焦点归还
- [ ] Tab 用箭头键导航，aria-selected 同步更新
- [ ] Disclosure 的 aria-expanded 随状态切换
- [ ] Combobox 用 aria-activedescendant（不移真焦点）
- [ ] Menu 关闭后焦点回触发按钮
- [ ] 所有可交互元素有 :focus-visible 样式
- [ ] 焦点从不丢失到 <body>
- [ ] 动态内容有 aria-live 播报
- [ ] 加载状态有 aria-busy
- [ ] 颜色不是唯一信息载体（加图标/文字）
- [ ] 用键盘走完全流程无卡点
- [ ] 屏幕阅读器能报出所有操作的名称和状态
