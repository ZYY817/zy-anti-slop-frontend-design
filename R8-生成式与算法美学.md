# R8 · 生成式与算法美学（提示词库 J 组）

> 定位：用规则生成美，而不是逐个手摆——算法美学既是省力的手段，也是一种独特的视觉气质。
> 覆盖层：L0 意（观念/情绪）+ L1 骨（结构/系统）+ L3 肉（形态/质感），并含 L4/L5 的色彩与动画扩展档。
> 条目卡格式：每条为 **名称（英文）· L 层级 · 档位（基础/进阶/电影级）· 成本**，其下含「视觉收益 / 强度档位（如有）/ 同族拓展 / 提示词片段」。
> 来源：融合自外部「视觉提示词库 J 组」（v1.x，2026-08-16）；技术术语与参数值忠实保留原样。库内交叉引用已改写为目标库的 R 编号（详见文末「参考资料」）。

---

## 使用说明

前面的组回答"用什么效果"，J 组回答"**如何用规则批量生成形态与图案**"。它的价值有两面：

1. **省力**：一条规则生成 1000 个协调的元素，比手摆 1000 个快且更一致
2. **气质**：generative art 本身是一种被高度认可的视觉语言（fxhash/Art Blocks 生态、包豪斯、瑞士网格）

核心心法：**受控的随机**。纯随机 = 噪声（丑），纯规则 = 呆板（无聊）。好的生成美学在两者之间——有规则约束的变化。

```
J1 受控随机与种子          J5 图案与镶嵌
J2 网格系统与破格          J6 数据可视化美学
J3 参数化与变体            J7 曲线与路径生成
J4 自然算法与涌现          J8 色彩的算法生成
```

---

## J1 受控随机与种子（Controlled Randomness）

**随机的质量决定生成美学的成败。**

- **可复现随机（Seeded Random）** · L1 · 基础 · 成本:低
  - 视觉收益：同一种子永远生成同一结果——可分享、可调试、可"抽卡直到满意"再固定
  - 同族拓展：mulberry32、xorshift、hash-based PRNG、`Math.random` 的不可复现问题、URL 存种子
  - 提示词片段：`用可复现的 seeded PRNG（mulberry32）；种子存进 URL，支持"换一个"按钮重新抽取并可固定`

- **随机分布选择（Distribution Choice）** · L1 · 进阶 · 成本:低
  - 视觉收益：均匀分布 vs 高斯分布 vs 幂律分布产生完全不同的观感。**大多数自然现象不是均匀分布**
  - 同族拓展：uniform、gaussian（正态，聚集中心）、power-law（少数极大值，如星星亮度）、Poisson disk（均匀但不规则，最自然的散布）
  - 提示词片段：`元素位置用 Poisson disk 采样（均匀但不网格化）；尺寸用幂律分布（少数很大多数很小）`

- **蓝噪声分布（Blue Noise Distribution）** · L1 · 进阶 · 成本:低
  - 视觉收益：点既不聚堆也不成网格，**最符合"自然随机"直觉**的散布（撒盐、星空、植被）
  - 同族拓展：Poisson disk sampling、void-and-cluster、见 `R4·B3`/`R6·D6`（同一概念用于采样）
  - 提示词片段：`装饰点/粒子初始位置用蓝噪声/Poisson disk，避免既视感的聚堆或规则网格`

- **抖动与偏移（Jitter）** · L1 · 基础 · 成本:低
  - 视觉收益：在规则网格基础上加小随机偏移，得到"手工感"的半规则
  - 强度档位：微（±5%，精致）/ 中（±15%，活泼）/ 大（±40%，凌乱）
  - 同族拓展：见 `R7·E5` 有意的不对齐、grid jitter、rotation jitter
  - 提示词片段：`网格元素加位置抖动 ±8% + 旋转抖动 ±3°，打破机械感但保持整体秩序`

- **随机的约束（Constrained Random）** · L1 · 进阶 · 成本:低
  - 视觉收益：随机值限定在"和谐范围"内——从预设调色板挑而非随机 RGB、从字阶挑而非随机字号
  - 关键心法：**永远从"好的选项集"里随机，而不是从全空间随机**
  - 提示词片段：`所有随机都从预设集合抽取（调色板/字阶/间距档/角度档），禁止从连续全空间随机取值`

---

## J2 网格系统与破格（Grid Systems）

**网格是秩序的来源，破格是个性的来源。** 两者都需要。

- **模块化网格（Modular Grid）** · L1 · 基础 · 成本:低
  - 视觉收益：行列构成的模块，元素吸附其上，天然整齐
  - 同族拓展：column grid、baseline grid、CSS Grid、瑞士国际主义网格、见 `R9·H9` subgrid
  - 提示词片段：`建立 12 列 + 基线网格；所有块吸附网格线；用 CSS Grid 实现`

- **不对称网格（Asymmetric Grid）** · L1 · 进阶 · 成本:低
  - 视觉收益：非均分列宽（如 5:3:2 或黄金比分割），比均分更有张力
  - 同族拓展：golden ratio grid、Fibonacci columns、复合网格（多套网格叠加）
  - 提示词片段：`用黄金比分割主次区域（62:38）；列宽不均分制造张力`

- **模块化排布系统（Bento / Modular Layout）** · L1 · 进阶 · 成本:低
  - 视觉收益：大小不一的卡片拼成整体（便当盒布局），信息密度与节奏兼得
  - 同族拓展：bento grid、masonry（`grid-template-rows: masonry`）、`grid-auto-flow: dense`、不同 span 的模块
  - 提示词片段：`用 bento 布局：不同尺寸模块拼合（1×1/2×1/2×2），grid-auto-flow:dense 自动填补空隙`

- **破格与溢出（Breaking / Bleeding）** · L1 · 电影级 · 成本:低
  - 视觉收益：见 `R1·G2` 打破网格。一个元素越界/出血/旋转，成为焦点
  - 关键约束：先严格网格，再破一处
  - 提示词片段：`严格网格中让一个元素跨越网格线/出血到边缘/轻微旋转，作为视觉签名`

- **系统化间距（Spacing System）** · L1 · 基础 · 成本:低
  - 视觉收益：间距全部来自一个数列（4/8/12/16/24/32/48/64），天然协调
  - 同族拓展：8pt grid、几何间距（4/8/16/32 翻倍）、`space` token、见 `R1·G7`
  - 提示词片段：`间距只用 8pt 系统的档位（4/8/16/24/32/48/64/96），禁止任意数值`

- **响应式网格重排（Responsive Reflow）** · L1 · 基础 · 成本:低
  - 视觉收益：不同尺寸下网格重新编排（不是等比缩小），层级保持
  - 同族拓展：见 `R9·H9` container query、`auto-fit`/`minmax()`、断点处重排
  - 提示词片段：`用 grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)) 实现无断点自适应`

---

## J3 参数化与变体（Parametric & Variants）

**定义参数空间，而非定义单个结果。**

- **参数化设计（Parametric Design）** · L0 · 进阶 · 成本:低
  - 视觉收益：把视觉抽象成一组参数（数量/密度/角度/色相/尺寸），调参数即调整整体
  - 同族拓展：design system as parameters、preset 配方、参数暴露为 UI 控件
  - 提示词片段：`把视觉的关键变量抽成参数（8-12 个），暴露为可调 uniform/CSS 变量；预设几套参数组合作为"风格档"`

- **变体系统（Variant System）** · L1 · 基础 · 成本:低
  - 视觉收益：一套设计生成协调的多个变体（配色变体、密度变体、尺寸变体）
  - 同族拓展：design tokens 派生、`color-mix` 派生色阶、程序化生成状态色
  - 提示词片段：`从 1 个主色用 color-mix + 相对颜色语法派生完整色阶与状态色，而非手挑每个色`

- **约束系统（Constraint System）** · L1 · 进阶 · 成本:低
  - 视觉收益：定义"什么组合是允许的"，在约束内随机/生成，永远不出错
  - 同族拓展：rule-based generation、Wave Function Collapse（瓦片约束生成）、grammar-based
  - 提示词片段：`定义组合约束（哪些颜色可相邻、哪些尺寸可组合），在约束内生成，保证任何生成结果都协调`

- **程序化变化（Procedural Variation）** · L3 · 进阶 · 成本:低
  - 视觉收益：同类元素每个都略有不同（花瓣、砖块、树叶），避免"复制粘贴感"
  - 同族拓展：per-instance random、noise-driven variation、见 `R7·E5` 材质瑕疵
  - 提示词片段：`重复元素每个实例用 index 做种子产生微变化（尺寸±10%/色相±5°/旋转±4°），杜绝完全相同`

---

## J4 自然算法与涌现（Natural Algorithms & Emergence）

**用简单规则生成复杂的自然形态。** 这是 generative art 的核心魅力。

- **流场（Flow Field）** · L3 · 电影级 · 成本:中
  - 视觉收益：粒子沿噪声向量场流动，形成丝缕状的有机图案（generative art 经典）
  - 同族拓展：见 `R4·B5` 流场、Perlin flow field、curl noise、粒子轨迹叠加
  - 提示词片段：`用 Perlin 流场引导上千粒子留下轨迹，形成丝缕状有机构图；轨迹半透明叠加`

- **L 系统（L-System）** · L3 · 电影级 · 成本:中
  - 视觉收益：递归重写规则生成植物、树、分支结构
  - 同族拓展：Lindenmayer system、fractal tree、递归分支、turtle graphics
  - 提示词片段：`用 L-system 生成分支结构（如植物/闪电/河流），随机化分支角度与长度`

- **元胞自动机（Cellular Automata）** · L3 · 进阶 · 成本:中
  - 视觉收益：Game of Life、规则 30 等，简单规则涌现复杂图案
  - 同族拓展：Conway's Life、rule 30/110、reaction-diffusion（见 `R4·B3`）、需 ping-pong RT（见 `R4·B7`）
  - 提示词片段：`用元胞自动机生成演化图案作为背景纹理；在 ping-pong RT 上迭代`

- **群体行为（Flocking / Boids）** · L3 · 电影级 · 成本:中
  - 视觉收益：分离/对齐/聚合三规则涌现鸟群/鱼群的集体运动
  - 同族拓展：Reynolds boids、separation/alignment/cohesion、agent-based、见 `R4·B7` 粒子受力
  - 提示词片段：`用 boids 三规则做群体运动（分离/对齐/聚合），加轻微对鼠标的响应`

- **差分生长（Differential Growth）** · L3 · 电影级 · 成本:高
  - 视觉收益：线条/曲面不断细分生长，产生珊瑚、脑回、褶皱的有机形态
  - 同族拓展：differential line growth、space colonization、reaction-diffusion
  - 提示词片段：`用差分生长让曲线不断细分与排斥，生成脑回/珊瑚状有机形态`

- **物理模拟（Physics Simulation）** · L3 · 进阶 · 成本:中
  - 视觉收益：弹簧、引力、碰撞、软体产生真实动态，UI 里的"物理感"
  - 同族拓展：spring-mass、Verlet integration、matter.js/rapier、软体、绳索、布料
  - 提示词片段：`导航/标签用弹簧物理连接，拖动时有真实的惯性与回弹（Verlet 积分）`

- **奇异吸引子（Strange Attractors）** · L3 · 电影级 · 成本:中
  - 视觉收益：Lorenz、Aizawa、Clifford 吸引子产生优雅的数学曲线云
  - 同族拓展：Lorenz/Aizawa/de Jong/Clifford attractor、见 `R4·B7` GPGPU
  - 提示词片段：`用 Clifford/de Jong 吸引子生成点云构图，配合 GPGPU 渲染百万点`

---

## J5 图案与镶嵌（Pattern & Tessellation）

- **几何镶嵌（Tessellation）** · L1 · 进阶 · 成本:低
  - 视觉收益：正多边形/彭罗斯/Voronoi 铺满平面，装饰性图案
  - 同族拓展：regular tiling、Penrose tiling（非周期，高级）、Truchet tiles（随机拼接）、Voronoi/Delaunay
  - 提示词片段：`背景用 Truchet tiles（每格随机选朝向的圆弧/斜线），生成连续但不重复的图案`

- **Truchet 瓷砖（Truchet Tiles）** · L3 · 进阶 · 成本:低
  - 视觉收益：极简的瓷砖单元随机旋转，涌现出复杂的连续图案（迷宫、管道、编织）
  - 同族拓展：quarter-circle Truchet、diagonal Truchet、multi-scale Truchet
  - 提示词片段：`用四分之一圆 Truchet 瓷砖随机拼接生成流动线条图案；可 shader 内程序化`

- **伊斯兰几何与曼陀罗（Islamic Geometry / Mandala）** · L1 · 电影级 · 成本:中
  - 视觉收益：基于旋转对称的复杂几何花纹，庄严华丽
  - 同族拓展：n-fold symmetry、star patterns、girih tiles、kaleidoscope（见 `R3·A8`）
  - 提示词片段：`用 n 重旋转对称生成星形几何花纹（8/12 重），线条等宽，配单色描边`

- **重复与节奏图案（Repeating Patterns）** · L1 · 基础 · 成本:低
  - 视觉收益：条纹/圆点/格纹/波浪，作为底纹或强调区
  - 同族拓展：见 `R9·H5` 渐变做图案、CSS pattern、SVG pattern、`background-repeat`
  - 提示词片段：`用 CSS 渐变生成图案底纹（点阵/斜纹/网格），透明度 ≤6% 作为质感层`

- **分形图案（Fractal Patterns）** · L1 · 电影级 · 成本:中
  - 视觉收益：见 `R3·A4` 分形。自相似的无限细节
  - 同族拓展：Mandelbrot/Julia、KIFS、分形噪声
  - 提示词片段：`装饰区用分形图案（自相似 3 层），作为一个视觉峰值`

---

## J6 数据可视化美学（Data Visualization Aesthetics）

**数据可视化不只是"准确"，也可以是主要的视觉奇观。**

- **数据驱动形态（Data-Driven Form）** · L0 · 进阶 · 成本:低
  - 视觉收益：真实数据决定视觉参数，产生"有意义的美"
  - 同族拓展：data-to-visual mapping、Tufte 原则、数据肌理（data as texture）
  - 提示词片段：`用真实数据驱动视觉参数（不是随机装饰）；数据本身成为图案，兼具信息价值与美感`

- **优雅图表（Elegant Charts）** · L1 · 进阶 · 成本:低
  - 视觉收益：去掉图表垃圾（网格线/边框/图例冗余），突出数据本身
  - 同族拓展：data-ink ratio、直接标注（代替图例）、动画入场、见 `B29` 数据可视化完整体系
  - 提示词片段：`图表去除多余网格线与边框；数据点直接标注代替图例；入场用错峰动画（按数值排序）`

- **流线与关系图（Flow & Network）** · L3 · 电影级 · 成本:中
  - 视觉收益：桑基图、弦图、力导向图，展示关系与流动
  - 同族拓展：sankey、chord diagram、force-directed graph、edge bundling（边捆绑，减少视觉混乱）
  - 提示词片段：`关系用力导向图 + edge bundling 减少连线混乱；节点大小映射重要度，颜色映射类别`

- **地图与地理（Maps & Geo）** · L3 · 进阶 · 成本:中
  - 视觉收益：数据叠加在地图上的空间美感
  - 同族拓展：choropleth、hexbin、投影选择、deck.gl、等值线
  - 提示词片段：`地理数据用 hexbin 聚合 + 高度挤出（3D）；配色用感知均匀色阶（viridis 类）`

- **感知均匀色阶（Perceptual Color Scales）** · L4 · 基础 · 成本:低
  - 视觉收益：数据到颜色的映射必须感知均匀，否则误导（彩虹色阶是错误的经典案例）
  - 同族拓展：viridis/magma/cividis、单色阶、发散色阶（diverging）、避免 rainbow/jet
  - 提示词片段：`数据色阶用 viridis 类感知均匀色阶或单色明度阶；禁止彩虹/jet 色阶（会误导且不可访问）`

- **动画的数据揭示（Animated Reveal）** · L5 · 进阶 · 成本:低
  - 视觉收益：数据不是一次性出现，而是随时间/滚动逐步揭示，引导理解
  - 同族拓展：见 `R11·F2` 滚动叙事、staggered reveal、数字滚动（见 `R10·I6`）
  - 提示词片段：`图表随滚动逐步揭示（先坐标轴后数据后标注）；数值用滚动动画（见 R10·I6）`

---

## J7 曲线与路径生成（Curve & Path Generation）

- **贝塞尔与样条（Bezier & Splines）** · L1 · 进阶 · 成本:低
  - 视觉收益：平滑曲线是优雅的基础；曲率连续的曲线才专业（见 `R1·G5`）
  - 同族拓展：cubic bezier、Catmull-Rom（过点样条）、B-spline、曲率连续（`R1·G2`）
  - 提示词片段：`连接点用 Catmull-Rom 样条（自动过所有点且平滑）；保证曲率连续无折点`

- **有机 Blob（Organic Blobs）** · L1 · 进阶 · 成本:低
  - 视觉收益：不规则圆润形状，柔和有机，做背景装饰/头像框/按钮
  - 同族拓展：噪声扰动的圆、`border-radius` 8 值语法、SVG path blob、metaball（见 `R3·A3`）
  - 提示词片段：`背景装饰用有机 blob（圆的半径按角度用噪声扰动），缓慢形变（morph）`

- **平滑噪声线（Smooth Noise Lines）** · L3 · 进阶 · 成本:低
  - 视觉收益：用噪声驱动的起伏线条，做山脉轮廓、声波、动态分隔
  - 同族拓展：1D Perlin、ridged noise（见 `R4·B3`）、多层叠加
  - 提示词片段：`分隔用噪声驱动的起伏线（1D Perlin），缓慢流动，比直线有机`

- **运动路径（Motion Paths）** · L5 · 进阶 · 成本:低
  - 视觉收益：元素沿自定义路径运动，比直线位移优雅
  - 同族拓展：CSS `offset-path`、`offset-distance` 动画、SVG path 跟随、`offset-rotate`
  - 提示词片段：`装饰元素用 offset-path 沿曲线运动 + offset-rotate 自动朝向；纯 CSS`

- **等值线与轮廓（Contours / Isolines）** · L3 · 电影级 · 成本:中
  - 视觉收益：地形图式的等高线，数据感与自然感兼具
  - 同族拓展：marching squares、noise 的等值线、topographic style
  - 提示词片段：`背景用噪声场的等值线（marching squares 或 shader 内 fract(noise*n) 取带），产生地形图质感`

---

## J8 色彩的算法生成（Algorithmic Color）

**用规则生成协调配色，而非手挑每个颜色。**

- **和谐色生成（Color Harmony Generation）** · L4 · 基础 · 成本:低
  - 视觉收益：从一个主色按色轮关系自动生成协调配色
  - 同族拓展：complementary/analogous/triadic/split-complementary、在 OKLCH 里旋转 hue、`color-mix`
  - 提示词片段：`从主色在 OKLCH 里按 ±30°（邻近）或 180°（互补）生成配色；保持 L 与 C 一致以确保和谐`

- **程序化色阶（Procedural Color Ramps）** · L4 · 基础 · 成本:低
  - 视觉收益：自动生成明度阶（用于 UI 的 50–900 色阶），且感知均匀
  - 同族拓展：OKLCH 明度插值、`color-mix` 派生、Radix/Tailwind 式色阶、相对颜色语法
  - 提示词片段：`用 OKLCH 固定 hue/chroma、变化 lightness 生成 10 级色阶；用相对颜色语法从主色派生`

- **色彩场（Color Fields）** · L3 · 进阶 · 成本:低
  - 视觉收益：位置驱动的色彩变化（渐变的高级版），用噪声/梯度生成连续色域
  - 同族拓展：见 `R9·H5` mesh gradient、noise-driven hue、position-based palette
  - 提示词片段：`用 2D 噪声驱动色相在画面内缓慢变化（限定在配色的 hue 范围内），产生活的色彩场`

- **约束调色板（Constrained Palette）** · L4 · 基础 · 成本:低
  - 视觉收益：限定 3–5 色，所有元素只能用这几个色，天然统一
  - 同族拓展：见 `R7·E4` 限定色板、`R1·G7` 令牌、色彩配比 60-30-10
  - 提示词片段：`全站限定 5 色（主/副/强调/2 中性）；配比 60-30-10；任何元素不得引入色板外的颜色`

- **对比度感知生成（Contrast-Aware Generation）** · L4 · 基础 · 成本:低
  - 视觉收益：自动确保文字/背景组合达到对比度要求，生成的配色都可用
  - 同族拓展：APCA（新对比算法，优于 WCAG）、`light-dark()`、自动挑前景色（黑或白）
  - 提示词片段：`任意背景色自动选对比达标的前景（APCA ≥60）；用 OKLCH 的 L 通道判断该配黑还是白字`

- **色彩情绪映射（Color Mood Mapping）** · L0 · 进阶 · 成本:低
  - 视觉收益：见 `R5·C7` 色彩心理、`R1·G8` 情绪先行。从情绪反推配色
  - 提示词片段：`先定情绪，再用算法从情绪关键词映射到 hue 范围与饱和度区间，然后在区间内生成具体色`

---

## J 组 · 组合速查

| 想要的效果 | 组合 |
|---|---|
| generative art 背景 | R8·J1 蓝噪声 + R8·J4 流场 + R8·J7 平滑噪声线 + R8·J8 色彩场 |
| 便当盒布局 | R8·J2 bento + R8·J2 系统间距 + R8·J3 变体系统 |
| 有机装饰 | R8·J7 有机 blob + R8·J4 差分生长 + R8·J8 和谐色 |
| 数据奇观 | R8·J6 数据驱动形态 + R8·J6 感知色阶 + R8·J6 动画揭示 |
| 几何图案 | R8·J5 Truchet/镶嵌 + R8·J8 约束调色 + R1·G5 几何构成 |
| 群体动态 | R8·J4 boids + R4·B7 GPGPU + R11·F1 鼠标响应 |
| 自动配色系统 | R8·J8 和谐生成 + R8·J8 程序化色阶 + R8·J8 对比感知 |
| 可分享的随机作品 | R8·J1 seeded + URL 种子 + "换一个"按钮 |

**常见坑**：
- 随机结果很丑很乱 → R8·J1 从全空间随机了，应从"好选项集"随机
- 点撒得不自然（聚堆或成网格） → R8·J1 用蓝噪声/Poisson disk
- 生成的元素完全一样 → R8·J3 缺 per-instance 变化
- 每次刷新都不同无法固定 → R8·J1 缺 seeded PRNG + URL 种子
- 数据色阶误导/不可读 → R8·J6 用了彩虹色阶，改 viridis
- 生成配色不协调 → R8·J8 应在 OKLCH 里保持 L/C 一致只转 hue
- 网格太死板 → R8·J2 缺破格点（R8·J2 破格 + R8·J1 抖动）
- 曲线有折点 → R8·J7 用了折线连接，改 Catmull-Rom 且曲率连续

> **J 组的心法**：不要问"这个位置放什么颜色/元素"，要问"什么规则能生成一整片协调的东西"。当你发现自己在手摆第 10 个相似元素时，就该转向 R8（J 组）了。

---

## 与库内衔接

| 本文小节 | 目标库相关文档 | 关系（地基·扩展·验证） |
|---|---|---|
| J2 网格系统与破格（模块化/不对称/破格/响应式重排） | B3 布局系统（网格系统与破格） | 地基——本文的网格与破格规则以 B3 的布局系统为底座落地 |
| J5 图案与镶嵌、J7 曲线与路径生成 | B39 形状与线条语言 | 扩展——把 B39 的形状/线条语言用算法批量生成 |
| J6 数据可视化美学（全部小节） | B29 数据可视化完整体系 | 扩展/验证——本文提供数据可视化的算法美学手段，B29 提供体系与验收 |
| J8 色彩的算法生成（和谐/色阶/约束/对比感知） | B2 色彩科学（算法配色） | 地基——OKLCH/APCA/相对颜色语法等算法配色以 B2 的色彩科学为依据 |
| J2 系统化间距、J3 参数化与变体 | B72 模块化比例与尺度工程 | 地基——间距数列、参数空间与模块尺度衔接 B72 的比例与尺度工程 |
| J4 自然算法与涌现、J5 图案与镶嵌、J7 曲线生成 | V8 背景与装饰效果 | 扩展——生成式图案/流场/等值线作为背景与装饰效果的实现路径 |

> 关于反 AI 味与视觉禁忌：可参考 `B82`（反 AI 味清单）与 `B44`（视觉禁忌）。二者是**参考清单，不是硬性铁律，请按项目自行取舍**。风格谱系可参考 `B16`、`E1`；全维度清单参考 `B15`。作品集与个人展示类页面尤其要做出受控随机与破格的"手工感/签名感"，避免模板化。

## 参考资料

本文融合自外部「视觉提示词库 J 组：生成式与算法美学」（v1.x，日期 2026-08-16）。迁移时保持原有的 L 层级、档位、成本、强度值域、同族拓展与提示词片段不变，仅将库内交叉引用改写为目标库的 R 编号，并移除项目专属内容。

技术术语与参数值（如 seeded random / mulberry32 / xorshift、Poisson disk / 蓝噪声、幂律分布、Voronoi / Delaunay、Penrose tiling、Truchet tiles、L-system、reaction-diffusion、tessellation、Wave Function Collapse、boids、Lorenz / Aizawa / Clifford / de Jong 吸引子、marching squares、OKLCH / APCA、viridis / magma / cividis 等）均来自该库的原始调研，未新增未经验证的数据，也未编造论文或网址来源。

_核心：受控的随机——有规则约束的变化，在"纯随机的丑"与"纯规则的呆"之间。_
