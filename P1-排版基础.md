# P1 · 排版基础

> 从零开始。所有排版的最基本概念,一个不落。
> 看完这份,你能读懂任何设计稿的排版部分,也能把排版要求说清楚。

> **蓝图接入**：排版不是页面完成后的装饰，而是界面结构的基础约束。生成页面前，先填写 [D5-界面结构蓝图与视觉规格系统.md](D5-界面结构蓝图与视觉规格系统.md) 中的 Typography Contract，再决定组件、背景和动效。

## 零、排版先决定界面结构

在选择 Card、Hero、Grid 或背景图之前，先确定：

```text
内容层级 → 阅读长度 → 字号阶梯 → 行宽 → 行高 → 对齐轴 → 组件宽度 → 媒体安全区
```

排版决策至少要回答：

- 页面唯一的主标题是什么？
- 哪些文字负责理解，哪些文字负责行动，哪些文字只是元数据？
- 标题在最短和最长版本下如何换行？
- 中文、英文、数字和代码是否需要不同字体？
- 正文、表格、按钮和导航是否处在同一套节奏中？
- 去掉颜色和背景后，内容层级是否仍然清楚？

如果这些问题没有答案，不要先用视觉效果掩盖结构问题。

---

## 一、字体的基本概念

### 字体家族（Font Family）
一套设计风格统一的字形集合。
```
Inter、Arial、思源黑体、微软雅黑 —— 这些都是"字体家族"
```

### 字体分类(四大类)
| 类型 | 特征 | 例子 | 感觉 |
|---|---|---|---|
| **无衬线体 Sans-serif** | 笔画末端无装饰 | Inter, Arial, 黑体 | 现代、干净 |
| **衬线体 Serif** | 笔画末端有小脚 | Times, Georgia, 宋体 | 传统、正式 |
| **等宽体 Monospace** | 每个字宽度相同 | Courier, JetBrains Mono | 代码、技术 |
| **手写体 Script** | 模仿手写 | Pacifico | 个性、装饰 |

**网页首选**:无衬线体。屏幕上最清晰。

### 字重（Font Weight）
字的粗细程度,用数字表示:
```
100  Thin      极细
200  Extra Light 特细
300  Light     细
400  Regular   常规 ← 正文默认
500  Medium    中等
600  Semibold  半粗 ← 标题常用
700  Bold      粗   ← 强调常用
800  Extra Bold 特粗
900  Black     极粗
```

**实践**:一个网页通常只用 3 个字重,比如 400 / 600 / 700。

### 字号（Font Size）
字的大小,单位通常是 px 或 rem。
```
12px  很小,辅助信息
14px  小,次要文字
16px  标准,正文 ← 网页正文最低标准
18px  舒适,长文正文
20px  小标题
24px  中标题
32px  大标题
48px  超大标题
64px+ 巨型标题
```

**铁律**:正文不要小于 16px。小于这个数,手机上会看不清。

---

## 二、文字的空间属性

### 行高（Line Height）
每一行文字占的垂直高度。用倍数表示。

```
1.0   紧贴,只适合超大标题
1.2   标题用
1.5   正文标准 ← 最常用
1.6   长文舒适
1.8   非常宽松
```

**图解**:
```
字号 16px + 行高 1.5 = 每行占 24px 高度

┌─────────────────┐  ← 上方留白 4px
│ 这是一行文字      │  ← 文字本身 16px
└─────────────────┘  ← 下方留白 4px
    总共 24px
```

**规律**:字越大,行高倍数越小;字越小,行高倍数越大。

### 字间距（Letter Spacing）
字与字之间的距离。
```
负值(-0.02em)  收紧 ← 大标题用,更紧凑好看
0             默认 ← 正文用
正值(0.05em)   撑开 ← 全大写字母、小标签用
```

### 段间距（Paragraph Spacing）
段落之间的距离,通常等于一行的高度(1em 或 24px)。

### 字符宽度 / 行宽（Line Length / Measure）
一行放多少字最舒服:
```
英文:45-75 个字符
中文:25-40 个字
```

**CSS 写法**:`max-width: 65ch`(65 个字符宽)

**为什么重要**:行太长,眼睛读到行尾找不到下一行开头,容易跳行。

---

## 三、标题层级（Heading Hierarchy）

网页的标题分 6 级,像书的目录:

```html
<h1>页面主标题</h1>        ← 每页只有一个
  <h2>章节标题</h2>
    <h3>小节标题</h3>
      <h4>子标题</h4>
        <h5>更小标题</h5>
          <h6>最小标题</h6>
```

### 规则
- **每页只有一个 `<h1>`** —— 就是这个页面的主题
- **不能跳级** —— h1 后面是 h2,不能直接 h3
- **用于结构,不是为了字大** —— 字大用 CSS 控制,标签用于表达层级

### 典型字号搭配
```
h1: 36-48px / 700 字重 / 行高 1.2
h2: 28-32px / 600 字重 / 行高 1.25
h3: 22-24px / 600 字重 / 行高 1.3
h4: 18-20px / 600 字重 / 行高 1.4
正文: 16px / 400 字重 / 行高 1.5
小字: 14px / 400 字重 / 行高 1.5
```

---

## 四、对齐方式（Text Alignment）

| 方式 | 效果 | 用在哪 |
|---|---|---|
| **左对齐 left** | 左边整齐,右边不齐 | 正文首选(最易读) |
| **居中 center** | 两边不齐,中间对称 | 标题、卡片、短文字 |
| **右对齐 right** | 右边整齐 | 数字列、阿拉伯语 |
| **两端对齐 justify** | 左右都齐 | 报纸/杂志(网页慎用,会出现难看的空隙) |

**铁律**:长段落用左对齐。居中的长段落非常难读。

---

## 五、文字的基本样式

### 粗体与斜体
```html
<strong>重要内容</strong>   ← 粗体,有语义(重要)
<b>粗字</b>               ← 只是视觉粗,无语义
<em>强调内容</em>          ← 斜体,有语义(强调)
<i>斜字</i>               ← 只是视觉斜,无语义
```
**推荐**:用 `<strong>` 和 `<em>`,有语义,屏幕阅读器能识别。

**中文注意**:中文没有真正的斜体,斜体中文很难看。中文强调用**加粗**或改变颜色。

### 下划线与删除线
```html
<u>下划线</u>       ← 慎用!会被误认为链接
<s>删除线</s>       ← 表示已废弃
<del>已删除</del>   ← 有语义的删除
```

### 上标下标
```html
X<sup>2</sup>   ← 上标 X²
H<sub>2</sub>O  ← 下标 H₂O
```

### 文字变形
```css
text-transform: uppercase;   /* 全大写 */
text-transform: lowercase;   /* 全小写 */
text-transform: capitalize;  /* 首字母大写 */
```
**注意**:全大写正文很难读,只用于短标签。

---

## 六、列表（Lists）

### 无序列表(圆点)
```html
<ul>
  <li>第一项</li>
  <li>第二项</li>
</ul>
```

### 有序列表(数字)
```html
<ol>
  <li>第一步</li>
  <li>第二步</li>
</ol>
```

### 描述列表(术语+解释)
```html
<dl>
  <dt>术语</dt>
  <dd>解释内容</dd>
</dl>
```

### 排版要点
- 项目符号与文字间距 8px
- 每项之间间距 4-8px
- 多级列表缩进 24px
- 列表项内的文字行高与正文一致

---

## 七、引用（Quotes）

### 块引用
```html
<blockquote>
  这是一段引用的话。
  <cite>—— 作者名</cite>
</blockquote>
```

### 视觉设计
```
常见做法:
- 左侧加 3-4px 竖线(用主色或灰色)
- 文字左边距 16-24px
- 字体可以稍大或用斜体
- 背景可以稍微变色
```

### 行内引用
```html
他说<q>这样很好</q>。
```

---

## 八、链接（Links）

### 基本状态
```css
a          { color: blue; }              /* 默认 */
a:visited  { color: purple; }            /* 访问过 */
a:hover    { text-decoration: underline; } /* 悬浮 */
a:active   { color: red; }               /* 按下 */
a:focus-visible { outline: 2px solid; }  /* 键盘聚焦 */
```

### 设计要点
- **要能看出是链接** —— 颜色不同 + 下划线(至少满足一个)
- **不要只用颜色区分** —— 色盲用户看不出
- **正文内的链接建议加下划线**
- **导航里的链接可以不加下划线**(位置已说明是导航)
- **外链最好有图标提示** ↗

---

## 九、单位基础

| 单位 | 含义 | 什么时候用 |
|---|---|---|
| **px** | 像素,固定大小 | 边框、小间距 |
| **rem** | 根字号的倍数(通常 1rem = 16px) | **字号首选**(用户能缩放) |
| **em** | 当前字号的倍数 | 与字号关联的间距 |
| **%** | 百分比 | 宽度自适应 |
| **vw / vh** | 视口宽/高的 1% | 全屏元素 |
| **ch** | 一个字符"0"的宽度 | 限制行宽 |

**推荐**:字号用 `rem`,间距用 `px` 或 `rem`,行宽用 `ch`。

---

## 十、中文排版特别注意

### 与英文的区别
| | 英文 | 中文 |
|---|---|---|
| 行高 | 1.5 够 | 1.6-1.8 更舒服 |
| 字重 | 400 正文清晰 | 400 可能显细,可用 450-500 |
| 字距 | 不用调 | 可微调 0.02em 更透气 |
| 断行 | 按单词 | 按字符,不会有"孤字"问题 |

### 中文常用字体
```
macOS/iOS:  PingFang SC(苹方)
Windows:    Microsoft YaHei(微软雅黑)
开源:       Noto Sans SC(思源黑体)、HarmonyOS Sans
```

### 中英混排字体栈写法
```css
font-family:
  "Inter",                    /* 先英文 */
  "PingFang SC",              /* 再中文 macOS */
  "Microsoft YaHei",          /* 中文 Windows */
  sans-serif;                 /* 最后兜底 */
```

**为什么先英文**:让英文用英文字体渲染(更好看),中文自动落到中文字体。

### 中英之间加空格
```
❌ 使用API接口
✅ 使用 API 接口
```
更易读,是中文排版规范。

---

## 十一、最小可用排版系统(照抄就能用)

```css
/* 字体 */
--font-sans: "Inter", "PingFang SC", "Microsoft YaHei", sans-serif;

/* 字号(1.25 比例阶梯) */
--text-xs:   12px;
--text-sm:   14px;
--text-base: 16px;   /* 正文 */
--text-lg:   18px;
--text-xl:   20px;
--text-2xl:  24px;
--text-3xl:  30px;
--text-4xl:  36px;
--text-5xl:  48px;

/* 字重 */
--weight-normal:   400;
--weight-medium:   500;
--weight-semibold: 600;
--weight-bold:     700;

/* 行高 */
--leading-tight:  1.2;   /* 标题 */
--leading-normal: 1.5;   /* 正文 */
--leading-loose:  1.7;   /* 长文 */

/* 应用 */
h1 { font-size: 36px; font-weight: 700; line-height: 1.2; }
h2 { font-size: 30px; font-weight: 600; line-height: 1.25; }
h3 { font-size: 24px; font-weight: 600; line-height: 1.3; }
p  { font-size: 16px; font-weight: 400; line-height: 1.6; max-width: 65ch; }
small { font-size: 14px; }
```

---

## 十二、排版新手最常犯的 10 个错

1. ❌ 正文小于 16px(手机看不清)
2. ❌ 行高太小(<1.4,文字挤在一起)
3. ❌ 行宽太长(超过 80 字符,眼睛跳行)
4. ❌ 长段落居中对齐(难读)
5. ❌ 一个页面用了 5 种字体
6. ❌ 字号随手写(15px / 17px / 19px,不成体系)
7. ❌ 灰色文字太浅(对比度不够看不清)
8. ❌ 标题标签跳级(h1 直接跳 h3)
9. ❌ 中文用斜体(很丑)
10. ❌ 中英之间不加空格

---

## 十三、给 AI 的基础排版 prompt

```
【排版要求】
- 字体:Inter + PingFang SC 中英混排,中英之间自动加空格
- 字号阶梯(1.25 比例):12 / 14 / 16 / 20 / 24 / 30 / 36 / 48
- 正文 16px,行高 1.6,行宽限制 65ch
- 标题:h1 36px/700/1.2,h2 30px/600/1.25,h3 24px/600/1.3
- 字重只用 400 / 600 / 700 三档
- 长段落左对齐,标题可居中
- 大标题加负字距 -0.02em
- 链接:颜色区分 + hover 下划线
- 正文颜色 #1E293B,次要 #64748B(对比度达标)
- 字号用 rem 单位(支持用户缩放)
```

---

## 下一步

- 排版进阶(比例系统/垂直节奏/可变字体)→ 见 **B1 字体排版**
- 排版高端(光学尺寸/灰度值/文字河流)→ 见 **B6 高级视觉进阶**
