# B42 · 尺寸、对齐与密度档位

> 尺寸阶梯、对齐精度、密度模式——这三个是"系统感"的骨架。
> 大多数界面"看起来不专业",根源就在这三层没成系统。

---

## 一、尺寸系统(Sizing System)

### 组件尺寸阶梯

| 档位 | 高度 | 字号 | 内边距(横×纵) | 用途 |
|---|---|---|---|---|
| **xs** | 24px | 12px | 8×4 | 密集表格内的按钮 |
| **sm** | 32px | 14px | 12×6 | 工具栏、紧凑表单 |
| **md** | 40px | 14-16px | 16×10 | **标准(默认)** |
| **lg** | 48px | 16px | 24×12 | 主 CTA、移动端 |
| **xl** | 56px | 18px | 32×16 | Hero CTA |

**规律**:横向 padding 约为纵向的 2-2.5 倍(否则按钮太"方")。

### 图标尺寸阶梯
```
12px  内联小图标(标签内)
14px  小按钮内
16px  表单、列表、标准 UI ★最常用
20px  按钮内、卡片
24px  导航、工具栏 ★基准
32px  功能入口
40-48px 空态、大特性图标
64px+ 插画式图标
```
**规则**:图标尺寸要与相邻文字的 cap-height 视觉匹配。

### 头像尺寸阶梯
```
20px  内联提及(@user)
24px  评论列表
32px  列表项、导航
40px  卡片、消息列表 ★最常用
48px  个人资料卡
64px  个人主页
96px+ 大头像展示
```

### 容器尺寸
```
xs:  384px   小卡片、Toast
sm:  448px   登录框、Modal 小
md:  512px   Modal 标准
lg:  640px   Modal 大、窄内容
xl:  768px   文章内容
2xl: 896px
3xl: 1024px  内容区
4xl: 1152px
5xl: 1280px  ★标准网站
6xl: 1440px  宽网站
7xl: 1536px  超宽
prose: 65ch  ★纯文本阅读
```

### 触控目标尺寸
```
最小(WCAG AA):     24×24px
推荐(WCAG AAA):    44×44px ★
Apple HIG:          44×44pt
Material Design:    48×48dp
```
**技巧**:图标可视 20px,但点击区撑到 44px。
```css
.icon-btn {
  width: 44px; height: 44px;
  display: grid; place-items: center;
}
.icon-btn svg { width: 20px; height: 20px; }
```

---

## 二、对齐系统(Alignment System)★ 精工核心

### 对齐的六种类型

**1. 几何对齐(Geometric)**
按边界框对齐。CSS 默认行为。

**2. 光学对齐(Optical)★**
按视觉重心对齐,而非边界框。
```
圆形与方形并排:圆形要放大 2-5% 才"看起来一样大"
播放三角形:要向右偏 1-2px 才"看起来居中"
引号:要伸出边界(悬挂标点)才"看起来对齐"
```

**3. 基线对齐(Baseline)**
不同字号的文字底线对齐。
```css
.row { display: flex; align-items: baseline; }
```
**用途**:大数字 + 小单位("42" + "GB")

**4. Cap-height 对齐**
大写字母顶部对齐。图标与文字对齐时更精确。

**5. x-height 对齐**
小写字母主体高度对齐。

**6. 中心对齐(Center)**
视觉中心对齐(不是几何中心)。

### 图标与文字对齐 ★最常见问题
```css
/* ❌ 默认对齐经常"感觉偏" */
.item { display: flex; align-items: center; }

/* ✅ 更精确:图标微调 */
.item {
  display: flex;
  align-items: center;
  gap: 8px;
}
.item svg {
  flex-shrink: 0;
  /* 有时需要 margin-top: -1px 视觉微调 */
}
```

### 基线网格(Baseline Grid)
所有文字的基线落在统一的 4px 或 8px 网格上。
```
行高必须是基准的整数倍:
16px 字 × 1.5 = 24px 行高 ✅ (4 的倍数)
16px 字 × 1.4 = 22.4px    ❌ (不是整数倍)
```
**规律**:选行高时要保证 `font-size × line-height` 是 4 的倍数。

### 数字对齐
```css
/* 表格数字必须右对齐 + 等宽 */
td.number {
  text-align: right;
  font-variant-numeric: tabular-nums;
}
```

### 对齐的检验方法
- 眯眼看是否有"歪"的感觉
- 拉辅助线检查是否严格对齐
- 用 DevTools 检查像素值是否整数
- 检查圆形元素是否做了光学补偿

---

## 三、密度档位(Density Modes)★

同一个界面提供多种密度,适应不同用户/场景。

### 三档密度系统

| 档位 | 名称 | 特征 | 适合 |
|---|---|---|---|
| **Compact** | 紧凑 | 行高 32px,padding 8-12px,字号 13-14px | 专业用户、数据密集、大屏 |
| **Comfortable** | 舒适 | 行高 40px,padding 16px,字号 14-16px | **默认** |
| **Spacious** | 宽松 | 行高 48px,padding 24px,字号 16-18px | 触屏、无障碍、新用户 |

### 实现方式
```css
:root {
  --density-scale: 1;   /* Comfortable */
}
[data-density="compact"]  { --density-scale: 0.75; }
[data-density="spacious"] { --density-scale: 1.25; }

.row {
  height: calc(40px * var(--density-scale));
  padding: calc(16px * var(--density-scale));
}
```

### 密度影响的元素
- 行高 / 表格行
- 组件 padding
- 组件之间的 gap
- 字号(可选,通常不变)
- 图标尺寸(可选)

**注意**:触控目标不能小于 44px,即使 compact 模式。

### 密度的产品策略
- Gmail:紧凑/默认/舒适 三档
- Notion:紧凑模式(小字)
- Linear:默认紧凑(专业用户)
- 消费级 App:通常只有一档(舒适)

---

## 四、尺寸的响应式

### 组件尺寸在移动端放大
```css
.button { height: 40px; }               /* 桌面 md */
@media (max-width: 768px) {
  .button { height: 48px; }             /* 移动 lg(触控友好) */
}
```

### 流体尺寸
```css
/* 按钮高度流体 */
height: clamp(40px, 5vw, 56px);

/* 头像流体 */
width: clamp(32px, 6vw, 48px);
```

### 图标不缩放
图标通常保持固定尺寸(不随视口缩放),否则会模糊或过大。

---

## 五、尺寸系统的建立步骤

```
1. 选基准单位(4px)
2. 定义间距阶梯(4/8/12/16/24/32/48/64/96/128)
3. 定义字号阶梯(12/14/16/20/24/30/36/48/64)
4. 定义组件尺寸阶梯(xs/sm/md/lg/xl 高度与 padding)
5. 定义图标阶梯(12/16/20/24/32/48)
6. 定义头像阶梯(24/32/40/48/64)
7. 定义容器最大宽度(384-1536 + 65ch)
8. 定义圆角阶梯(2/4/8/12/16/24/full)
9. 确保行高 × 字号 = 4 的倍数(基线网格)
10. 全部写成 tokens
```

---

## 六、常见错误

- ❌ 组件尺寸随手定(高度 37px)
- ❌ 图标尺寸不统一(有 18px 有 22px)
- ❌ 按钮 padding 横纵相同(显得太方)
- ❌ 触控目标 <44px
- ❌ 行高导致基线不对齐(22.4px)
- ❌ 数字表格不右对齐
- ❌ 圆形元素没做光学放大补偿
- ❌ 图标与文字对齐"感觉偏"但不调
- ❌ 移动端组件尺寸不放大
- ❌ 无密度档位(专业用户觉得太松/新用户觉得太密)

---

## 七、Prompt 模板

```
【尺寸、对齐与密度系统】

尺寸阶梯:
- 组件高度:xs 24 / sm 32 / md 40(默认)/ lg 48 / xl 56
- 按钮 padding:横向是纵向 2-2.5 倍(如 md: 16px × 10px)
- 图标:12/16/20/24(基准)/32/48
- 头像:24/32/40(常用)/48/64
- 容器 max-width:1280px(标准)/ 65ch(纯文本)
- 圆角:2/4/8(标准)/12/16/24/full

触控:
- 所有可点元素 ≥44×44px
- 图标可视 20px,点击区撑到 44px(grid place-items center)
- 移动端组件用 lg 档(48px)

对齐:
- 严格 4px 网格对齐
- 光学补偿:圆形元素放大 3%,播放三角向右偏 1px
- 大数字+小单位用 baseline 对齐
- 表格数字右对齐 + tabular-nums
- 行高 × 字号 = 4 的倍数(基线网格)
  16px × 1.5 = 24px ✅

密度档位:
- 提供 compact / comfortable(默认) / spacious 三档
- 用 CSS 变量 --density-scale 统一控制
- compact 时触控目标仍不低于 44px

响应式:
- 组件尺寸移动端放大一档
- 图标尺寸不随视口缩放
- 可选流体:height: clamp(40px, 5vw, 56px)
```

---

## 八、检验清单

- [ ] 组件尺寸有阶梯(xs-xl)不是随手值
- [ ] 图标尺寸统一(12/16/20/24/32)
- [ ] 按钮横向 padding 是纵向 2-2.5 倍
- [ ] 触控目标全部 ≥44px
- [ ] 行高 × 字号 = 4 的倍数
- [ ] 表格数字右对齐 + tabular-nums
- [ ] 圆形元素做了光学放大补偿
- [ ] 图标与文字视觉对齐(不只是几何居中)
- [ ] 移动端组件尺寸放大
- [ ] 有密度档位或明确选择了单一密度
- [ ] 所有尺寸来自 tokens
- [ ] 无小数像素值
