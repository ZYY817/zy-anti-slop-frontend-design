# B43 · 视觉可访问性深挖

> 无障碍不是"合规清单",是让**视觉设计对所有人可用**的深层思考。
> C4 讲了 WCAG 技术规范,这份讲"视觉设计决策如何影响不同能力的用户"。

---

## 一、色觉障碍的完整设计

### 类型与比例
```
红色盲 Protanopia      ~1% 男性(红色变暗)
绿色盲 Deuteranopia    ~6% 男性(最常见)
蓝色盲 Tritanopia      ~0.01%(极少)
全色盲 Achromatopsia   ~0.003%(只看明暗)
红绿色弱 Anomaly       ~4%(部分辨色力下降)

总计:约 8% 男性 / 0.5% 女性
```

### 核心规则:不能只用颜色
```
❌ 红色文字 = 错误,绿色文字 = 成功
   (红绿色盲看起来一样)

✅ 红色 + ✗ 图标 + "错误"文字
✅ 绿色 + ✓ 图标 + "成功"文字
```

### 安全的颜色区分
```
✅ 用蓝-橙区分(而非红-绿)
✅ 用明度差区分(深-浅)
✅ 加图案/纹理(图表)
✅ 加形状(圆/方/三角)
✅ 加直接标签
```

### 图表的色盲设计
```
- 不用红绿并排
- 用明度递进(单色渐变)
- 数据系列加图案/直接标注
- 用 ColorBrewer 的色盲安全色板
- 用 Viridis 色板(专为色盲设计)
```

### 数据涨跌的处理
```
❌ 只用红涨绿跌(中国)/ 绿涨红跌(西方)
✅ 颜色 + 箭头(↑↓)+ 正负号(+/-)
```

### 检测工具
- **Coblis** — 色盲模拟器
- **Sim Daltonism**(Mac)— 实时模拟
- **Chrome DevTools** — Rendering > Emulate vision deficiencies
- **Figma Stark 插件**

---

## 二、低视力(Low Vision)设计

### 对比度(超越 AA)
```
WCAG AA:   正文 4.5:1 / 大字 3:1
WCAG AAA:  正文 7:1 / 大字 4.5:1

低视力用户建议:尽量接近 AAA
```

### 缩放支持
```
✅ 支持 200% 缩放不破版(AA 要求)
✅ 支持 400% 缩放内容可用(AAA)
✅ 用 rem 定义字号(跟随用户浏览器设置)
✅ 不禁止缩放:
   <meta name="viewport" content="width=device-width, initial-scale=1">
   (不加 user-scalable=no)
```

### 大字模式
- 支持系统"更大字体"设置
- 用相对单位,让布局跟随字号变化
- 测试超大字号下不破版

### 焦点可见
```css
/* 低视力用户依赖清晰的焦点指示 */
:focus-visible {
  outline: 3px solid var(--focus);   /* 够粗 */
  outline-offset: 2px;
}
```

### 避免纯细字
```
❌ font-weight: 100/200(低视力难读)
✅ 正文至少 400,弱视友好用 500
```

---

## 三、认知障碍(Cognitive)设计

### 减少认知负荷
- 一屏一个主任务
- 语言简单直接(避免行话)
- 步骤清晰编号
- 一致的布局和交互
- 避免时间压力(不倒计时逼决策)

### 清晰的语言
```
❌ "身份验证失败,请重新鉴权"
✅ "登录失败,请重新输入密码"
```

### 帮助与容错
- 明确的错误提示 + 如何修复
- 允许撤销
- 保存进度(不丢失已填)
- 确认破坏性操作

### 一致性
- 相同功能相同位置
- 相同操作相同结果
- 遵循平台惯例(不标新立异)

### 专注辅助
- 阅读模式(去干扰)
- 减少动画干扰
- 高亮当前位置

---

## 四、动效敏感(Vestibular / Motion Sensitivity)

### 前庭障碍
某些动效会引发眩晕、恶心、偏头痛。

### 危险的动效
```
❌ 大幅视差滚动
❌ 快速缩放
❌ 旋转
❌ 大面积移动
❌ 自动播放的运动背景
❌ 闪烁(>3 次/秒 可能诱发癫痫)
```

### prefers-reduced-motion
```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
  /* 去掉视差 */
  .parallax { transform: none !important; }
}
```

### 降级策略(不是全去掉)
```
正常:  fade + translateY(20px) + scale
降级:  只保留 fade(去掉位移和缩放)
```
保留反馈,去掉引发不适的部分。

### 提供开关
除了系统设置,产品内也可提供"减少动效"toggle。

---

## 五、屏幕阅读器(Screen Reader)的视觉配合

虽然屏幕阅读器用户"听"界面,但视觉结构影响朗读:

### 语义化 HTML = 朗读顺序
```html
<header>, <nav>, <main>, <aside>, <footer>
<h1>-<h6>(标题层级 = 导航结构)
<button> vs <div>(可交互性)
```

### 视觉隐藏但可读
```css
.sr-only {
  position: absolute; width: 1px; height: 1px;
  padding: 0; margin: -1px; overflow: hidden;
  clip: rect(0,0,0,0); white-space: nowrap; border: 0;
}
```
用于:图标按钮的文字标签、跳过链接、上下文说明。

### 阅读顺序 = DOM 顺序
```
视觉顺序应该 = DOM 顺序
用 CSS order/flex-direction 改变视觉顺序时,
要注意 DOM 顺序仍然合理(屏幕阅读器按 DOM 读)
```

### 装饰 vs 信息
```html
<!-- 装饰图标:隐藏 -->
<svg aria-hidden="true">...</svg>

<!-- 信息图标:标注 -->
<svg role="img" aria-label="警告">...</svg>
```

---

## 六、运动/操作障碍(Motor)设计

### 大触控目标
```
最小 44×44px,理想更大
按钮之间有间距(避免误触)
```

### 减少精确操作
- 避免需要精准拖拽
- 提供按钮替代拖拽
- 避免需要长按+精确移动
- 悬停菜单要有延迟(避免误消失)

### 键盘完全可达
```
所有功能键盘可完成
Tab 顺序合理
快捷键(高级用户提速)
Focus trap(Modal 内)
Skip link(跳过导航)
```

### 减少时间压力
- 不自动消失重要内容
- Toast 停留足够长(或可暂停)
- Session 超时前警告

---

## 七、光敏感(Photosensitivity)

### 癫痫风险
```
❌ 闪烁 >3 次/秒
❌ 大面积红色闪烁
❌ 高对比条纹快速移动(摩尔纹)
```

### 安全做法
- 避免快速闪烁
- 动画平滑不突变
- 提供暂停控制

---

## 八、深色模式与无障碍

- 深色模式帮助部分低视力/畏光用户
- 但深色下对比度容易不达标(重测!)
- 提供 Light / Dark / High-contrast 三选
- 尊重 `prefers-color-scheme` 和 `prefers-contrast`

```css
@media (prefers-contrast: more) {
  :root {
    --border: rgba(0,0,0,0.3);   /* 加强边框 */
    --text-muted: #333;           /* 加强次要文字 */
  }
}
```

---

## 九、无障碍的"顺带好处"

无障碍设计往往让**所有人**受益:

| 无障碍特性 | 顺带好处 |
|---|---|
| 高对比 | 强光下也能看清 |
| 大触控目标 | 所有人少误触 |
| 清晰焦点 | 键盘用户更快 |
| 字幕 | 嘈杂/静音环境可看视频 |
| 清晰语言 | 所有人少犯错 |
| 键盘可达 | 高级用户提速 |
| 语义化 | SEO 更好 |
| reduced-motion | 省电、省流量 |

**这就是"通用设计(Universal Design)"——为边缘用户设计,所有人受益。**

---

## 十、Prompt 模板

```
【视觉无障碍要求】

色觉:
- 不只用颜色传达状态(配图标 + 文字)
- 用蓝-橙而非红-绿区分
- 数据涨跌:颜色 + 箭头 + 正负号
- 图表用明度递进 + 直接标注 + 图案
- 用色盲安全色板(ColorBrewer/Viridis)

低视力:
- 对比度尽量接近 AAA(正文 7:1)
- 字号用 rem,支持 200% 缩放不破版
- 不禁止缩放
- 正文字重 ≥400,不用 100/200
- focus-visible 3px 粗描边

认知:
- 一屏一主任务
- 语言简单(无行话)
- 步骤编号清晰
- 布局一致
- 不倒计时逼决策
- 错误提示说明如何修复

动效敏感:
- prefers-reduced-motion 降级(保 fade 去位移)
- 无大幅视差/旋转/快速缩放
- 无闪烁 >3次/秒
- 可选:产品内减少动效 toggle

屏幕阅读器:
- 语义化 HTML(header/nav/main/标题层级)
- 图标按钮有 aria-label
- 装饰图标 aria-hidden
- DOM 顺序 = 视觉顺序
- skip link

运动障碍:
- 触控目标 ≥44px,按钮间有间距
- 全键盘可达 + Focus trap + skip link
- 避免精确拖拽(提供按钮替代)

对比偏好:
- 尊重 prefers-contrast: more
- 提供 Light/Dark/High-contrast
```

---

## 十一、检验清单

- [ ] 状态不只用颜色(配图标+文字)
- [ ] 用蓝橙而非红绿区分
- [ ] 图表色盲安全 + 直接标注
- [ ] 对比度达标(尽量 AAA)
- [ ] 支持 200% 缩放不破版
- [ ] 不禁止用户缩放
- [ ] 字重 ≥400
- [ ] focus-visible 清晰(3px)
- [ ] prefers-reduced-motion 降级
- [ ] 无危险闪烁/大幅视差
- [ ] 语义化 HTML
- [ ] 图标按钮有 aria-label
- [ ] 触控目标 ≥44px
- [ ] 全键盘可达
- [ ] 尊重 prefers-contrast
- [ ] 用 axe DevTools 零错误
