# B5 · 动效系统

> 动效不是装饰，是反馈、引导和情绪的一部分。

---

## 一、动效的目的

### 功能性动效（优先级高）
- **反馈** — 告诉用户操作被接收（按钮 press / 表单 submit）
- **引导** — 引导注意力到变化位置（新消息、通知）
- **连续** — 页面间过渡让用户不迷失
- **映射** — 展示元素关系（展开 → 内容属于这个按钮）
- **状态** — 告知系统状态（loading spinner）

### 表现性动效（锦上添花）
- **品牌** — 独特的开场动画、logo 动效
- **惊喜** — 完成任务的庆祝（confetti）
- **氛围** — 背景微动，增加"活"的感觉
- **叙事** — scrollytelling 里的动画驱动

### 口诀
**先解决功能，再考虑表现。**

---

## 二、缓动函数（Easing）

### 类型
| 名字 | 特征 | 用途 |
|---|---|---|
| ease-in | 慢 → 快 | 元素离开视图 |
| ease-out | 快 → 慢 | 元素进入视图 |
| ease-in-out | 慢 → 快 → 慢 | 状态切换 |
| linear | 匀速 | 进度条、旋转 |
| spring | 有弹性回弹 | 自然运动、iOS 感 |
| bounce | 到达后弹跳 | 游戏化 |

### 常见 cubic-bezier

```css
/* Material Standard (入场) */
cubic-bezier(0.0, 0.0, 0.2, 1)

/* Material Decelerate (停住) */
cubic-bezier(0.0, 0.0, 0.2, 1)

/* Material Accelerate (离开) */
cubic-bezier(0.4, 0.0, 1, 1)

/* Material Standard (通用) */
cubic-bezier(0.4, 0.0, 0.2, 1)

/* Apple Fluid */
cubic-bezier(0.25, 0.1, 0.25, 1)

/* Vercel Snappy */
cubic-bezier(0.4, 0.0, 0.0, 1)
```

### Spring 参数
```
stiffness: 刚度（越大越快）
damping: 阻尼（越大越少弹）
mass: 质量（越大越迟缓）
```

---

## 三、持续时间（Duration）

### 分档
| 档位 | 毫秒 | 用途 |
|---|---|---|
| instant | 0-50ms | 微反馈（hover 高亮） |
| fast | 100-150ms | 按钮 hover/active、Toggle |
| normal | 200-300ms | 菜单展开、Tab 切换、Fade |
| slow | 400-500ms | Modal 入场、大区块过渡 |
| emphasis | 600-800ms | 首次入场、庆祝动效 |
| dramatic | 1000+ms | Logo 动画、loading 全屏 |

### 尺寸规则
- 元素越小，动画越快（按钮 100ms / 全屏 300-500ms）
- 进入比离开慢（enter 200ms / exit 150ms）
- 首次出现可以慢点（留深印象）

---

## 四、变换（Transform）

| 属性 | 用途 |
|---|---|
| `translateX/Y` | 位移，滑入滑出 |
| `scale` | 缩放，按压反馈（0.95）/入场（0.9→1） |
| `rotate` | 旋转，展开箭头、loading |
| `skew` | 倾斜，个性化 |
| `opacity` | 透明度，淡入淡出 |
| `blur` | 模糊进出场 |

### 组合
```css
/* 入场：从下方淡入 */
from { opacity: 0; transform: translateY(8px); }
to   { opacity: 1; transform: translateY(0); }

/* 按压反馈 */
.button:active { transform: scale(0.96); }

/* 展开面板 */
from { opacity: 0; transform: scaleY(0.95); }
to   { opacity: 1; transform: scaleY(1); }
```

---

## 五、时序与编排（Stagger / Orchestration）

### Stagger（错开）
列表项依次出现，每个延迟 30-50ms。

```css
.item:nth-child(1) { animation-delay: 0ms; }
.item:nth-child(2) { animation-delay: 50ms; }
.item:nth-child(3) { animation-delay: 100ms; }
```

### 编排原则
- **先入后出**：入场动画先于交互动画
- **由主到次**：先标题后正文后按钮
- **由近及远**：靠近触发点的先动
- **保持方向一致**：全部从左入或全部从下入

---

## 六、微交互（Micro-interaction）

### 经典微交互
- 按钮 hover 颜色变化 + scale 微涨
- 按钮 click → scale(0.95) → scale(1)
- Toggle 开关滑动 + 背景色变
- 输入框 focus 边框色 + label 缩小浮起
- 数字增减（tween 动画）
- 点赞心跳 + 红色填充
- 购物车图标弹跳
- Checkbox → ✓ 路径绘制
- 加载成功 → ✓ 出现
- 删除 → 项滑出 + 高度折叠

### 设计原则
- 触发 → 规则 → 反馈 → 结束 四步模型
- 不超过 300ms
- 物理直觉（重力、惯性、弹性）

---

## 七、页面过渡（Page Transition）

### 类型
- **Fade** — 淡入淡出（最安全）
- **Slide** — 滑动切换（前后关系）
- **Shared Element** — 共享元素动画（Material, View Transitions API）
- **Morph** — 一个元素变形成另一个
- **Crossfade** — 交叉淡入

### View Transitions API（2024 原生）
```css
::view-transition-old(root) { animation: fade-out 0.2s; }
::view-transition-new(root) { animation: slide-in 0.3s; }
```

### SPA 路由动画
- React: Framer Motion + AnimatePresence
- Vue: `<Transition>` / GSAP
- 通用: Lenis + GSAP

---

## 八、滚动动画（Scroll-driven）

### 类型
- **Parallax** — 多层不同速度
- **Reveal** — 滚到视口触发入场
- **Pin** — 元素固定，内容滚过
- **Progress** — 阅读进度条

### Scroll-driven Animations（2024 原生 CSS）
```css
@keyframes reveal {
  from { opacity: 0; }
  to   { opacity: 1; }
}
.element {
  animation: reveal;
  animation-timeline: view();
  animation-range: entry;
}
```

### 库
- **Framer Motion** — `useScroll` / `useTransform`
- **GSAP ScrollTrigger** — 老牌，功能最全
- **Lenis** — 平滑滚动
- **AOS** — 简单入场
- **Motion One** — 轻量

---

## 九、状态动画

| 状态变化 | 动画 |
|---|---|
| 出现 | fade-in + translateY(8→0) |
| 消失 | fade-out + translateY(0→8) |
| 展开 | height auto + fade-in |
| 折叠 | height 0 + fade-out |
| Loading 开始 | skeleton shimmer |
| Loading 结束 | skeleton → 真实内容 crossfade |
| Error | 红色 flash + shake |
| Success | green flash + ✓ path draw |
| Disabled → Enabled | opacity 0.5 → 1 |

---

## 十、Motion Tokens

### 定义
```
motion.duration.instant:   50ms
motion.duration.fast:      100ms
motion.duration.normal:    200ms
motion.duration.slow:      400ms
motion.duration.emphasis:  600ms

motion.easing.standard:    cubic-bezier(0.4, 0, 0.2, 1)
motion.easing.enter:       cubic-bezier(0, 0, 0.2, 1)
motion.easing.exit:        cubic-bezier(0.4, 0, 1, 1)
motion.easing.spring:      spring(1, 80, 10)

motion.stagger.gap:        50ms
```

### 用法
设计系统里所有动画引用 tokens，不写硬编码。

---

## 十一、减弱动效（Reduced Motion）

### 尊重用户偏好
```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

### 最佳实践
- 不完全去除动画（否则失去反馈）
- 改为：更短 duration + 去掉 translate + 只保留 fade
- 前庭障碍用户：去掉视差、大幅位移

---

## 十二、性能

### GPU 加速属性
- ✅ `transform` — GPU
- ✅ `opacity` — GPU
- ❌ `width/height` — 触发 reflow
- ❌ `top/left` — 触发 reflow
- ❌ `color/background` — 触发 repaint

### will-change
```css
.animating { will-change: transform, opacity; }
```
提前告诉浏览器。用完移除。

### 帧率
- **60fps** = 每帧 16.6ms（及格线）
- **120fps** = 每帧 8.3ms（Pro Motion）
- 超过预算 → 丢帧卡顿

---

## 十三、动效工具与库

| 工具 | 场景 |
|---|---|
| CSS Transitions | 简单状态变化 |
| CSS Animations | 关键帧动画 |
| Framer Motion | React 首选 |
| GSAP | 最强大，跨框架 |
| Motion One | 轻量，Web Animations API |
| Lottie | 从 AE 导出矢量动画 |
| Rive | 交互式动画（游戏化） |
| Three.js / R3F | 3D 动效 |
| Spring.js | 物理弹性动画 |
| Auto-animate | 一行代码自动动画 |

---

## 十四、常见错误

- ❌ 动画太慢（>500ms 会感觉拖沓）
- ❌ 所有动画 ease-in-out（应区分进入/退出）
- ❌ 忽略 reduced-motion
- ❌ 用 top/left 做动画（触发回流）
- ❌ stagger 间隔太大（>100ms 就显得故意）
- ❌ 装饰动效比功能动效多
- ❌ 多个动画同时开始（信息过载）
- ❌ 没有退出动画（元素突然消失）

---

## 十五、Prompt 表达模板

```
【动效系统】：
- 缓动：标准 cubic-bezier(0.4, 0, 0.2, 1)，入场 ease-out，退出 ease-in
- 持续时间：hover 100ms / 展开 200ms / 页面过渡 300ms
- 入场方式：fadeIn + translateY(8px→0)，stagger 50ms
- 按钮反馈：hover scale(1.02) / active scale(0.96)
- 状态：loading 骨架屏 shimmer / success ✓ path draw / error shake
- 页面过渡：View Transitions API + crossfade
- 减弱动效：prefers-reduced-motion 时仅保留 fade
- 实现：Framer Motion（React）/ GSAP（复杂场景）
```

---

## 检验清单

- [ ] 每个动效都有功能目的
- [ ] 缓动函数使用 motion tokens
- [ ] 持续时间分档明确
- [ ] 入场/退出动画配对
- [ ] 尊重 prefers-reduced-motion
- [ ] 只用 transform + opacity 做动画
- [ ] 60fps 无卡顿
- [ ] 装饰 < 功能
