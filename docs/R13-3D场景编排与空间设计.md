# R13 · 3D场景编排与空间设计（提示词库 M 组）

> **定位**：从"一个 shader 效果"升级到"一个可游历的空间/世界"——含城市、建筑、地形、场景关卡与镜头调度。
> **覆盖层**：主要落在 L1–L2（场景骨架与空间组织层）；原库标注为 L0 意 + L1 骨 + L3 肉，条目上的 L0–L5 标注原样保留。L 层级沿用提示词库的分层（L0 意 / L1 骨 / L2 形 / L3 肉 / L4 气 / L5 髓）。
> **条目卡格式**：每条为 `**中文名（English Term）** · L层级 · 档位（基础/进阶/电影级） · 成本:低/中/高`，下挂「视觉收益 / 参考映射 / 同族拓展 / 提示词片段」。提示词片段可直接粘进生成 prompt。
> **关键判断**：**3D 世界永远从本组起步，撞到性能墙才上 WebGPU**（`R14` WebGPU与计算管线）。判断你需不需要本组：如果你要做的是"进入一个空间"而不是"看一个效果"——需要。
> **来源**：融合自外部「视觉提示词库 M 组：3D 场景编排与空间设计」（v1.x，2026-08-16），总索引见 `R0`。原库的组内交叉引用（A–R 组）已改写为本库 `R{n}·{原引用}` 形式，避免与本库 A/B/C/D 板块含义冲突（本库 `C4` 是无障碍、`B5` 是动效系统，不可与原库 C/B 组混读）。
> **与反 AI 味清单的关系**：本文参数与 `B82`（反 AI 味）、`B44`（视觉禁忌）并不冲突；那两份是**参考清单，不是硬性铁律，按项目自行取舍**。风格方向定位见 `B16`、`E1`，全维度清单见 `B15`。

---

## 使用说明

`R3`（A 组 几何与光线）–`R6`（D 组 后期与镜头）做"一帧画面"或"一个物体"，本组（M 组）做**"一整个场景/世界"**——有建筑、有地形、有天气、有时间流逝、有镜头带你游历。这是 Awwwards 年度作品最常见的形态（沉浸式 3D 世界）。

「城市学」维度（`R13·M2`/`R13·M3`）是本组重点：程序化城市、建筑生成、街景、天际线，这些是极高辨识度且极出效果的题材。

```
M1 场景图与空间组织       M5 天气与时间系统
M2 程序化城市（城市学）   M6 关卡编排与镜头调度
M3 建筑与结构语言         M7 DOM 与 WebGL 混合
M4 地形与自然环境         M8 LOD 与大世界优化
```

---

## M1 场景图与空间组织（Scene Graph）

- **场景层级（Scene Hierarchy）** · L1 · 基础 · 成本:低
  - 视觉收益：父子变换、分组、实例化，让复杂场景可管理
  - 同族拓展：transform hierarchy、Group、Object3D、局部/世界坐标、pivot 设计
  - 提示词片段：`场景按语义分组（环境/主体/装饰/UI锚点）；重复物体用 InstancedMesh；每组独立可控可动画`

- **实例化渲染（Instancing）** · L1 · 进阶 · 成本:低
  - 视觉收益：一次 draw call 画上万个相同网格（树、窗、砖、人群），性能关键
  - 同族拓展：InstancedMesh、instanced attributes（每实例颜色/尺寸/相位）、见 `R8·J3` 程序化变化
  - 提示词片段：`重复建筑/植被/人群用 instancing（每实例随机尺寸±10%、色相±5°、动画相位偏移），杜绝复制感`

- **相机系统（Camera Systems）** · L1 · 进阶 · 成本:低
  - 视觉收益：多相机、相机切换、跟随、约束
  - 同族拓展：见 `R3·A7` 相机、OrbitControls、第一/第三人称、cinemachine 式虚拟相机、相机碰撞（防穿墙）
  - 提示词片段：`相机加碰撞检测防穿墙；提供轨道/漫游/跟随三种模式，切换有过渡（见 R11·F3）`

- **坐标与尺度（Scale & Units）** · L1 · 基础 · 成本:低
  - 视觉收益：统一尺度基准（1 单位 = 1 米），避免精度问题与阴影 bug
  - 同族拓展：real-world scale、near/far 平面比、大坐标的浮点精度问题（用相对原点）
  - 提示词片段：`统一 1 单位=1 米；near/far 比控制在 1:10000 内避免 z-fighting；大世界用相对相机原点`

---

## M2 程序化城市（Procedural City）※ 城市学核心

**极高辨识度、极出效果的题材。** 从数据或规则生成整座城市。

- **路网生成（Road Network）** · L1 · 电影级 · 成本:中
  - 视觉收益：城市的骨架。网格路网（曼哈顿）/ 放射路网（巴黎）/ 有机路网（老城）
  - 同族拓展：L-system 路网（见 `R8·J4`）、Voronoi 街区、tensor field roads、grid vs organic、主干道+支路层级
  - 提示词片段：`用 tensor field 或 L-system 生成路网，分主干道/次干道/支路三级；街区用路网切分的多边形`

- **地块与建筑占位（Lots & Footprints）** · L1 · 电影级 · 成本:中
  - 视觉收益：街区切分成地块，每块放一栋建筑，密度随位置变化（市中心密、郊区疏）
  - 同族拓展：lot subdivision、setback（退界）、density falloff from center、zoning（商业/住宅/工业分区）
  - 提示词片段：`街区按最短边切分地块；建筑高度随到市中心距离衰减（幂律）；商业区高密、住宅区中密、边缘低密`

- **建筑体量生成（Building Massing）** · L1 · 电影级 · 成本:中
  - 视觉收益：从占地生成建筑体量——挤出、退台、收分、屋顶
  - 同族拓展：extrude footprint、setback tower（退台高楼）、tapering、程序化屋顶（平/坡/尖）、随机高度分布（幂律，少数摩天楼）
  - 提示词片段：`建筑从地块挤出+随机退台；高度用幂律分布（多数中低、少数摩天）；屋顶按类型随机（平顶/坡顶/水箱/天线）`

- **立面生成（Facade Generation）** · L3 · 电影级 · 成本:中
  - 视觉收益：窗户网格、楼层线、材质——决定城市"活"还是"盒子堆"
  - 同族拓展：window grid（instanced 或 shader）、floor lines、随机亮灯窗（夜景关键）、程序化立面 shader、split grammar（CGA shape grammar）
  - 提示词片段：`立面用 shader 程序化窗格；夜景随机点亮 30% 窗户（暖色，随机闪烁）；不同建筑用不同窗格密度`

- **城市氛围（City Atmosphere）** · L3 · 电影级 · 成本:中
  - 视觉收益：让城市有生命——车流光带、街灯、雾霾、霓虹、飞机
  - 同族拓展：car light trails（长曝光光带）、street lights、neon（见 `R7·E3`/`R5·C3`）、smog（见 `R4·B8`）、windows flicker、天际线剪影
  - 提示词片段：`加车流光带（沿路网移动的发光条）+ 街灯点阵 + 低空雾霾 + 霓虹招牌；夜景是城市最出效果的时刻`

- **天际线与地标（Skyline & Landmarks）** · L1 · 电影级 · 成本:低
  - 视觉收益：一两个独特地标 + 起伏的天际线 = 城市的"脸"
  - 同族拓展：landmark placement、skyline silhouette（见 `R2·K5` 剪影测试）、地标作为视觉焦点（见 `R1·G1`）
  - 提示词片段：`放 1-2 个独特地标建筑作为焦点（其余是背景群）；天际线要有起伏节奏，通过剪影测试`

---

## M3 建筑与结构语言（Architecture & Structure）

- **建筑风格语言（Architectural Style）** · L0 · 进阶 · 成本:低
  - 视觉收益：风格决定气质——现代玻璃幕墙/包豪斯/新粗野主义/古典/赛博朋克/东方
  - 参考映射：玻璃幕墙=商务未来｜清水混凝土=冷峻艺术｜新粗野=力量原始｜古典石材=庄严权威｜赛博=霓虹反乌托邦｜木构=温暖东方
  - 同族拓展：见 `R1·G5` 形状语言、建筑材质对应（见 `R5·C4`）
  - 提示词片段：`建筑风格锁定为 <风格>，据此定材质（幕墙/混凝土/石材）、比例、开窗方式；全场景统一`

- **形状语法（Shape Grammar / CGA）** · L1 · 电影级 · 成本:中
  - 视觉收益：用规则递归生成建筑细节（CityEngine 的核心方法）
  - 同族拓展：CGA shape grammar、split/repeat/component split rules、程序化装饰
  - 提示词片段：`用 split grammar 生成立面细节：楼层分割→开间分割→窗/墙/柱构件，每级可随机变体`

- **结构真实感（Structural Plausibility）** · L1 · 进阶 · 成本:低
  - 视觉收益：让结构"站得住"——承重、比例、开窗逻辑符合直觉
  - 同族拓展：柱网、悬挑限制、比例系统（古典柱式/模数）、避免"漂浮"结构（见 `R1·G4` 接地）
  - 提示词片段：`建筑比例遵循模数系统；结构看起来能承重（底部实、顶部轻）；元素明确接地`

- **室内与空间序列（Interior & Spatial Sequence）** · L1 · 电影级 · 成本:中
  - 视觉收益：进入建筑内部，空间的开合明暗节奏（建筑叙事）
  - 同族拓展：compression-release（压缩-释放）、light well、enfilade（串联房间）、见 `R1·G8` 叙事节奏
  - 提示词片段：`室内空间做开合节奏：窄暗的入口→豁然开朗的中庭；用光的明暗引导路径`

- **构造细节（Construction Details）** · L3 · 电影级 · 成本:低
  - 视觉收益：接缝、材质分格、边角处理，决定"精致 vs 塑料"
  - 同族拓展：panel seams、material transitions、AO in crevices（见 `R5·C2`）、edge wear（见 `R7·E5`）
  - 提示词片段：`建筑表面加分格缝（幕墙/石材接缝）+ 缝隙 AO + 边角轻微磨损，避免纯净塑料感`

---

## M4 地形与自然环境（Terrain & Nature）

- **高度场地形（Heightmap Terrain）** · L1 · 电影级 · 成本:中
  - 视觉收益：山川、丘陵、峡谷的地貌
  - 同族拓展：FBM heightmap（见 `R4·B3`）、erosion simulation（水力/热力侵蚀）、ridged noise 做山脊、terrain LOD（见 `R13·M8`）
  - 提示词片段：`地形用 FBM 高度场 + 侵蚀模拟；山脊用 ridged noise；按坡度混合材质（草/岩/雪）`

- **坡度材质混合（Slope-Based Materials）** · L3 · 进阶 · 成本:低
  - 视觉收益：平地长草、陡坡露岩、高处积雪——自动的真实感
  - 同族拓展：triplanar mapping（避免拉伸）、height/slope blending、macro/micro 双尺度纹理
  - 提示词片段：`地形材质按高度+坡度混合（低平=草、陡=岩、高=雪）；用 triplanar 避免陡坡拉伸`

- **植被散布（Vegetation Scattering）** · L3 · 电影级 · 成本:中
  - 视觉收益：树木、草、灌木的自然分布
  - 同族拓展：见 `R8·J1` 蓝噪声/Poisson 散布、density map、instancing（见 `R13·M1`）、wind animation（顶点位移）、billboard 远景
  - 提示词片段：`植被用 Poisson disk 散布（避坡度过陡处）；instanced 渲染 + 风吹顶点动画；远景转 billboard`

- **水体（Water Bodies）** · L4 · 电影级 · 成本:高
  - 视觉收益：见 `R7·E6` 水面。河流、湖泊、海洋
  - 同族拓展：见 `R7·E6` Gerstner 波/焦散/泡沫、河流流向、水岸线泡沫、水下雾
  - 提示词片段：`水体用 R7·E6 水面全套；河流加流向、湖泊平静、海洋加大浪；岸线加泡沫`

- **天空与体积云（Sky & Volumetric Clouds）** · L4 · 电影级 · 成本:高
  - 视觉收益：见 `R4·B8`/`R4·B4`。真实天空是户外场景的一半
  - 同族拓展：见 `R4·B8` 大气散射、`R4·B1` 体积云、天空盒 vs 程序化天空
  - 提示词片段：`天空用程序化大气散射（R4·B8）+ 体积云（R4·B1）；随时间变化（见 R13·M5）`

---

## M5 天气与时间系统（Weather & Time-of-Day）

- **日夜循环（Day-Night Cycle）** · L4 · 电影级 · 成本:中
  - 视觉收益：太阳位置驱动整个场景的光照与色调，一套系统覆盖所有时间
  - 同族拓展：sun position（时角/赤纬）、golden hour（黄金时刻）、blue hour、太阳色温随高度变化（见 `R5·C3`）、星空切换（见 `R4·B8`）
  - 提示词片段：`太阳高度角驱动全局光照与色调；黄昏黄金时刻最出效果（暖光+长影）；夜晚切星空+人工光`

- **天气状态（Weather States）** · L4 · 电影级 · 成本:中
  - 视觉收益：晴/阴/雨/雾/雪，改变整个场景情绪
  - 同族拓展：雨（粒子+涟漪+湿反射）、雪（粒子+积雪材质）、雾（见 `R3·A2`/`R4·B8`）、天气过渡（平滑插值）
  - 提示词片段：`天气状态可切换且平滑过渡；雨天加雨粒子+地面涟漪+湿滑反射增强；雾天降能见度增氛围`

- **湿润与积累（Wetness & Accumulation）** · L4 · 电影级 · 成本:低
  - 视觉收益：雨后地面变暗变亮（湿反射）、积水、积雪——极强的真实感
  - 同族拓展：wetness map（roughness 降低）、puddle（法线平滑+反射）、snow accumulation（朝上面积雪）
  - 提示词片段：`雨后地面 roughness 降低+反射增强；低洼处积水（镜面反射）；雪落在朝上表面`

- **风与动态（Wind & Motion）** · L3 · 进阶 · 成本:低
  - 视觉收益：植被摇摆、旗帜飘动、粒子飘移，让静态场景"活"
  - 同族拓展：wind vector、见 `R4·B5` 流场、顶点动画、见 `R7·E5` 动态
  - 提示词片段：`全局风向量驱动植被/旗帜/粒子；用噪声让风有阵性（不是匀速）`

---

## M6 关卡编排与镜头调度（Level & Camera Choreography）

- **空间叙事路径（Spatial Narrative Path）** · L0 · 电影级 · 成本:中
  - 视觉收益：设计一条游历路径，沿途场景按叙事展开（滚动或自动飞行驱动）
  - 同族拓展：见 `R11·F2` 滚动叙事、`R1·G8` 叙事节奏、camera spline、waypoint、场景分幕
  - 提示词片段：`设计一条相机路径分 4-5 幕，每幕一个空间场景与情绪；滚动或自动飞行驱动，交互可接管（见 R6·D5）`

- **兴趣点与聚焦（Points of Interest）** · L1 · 进阶 · 成本:低
  - 视觉收益：相机在关键位置停留聚焦，引导注意力
  - 同族拓展：POI markers、camera focus、景深聚焦（见 `R6·D4`）、hotspot 交互
  - 提示词片段：`路径上设兴趣点，相机到达时减速停留+景深聚焦主体+可选信息浮层`

- **场景转换（Scene Transitions）** · L5 · 电影级 · 成本:中
  - 视觉收益：场景间的过渡不是硬切——穿越、溶解、镜头穿过物体
  - 同族拓展：见 `R11·F3` 转场、camera fly-through、portal（传送门效果）、match cut（形状匹配剪辑）
  - 提示词片段：`场景转换用镜头穿越或形状匹配剪辑（match cut）；避免硬切黑场`

- **视差与纵深层（Parallax Depth Layers）** · L1 · 进阶 · 成本:低
  - 视觉收益：见 `R3·A4`/`R11·F2`。多层不同深度营造空间感
  - 提示词片段：`前中后景分层，滚动/移动时不同视差速度；远景用大气透视（见 R1·G4）淡入`

- **引导与可读性（Wayfinding）** · L1 · 进阶 · 成本:低
  - 视觉收益：用户在 3D 空间里不迷失——明确的方向暗示、当前位置指示
  - 同族拓展：见 `R11·F7` 进度、minimap、章节指示、breadcrumb、光线/箭头引导（见 `R1·G2`）
  - 提示词片段：`3D 漫游提供进度指示与当前位置；用光/路径/地标引导方向，避免用户迷失`

---

## M7 DOM 与 WebGL 混合（DOM-WebGL Hybrid）

**最实用的一节。** 纯 3D 难做好文字与交互，混合方案兼得两者之长。

- **DOM 覆盖同步（DOM Overlay Sync）** · L2 · 进阶 · 成本:中
  - 视觉收益：HTML 元素跟随 3D 物体位置（标签、信息卡、热点），文字清晰可访问
  - 同族拓展：3D→2D 投影（project）、drei `<Html>`、遮挡剔除（被挡住时隐藏）、CSS3DRenderer
  - 提示词片段：`3D 物体的标签用 DOM 覆盖（投影到屏幕坐标），被遮挡时淡出；文字永远 DOM 不进 canvas（见 R10·I8）`

- **滚动同步 3D（Scroll-Linked 3D）** · L2 · 电影级 · 成本:中
  - 视觉收益：滚动页面驱动 3D 相机/动画，图文与 3D 联动（产品站经典）
  - 同族拓展：见 `R11·F2` 滚动驱动、scroll progress→camera、pinned canvas + scrolling content
  - 提示词片段：`canvas 固定，DOM 内容滚动；滚动进度驱动 3D 相机沿路径移动，文字段落与 3D 场景对应`

- **无缝背景（Seamless Background 3D）** · L1 · 基础 · 成本:低
  - 视觉收益：3D 作为整站背景，UI 浮在其上，两者色调协调
  - 同族拓展：fixed canvas、pointer-events 管理、性能（背景 3D 要轻量）
  - 提示词片段：`3D 作为固定背景，UI 用毛玻璃层浮于其上（见 R9·H3）；背景 3D 保持轻量，交互穿透到需要的层`

- **混合景深与融合（Blended Depth）** · L3 · 电影级 · 成本:中
  - 视觉收益：DOM 元素看起来"在 3D 空间里"——共享景深、共享光照色调
  - 同族拓展：CSS 3D transform 配合、DOM 元素的模糊匹配景深、色调统一
  - 提示词片段：`DOM 卡片用 CSS transform 匹配 3D 透视；远处 DOM 元素加 blur 匹配景深；色调取自 3D 场景`

---

## M8 LOD 与大世界优化（LOD & Large Worlds）

- **细节层级（Level of Detail）** · L2 · 进阶 · 成本:中
  - 视觉收益：远处用低模/billboard，近处用高模，大世界的性能基础
  - 同族拓展：discrete LOD、impostor/billboard、mesh 简化、nanite 式虚拟几何（前沿）
  - 提示词片段：`远物体切低模或 billboard；建筑分 3 级 LOD；超远处只留天际线剪影`

- **视锥与遮挡剔除（Culling）** · L2 · 进阶 · 成本:中
  - 视觉收益：不画看不见的东西
  - 同族拓展：frustum culling、occlusion culling、GPU culling（见 `R14·L2`）、portal culling（室内）
  - 提示词片段：`视锥剔除 + 遮挡剔除；室内用 portal 剔除；大量实例用 GPU 剔除（见 R14·L2）`

- **分块流式加载（Chunked Streaming）** · L2 · 电影级 · 成本:高
  - 视觉收益：大世界分块按需加载，无限探索不爆内存
  - 同族拓展：chunk loading、tile streaming、异步加载+淡入、内存预算
  - 提示词片段：`大世界分块流式加载（相机接近时载入、远离时卸载）；载入用淡入避免突现`

- **纹理与几何预算（Asset Budget）** · L5 · 基础 · 成本:低
  - 视觉收益：控制总量，避免加载慢与内存爆
  - 同族拓展：texture atlas、纹理压缩（KTX2/Basis）、draco 几何压缩、instancing（见 `R13·M1`）
  - 提示词片段：`纹理用 KTX2 压缩 + atlas 合并；几何用 draco 压缩；重复物体一律 instancing`

- **移动端 3D 降级（Mobile 3D Fallback）** · L5 · 基础 · 成本:低
  - 视觉收益：手机跑得动的低配版
  - 同族拓展：见 `R6·D8` 档位、降多边形/降纹理/减实例、或退到 2D 预渲染
  - 提示词片段：`移动端 3D 降档：减实例数、降 LOD 阈值、关阴影/后期；极低端退到 2D 预渲染海报`

---

## 本组 · 组合速查

| 想要的效果 | 组合 |
|---|---|
| 程序化夜景城市 | `R13·M2` 全套 + `R13·M2` 城市氛围(车流光带/霓虹) + `R13·M5` 夜晚 + `R6·D2` Bloom |
| 白天鸟瞰城市 | `R13·M2` 路网+体量 + `R13·M3` 立面 + `R13·M5` 黄金时刻 + `R13·M8` LOD |
| 自然地貌世界 | `R13·M4` 地形+坡度材质 + `R13·M4` 植被 + `R13·M4` 水体 + `R13·M5` 日夜 |
| 滚动驱动产品站 | `R13·M7` 滚动同步 3D + `R13·M6` 空间叙事 + `R13·M7` DOM 标签 |
| 沉浸漫游世界 | `R13·M1` 相机系统 + `R13·M6` 关卡编排 + `R13·M6` wayfinding + `R13·M8` 流式加载 |
| 建筑可视化 | `R13·M3` 风格+形状语法 + `R13·M3` 构造细节 + `R13·M5` 天光 + `R5·C4` PBR |
| 3D 背景站 | `R13·M7` 无缝背景 + `R9·H3` 毛玻璃 UI + `R13·M8` 轻量化 |

**常见坑**：

- 城市像"盒子堆" → `R13·M2` 缺立面细节与夜景亮窗
- 建筑在漂浮 → `R13·M3` 缺接地、`R13·M1` 尺度不对
- 城市每栋楼一样 → `R13·M1` 缺 instancing 的 per-instance 变化
- 户外场景很假 → `R13·M4` 缺天空系统、`R13·M5` 缺日夜光照
- 地形材质拉伸 → `R13·M4` 缺 triplanar
- 3D 里文字糊/不可选 → `R13·M7` 应该 DOM 覆盖不进 canvas（见 `R10·I8`）
- 大世界卡死/爆内存 → `R13·M8` 缺 LOD/剔除/流式
- 用户在 3D 里迷路 → `R13·M6` 缺 wayfinding
- 手机直接崩 → `R13·M8` 缺移动端降级
- 场景转换硬切黑场 → `R13·M6` 用穿越/match cut

> **本组心法**：先决定"这是一个效果还是一个世界"。是世界，就先搭 `R13·M1` 场景组织 + `R13·M6` 镜头路径（骨架），再往里填 `R13·M2`–`R13·M5` 的内容，最后 `R13·M8` 优化。**城市题材永远做夜景**——车流光带、霓虹、亮窗，是白天做不出的辨识度。作品集与个人展示类页面尤其要做出这种"只有夜景才有"的辨识度。

---

## 与库内衔接

| 本文小节 | 目标库相关文档 | 关系 |
|---|---|---|
| `R13·M1` 场景图与空间组织（scene graph / instancing / 相机系统 / 尺度） | `V6` 3D与空间效果 | 地基——V6 给出 3D 能力全景与技术选型，本节补齐场景组织、InstancedMesh、坐标尺度（1 单位=1 米、near/far ≤ 1:10000）的工程细节 |
| `R13·M7` DOM 与 WebGL 混合（DOM overlay / 滚动同步 / 无缝背景 / blended depth） | `V6` 3D与空间效果、`B37` 空间设计AR-VR | 扩展——把纯 3D 扩展成"3D + DOM"混合形态：文字与交互留在 DOM，3D 只做空间与氛围 |
| `R13·M6` 关卡编排与镜头调度、`R13·M6` wayfinding | `B37` 空间设计AR-VR | 扩展——空间型界面的导览、位置感与防迷失规则；B37 讲空间设计原则，本节给相机路径与 POI 的可执行做法 |
| `R13·M6` 视差与纵深层、`R13·M8` 天际线剪影/大气透视 | `B59` 视觉的层与深度系统 | 地基——层与深度的分层模型（前中后景、大气透视）来自 B59；本文把它落到 3D 场景的具体参数与视差速度 |
| `R13·M8` LOD 与大世界优化（LOD / frustum & occlusion culling / chunked streaming / KTX2 / draco / 移动端降级） | `F3` 性能与Core Web Vitals | 验证——draw call、纹理与几何预算、分块加载、移动端降档应作为 LCP/INP/内存的验收项；3D 站的性能红线在这里核对 |
| `R13·M6` 空间叙事路径、场景转换（match cut / fly-through）、POI 景深聚焦 | `B78` 摄影与影像艺术指导（镜头调度） | 地基——镜头语言、焦段、景深与剪辑逻辑的原理出处；本文提供 camera spline、waypoint、转场的实现方式 |
| `R13·M2` 城市氛围与夜景、`R13·M5` 日夜循环与天气状态 | `B80` 页面视觉叙事与场景编排 | 扩展——把"页面分幕叙事"扩展到"空间分幕 + 时间/天气驱动情绪"；B80 定叙事结构，本文定场景内容 |
| `R13·M3` 建筑风格语言、`R13·M4` 地形与自然、`R13·M5` 天气情绪 | `B16` 风格谱系、`E1` 趋势与竞品参考 | 地基——风格方向先在 B16/E1 定位，再进本文选建筑/地貌/天气的具体语言，避免风格漂移 |
| 全文档位（基础/进阶/电影级）与成本标注 | `B15` 全维度清单 | 验证——本文的档位应对齐 B15 的维度清单逐项过一遍，防止只做效果不做体系 |
| `R13·M7` DOM 文字与可访问性、`R13·M8` 移动端降级 | `C4` 无障碍 | 验证——文字必须留在 DOM（可选中、可读屏）；自动飞行/视差在 `prefers-reduced-motion` 下降级为静态或极短过渡 |
| `R13·M8` 移动端 3D 降级、纹理与几何预算 | `R6·D8` 档位（提示词库 D 组） | 扩展——档位体系的统一出处；本节给 3D 场景的具体降档手段 |
| 撞到性能墙后的升级路径（GPU culling、计算管线） | `R14` WebGPU与计算管线 | 扩展——**3D 世界永远从本组起步，撞到性能墙才上 WebGPU**；`R14·L2` 提供 GPU 剔除与计算驱动的实例管理 |
| 风格取舍与禁忌核对 | `B82` 反 AI 味、`B44` 视觉禁忌 | 验证——参考清单，不是硬性铁律，按项目自行取舍 |

---

## 参考资料

本文融合自外部「视觉提示词库 M 组：3D 场景编排与空间设计」（v1.x，2026-08-16），按本库命名与引用规范做了适配：条目结构、L 层级、档位、成本、强度值域、同族拓展与提示词片段均为忠实迁移；原库的组内交叉引用（A–R 组）已统一改写为本库 `R{n}·{原引用}` 形式（A组=`R3` / B组=`R4` / C组=`R5` / D组=`R6` / E组=`R7` / F组=`R11` / G组=`R1` / H组=`R9` / I组=`R10` / J组=`R8` / K组=`R2` / L组=`R14` / M组=`R13` / N组=`R12` / O组=`R15` / P组=`R16` / Q组=`R17` / R组=`R18` / 总索引=`R0`），本组自引用写作 `R13·M{n}`。

技术术语与参数值（scene graph、InstancedMesh / instanced attributes、LOD、impostor/billboard、frustum culling、occlusion culling、GPU culling、portal culling、chunked streaming、KTX2/Basis、draco、glTF 资产管线、matcap、HDRI、tensor field roads、L-system、CGA split grammar、lot subdivision / setback / density falloff、FBM heightmap、ridged noise、erosion simulation、triplanar mapping、Poisson disk 散布、Gerstner 波、大气散射、golden hour / blue hour、wetness map、snow accumulation、camera spline / waypoint、match cut、CSS3DRenderer、drei `<Html>`、near/far ≤ 1:10000、夜景亮窗 30%、per-instance 尺寸±10% / 色相±5° 等）来自该库原始调研，**未新增未经验证的数据**。

_城市学重点：程序化路网→地块→体量→立面→氛围；城市永远做夜景最出效果_
