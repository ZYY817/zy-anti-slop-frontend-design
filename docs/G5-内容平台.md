# G5 · 内容平台场景 Prompt 库

> Blog / 资讯 / 社交 Feed / 编辑器 / 评论系统——内容型产品的视觉核心是"阅读舒适"。
> 本文件给出内容平台各页面的完整 prompt + 阅读体验的精工细节。

---

## 一、文章阅读页

```
【页面】文章/博客阅读页——内容平台的核心

【布局（桌面三栏）】
┌─────┬─────────────────┬──────┐
│ 目录 │    正文区          │ 相关  │
│sticky│  max-w 680px      │sticky│
└─────┴─────────────────┴──────┘

【正文排版（决定阅读体验的核心）】
- 正文宽度：max-width 65-70ch（每行 60-70 字符最舒适）
- 字号：17-18px（阅读型比通常偏大 1px）
- 行高：1.7-1.8（比 UI 的 1.5 更松）
- 段间距：1.5em
- 字体：衬线体（长文阅读更舒适）或 思源宋体/Noto Serif SC
  · 中英混排：中文衬线 + 英文衬线（或中文黑体 + 英文 sans）
- 标题：text-wrap: balance（标题自动平衡换行）
- 正文：text-wrap: pretty（避免孤字行；Firefox 尚未支持，可渐进增强）
- 首段：可选首字下沉 initial-letter: 2（杂志感；Firefox 未支持，用 `@supports` 兜底或 `::first-letter { float: left; font-size: 3em; }`）

【Markdown 渲染样式（关键细节）】
- h1：text-3xl + font-bold + mt-12 mb-4
- h2：text-2xl + font-semibold + mt-10 mb-3 + 上方分隔线
- h3：text-xl + font-semibold + mt-8 mb-2
- p：text-base + leading-relaxed + mb-6
- ul/ol：pl-6 + 自定义 marker 颜色
- blockquote：border-l-4 品牌色 + pl-4 + italic + bg-muted/20
- code inline：px-1.5 py-0.5 bg-muted rounded text-sm font-mono
- code block：语法高亮 + 语言标签 + 复制按钮 + 行号
- table：border + 斑马纹 + 响应式横向滚动
- img：圆角 + caption（figcaption 灰小字居中）+ 点击放大
- hr：my-12 + 细线或装饰符号 ···
- a 链接：品牌色 + 下划线 + hover 加深

【目录 TOC（左侧 sticky）】
- 自动从 h2/h3 生成
- 当前阅读到的章节高亮（scroll spy）
- 点击平滑跳转（scroll-behavior: smooth）
- 隐藏在移动端或收进顶部折叠

【阅读进度】
- 顶部细进度条（2px 高，品牌色）
- scroll-driven animation 驱动

【预计阅读时间】
- 标题下："5 分钟阅读"（按 300 字/分钟）

【Meta 信息】
- 作者头像 + 名字 + 发布日期 + 阅读时间 + 标签
- 位置：标题下方或正文前

【交互】
- 选中文字 → 分享/高亮菜单（Medium 式）
- 文末：点赞/分享/收藏
- 评论（见第四节）
- 相关文章推荐

【暗色模式阅读】
- 背景不用纯黑（用 oklch(0.12 0 0) 或 #1a1a1a）
- 正文不用纯白（用 oklch(0.9 0 0) 或 #e0e0e0）
- 图片降亮度 10%（filter: brightness(0.9)）
- 代码块背景比正文背景略深

【性能】
- 图片 lazy + srcset + aspect-ratio 占位
- 代码高亮按需加载（见到代码块才加载 Prism/Shiki）
- 长文 content-visibility: auto（滚动外不渲染）
```

---

## 二、Feed / 信息流

```
【页面】内容 Feed（资讯/社交/短文）

【Feed 卡片类型（混排）】

1. 图文卡（最常见）
┌──────────┬───────┐
│ 标题（2行）│       │
│ 摘要（2行）│ 缩略图 │
│ 来源·时间  │       │
└──────────┴───────┘

2. 大图卡（重要内容）
┌─────────────────────┐
│  [大图]               │
│  标题                 │
│  摘要 · 来源 · 时间    │
└─────────────────────┘

3. 纯文字卡（社交短文）
┌─────────────────────┐
│ 👤 用户名 · 2h       │
│ 正文内容...           │
│ ❤️ 💬 ↗️              │
└─────────────────────┘

4. 视频卡
┌─────────────────────┐
│  [视频封面 + ▶]       │  ← 16:9
│  标题 · 3:24         │
└─────────────────────┘

【布局选择】
- 单列宽流（max-w-2xl）：阅读体验最佳（Medium/Twitter）
- 两列网格：信息密度高（Pinterest/小红书）
- 混排（大小卡交替）：有节奏感

【交互】
- 下拉刷新
- 无限滚动 + 保持位置
- 长按/右键 → 稍后再看/不感兴趣/举报
- 点赞动效（见 B74）

【个性化标签栏】
- 顶部横向 Tab："推荐 | 关注 | 热榜 | 科技 | 设计 | ..."
- 可滚动（左右箭头 或 横向 scroll），当前高亮 + 下划线
- 最后一个"+"：自定义频道顺序

【算法透明度（2026 趋势）】
- "基于你关注的 XX 推荐"
- "正在热门讨论"
- 不感兴趣 → "已减少此类推荐"
```

---

## 三、内容编辑器

```
【组件】富文本编辑器（Notion/语雀风格）

【结构】
┌─────────────────────────┐
│  封面图（可选，点击上传）   │
│  标题（h1，无边框直接输入） │
│  正文区（块编辑器）         │
│  · 段落                   │
│  · /命令菜单               │
│  · 拖拽手柄（左侧 hover）  │
└─────────────────────────┘

【块类型】
- 段落、标题(h1-h3)、列表(ul/ol/todo)、引用
- 代码块（语言选择 + 语法高亮）
- 图片/视频（拖拽上传/粘贴/URL）
- 表格、分栏、折叠块、数学公式
- 嵌入（YouTube/Twitter/Figma/CodeSandbox）

【Slash 命令菜单 /】
- 输入 "/" 弹出命令面板
- 搜索过滤 + 分组（基础/媒体/高级/嵌入）
- 键盘导航 + Enter 插入

【工具栏（选中文字时浮现）】
- 加粗/斜体/删除线/代码/链接/高亮颜色/对齐
- 不常驻顶部，选中时浮于文字上方

【拖拽排序】
- 每个块左侧 hover 显示拖拽把手（⋮⋮）
- 拖拽时半透明 + 插入线指示位置

【协作（可选）】
- 多人光标（彩色名字标签）
- 评论（选中文字 → 添加评论，右侧浮现）

【自动保存】
- 每次编辑后 1.5s 自动保存
- 顶部状态："已保存" / "保存中..." / "保存失败（重试）"

【响应式】
- 桌面：居中 max-w-3xl
- 移动：全宽 + 底部工具栏（块类型快捷按钮）

【prompt 措辞】
"编辑器用 Notion 风格块编辑器（Tiptap/ProseMirror），
 / 命令菜单，选中文字浮现格式栏，左侧拖拽排序，
 自动保存 + 顶部保存状态指示"
```

---

## 四、评论系统

```
【组件】评论区

【结构】
┌─────────────────────────┐
│ 评论 (128)               │
│                         │
│ ┌─ 写评论 ──────────────┐│
│ │ [头像] [输入框] [发布] ││
│ └───────────────────────┘│
│                         │
│ 👤 用户A · 2小时前        │
│ 评论内容...               │
│ ❤️ 12  💬 回复            │
│   └─ 👤 用户B · 1小时前   │
│      回复内容             │
│      ❤️ 3  💬 回复         │
│   └─ 👤 用户A · 30分钟前  │
│      @用户B 追加回复       │
│                         │
│ [加载更多评论]            │
└─────────────────────────┘

【交互】
- 回复嵌套（最深 3 级，再深平铺）
- 点击"回复" → 输入框聚焦 + @用户名 自动填入
- 发布后乐观更新（即时显示，后台确认）
- 点赞即时反馈
- 长评论折叠（>3 行 + "展开"）
- 排序：最新/最热
- 举报（下拉菜单）

【输入框】
- 支持 @ 提及（弹出用户列表）
- 支持 emoji 面板
- 支持图片（粘贴/选择）
- 未登录时点击输入框 → 引导登录
- Shift+Enter 换行，Enter 发布

【空态】
- 首条评论引导："成为第一个评论的人"

【审核】
- 敏感词 → 提示修改
- 审核中 → "您的评论正在审核"（仅本人可见）
```

---

## 五、搜索系统

```
【组件】全站搜索

【搜索入口】
- 顶部导航搜索图标/输入框 → 点击展开全屏搜索
- 或 ⌘K 快捷键 → Command 面板

【搜索页面布局】
┌─────────────────────────┐
│ [搜索框（大，居中）]       │
│ 热门搜索：tag tag tag    │
├─────────────────────────┤
│ "关键词" 的搜索结果 (128) │
│ Tab: 全部 | 文章 | 用户 | 标签│
│                         │
│ 结果列表...              │
│ 高亮匹配文字              │
│                         │
│ [加载更多]               │
└─────────────────────────┘

【搜索交互】
- 即时搜索（输入 300ms debounce 后出结果）
- 搜索建议（下拉补全）
- 搜索历史（可清除）
- 匹配文字高亮（标记色或 <mark>）
- 无结果："未找到 '{query}' 的结果" + 拼写建议 + 热门推荐

【筛选】
- 类型（文章/视频/用户/标签）
- 时间范围（最近一周/一月/一年）
- 排序（相关性/最新/最热）

【性能】
- debounce 300ms
- 取消前一次请求（AbortController）
- 结果骨架屏
- URL 同步（/search?q=xxx&type=article）
```

---

## 六、用户主页 / Profile

```
【页面】用户 Profile

【布局】
┌─────────────────────────┐
│  [封面图（banner）]       │
│  [头像]  用户名           │
│  简介（2 行）             │
│  关注 128 · 粉丝 1.2k    │
│  [关注] [消息]            │
├─────────────────────────┤
│ Tab: 文章 | 动态 | 收藏   │
├─────────────────────────┤
│  内容列表...              │
└─────────────────────────┘

【交互】
- 封面图 parallax 或滚动渐隐
- 头像：点击放大查看
- 关注按钮：未关注（outline）/ 已关注（filled）/ hover 变"取消关注"
- Tab 切换内容（保持滚动位置）
- 滚动时顶栏显示用户名 + 头像缩小（sticky header）

【自己的主页额外功能】
- [编辑资料] 按钮
- 数据统计（阅读量/获赞/收藏总数）
```

---

## 七、Prompt 模板（通用内容平台）

```
生成一个 {博客阅读页 / Feed 信息流 / 编辑器}，React + Tailwind + shadcn。

【文章阅读】
- 正文 max-w-[680px] 居中，字号 17px，行高 1.75，段间距 1.5em
- 字体：衬线或优质无衬线，标题 text-wrap: balance
- 左侧 sticky 目录（scroll spy 高亮当前章节）
- 顶部 2px 阅读进度条（scroll-driven）
- Markdown 样式：h2 带上方分隔线，代码块有语言标签+复制+行号，
  图片有 figcaption + 点击放大，引用有品牌色边
- 暗色：不纯黑（#1a1a1a），正文不纯白（#e0e0e0），图片 brightness(0.9)
- 文末：点赞/分享/收藏 + 评论区 + 相关推荐

【Feed 信息流】
- 单列宽流 max-w-2xl，卡片混排（图文/大图/纯文/视频）
- 顶部可滚动标签栏（推荐/关注/热榜/...）
- 下拉刷新 + 无限滚动 + 保持位置
- 点赞 spring 弹跳

【编辑器】
- 块编辑器（Tiptap/ProseMirror），/ 命令菜单，浮动格式栏
- 拖拽排序 + 自动保存 + 状态指示
- 响应式：桌面居中 max-w-3xl，移动端底部快捷工具栏

【评论】
- 嵌套最深 3 级，乐观更新，长评折叠
- 支持 @ 提及 + emoji + 图片

【无障碍】
- 正文结构正确（heading 层级 + landmark）
- 代码块有 aria-label "代码块，语言: TypeScript"
- 图片有 alt，装饰图 aria-hidden
- 目录导航 role="navigation" aria-label="文章目录"
```

---

## 八、检验清单

- [ ] 正文宽度 65-70ch，字号 ≥17px，行高 ≥1.7
- [ ] 标题 text-wrap: balance，正文 text-wrap: pretty
- [ ] 代码块有语法高亮 + 语言标签 + 复制按钮
- [ ] 图片有 aspect-ratio 占位 + lazy + alt + 可放大
- [ ] 有阅读进度条（顶部 2px）
- [ ] 有 sticky TOC（桌面）+ scroll spy
- [ ] Feed 有下拉刷新 + 无限滚动
- [ ] Feed 保持滚动位置（返回时不跳回顶部）
- [ ] 评论嵌套 ≤3 级
- [ ] 评论乐观更新（发布即显示）
- [ ] 搜索有 debounce + 匹配高亮 + 无结果引导
- [ ] 暗色阅读：非纯黑/纯白 + 图片降亮度
- [ ] 编辑器有自动保存 + 保存状态指示
- [ ] 编辑器有 / 命令菜单
- [ ] heading 层级正确（无跳级）
- [ ] 长内容 content-visibility: auto
