# B68 · 图标设计深度工艺

> 图标是界面里最小的东西,却最容易暴露"业余"。
> 差 0.5px 的线宽、不对齐的像素、混搭的风格——放大镜下全是破绽。
> 顶级图标系统有一套严格的**网格、光学、对齐**规则,像瑞士钟表一样精密。
> 这份文档拆的是:图标的网格系统、光学补偿、像素对齐、动态图标,以及如何让一套图标"像一家人"。

---

## 一、图标术语表

| 中文术语 | 英文术语 | 一句话含义 |
|---|---|---|
| 图标网格 | Icon Grid | 图标绘制的基准网格系统 |
| 实时区域 | Live Area | 图标内容的有效绘制区 |
| 描边区域 | Trim Area | 内容可延伸到的最大区 |
| 内边距 | Padding | 图标边缘的留白 |
| 关键形状 | Keyline Shapes | 网格中的圆/方/竖/横基准形 |
| 光学尺寸 | Optical Size | 不同尺寸单独绘制的图标 |
| 光学补偿 | Optical Correction | 依视觉调整而非数学对齐 |
| 像素对齐 | Pixel Snapping | 线条对齐像素网格保持锐利 |
| 线宽 | Stroke Width | 图标线条的粗细 |
| 线端 | Line Cap | 线条末端形状(圆/方/凸) |
| 线接 | Line Join | 线条转角形状(圆/斜切/尖) |
| 线性图标 | Outline / Line Icon | 只有轮廓线的图标 |
| 填充图标 | Filled / Solid Icon | 实心填充的图标 |
| 双色图标 | Duotone Icon | 两种色调/透明度的图标 |
| 图标字重 | Icon Weight | 图标的粗细变体(细/常规/粗) |
| 图标语义 | Icon Semantics | 图标传达的含义 |
| 多义图标 | Ambiguous Icon | 含义不明确的图标 |
| 图标+标签 | Icon with Label | 图标配文字说明 |
| 动态图标 | Animated Icon | 有状态变化动画的图标 |
| 图标变形 | Icon Morphing | 一个图标平滑变成另一个 |
| currentColor | currentColor | 图标继承文字颜色的技术 |

---

## 二、图标网格系统

### 2.1 标准 24 网格

```
┌─────────────────────────┐  24×24 画布
│ ┌─────────────────────┐ │
│ │                     │ │  padding 2px
│ │   ┌─────────────┐   │ │
│ │   │             │   │ │  Live Area 20×20
│ │   │   内容区     │   │ │
│ │   │             │   │ │
│ │   └─────────────┘   │ │
│ │                     │ │
│ └─────────────────────┘ │
└─────────────────────────┘
```

### 2.2 关键形状(Keyline)

```
四种基准形保证不同形状图标的视觉大小一致:

方形:  18×18(如"设置"齿轮外框)
圆形:  20直径(如"信息"圆圈)
竖矩形: 16宽×20高(如"手机""文档")
横矩形: 20宽×16高(如"图片""信封")

关键: 圆形要比方形大 2px,视觉上才一样大(光学补偿)
```

```css
/* SVG viewBox 统一 24 网格 */
.icon {
  width: 24px;
  height: 24px;
}
.icon svg {
  width: 100%;
  height: 100%;
}
```

### 2.3 多档尺寸(每档单独优化)

```
16px: UI 密集区(表格/输入框)— 线宽 1.5px,细节简化
20px: 标准 UI(按钮/菜单)    — 线宽 1.5-2px
24px: 常规图标(导航)         — 线宽 2px
32px: 强调图标(空态/引导)    — 线宽 2-2.5px
48px+: 特色图标(营销)        — 可用填充/彩色
```

**核心**:小尺寸不是大尺寸的等比缩小!16px 要**手动简化细节**,否则糊成一团。

```css
/* 不同尺寸用不同 SVG(而非缩放同一个) */
.icon-sm { /* 加载 icon-16.svg */ }
.icon-md { /* 加载 icon-24.svg */ }
```

---

## 三、线条工艺

### 3.1 线宽规范

```css
:root {
  --icon-stroke-thin:    1.5px;   /* 16-20px 图标 */
  --icon-stroke-regular: 2px;     /* 24px 图标 */
  --icon-stroke-bold:    2.5px;   /* 强调 */
}

.icon {
  stroke-width: var(--icon-stroke-regular);
  stroke: currentColor;
  fill: none;
}
```

**规律**:线宽随图标尺寸缩放,但要保持"视觉比例"。16px 用 2px 太粗,用 1.5px 刚好。

### 3.2 线端与线接

```css
.icon {
  stroke-linecap: round;    /* 圆头(友好) / butt(锐利) / square */
  stroke-linejoin: round;   /* 圆角(友好) / miter(尖) / bevel(斜切) */
}
```

| 线端/线接 | 气质 | 适用 |
|---|---|---|
| round + round | 友好/柔和/现代 | 消费级产品(最常用) |
| butt + miter | 锐利/专业/技术 | 工具/开发者产品 |
| square + bevel | 粗犷/工业 | 特殊风格 |

**铁律**:整套图标**只用一种**线端+线接组合,不混。

### 3.3 线性 vs 填充

```
线性(Outline):  轻盈/现代/未选中态
填充(Filled):   醒目/选中态/强调

经典用法: 未选中用线性,选中用填充(Tab Bar 标配)
```

```css
/* Tab 图标:选中切换填充 */
.tab-icon .outline { display: block; }
.tab-icon .filled  { display: none; }

.tab.active .tab-icon .outline { display: none; }
.tab.active .tab-icon .filled  { display: block; }
```

---

## 四、光学补偿(Optical Correction)

### 4.1 视觉居中 ≠ 几何居中

```
播放三角形: 几何居中会显得偏左
→ 向右偏移 1-2px 才视觉居中

原因: 三角形的"视觉重心"偏向尖端相反侧
```

```css
.icon-play {
  /* 三角形向右微调 */
  transform: translateX(1px);
}
```

### 4.2 圆形比方形大

```
同样"占满网格":
- 方形 18×18 视觉大小 = 圆形 20直径
- 圆形必须比方形大 10% 才看起来一样大
```

### 4.3 视觉重量均衡

```
一套图标里,复杂图标(如"设置"齿轮)和简单图标(如"加号")
放在一起,齿轮显得"重"。

补偿:
- 简单图标可以画大一点点(占满 live area)
- 复杂图标留多一点 padding(不要挤满)
- 目标: 所有图标"视觉重量"一致
```

### 4.4 尖角的视觉补偿

```
尖锐的角(如星星尖端)在小尺寸会"消失"
→ 尖角要略微加圆(0.5-1px 圆角)保证可见
```

---

## 五、像素对齐(Pixel Snapping)

### 5.1 为什么图标会糊

```
问题: 1px 的线画在 x=10.5 的位置 → 跨越两个像素 → 渲染成 2px 的灰线(糊)
解法: 线条对齐到整数或半像素网格
```

### 5.2 SVG 像素对齐规则

```
线宽 1px(奇数): 线的中心放在 x.5(如 10.5),边缘落在整数
线宽 2px(偶数): 线的中心放在整数(如 10),边缘落在整数

例:
<line x1="10.5" y1="4" x2="10.5" y2="20" stroke-width="1"/>  ✅ 锐利
<line x1="10" y1="4" x2="10" y2="20" stroke-width="1"/>      ❌ 糊
```

```css
/* CSS 层面保持锐利 */
.icon svg {
  shape-rendering: geometricPrecision;  /* 或 crispEdges 保持直线锐利 */
}
```

### 5.3 缩放时的对齐

```css
/* 图标缩放到非整数尺寸会糊 */
/* ❌ */ .icon { width: 17px; }
/* ✅ */ .icon { width: 16px; }  /* 用设计好的档位 */
```

---

## 六、图标一致性("像一家人")

### 6.1 一致性 8 要素

```
□ 1. 网格统一(都是 24 网格)
□ 2. 线宽统一(都是 2px)
□ 3. 线端统一(都是 round)
□ 4. 线接统一(都是 round)
□ 5. 圆角半径统一(内部圆角都是 2px)
□ 6. 视觉重量统一(复杂和简单图标看起来一样重)
□ 7. 隐喻风格统一(都写实 / 都抽象)
□ 8. 细节密度统一(不能有的极简有的超详细)
```

### 6.2 隐喻一致性

```
❌ 混用:
- "首页" 用房子(写实)
- "设置" 用齿轮(写实)
- "搜索" 用抽象放大镜(抽象)
→ 隐喻层次不一致

✅ 统一:全部用同一抽象层次的隐喻
```

### 6.3 用图标库还是自绘

```
用现成图标库(推荐):
- Lucide / Heroicons / Phosphor / Radix Icons / Tabler
- 优点: 已经保证一致性,数量全
- 关键: 只用一套,不混用!

自绘:
- 只有品牌需要独特图标语言时
- 必须严格遵守网格/线宽/一致性规则
```

---

## 七、动态图标

### 7.1 状态切换动画

```css
/* 汉堡 → X */
.menu-icon line {
  transition: transform 300ms cubic-bezier(0.4, 0, 0.2, 1);
  transform-origin: center;
}
.menu-icon.open line:nth-child(1) { transform: translateY(6px) rotate(45deg); }
.menu-icon.open line:nth-child(2) { opacity: 0; }
.menu-icon.open line:nth-child(3) { transform: translateY(-6px) rotate(-45deg); }
```

### 7.2 路径绘制动画

```css
/* 勾号绘制 */
.check-icon path {
  stroke-dasharray: 24;
  stroke-dashoffset: 24;
  transition: stroke-dashoffset 400ms cubic-bezier(0.65, 0, 0.35, 1);
}
.check-icon.done path {
  stroke-dashoffset: 0;   /* 从无到有"画"出来 */
}
```

### 7.3 图标变形(Morphing)

```css
/* SVG path 之间的平滑变形(需要相同点数) */
.morph-icon path {
  transition: d 300ms cubic-bezier(0.4, 0, 0.2, 1);  /* d 属性可动画(2025+) */
}
.morph-icon.state-b path {
  d: path("M...新形状...");
}
```

### 7.4 微交互反馈

```css
/* hover 时图标微动 */
.icon-btn:hover .icon-bell {
  animation: bell-ring 500ms ease;
}
@keyframes bell-ring {
  0%, 100% { transform: rotate(0); }
  20%      { transform: rotate(12deg); }
  40%      { transform: rotate(-10deg); }
  60%      { transform: rotate(6deg); }
  80%      { transform: rotate(-4deg); }
}

/* 心形点赞 */
.like-icon.active {
  animation: heart-pop 400ms cubic-bezier(0.34, 1.56, 0.64, 1);
}
@keyframes heart-pop {
  0%   { transform: scale(1); }
  50%  { transform: scale(1.3); }
  100% { transform: scale(1); }
}
```

---

## 八、图标可访问性

### 8.1 图标 + 标签

```
铁律: 关键功能的图标必须配文字(至少 aria-label)
纯图标歧义率极高(汉堡菜单/更多/分享 常被误解)
```

```html
<!-- 装饰性图标:aria-hidden -->
<button>
  <svg aria-hidden="true">...</svg>
  保存
</button>

<!-- 纯图标按钮:必须 aria-label -->
<button aria-label="删除">
  <svg aria-hidden="true">...</svg>
</button>

<!-- 或用视觉隐藏文字 -->
<button>
  <svg aria-hidden="true">...</svg>
  <span class="sr-only">删除</span>
</button>
```

### 8.2 图标对比度

```
图标属于"非文字元素",WCAG 要求对比度 ≥ 3:1
纯装饰图标不受此限,但功能图标必须达标
```

### 8.3 触控目标

```css
/* 图标本身 24px,但点击区要 44px */
.icon-btn {
  width: 44px;
  height: 44px;
  display: grid;
  place-items: center;
}
.icon-btn .icon {
  width: 24px;
  height: 24px;
}
```

---

## 九、Prompt 模板

```
【图标设计工艺】

═══ 网格系统 ═══
基准网格: 24×24(padding 2px, live area 20×20)
关键形状: 方形 18 / 圆形 20直径 / 竖矩形 16×20 / 横矩形 20×16
多尺寸: 16/20/24/32/48,每档单独优化(不等比缩放)

═══ 线条规范 ═══
线宽: 16-20px 图标用 1.5px, 24px 用 2px
线端: round(友好) 或 butt(专业)—— 全套统一
线接: round 或 miter —— 全套统一
风格: 线性(默认) / 填充(选中态) —— 不混用

═══ 光学补偿 ═══
- 播放三角形向右偏 1-2px
- 圆形比方形大 10%(视觉等大)
- 复杂图标留多点 padding,简单图标画满(视觉重量均衡)
- 尖角加 0.5-1px 圆角(小尺寸保可见)

═══ 像素对齐 ═══
- 1px 线中心放 x.5,2px 线中心放整数
- shape-rendering: geometricPrecision
- 只用设计档位尺寸(不缩放到 17px 这种)

═══ 一致性 8 要素 ═══
网格/线宽/线端/线接/圆角/视觉重量/隐喻层次/细节密度 全部统一
推荐: 用一套图标库(Lucide/Phosphor/Heroicons),不混用

═══ 颜色 ═══
stroke: currentColor(继承文字色,自动适配主题)
fill: none(线性) 或 currentColor(填充)

═══ 动态图标 ═══
- 汉堡 → X: line rotate + translate 300ms
- 勾号绘制: stroke-dasharray + dashoffset 400ms
- 点赞: scale 1→1.3→1 spring
- 变形: d 属性过渡

═══ 可访问性 ═══
- 纯图标按钮必须 aria-label
- 装饰图标 aria-hidden="true"
- 功能图标对比度 ≥ 3:1
- 点击区 44×44(图标 24)
```

---

## 十、踩坑指南

### 坑1:小尺寸图标糊成一团
**症状**:24px 图标缩到 16px,细节挤在一起。
**解法**:16px 单独绘制,简化细节。

### 坑2:线条不对齐像素
**症状**:1px 线渲染成 2px 灰线。
**解法**:1px 线中心放 x.5 位置。

### 坑3:混用图标库
**症状**:一部分 Heroicons 一部分 Font Awesome,风格打架。
**解法**:全站只用一套图标库。

### 坑4:线性填充混用
**症状**:导航一些线性一些填充,不统一。
**解法**:统一线性,选中态才用填充。

### 坑5:纯图标无标签
**症状**:用户不认识某个图标含义。
**解法**:关键图标配文字或 aria-label + tooltip。

### 坑6:图标没用 currentColor
**症状**:深色模式下图标还是黑色,看不见。
**解法**:`stroke: currentColor` 继承文字色。

### 坑7:视觉重量不均
**症状**:齿轮图标看起来比加号"重"很多。
**解法**:复杂图标留多点 padding 减轻,简单图标画满。

### 坑8:播放按钮偏左
**症状**:三角形几何居中但看起来偏。
**解法**:向右偏 1-2px 光学补偿。

---

## 十一、谁在用(图标典范)

| 图标库/产品 | 特点 | 值得学的 |
|---|---|---|
| **Lucide** | 开源,一致性极高 | 24 网格 + 2px + round,可变字重 |
| **Phosphor** | 6 种字重变体 | 从 thin 到 fill 的完整字重系统 |
| **Heroicons** | Tailwind 官方 | outline/solid/mini 三套配套 |
| **SF Symbols** | Apple 系统 | 与字体基线对齐,光学尺寸自动 |
| **Radix Icons** | 15×15 精密网格 | 极致的像素对齐 |
| **Material Symbols** | 可变字体图标 | wght/fill/grad 轴动画 |
| **Duolingo** | 动态图标 | 图标微交互丰富,有性格 |

---

## 检验清单

- [ ] 全套图标用统一网格(如 24×24)
- [ ] 线宽统一,随尺寸档位调整(16px 用 1.5px)
- [ ] 线端/线接全套统一(如都 round)
- [ ] 线性/填充不混用(选中态才切填充)
- [ ] 多尺寸单独优化(16px 简化,不是缩放)
- [ ] 播放/箭头等做了光学补偿
- [ ] 圆形比方形大(视觉等大)
- [ ] 复杂/简单图标视觉重量一致
- [ ] 线条对齐像素(1px 线放 x.5)
- [ ] stroke: currentColor(适配主题)
- [ ] 纯图标按钮有 aria-label
- [ ] 装饰图标 aria-hidden
- [ ] 功能图标对比度 ≥ 3:1
- [ ] 图标点击区 ≥ 44px
- [ ] 只用一套图标库(不混搭)
