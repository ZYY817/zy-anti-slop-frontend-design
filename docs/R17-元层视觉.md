# R17 · 元层视觉（提示词库 Q 组）

> 定位一句话：**把"辅助性的东西"本身做成奇观。** loading、404、调试面板、光标、空状态、控制台——顶级作品让这些"边角料"也成为记忆点。
> 覆盖层：L0 意 + L5 髓（本组以 L5 髓层落地为主，少数条目上探到 L0 意层）。
> 条目卡格式：`**中文名（English Name）** · L 层级 · 档位（基础 / 进阶 / 电影级）· 成本（低 / 中 / 高）`，下挂「视觉收益 / 关键约束 / 同族拓展 / 提示词片段」四类子项（无则省略）。
> 来源说明：本文融合自外部「视觉提示词库」的 Q 组（元层视觉·工具即作品），条目、技术参数、英文术语原样保留；库内交叉引用已按组别映射改写为本库 `R{n}·{原引用}` 形式（映射总表见 `R0`），避免与本库 A/B/C/D 板块字母冲突。
> 判断：**本组投入产出比最高**——几乎全是"低成本高印象"的项目。主视觉做到 90 分很难，但把 loading 做出彩只需要一点巧思。

---

## 使用说明

Q 组是"最容易被忽视、最能体现用心"的一组。普通作品把 loading/404/调试面板当作功能凑合做；顶级作品把它们当作**展示才华的舞台**。

一个反直觉的事实：**用户对"边角料"的记忆往往比对主内容更深**——因为没人期待 404 页好看，所以做好了就是惊喜（见 `R2·K6` 峰终定律的应用）。

判断价值：这一组几乎全是"低成本高印象"的项目。主视觉做到 90 分很难，但把 loading 做出彩只需要一点巧思。

```
Q1 加载态即体验          Q4 光标与指针作为角色
Q2 错误与边缘态          Q5 调试视图与开发者彩蛋
Q3 空态与引导            Q6 元信息与浏览器边界
```

---

## Q1 加载态即体验（Loading as Experience）

- **主题化 Loader（Themed Loader）** · L0 · 进阶 · 成本:低
  - 视觉收益：loading 是作品主题的极简预演，而非通用 spinner
  - 参考做法：黑洞主题 → 事件视界收缩｜城市主题 → 天际线逐栋点亮｜数据主题 → 数字滚动到位｜排版主题 → 字母逐个落位
  - 关键约束："Loading 旋转 spinner"被 `B82`/`B44` 列为高风险默认值，建议主动偏离，但不作硬性禁令——两份清单都是参考清单，不是硬性铁律，按项目自行取舍
  - 同族拓展：见 `R11·F6` 有意义的加载态、`R1·G8` 视觉概念、真实进度
  - 提示词片段：`loader 是主题的极简预演（不是通用 spinner）；显示真实资源进度；加载完成动画顺势过渡到主内容`

- **进度的诚实（Honest Progress）** · L5 · 基础 · 成本:低
  - 视觉收益：真实进度比假进度条更可信；长加载给文字说明
  - 同族拓展：真实资源进度、分阶段说明（"加载着色器…编译中…就绪"）、避免卡在 99%
  - 提示词片段：`进度条反映真实加载（不是定时假进度）；>3s 时显示当前阶段文字；绝不卡在 99%`

- **加载即过渡（Load-to-Content Transition）** · L5 · 进阶 · 成本:低
  - 视觉收益：loader 不是"消失"，而是"变成"主内容——连续而非切换
  - 同族拓展：见 `R11·F3` 共享元素转场、loader 元素形变为首屏元素、mask 揭示
  - 提示词片段：`加载完成时 loader 元素形变/延展为首屏主元素（连续过渡），而非淡出后另起`

- **渐进式内容（Progressive Content）** · L5 · 进阶 · 成本:中
  - 视觉收益：见 `R11·F6`/`R6·D8`。先出骨架/低清，逐步精修
  - 提示词片段：`首屏骨架屏 + 低清占位先出，资源到位后渐进精修；感知速度优先`

- **等待中的趣味（Delight While Waiting）** · L0 · 电影级 · 成本:低
  - 视觉收益：长等待时给可交互的小玩意（可拖的粒子、迷你游戏），化焦虑为乐趣
  - 同族拓展：interactive loader、彩蛋、见 `R17·Q5`
  - 提示词片段：`若加载 >5s，loader 提供轻交互（如可拨动的粒子/可拖的元素），把等待变成玩耍`

---

## Q2 错误与边缘态（Errors & Edge States）

- **404 作为奇观（404 as Showcase）** · L0 · 电影级 · 成本:低
  - 视觉收益：没人期待 404 好看，所以做好了就是惊喜与记忆点
  - 同族拓展：主题化 404、可交互 404、幽默文案、返回引导、迷你彩蛋
  - 提示词片段：`404 页做成主题化的小场景（不是默认报错）；一句有个性的文案 + 明确返回路径 + 一个小交互彩蛋`

- **优雅降级的视觉（Graceful Degradation Visuals）** · L5 · 基础 · 成本:低
  - 视觉收益：WebGL 失败/网络差/低端设备时，退化版本仍然好看（见 `R6·D8`）
  - 同族拓展：静态海报回退、CSS-only 版本、`<noscript>` 的用心、见 `R6·D8` context 恢复
  - 提示词片段：`WebGL 不可用时显示精心设计的静态海报（不是空白）；noscript 也给一句话与静态视觉`

- **错误态的情绪管理（Error Emotion）** · L0 · 进阶 · 成本:低
  - 视觉收益：错误不该让用户感到被指责或焦虑；视觉与文案要"共情 + 给出路"
  - 同族拓展：见 `R11·F7` 空态边缘态、非红色警报、人话文案、恢复动作
  - 提示词片段：`错误态用共情文案（不指责用户）+ 明确的下一步；视觉不用刺眼红警，用柔和的引导色`

- **离线与弱网（Offline & Slow）** · L5 · 进阶 · 成本:低
  - 视觉收益：断网时有明确且不慌的提示，恢复时平滑
  - 同族拓展：offline banner、`navigator.onLine`、Service Worker 离线页、重试机制
  - 提示词片段：`离线时显示不打断的横幅提示；恢复网络平滑消失；关键操作提供重试`

- **超时与卡顿的坦诚（Timeout Honesty）** · L5 · 基础 · 成本:低
  - 视觉收益：慢的时候告诉用户"在处理"，而不是假装没事或卡死
  - 提示词片段：`操作 >1s 显示处理中状态；>5s 给可取消选项与说明；绝不静默卡死`

---

## Q3 空态与引导（Empty States & Onboarding）

- **空态设计（Empty State Design）** · L0 · 进阶 · 成本:低
  - 视觉收益：空列表/无结果/首次使用是"教用户 + 展示个性"的机会，不是留白凑合
  - 同族拓展：见 `R11·F7` 空态、插画/主题图形、引导下一步、避免通用 stock 插画
  - 提示词片段：`空态给主题化视觉 + 一句人话说明 + 一个明确动作；不用通用 stock 插画；无结果时给建议`

- **首次引导（First-Run Onboarding）** · L5 · 进阶 · 成本:低
  - 视觉收益：第一次访问的一次性提示（快捷键、可交互暗示），之后不再打扰
  - 同族拓展：见 `R11·F7` 快捷键可发现性、coach marks、一次性 hint（localStorage 记住）、渐进披露
  - 提示词片段：`首次访问显示一次性引导提示（4s 后淡出或点击关闭）；localStorage 记住不再显示；提示可交互的入口`

- **交互可发现性（Discoverability Hints）** · L5 · 进阶 · 成本:低
  - 视觉收益：暗示"这里可以拖/点/滚"，避免用户不知道能交互
  - 同族拓展：微动效暗示（元素轻微呼吸/漂移）、hover 前的静态提示、光标变化（见 `R17·Q4`）、滚动提示箭头
  - 提示词片段：`可交互元素用微动效暗示可操作性（轻微呼吸/漂移）；页面底部若可滚动给下滑提示`

- **进度与成就（Progress & Reward）** · L5 · 进阶 · 成本:低
  - 视觉收益：探索进度可视化、发现彩蛋的奖励，激励深入
  - 同族拓展：见 `R11·F7` 进度、探索度指示、解锁反馈、见 `R2·K6` 峰终
  - 提示词片段：`长内容显示探索进度；发现隐藏内容给愉悦反馈（微动画+音效，见 R16·P2）`

---

## Q4 光标与指针作为角色（Cursor as Character）

- **自定义光标（Custom Cursor）** · L5 · 进阶 · 成本:低
  - 视觉收益：见 `R11·F1`。光标是"这不是普通网页"的第一信号
  - 关键约束：必须保留可用性反馈；触屏禁用（见 `R11·F5`）
  - 提示词片段：`自定义光标（双层：即时内点 + 阻尼外环）；hover 可交互元素时变形/放大/显示动作词；触屏隐藏`

- **上下文光标（Contextual Cursor）** · L5 · 电影级 · 成本:低
  - 视觉收益：光标根据悬停对象变化——"拖拽""查看""播放""外链"各有形态
  - 同族拓展：cursor morph、magnetic（见 `R11·F1`）、显示文字标签（"drag" / "view"）、方向指示
  - 提示词片段：`光标按悬停对象变形并显示动作词（拖拽区显示"drag"、图片显示"view"、外链显示箭头）`

- **光标拖尾与轨迹（Cursor Trail）** · L5 · 进阶 · 成本:中
  - 视觉收益：见 `R11·F1`/`R4·B7`。移动留下痕迹，画面更有生命
  - 提示词片段：`光标拖尾用衰减轨迹或粒子（见 R11·F1 / R4·B7）；克制，不喧宾夺主`

- **光标即工具（Cursor as Instrument）** · L0 · 电影级 · 成本:中
  - 视觉收益：光标本身参与视觉——扰动流体、吸引粒子、揭示隐藏内容（探照灯）
  - 同族拓展：见 `R11·F1` 指针驱动、`R12·N4` 流体交互、`R9·H4` 径向遮罩揭示
  - 提示词片段：`让光标参与主视觉：扰动背景流体/吸引粒子/作为探照灯揭示隐藏层（见 R11·F1 / R12·N4 / R9·H4）`

---

## Q5 调试视图与开发者彩蛋（Debug Views & Dev Easter Eggs）

**给懂行的人看的东西。** 作品集与个人展示类页面尤其要做 `R17·Q5`——审阅者往往是工程师。

- **可切换调试视图（Toggleable Debug Views）** · L5 · 进阶 · 成本:低
  - 视觉收益：数字键切换查看法线/深度/步数/线框/各 pass——展示"我知道我在做什么"
  - 同族拓展：0-9 切换视图、debug uniform、可视化中间缓冲、性能叠层（FPS/GPU 时间）
  - 提示词片段：`数字键 0-9 切换调试视图（法线/深度/步数热力图/各后期 pass/线框）；本身也做得好看`

- **性能叠层（Performance HUD）** · L5 · 进阶 · 成本:低
  - 视觉收益：常驻的精致小 HUD 显示 FPS/绘制数/内存——技术感与透明度
  - 同族拓展：stats.js 的美化版、迷你图表、见 `R8·J6` 数据可视化、常驻微 HUD
  - 提示词片段：`可选性能 HUD（FPS/draw call/GPU ms）做成精致迷你图表，风格与主视觉一致，非默认 stats.js`

- **控制台彩蛋（Console Easter Egg）** · L0 · 进阶 · 成本:低
  - 视觉收益：打开 DevTools 的人（=技术审阅者）会看到 ASCII art / 联系方式 / 隐藏消息
  - 同族拓展：`console.log` ASCII 艺术、样式化 console（`%c`）、联系/署名彩蛋、`console.table` 数据
  - 提示词片段：`控制台打印样式化的 ASCII logo + 一句联系/署名信息（用 %c 样式化）；给打开 DevTools 的技术人惊喜`

- **参数面板（Parameter Panel）** · L5 · 进阶 · 成本:低
  - 视觉收益：暴露可调参数让人玩，展示系统的可控性（参数数量按项目复杂度定，十几到几十项都常见）
  - 同族拓展：tweakpane / lil-gui 的美化、参数分组、预设、URL 同步（见 `R6·D8`）
  - 提示词片段：`提供参数面板（美化的 tweakpane），分组暴露关键参数 + 预设 + URL 同步；面板本身符合设计语言`

- **秘密模式与快捷键（Secret Modes）** · L0 · 电影级 · 成本:低
  - 视觉收益：Konami code / 特定快捷键解锁隐藏模式或视觉，深度探索的奖励
  - 同族拓展：Konami code、隐藏快捷键、彩蛋模式、见 `R17·Q3` 成就
  - 提示词片段：`加隐藏快捷键/Konami code 解锁一个隐藏视觉模式或彩蛋；帮助面板(?)里不明说，留给探索者`

- **源码的用心（Crafted Source）** · L0 · 进阶 · 成本:低
  - 视觉收益：查看源码的人（技术审阅者）看到整洁的代码、有意的注释、HTML 里的隐藏消息
  - 同族拓展：HTML 注释彩蛋、语义化标签、整洁结构、`<meta>` 里的巧思
  - 提示词片段：`HTML 结构整洁语义化；关键处有说明注释；可在注释里藏一句给读源码者的话`

---

## Q6 元信息与浏览器边界（Meta & Browser Chrome）

- **社交分享卡（OG / Social Cards）** · L1 · 基础 · 成本:低
  - 视觉收益：作品被分享时的预览图是"第二张脸"，多数人忽视
  - 同族拓展：`og:image`（1200×630）、`twitter:card`、动态生成 OG 图、见 `R2·K5` 缩略图测试
  - 提示词片段：`精心设计 OG 分享图（1200×630，通过缩略图测试）；标题/描述用心写；考虑动态生成`

- **浏览器主题色与图标（Theme Color & Favicon）** · L5 · 基础 · 成本:低
  - 视觉收益：见 `R9·H7`。移动端浏览器栏染色、完整 favicon 套件、动态 favicon
  - 同族拓展：`theme-color`（深浅两版）、favicon 全尺寸、动态 favicon（加载时旋转/进度）、apple-touch-icon
  - 提示词片段：`theme-color 匹配主色（深浅两版）；favicon 全套；可做动态 favicon（加载进度/状态指示）`

- **标签页标题的活用（Tab Title Play）** · L5 · 进阶 · 成本:低
  - 视觉收益：切走标签页时改标题（"回来看看~"）、标题显示进度/状态
  - 同族拓展：`document.title` 动态、blur 时改标题、标题彩蛋、避免滥用（招人烦要克制）
  - 提示词片段：`可在切走标签页时改标题为一句召回文案；克制使用，不闪烁不滥用`

- **PWA 与安装体验（PWA / Install）** · L5 · 进阶 · 成本:中
  - 视觉收益：可安装为应用，启动画面/图标/独立窗口都是品牌触点
  - 同族拓展：manifest、splash screen、maskable icon、独立显示模式、见 `R11·F6`
  - 提示词片段：`配 PWA manifest（图标/启动画面/主题色）；启动画面延续主视觉；maskable icon 适配各平台`

- **滚动与地址栏行为（Scroll & Chrome Behavior）** · L5 · 基础 · 成本:低
  - 视觉收益：见 `R9·H7`/`R11·F5`。overscroll、地址栏收起、全屏行为的处理
  - 同族拓展：`overscroll-behavior`、`100dvh`（见 `R9·H9`）、fullscreen API、`viewport-fit=cover`
  - 提示词片段：`overscroll-behavior 防橡皮筋穿透；用 100dvh 适配地址栏；可提供全屏按钮增强沉浸`

- **打印与导出（Print & Export）** · L5 · 进阶 · 成本:低
  - 视觉收益：见 `R9·H7`。作品集/简历打印或存 PDF 的样式；截图/分享功能
  - 同族拓展：`@media print`、截图接口（见 `R6·D8` 的 `?shot=1`）、导出为图片/视频
  - 提示词片段：`加 @media print 样式；提供"保存为图片"功能（canvas toBlob）；截图接口见 R6·D8`

---

## Q 组 · 组合速查

| 想要的效果 | 组合 |
|---|---|
| 令人惊喜的加载 | Q1 主题化 loader + Q1 加载即过渡 + Q1 等待趣味 |
| 值得截图的 404 | Q2 404 奇观 + Q2 错误情绪 + Q4 上下文光标 |
| 技术审阅者的惊喜 | Q5 调试视图 + Q5 控制台彩蛋 + Q5 参数面板 + Q5 源码用心 |
| 光标成为记忆点 | Q4 自定义 + Q4 上下文 + Q4 光标即工具 |
| 分享时的第二张脸 | Q6 OG 卡 + Q6 theme-color + Q6 favicon |
| 完整的"边角料"打磨 | Q1 loading + Q2 404/错误 + Q3 空态 + Q6 元信息 |

**常见坑**：
- loading 是通用 spinner → Q1 应主题化（"Loading 旋转 spinner"被 `B82`/`B44` 列为高风险默认值，建议主动偏离，但不作硬性禁令）
- 进度条卡在 99% / 假进度 → Q1 要诚实进度
- 404 是浏览器默认报错 → Q2 是被浪费的机会
- WebGL 失败就白屏 → Q2 缺静态海报回退
- 空列表就一片空白 → Q3 缺空态设计
- 用户不知道能交互 → Q3 缺可发现性暗示
- 触屏上自定义光标卡住 → Q4 需禁用（见 `R11·F5`）
- 参数面板很丑（默认 dat.gui） → Q5 要符合设计语言
- 分享出去预览图是空白/丑 → Q6 缺 OG 图
- 移动端浏览器栏颜色不搭 → Q6 缺 theme-color

> **Q 组心法**：这是**投入产出比最高的一组**。主视觉从 85 分到 90 分要花很大力气，但一个惊喜的 404、一个主题化 loader、一句控制台彩蛋，几乎零成本就能制造"这个人很用心"的强烈印象。**作品集与个人展示类页面尤其要做 `R17·Q5`**——审你的人大概率会打开 DevTools。

---

## 与库内衔接

| 本文小节 | 目标库相关文档 | 关系 |
|---|---|---|
| Q1 加载态即体验（进度诚实 / 渐进式内容） | `B73` 状态体验与感知性能工艺 | 地基 |
| Q1 加载态即体验（主题化 loader / 加载即过渡 / 等待趣味） | `V13` 加载与微交互效果 | 扩展 |
| Q2 错误与边缘态（404 / 优雅降级 / 离线 / 超时） | `B14` 边界情况美学 | 地基 |
| Q2 错误与边缘态（错误情绪管理的文案侧） | `C5` 微文案 | 扩展 |
| Q3 空态与引导（空态设计 / 无结果） | `B52` 空状态设计大全 | 地基 |
| Q3 进度与成就 + Q5 秘密模式与彩蛋 | `B47` 情感设计与产品生命周期 | 扩展 |
| Q4 光标与指针作为角色（全小节） | `V9` 悬浮与光标效果 | 地基 |
| Q5 调试视图与开发者彩蛋（风格一致性要求） | `B16` 风格谱系、`E1` 趋势与竞品参考 | 扩展 |
| Q6 元信息与浏览器边界（OG / theme-color / PWA / print） | `B14` 边界情况美学、`B73` 状态体验与感知性能工艺 | 扩展 |
| 全组「常见坑」清单 | `B82` 反 AI 味、`B44` 视觉禁忌 | 验证（参考清单，不是硬性铁律，按项目自行取舍） |
| 条目取舍与维度覆盖自查 | `B15` 全维度清单 | 验证 |

---

## 参考资料

本文融合自外部「视觉提示词库」的 **Q 组：元层视觉（工具即作品）**，源版本 v1.x，日期 2026-08-16。

- 条目结构、L 层级标注、档位（基础/进阶/电影级）、成本标注、同族拓展与提示词片段均忠实迁移，未压缩、未删条目。
- 技术术语与参数值（如 `og:image` 1200×630、`>3s`/`>5s` 阈值、`100dvh`、`overscroll-behavior`、`theme-color`、Konami code、tweakpane / lil-gui / stats.js、`canvas toBlob` 等）来自该库原始调研，未新增未经验证的数据。
- 库内交叉引用已按提示词库组别 → 本库 R 编号的映射整体改写（映射总表见 `R0`），源库的项目专属示例已替换为通用表述。
