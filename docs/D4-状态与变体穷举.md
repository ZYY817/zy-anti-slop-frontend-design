# D4 · 状态与变体穷举

> 状态穷举是 AI 生成 UI 最常缺失的部分。这里列全，直接抄进 prompt。

---

## 通用交互状态

任何可交互组件都要覆盖：

```
✅ default    默认态
✅ hover      鼠标悬浮
✅ focus      焦点（键盘）
✅ focus-visible  仅键盘焦点显示
✅ active/pressed 按下
✅ disabled   禁用
⚙ loading    加载中
⚙ error      错误
⚙ success    成功
⚙ selected   选中
⚙ indeterminate 部分选中
⚙ readonly   只读
⚙ dragging   拖拽中
⚙ empty      空状态
```

---

## 按钮（Button）

### 变体（Variant）
- primary — 主操作
- secondary — 次要
- outline — 边框
- ghost — 透明底
- link — 链接样式
- destructive — 危险
- icon-only — 纯图标

### 尺寸（Size）
- xs (24px高)
- sm (32px高)
- md (40px高)
- lg (48px高)
- xl (56px高)

### 状态
```
default / hover / active / focus-visible /
disabled / loading / with-icon-left / with-icon-right /
icon-only / full-width
```

### 完整矩阵示例
```
Button.primary.md.default
Button.primary.md.hover     → bg 加深 100
Button.primary.md.active    → bg 加深 200 + scale(0.97)
Button.primary.md.focus     → outline 2px offset 2px
Button.primary.md.disabled  → opacity 0.5, cursor not-allowed
Button.primary.md.loading   → spinner 替代文字, disabled 交互
```

---

## 输入框（Input）

### 状态
```
empty       # 无内容，显示 placeholder
filled      # 有内容
focus       # 焦点，边框高亮
error       # 校验失败，红色边框 + 错误文字
success     # 校验通过（可选，绿色）
disabled    # 禁用，灰色背景
readonly    # 只读，无 focus 效果
loading     # 后台验证中
```

### 变体
```
default outline
filled (灰底)
underline (仅底部线)
with-prefix-icon
with-suffix-icon
with-clear-button
with-password-toggle
with-character-counter
```

### 尺寸
```
sm (32h) / md (40h) / lg (48h)
```

---

## 复选框（Checkbox）

### 状态
```
unchecked   # 空
checked     # 选中
indeterminate # 部分选中（父级）
disabled-unchecked
disabled-checked
focus
error       # 未勾选但必选
```

### 变体
```
default
with-label
with-description
card-style (整卡可点)
```

---

## 单选按钮（Radio）

### 状态
```
unselected
selected
disabled-unselected
disabled-selected
focus
```

### 变体
```
default
button-group (按钮组样式)
card-style
segmented (分段控件)
```

---

## 开关（Switch）

### 状态
```
off
on
disabled-off
disabled-on
focus
loading (切换中)
```

---

## 下拉选择（Select）

### 状态
```
closed-empty
closed-filled
open
loading (选项加载中)
disabled
error
searching (可搜索)
no-results (搜索无结果)
```

### 变体
```
single-select
multi-select (chips 显示)
grouped
creatable
async (异步加载)
```

---

## 卡片（Card）

### 状态
```
default
hover (可点击时: shadow 增强 + translateY(-2px))
active (按下)
selected (选中态)
loading (skeleton)
disabled
```

### 变体
```
flat (无阴影)
elevated (有阴影)
outlined (仅边框)
filled (背景色)
interactive (可点击)
horizontal / vertical
with-media
with-actions
```

---

## Modal / Dialog

### 状态
```
closed
opening (入场动画中)
open
closing (出场动画中)
```

### 变体
```
alert (仅确认)
form (含表单)
confirmation (二次确认)
full-screen (移动端全屏)
drawer (侧边抽屉)
sheet (底部弹层)
```

---

## Toast / 通知

### 变体（按状态）
```
default (中性)
success (成功, 绿)
warning (警告, 黄)
error (错误, 红)
info (信息, 蓝)
loading (加载, 带 spinner)
```

### 变体（按行为）
```
transient (3-5s 自动消失)
persistent (需手动关闭)
with-action (含按钮如"撤销")
```

### 位置
```
top-left / top-center / top-right
bottom-left / bottom-center / bottom-right
```

---

## Tab 标签页

### 每个 Tab 状态
```
default
hover
active (当前选中)
disabled
focus
```

### 变体
```
line (下划线)
enclosed (包围)
soft-rounded (软圆角)
pill (胶囊)
segmented (分段)
scrollable (可滚动)
```

---

## Menu 菜单项

### 状态
```
default
hover
focus (键盘)
selected (当前)
disabled
danger (危险操作)
with-icon
with-shortcut
with-submenu
```

---

## Tag / Chip 标签

### 变体
```
outline / solid / soft
neutral / primary / success / warning / danger / info
```

### 状态
```
default
hover (可点击)
disabled
closable (带 X)
with-icon
with-avatar
```

---

## Avatar 头像

### 状态
```
image (有图)
initials (首字母)
fallback (图标)
loading (加载中)
```

### 附加
```
with-status-dot (在线/离线)
with-badge (数字徽标)
group (多个叠加)
```

---

## Progress 进度

### 变体
```
determinate (确定进度 0-100%)
indeterminate (不确定, 动画循环)
```

### 显示
```
linear (线性)
circular (圆形)
semi-circle (半圆)
step-based (步骤式)
```

### 状态
```
active (进行中)
complete (完成)
error (失败)
paused (暂停)
```

---

## 表格（Table）

### 行状态
```
default
hover
selected
expanded (展开子内容)
disabled
loading
error
```

### 单元格状态
```
default
editing (内联编辑)
error
loading
```

### 表格状态
```
default
loading (skeleton rows)
empty
error
sorting
filtering
```

---

## 列表项（List Item）

### 状态
```
default
hover
active/pressed
selected
disabled
with-swipe-actions (移动端)
dragging
drag-over (放置目标)
```

---

## Skeleton 骨架屏

### 类型
```
text-line (文字行)
avatar-circle (头像圆)
rectangle (矩形块)
card (卡片组合)
table (表格行)
```

### 动效
```
static (静态)
pulse (脉冲)
shimmer (光带)
wave (波浪)
```

---

## 空态（Empty State）

### 类型
```
first-time (首次使用)
no-search-results (搜索无结果)
no-data (暂无数据)
error (加载失败)
completed (已清空/已完成)
permission-denied (无权限)
```

### 组件
```
illustration / icon
title
description
primary-action
secondary-action (可选)
```

---

## 表单（Form）

### 表单状态
```
pristine (未修改)
dirty (已修改)
valid (校验通过)
invalid (校验失败)
submitting (提交中)
submitted (已提交)
success (成功)
error (失败)
```

### 字段状态
详见 Input。

---

## 通用响应式变体

每个组件应有：
```
mobile (< 768)
tablet (768-1024)
desktop (> 1024)
```

移动端通常：
- 更大触控目标
- 更少信息密度
- 简化 hover（改为 tap）
- 全宽按钮

---

## 状态覆盖 Prompt 模板

```
【状态穷举】：
所有交互组件必须实现：
- Button：default/hover/focus/active/disabled/loading（六态）
- Input：empty/filled/focus/error/disabled/readonly（六态）
- Select：closed/open/searching/no-results/loading/disabled
- Modal：opening/open/closing 三阶段动画
- Card：default/hover(elevation+2px上移)/selected/loading(skeleton)
- Toast：success/warning/error/info + 3s auto-dismiss + 手动关闭
- Table：hover row/selected/sorting/filtering/loading skeleton/empty
- 骨架屏用 shimmer 效果，骨架 → 内容 crossfade 200ms
- 空态：插图 + 标题 + 描述 + 主 CTA
- 错误态：图标 + 说明 + 重试按钮
- 所有变化尊重 prefers-reduced-motion
```

---

## 检验清单

- [ ] 每个可交互组件有 6 种基础态
- [ ] 表单字段有校验态和提交态
- [ ] 长任务有 loading + 进度
- [ ] 每个页面有 empty / error 兜底
- [ ] 移动端与桌面端有专属变体
- [ ] 状态转换有恰当动效
