# B33 · 排版完整技术

> 排版是设计的脊柱。这份把"字体技术"彻底挖透——OpenType 特性、可变字体全轴、禁则、竖排、Ruby、微排版。
> 知道这些,你能让 AI 生成"设计师都佩服"的排版精度。

---

## 一、OpenType 特性完整表

OpenType 字体内置的"隐藏功能",默认关闭,需要手动启用。

### 数字类(表格/金融/数据必备)

| 特性代码 | 名称 | 效果 | 场景 |
|---|---|---|---|
| `tnum` | Tabular Figures | 数字等宽对齐 | **表格/数据对齐必备** |
| `lnum` | Lining Figures | 等高数字(齐基线) | 标题/UI |
| `onum` | Oldstyle Figures | 不等高数字(有上下伸出) | 正文/编辑感 |
| `pnum` | Proportional Figures | 比例数字(宽度不等) | 正文(默认) |
| `frac` | Fractions | 真正分数(½ ¾) | 食谱/数学 |
| `ordn` | Ordinals | 上标序号(1st 2nd) | 日期/序号 |
| `zero` | Slashed Zero | 带斜线的 0(区分 O) | 代码/ID |

**CSS 用法**:
```css
/* 表格数字 */
.data { font-variant-numeric: tabular-nums lining-nums; }
/* 或 */
.data { font-feature-settings: "tnum" 1, "lnum" 1; }

/* 正文老式数字 */
.body { font-variant-numeric: oldstyle-nums proportional-nums; }

/* 代码区零带斜线 */
.code { font-feature-settings: "zero" 1; }
```

### 连字类(标题/品牌)

| 特性 | 名称 | 效果 |
|---|---|---|
| `liga` | Standard Ligatures | fi fl 连写(默认开) |
| `dlig` | Discretionary Ligatures | ct st 等装饰连字 |
| `calt` | Contextual Alternates | 字母根据上下文变形 |

### 大写类(标签/导航)

| 特性 | 名称 | 效果 |
|---|---|---|
| `smcp` | Small Caps | 小型大写字母(高度等同 x-height) |
| `c2sc` | Caps to Small Caps | 全部转小型大写 |
| `case` | Case-Sensitive Forms | 括号/连字符在全大写中居中 |
| `cpsp` | Capital Spacing | 大写字母自动加字距 |

**CSS**:
```css
.label { font-variant-caps: all-small-caps; }
```

### 风格类(品牌个性)

| 特性 | 名称 | 效果 |
|---|---|---|
| `ss01`-`ss20` | Stylistic Sets | 替代字形集(如 Inter 的 ss01 改变 a/g 形态) |
| `salt` | Stylistic Alternates | 单字替代 |
| `swsh` | Swash | 花体装饰延伸 |

**CSS**:
```css
/* Inter 的 ss01:开放的 a 和双层 g */
.text { font-feature-settings: "ss01" 1; }
```

### 位置类

| 特性 | 名称 | 效果 |
|---|---|---|
| `sups` | Superscript | 上标(真正的上标字形,不是 CSS 缩放) |
| `subs` | Subscript | 下标 |
| `sinf` | Scientific Inferiors | 科学下标 |

---

## 二、可变字体(Variable Fonts)完整

### 注册轴(标准轴)

| 轴 | 代码 | 范围(常见) | 用途 |
|---|---|---|---|
| **Weight** | `wght` | 100-900 | 字重(最常用) |
| **Width** | `wdth` | 75-125% | 字宽 |
| **Italic** | `ital` | 0-1 | 正体↔斜体 |
| **Slant** | `slnt` | -12~0° | 倾斜角度 |
| **Optical Size** | `opsz` | 8-144 | 光学尺寸(自动按字号调整笔画) |

### 自定义轴(字体特有)

| 字体 | 自定义轴 | 用途 |
|---|---|---|
| Recursive | `MONO` 0-1 | 无衬线↔等宽 过渡 |
| Recursive | `CASL` 0-1 | 常规↔手写 过渡 |
| Fraunces | `SOFT` 0-100 | 锐角↔圆角 |
| Roboto Flex | `GRAD` -200~150 | 字重渐变(不改宽度) |

### CSS 用法
```css
/* 注册轴用 font-weight 等简写 */
h1 { font-weight: 750; }   /* 无级字重 */

/* 自定义轴用 font-variation-settings */
.text { font-variation-settings: "wght" 450, "opsz" 24, "CASL" 0.5; }

/* 字重动画(hover 变粗) */
.link {
  font-variation-settings: "wght" 400;
  transition: font-variation-settings 200ms;
}
.link:hover {
  font-variation-settings: "wght" 700;
}
```

### 光学尺寸(Optical Size)★精工级
好的可变字体在不同字号有不同"最优解":
- **大字**(标题):笔画细、字距紧、装饰细节多
- **小字**(正文):笔画加粗、字距松、开口(counter)大

**启用**:
```css
font-optical-sizing: auto;   /* 浏览器自动按 font-size 调整 */
```
需要字体支持 `opsz` 轴(Inter、Roboto Flex、Source Serif 4 等)。

### 性能优势
一个可变字体文件 = N 个静态字体文件。
Inter Variable(~300KB)< Inter 6 个静态文件(~600KB)。

---

## 三、中日韩(CJK)排版规范

### 中文排版

**行高**: 1.6-1.8(比拉丁高)
**标点处理**:
```css
/* 标点挤压(去多余空白) */
text-spacing-trim: space-all;

/* 行首不出现的标点(禁则) */
/* 。,!?)]》」』】都不能在行首 */
/* 自动处理:word-break: normal + lang="zh" */
```

**首行缩进**:
```css
p + p { text-indent: 2em; }  /* 段首缩进 2 字 */
/* 或用段间距代替(Web 惯例) */
```

**竖排**:
```css
.vertical {
  writing-mode: vertical-rl;   /* 从右到左竖排 */
  text-orientation: mixed;     /* 中文竖排,英文数字横转 */
}
```

**中英混排空格**:
```css
/* 2025+ 原生方案 */
text-autospace: ideograph-alpha ideograph-numeric;
```
或手动加半角空格(使用 API → 使用 API)。

### 日文排版

**假名与汉字混排**:字重要平衡(假名视觉偏轻)。

**Ruby 注音**:
```html
<ruby>漢字<rt>かんじ</rt></ruby>
```
```css
ruby { ruby-position: over; }   /* 注音在上 */
rt { font-size: 50%; }
```

**禁则处理(Kinsoku)**:
- 行首禁止:。、!?」』】
- 行尾禁止:「『【(
- 浏览器默认处理(需 `lang="ja"`)

**竖排常见**(小说/传统):
```css
writing-mode: vertical-rl;
text-combine-upright: all;   /* 让短数字横排 */
```

### 韩文排版
- 有词间空格(与中日不同)
- 可以 `word-break: keep-all`(按词断行)

---

## 四、高级排版技巧

### 1. 断词连字(Hyphenation)
```css
.text {
  hyphens: auto;
  hyphenate-limit-chars: 6 3 3;  /* 最短6字符,连字符前后各至少3字 */
}
```
需要 `lang` 属性正确(浏览器按语言字典断词)。

### 2. 段落对齐的精细控制
```css
/* 两端对齐但最后一行左对齐 */
text-align: justify;
text-align-last: left;

/* 或不用两端对齐(Web 一般不用,英文好用) */
```

### 3. 首字下沉(Drop Cap)
```css
p::first-letter {
  font-size: 3.5em;
  float: left;
  line-height: 0.8;
  padding-right: 8px;
  font-weight: 700;
  color: var(--primary);
}
```
编辑感/杂志感必备细节。

### 4. 悬挂标点(Hanging Punctuation)
```css
blockquote {
  hanging-punctuation: first last;  /* 引号伸出到外面 */
}
```
让左边缘在视觉上更整齐(引号不占文字位)。

### 5. 多栏排版
```css
.article {
  columns: 2;                /* 两栏 */
  column-gap: 48px;          /* 栏间距 */
  column-rule: 1px solid #E5E7EB;  /* 栏间线 */
}
.article h2 {
  column-span: all;          /* 标题跨栏 */
}
```

### 6. 文字平衡
```css
h1 { text-wrap: balance; }   /* 标题各行长度平衡 */
p  { text-wrap: pretty; }    /* 正文避免孤字 */
```
**一行 CSS 提升精致度。**

### 7. 省略号(多行截断)
```css
.text {
  display: -webkit-box;
  -webkit-line-clamp: 3;     /* 最多 3 行 */
  -webkit-box-orient: vertical;
  overflow: hidden;
}
/* 2025+ 简写 */
.text { line-clamp: 3; }
```

### 8. 文字方向控制(Bidi)
```css
/* 在 RTL 文本中嵌入 LTR(代码/URL) */
.code { unicode-bidi: embed; direction: ltr; }
```

---

## 五、微排版(Micro-typography)

> 肉眼看不出但"感觉精致"的来源。

### 1. 正确引号
```
❌ "直引号"  (打字机引号)
✅ "弯引号"  (排版引号,又叫 smart quotes)
中文用 ""(双)和 ''(单)
```

### 2. 破折号三种
```
连字符 hyphen:    -   (复合词 well-known)
短破折号 en-dash: –   (范围 2020–2026)
长破折号 em-dash: —   (句中插入——像这样)
```
大多数人全用 `-`,精致排版区分三种。

### 3. 省略号
```
❌ ...    (三个点)
✅ …      (U+2026 真正的省略号字符)
```

### 4. 乘号
```
❌ x (小写字母)
✅ × (U+00D7 乘号)
屏幕尺寸: 1920×1080
```

### 5. 版权/商标
```
© ® ™ — 用真正符号不是 (c) (r) (tm)
```

### 6. 数字字体特性组合
```
表格数据: tabular-nums + lining-nums
正文数字: proportional-nums + oldstyle-nums
代码:     tabular-nums + slashed-zero
```

---

## 六、字体加载策略

### font-display 四值
```css
@font-face {
  font-family: "Inter";
  src: url("inter.woff2") format("woff2");
  font-display: swap;      /* ★ 推荐:先系统字体,加载完替换 */
}
```

| 值 | 行为 | 推荐场景 |
|---|---|---|
| `auto` | 浏览器决定 | 不推荐 |
| `block` | 等待加载(最多 3s 隐形) | 品牌标题(必须特定字体) |
| `swap` | 先用系统字体,加载完替换 | **正文首选** |
| `fallback` | 短暂等待(100ms),然后系统字体 | 平衡 |
| `optional` | 如果不能立即用就永远不用 | 极致性能 |

### 预加载关键字体
```html
<link rel="preload" href="/fonts/inter-var.woff2" as="font" type="font/woff2" crossorigin>
```
只预加载 1-2 个最关键的字体文件。

### 子集化(Subsetting)
```
完整 Inter:~300KB
仅拉丁子集:~50KB
仅中文常用字:~200KB(从 5MB 压缩)
```
工具:fonttools / glyphhanger / Google Fonts 自动子集

### 字体加载 4 步最佳实践
```
1. preload 关键字体(head 标签里)
2. font-display: swap(不阻塞渲染)
3. 子集化(只要用到的字符)
4. woff2 格式(最小)
```

---

## 七、Prompt 模板

```
【排版完整系统】

字体技术:
- 启用 OpenType 特性:
  - 表格数字:font-variant-numeric: tabular-nums lining-nums
  - 正文:oldstyle-nums(编辑感)
  - 代码:slashed-zero
  - 品牌:ss01 替代字形(如果字体支持)
- 可变字体:font-optical-sizing: auto(不同字号自动优化)
- 字重 hover 动画:font-variation-settings "wght" 400→700

CJK 排版:
- text-autospace: ideograph-alpha(中英自动加空格)
- 行高 ≥1.7(比拉丁高)
- 标点挤压:text-spacing-trim

微排版:
- 使用正确引号 ""(不是 "")
- 破折号区分:连字符 - / 范围 – / 插入 —
- 省略号用 … 不是 ...
- text-wrap: balance(标题) / pretty(正文)

字体加载:
- preload 关键字体(1-2 个)
- font-display: swap
- woff2 格式 + 子集化
- 可变字体减少请求数

高级排版:
- 首字下沉 ::first-letter(长文编辑感)
- 悬挂标点 hanging-punctuation: first(对齐)
- 多栏 columns: 2 + column-gap(长文桌面)
- 行内代码用 code + 等宽字体(区分正文)
```

---

## 八、检验清单

- [ ] 表格数字用 tabular-nums
- [ ] 正文数字用 oldstyle-nums(可选,编辑感)
- [ ] 代码区用 slashed-zero
- [ ] 可变字体启用 optical-sizing
- [ ] 正确引号 ""(不是 "")
- [ ] 破折号三种区分
- [ ] text-wrap: balance(标题)
- [ ] text-wrap: pretty(正文)
- [ ] 中英混排有空格(手动或 text-autospace)
- [ ] CJK 行高 ≥1.7
- [ ] 字体 preload + swap + woff2
- [ ] 只预加载 1-2 个关键字体
- [ ] 首字下沉(长文)
- [ ] 悬挂标点(引用)
