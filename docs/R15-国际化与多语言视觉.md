# R15 · 国际化与多语言视觉（提示词库 O 组）

> **定位**：同一套设计在不同语言下都要好看——多数作品切到阿拉伯语／德语／日语时会崩（布局、字体、方向、长度全出问题），本文是"多语言并存"的可粘贴提示词条目清单。
> **覆盖层**：L1 骨 + L5 髓（含少量 L3 电影级、L0 定调项）。L 层级含义：L0 定调 / L1 骨架 / L2 结构 / L3 表现 / L4 细节 / L5 髓（不可见的专业度）。
> **条目卡格式**：`**条目名（英文名）** · L层级 · 档位（基础／进阶／电影级） · 成本:低/中/高` → `视觉收益` / `关键约束` / `参考数据` / `同族拓展`（可继续深挖的相邻技术）/ `提示词片段`（可直接粘进 prompt 的中文句式）。
> **来源**：融合自外部「视觉提示词库 O 组·国际化与多语言视觉」（v1.x，2026-08-16）。组内小节编号沿用原库 `O1–O6`，跨组引用已改写为本库 `R{n}·{原编号}` 形式，避免与本库 A/B/C/D 板块字母混淆。

---

## 使用说明

O 组是"看不见的专业度"。单语项目（纯中文或纯英文）可以跳过大半；**是否做多语言由项目自行决定，一旦要做，本组是完整清单**——不处理这些会当场露怯。

核心心法：**为最长和最不同的语言设计，而非为你的母语设计**。德语单词能比英语长 40%，阿拉伯语从右向左，中日文没有词间空格，泰语没有断词。

```
O1 书写方向与镜像        O4 字体覆盖与回退
O2 文本长度弹性          O5 数字·日期·货币
O3 CJK 与东亚排版        O6 语言切换与视觉一致
```

> 中文排印的深水区见 `R10·I5`（排印的图形化）；本组处理的是"多语言并存"的系统性问题。
> 视觉禁忌／反 AI 味相关判断见 `B82` 与 `B44`——那是**参考清单，不是硬性铁律，按项目自行取舍**。
> 需要定风格方向时配合 `B16`、`E1`（风格谱系）；需要全维度视觉清单时配合 `B15`。

---

## O1 书写方向与镜像（Direction & Mirroring）

- **逻辑属性（Logical Properties）** · L1 · 基础 · 成本:低
  - 视觉收益：用 `inline-start/end` 代替 `left/right`，RTL 自动镜像，无需两套 CSS
  - 同族拓展：`margin-inline`、`padding-block`、`inset-inline-start`、`text-align: start`、`float: inline-start`
  - 提示词片段：`全部用逻辑属性（margin-inline-start 而非 margin-left）；文本对齐用 start/end；RTL 自动适配`

- **RTL 布局（Right-to-Left）** · L1 · 进阶 · 成本:中
  - 视觉收益：阿拉伯语/希伯来语从右向左，整个布局镜像
  - 关键约束：**镜像文字与容器，但不镜像 logo、图标中的箭头方向要翻、播放键不翻、进度条方向翻**
  - 同族拓展：`dir="rtl"`、`:dir()` 选择器、双向文本 (bidi)、`unicode-bidi`、mirror-safe icons
  - 提示词片段：`RTL 时镜像布局与导航方向；翻转"下一步/返回"箭头与进度方向；不翻转 logo、媒体控制、时钟`

- **双向文本混排（Bidi）** · L5 · 进阶 · 成本:低
  - 视觉收益：RTL 文本里嵌入 LTR（英文/数字/URL）时正确排列
  - 同族拓展：`bdi` 元素、`unicode-bidi: isolate`、`dir="auto"`、防止标点跑到错误一侧
  - 提示词片段：`用户生成内容与混排片段用 <bdi> 或 dir="auto" 隔离，防止标点错位`

- **图标与方向语义（Directional Icons）** · L5 · 基础 · 成本:低
  - 视觉收益：方向性图标在 RTL 翻转，非方向性的不翻
  - 同族拓展：`[dir="rtl"] .icon-arrow { transform: scaleX(-1) }`、白名单管理
  - 提示词片段：`维护一个"需镜像图标"白名单（箭头/返回/缩进）；其余图标 RTL 下保持不变`

---

## O2 文本长度弹性（Text Expansion）

- **长度膨胀预留（Expansion Room）** · L1 · 基础 · 成本:低
  - 视觉收益：为文本变长留空间，避免溢出/截断/换行破坏布局
  - 参考膨胀率（相对英文）：德语 +35%、俄语 +20%、法语 +20%、中文 -30%、日语 -40%
  - 同族拓展：min-width 而非固定 width、允许换行、`text-overflow: ellipsis` 兜底、避免"文字撑爆按钮"
  - 提示词片段：`按钮/标签用 min-width + padding 不用固定宽度；为文本预留 +40% 膨胀空间；溢出用 ellipsis 兜底`

- **弹性容器（Flexible Containers）** · L1 · 基础 · 成本:低
  - 视觉收益：容器随内容伸缩，任何语言都不破版
  - 同族拓展：`min-content`/`max-content`/`fit-content`、`flex-wrap`、避免固定高度文本容器、`field-sizing: content`
  - 提示词片段：`文本容器高度自适应内容（不固定高度）；用 flex-wrap 允许换行；单行截断处保证 tooltip 可看全`

- **换行与断词（Wrapping & Hyphenation）** · L5 · 进阶 · 成本:低
  - 视觉收益：不同语言断词规则不同——德语长复合词要断，中文任意断，泰语无空格需特殊处理
  - 同族拓展：`hyphens: auto` + `lang` 属性、`overflow-wrap: break-word`、`word-break`（CJK vs 拉丁不同）、`line-break`
  - 提示词片段：`设正确的 lang 属性让 hyphens:auto 生效；德语长词允许断字；CJK 用 line-break:strict`

- **截断的礼貌（Graceful Truncation）** · L5 · 基础 · 成本:低
  - 视觉收益：必须截断时保留可读性与获取全文的途径
  - 同族拓展：`-webkit-line-clamp`（多行截断）、tooltip/title、展开按钮、渐变淡出（见 `R9·H4`）
  - 提示词片段：`多行截断用 line-clamp + 渐变淡出；截断内容必须可通过 hover/展开看全`

---

## O3 CJK 与东亚排版（CJK Typography）

- **CJK 断行与禁则** · L5 · 进阶 · 成本:低
  - 视觉收益：见 `R10·I5`。标点不能行首、词不能中断
  - 同族拓展：`line-break: strict`、`word-break: keep-all`、`text-spacing-trim`、避排孤字
  - 提示词片段：`CJK 用 line-break:strict + word-break:keep-all；见 R10·I5 完整规则`

- **中西文混排间距** · L5 · 基础 · 成本:低
  - 视觉收益：见 `R10·I5`。中文与拉丁/数字间加 1/4 空隙
  - 提示词片段：`中英间加 0.15em 间隙（text-autospace 或构建时处理，见 R10·I5）`

- **行高与密度差异** · L5 · 基础 · 成本:低
  - 视觉收益：CJK 行高需比拉丁大（1.7–1.9 vs 1.5）；同一组件在中英文下可能需要不同行高
  - 同族拓展：`:lang(zh)` / `:lang(ja)` 差异化、方块字密度
  - 提示词片段：`用 :lang() 给 CJK 设更大行高（1.75）；拉丁保持 1.5；同组件按语言微调`

- **竖排支持（Vertical Writing）** · L3 · 电影级 · 成本:中
  - 视觉收益：见 `R10·I4`/`R10·I5`。日文/中文竖排的传统美学
  - 提示词片段：`装饰性竖排用 writing-mode:vertical-rl + text-combine-upright（见 R10）`

- **字重可用性差异** · L5 · 进阶 · 成本:低
  - 视觉收益：CJK 字体的可用字重通常比拉丁少（常只有 4-7 档），设计层级时要考虑
  - 同族拓展：CJK 字重稀疏、用字号/颜色补充层级、fallback 时字重丢失
  - 提示词片段：`CJK 字重档位有限，层级多用字号+颜色+间距区分，不依赖细分字重`

---

## O4 字体覆盖与回退（Font Coverage & Fallback）

- **字体回退链（Font Stack）** · L1 · 基础 · 成本:低
  - 视觉收益：每种语言都有合适字体，缺字不显示豆腐块（□）
  - 同族拓展：`unicode-range` 分语言加载、系统字体栈、Noto 全覆盖、缺字检测
  - 提示词片段：`按 unicode-range 分语言指定字体；CJK 优先系统字体栈（PingFang/Yahei/Hiragino）；兜底 Noto Sans`

- **度量匹配（Metric Matching）** · L5 · 进阶 · 成本:低
  - 视觉收益：回退字体与主字体的度量差异会导致布局跳动（CLS）
  - 同族拓展：`size-adjust`、`ascent-override`、`descent-override`、`@font-face` 度量覆盖、fallback 微调
  - 提示词片段：`fallback 字体用 size-adjust + ascent/descent-override 匹配主字体度量，消除字体加载跳动`

- **子集化与按需加载（Subsetting）** · L5 · 进阶 · 成本:低
  - 视觉收益：CJK 字体巨大（数 MB），按语言/按用字子集化才能用
  - 同族拓展：`unicode-range` 分片、动态子集（subfont/fontmin）、`font-display: swap/optional`
  - 提示词片段：`CJK 字体做 unicode-range 分片按需加载；首屏用字可做动态子集；font-display:swap`

- **Emoji 与符号（Emoji & Symbols）** · L5 · 基础 · 成本:低
  - 视觉收益：emoji 在不同系统渲染不同；符号字体缺失显示豆腐
  - 同族拓展：`emoji` unicode-range、彩色 emoji 字体、符号回退、避免用 emoji 当功能图标（见 `R1·G5`）
  - 提示词片段：`emoji 单独指定字体族兜底；不用 emoji 当功能图标（见 R1·G5）；关键符号确保字体覆盖`

- **可变字体的多语言（VF Coverage）** · L5 · 进阶 · 成本:低
  - 视觉收益：并非所有可变字体都覆盖 CJK；混用时轴要对齐
  - 提示词片段：`确认可变字体覆盖目标语言；CJK 若无 VF 则拉丁用 VF、CJK 用静态字重匹配`

---

## O5 数字 · 日期 · 货币（Numbers, Dates, Currency）

- **本地化格式（Intl Formatting）** · L1 · 基础 · 成本:低
  - 视觉收益：数字千分位、小数点、日期顺序、货币符号位置都随地区变
  - 同族拓展：`Intl.NumberFormat`、`Intl.DateTimeFormat`、`Intl.RelativeTimeFormat`、阿拉伯数字 vs 本地数字
  - 提示词片段：`数字/日期/货币用 Intl API 格式化；不硬编码格式；注意小数点在有些地区是逗号`

- **表格数字对齐（Tabular Alignment）** · L5 · 基础 · 成本:低
  - 视觉收益：见 `R10·I2`。数字列右对齐 + 等宽数字
  - 提示词片段：`数字列右对齐 + tabular-nums（见 R10·I2）；小数点对齐`

- **文字方向中的数字（Numbers in RTL）** · L5 · 进阶 · 成本:低
  - 视觉收益：RTL 语言里数字仍从左到右，但整体位置要对
  - 同族拓展：见 `R15·O1` bidi、东阿拉伯数字（٠١٢٣）vs 西阿拉伯数字
  - 提示词片段：`RTL 中数字保持 LTR 内部顺序；用 bidi 隔离避免跑位`

- **单位与量词（Units & Measures）** · L5 · 进阶 · 成本:低
  - 视觉收益：公制/英制、单位符号位置、复数规则（英语 1 item/2 items，其他语言更复杂）
  - 同族拓展：`Intl.PluralRules`、单位本地化、`Intl.NumberFormat` unit style
  - 提示词片段：`复数用 Intl.PluralRules（很多语言有多种复数形式）；单位用 Intl unit 格式化`

---

## O6 语言切换与视觉一致（Switching & Consistency）

- **切换不破版（Switch Without Breaking）** · L1 · 基础 · 成本:低
  - 视觉收益：切语言时布局平滑适配，不跳不崩
  - 同族拓展：见 `R11·F3` 视图过渡、切换动画、保持滚动位置、预留最长语言空间
  - 提示词片段：`切语言用 View Transition 平滑过渡；保持当前滚动位置；布局按最长语言预留不重排崩坏`

- **视觉语言一致（Consistent Look Across Locales）** · L0 · 进阶 · 成本:低
  - 视觉收益：不同语言版本"看起来是同一个作品"——气质、层级、密度一致
  - 同族拓展：见 `R1·G7` 一致性、按语言微调而非重设计、共享令牌
  - 提示词片段：`各语言版本共享令牌与视觉语言；只按语言微调行高/字体/间距，不改变整体气质`

- **首选语言检测（Locale Detection）** · L5 · 基础 · 成本:低
  - 视觉收益：自动匹配用户语言，但允许手动切换且记住选择
  - 同族拓展：`navigator.languages`、`Accept-Language`、`lang` 属性、localStorage 记住选择
  - 提示词片段：`按 navigator.languages 自动选语言，提供明显切换入口，localStorage 记住手动选择`

- **文化色彩与符号（Cultural Semantics）** · L0 · 进阶 · 成本:低
  - 视觉收益：颜色与符号含义随文化变（红色在中国喜庆/西方警示；白色东西方含义相反）
  - 同族拓展：color symbolism by culture、手势符号禁忌、图像的文化敏感性
  - 提示词片段：`检查配色与符号的文化含义（红/白/手势符号因地而异）；面向特定市场时调整`

- **测试用伪本地化（Pseudo-Localization）** · L5 · 进阶 · 成本:低
  - 视觉收益：用加长加符号的伪文本测试布局弹性，无需真翻译
  - 同族拓展：pseudo-loc（`[!!! Ëxämplé Tëxt !!!]`）、+40% 长度、加重音符号测字体覆盖
  - 提示词片段：`用伪本地化（加长 40% + 重音符号 + 括号包裹）测试布局弹性与字体覆盖，无需真翻译`

---

## O 组 · 组合速查

| 想解决的问题 | 用哪些 |
|---|---|
| 支持 RTL（阿拉伯/希伯来） | O1 逻辑属性 + O1 RTL + O1 图标翻转 + O1 bidi |
| 布局不被长语言撑爆 | O2 膨胀预留 + O2 弹性容器 + O2 断词 |
| CJK 排版做对 | O3 全套（→ `R10·I5`）+ O4 CJK 字体回退 |
| 缺字不显示豆腐块 | O4 回退链 + O4 unicode-range + O4 emoji 兜底 |
| 字体加载不跳动 | O4 度量匹配（size-adjust） |
| 数字日期本地化 | O5 Intl API + O5 表格对齐 + O5 复数规则 |
| 切换语言不破版 | O6 View Transition + O6 一致性 + O2 弹性 |
| 无翻译先测布局 | O6 伪本地化 |

**常见坑**：
- 用了 left/right 硬编码 → `R15·O1` 应全用逻辑属性
- RTL 下 logo/播放键也被翻转 → `R15·O1` 需要镜像白名单
- 德语把按钮撑爆 → `R15·O2` 用了固定宽度
- 切到 CJK 行距太挤 → `R15·O3` 用 :lang() 加大行高
- 出现豆腐块 □ → `R15·O4` 字体回退链不全
- 字体加载时布局跳一下 → `R15·O4` 缺度量匹配
- CJK 字体加载 8MB → `R15·O4` 缺子集化（见 `R10·I5`）
- 日期显示成美式格式 → `R15·O5` 应用 Intl
- 各语言版本气质不一 → `R15·O6` 应共享令牌只微调

> **O 组心法**：**为最长最不同的语言设计**。做国际化项目时，至少测三种语言（一个长的如德语、一个 RTL 如阿拉伯语、一个 CJK），布局不崩、字体不豆腐、气质一致，就是专业度的硬证明。

---

## 与库内衔接

| 本文小节 | 目标库相关文档 | 关系 |
|---|---|---|
| 全文（O1–O6） | `B31` 国际化与多语言视觉（主文档） | **地基**：`B31` 讲原理、规范与决策依据；本文是可直接粘贴的提示词条目 |
| O3 CJK 与东亚排版 | `B33` 排版完整技术（CJK 部分） | 地基：断行禁则、混排间距、竖排的完整技术规则在 `B33`；本文给出可执行片段 |
| O5 数字·日期·货币 | `B60` 数字排版极致细节 | 扩展：`B60` 给等宽数字、小数点对齐、数字层级；本文补 Intl API 与地区差异 |
| O2 文本长度弹性 / O6 切换不破版 | `B30` 响应式设计完整系统 | 扩展：语言膨胀是"内容维度的响应式"，与断点系统同一套弹性思路 |
| O1 方向与镜像 / O2 截断的礼貌 / O6 语言检测 | `C4` 无障碍 | **验证**：`lang`/`dir` 属性、截断可达全文、切换入口可键盘操作，均需按 `C4` 复核 |
| O4 字体覆盖与回退 / O3 字重差异 | `B1` 字体排版 | 地基：字体选型、字重层级与回退栈策略先看 `B1`，本文补多语言覆盖与度量匹配 |
| 视觉禁忌自查 | `B82`、`B44` | 验证：作为**参考清单，不是硬性铁律，按项目自行取舍** |
| 风格方向 / 全维度清单 | `B16`、`E1` / `B15` | 扩展：定风格谱系与全维度视觉检查时配合使用 |

**与 `B31` 的分工**：`B31` 是国际化与多语言视觉的**原理与规范主文档**（为什么这样做、有哪些标准、如何决策）；本文（`R15`）是**可粘贴提示词条目**（写 prompt 或写代码时直接抄的句式与参数）。二者内容互补，不重复扩写；若参数冲突，以 `B31` 的规范为准，本文只作执行层补充。

---

## 参考资料

- 本文融合自外部「视觉提示词库 · O 组：国际化与多语言视觉」（v1.x，2026-08-16），迁移时保留全部小节、L 层级、档位、成本、参考膨胀率、同族拓展与提示词片段，仅改写库内交叉引用编号并去除项目专属内容。
- 技术术语与参数值（logical properties、`dir="rtl"`、bidi/`unicode-bidi`、Intl API、`font-family` fallback 与 `size-adjust`、`writing-mode`/`text-combine-upright`、语言膨胀率等）来自该库原始调研，**未新增未经验证的数据**。
- 与本库 `B31`、`B33`、`B60`、`B30`、`C4`、`B1` 的关系见上一节；组内编号 `O1–O6` 沿用原库，跨组引用统一为 `R{n}·{原编号}`。

---

_版本：v1.0（R15 · 国际化与多语言视觉，融合自提示词库 O 组）_
_日期：2026-08-17_
_心法：为最长和最不同的语言设计，而非为母语设计_
