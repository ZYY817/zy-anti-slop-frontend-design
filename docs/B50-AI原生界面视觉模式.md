# B50 · AI 原生界面视觉模式

> AI 产品(Chat/Agent/搜索/生成)有一套独立于传统 UI 的视觉模式。
> 2025-2026 这类产品爆发,但大多数视觉做得粗糙。这份系统化 AI 界面的每个视觉节点。

---

## 一、AI 界面的核心视觉节点

```
输入区(Composer)
  ↓ 发送
等待态(Thinking/Loading)
  ↓
输出区(Response)
  ├─ 流式输出(Streaming)
  ├─ 引用(Citation)
  ├─ 代码块(Code)
  ├─ 工具调用(Tool Calling)
  └─ 结构化结果(Cards/Tables)
  ↓
反馈区(Feedback/Actions)
  ↓
多轮上下文(Context)
```

---

## 二、输入区(Composer)

### 结构
```
┌────────────────────────────────────┐
│ [附件+]  输入文字...     [发送 ↑]  │
└────────────────────────────────────┘
  建议 prompts(chips 横向可滚动)
```

### 视觉规格
- 圆角:16-24px(友好)或 12px(专业)
- 边框:1px 中性色,focus 时主色 + ring
- 高度:auto-grow(随文字长度增长)
- 发送按钮:圆形/圆角方形,主色,有输入才激活
- 附件按钮:图标按钮,左侧
- 多行:Shift+Enter 换行,Enter 发送

### 建议 Prompts(Suggested)
```
[✨ 帮我写一封邮件] [📊 分析这份数据] [🎨 设计一个Logo]
```
- Pill 形状,横向可滚动
- 单行,超出右侧渐隐
- 点击即发送
- 有内容后可隐藏

### 附件区
- 图片预览(缩略图 + ✕ 移除)
- 文件 chip(文件名 + 图标 + ✕)
- 在输入框上方

---

## 三、等待态(Thinking / Loading)

### 1. 打字指示器(Typing Indicator)
```
●  ●  ●    (三点依次跳动)
```
- 点大小 6-8px,间距 4px
- 跳动:每点 stagger 0.2s,持续 1.4s 循环

### 2. 思考态(Thinking/Reasoning)
```
┌──────────────────────────────┐
│ 🧠 正在思考...                │
│    分析问题 → 搜索相关信息    │
│    → 组织回答                │
└──────────────────────────────┘
```
- 可展开/折叠的"思考过程"
- 步骤依次出现(stagger)
- 当前步骤有 pulse 动画
- 半透明卡片/边框微光

### 3. 工具调用中(Tool Using)
```
┌──────────────────────────────┐
│ 🔍 正在搜索"设计趋势 2026"   │  ← spinner + 描述
│ 📄 正在阅读 3 篇文档...      │
│ ✓  搜索完成(找到 5 条结果)   │  ← 完成态
└──────────────────────────────┘
```
- 每步有状态:pending(灰) → running(spinner) → done(✓ 绿)
- 实时更新
- 可折叠

### 时长感知
- <3s:只显示打字指示器
- 3-10s:显示"正在思考"
- >10s:显示具体步骤进度
- >30s:显示"这可能需要一会,请稍候" + 预期时长

---

## 四、流式输出(Streaming Response)★AI 特有

### 视觉特征
- **逐字出现**(每字 20-40ms)
- **末尾光标闪烁**(blink 1s)
- **完成后光标消失**
- **文字区域自动向下增长**(不跳动)
- **用户可随时中断("停止生成")**

### 实现细节
```css
/* 光标 */
.cursor::after {
  content: '▎';
  animation: blink 1s step-end infinite;
  color: var(--primary);
}
@keyframes blink { 50% { opacity: 0; } }
```

### 渲染质量
- Markdown 实时渲染(不是纯文本→渲染两步)
- 代码块语法高亮(流式中也要)
- 列表/标题/加粗 即时格式化
- 数学公式(LaTeX)即时渲染

### 性能
- 虚拟滚动(长回答不卡)
- 不阻塞主线程
- 用户可以在生成中滚动查看上文

---

## 五、引用与溯源(Citation)

### 内联引用
```
根据最新研究[1],设计趋势正在转向...[2]
```
- 上标数字,点击展开来源
- hover 显示 tooltip(来源标题+链接)

### 引用卡片
```
┌──────────────────────────┐
│ [favicon] 标题            │
│ 来源网站 · 日期            │
│ 摘要文字(2行)...          │
│                          │
│ [打开链接 ↗]              │
└──────────────────────────┘
```
- 紧凑卡片,点击打开原文
- 多引用:横向卡片列表(可滚动)

### 引用的视觉处理
- 引用数字:小圆角 badge,主色/10 背景
- 引用区域:与正文有明确视觉区分(淡底/左侧色条)

---

## 六、代码块(Code in AI)

### 基本结构
```
┌─ Python ──────────────────── [复制] ─┐
│ def hello():                          │
│     print("Hello World")              │
│                                       │
└───────────────────────────────────────┘
```

### 视觉规格
- 深色底(即使 UI 是浅色,代码块通常深色)
- 语法高亮(按语言)
- 顶部标签:语言名称(左)+ 操作按钮(右)
- 操作:复制 / 运行(如有)/ 折叠(长代码)
- 字体:等宽(JetBrains Mono / Fira Code)
- 字号:14px(比正文略小)
- 行号(可选)
- 圆角:8-12px

### 复制按钮状态
```
默认: [📋 复制]
点击: [✓ 已复制] (2s 后恢复)
```

### 流式中的代码块
- 代码逐行追加
- 语法高亮实时(不等写完)
- 长代码默认折叠(显示前 10 行 + "展开全部")

---

## 七、AI 消息气泡设计

### 用户 vs AI 的视觉区分
```
用户消息:右侧,主色底/深色底,白字
AI 消息: 左侧,透明底/极浅底,深字,有头像

或:
用户消息:右侧,有背景色
AI 消息: 左侧,无背景(直接在页面上)
```

### AI 头像
- 品牌 Logo 或 AI 角色图标
- 尺寸:28-36px
- 圆形或品牌形状

### 时间戳
- 不每条都显示(太密)
- 每组消息(连续同一方)显示一次
- 或 hover 显示

### 消息间距
- 同一方连续消息:8px
- 切换方(用户→AI):16-24px

---

## 八、反馈区(Feedback)

### 评价按钮
```
[👍] [👎]   或   [有帮助] [没帮助]
```
- 位置:AI 回答底部
- 默认态:灰色图标
- 点击态:选中高亮(👍 绿 / 👎 红或灰)
- 点击后可选:展开反馈文本框

### 操作按钮(Response Actions)
```
[📋 复制] [🔄 重新生成] [📌 保存] [↗ 分享]
```
- 位置:AI 回答底部,hover 时显示(或常驻)
- 样式:ghost 按钮,紧凑

### "停止生成"
```
[■ 停止]
```
- 位置:生成中出现在底部
- 样式:明确的红色/深色按钮
- 生成完成后消失

---

## 九、不确定性的视觉表达

AI 不确定时怎么视觉上告诉用户?

### 置信度指示
```
高确定: 正常文字,无标记
中确定: 文字 + 小标记("AI 生成,请核实")
低确定: 文字 + 黄色/橙色高亮 + 警示图标
```

### 免责声明
```
ℹ️ AI 生成的内容可能不准确,请验证关键信息。
```
- 位置:回答底部或固定底栏
- 样式:小字,灰色,低调但可见
- 法律要求(EU AI Act 2026)

### 多答案/不确定时
- 给出多个选项("可能是 A,也可能是 B")
- 视觉上并列展示(不是混在一段里)

---

## 十、多轮对话的视觉管理

### 长对话
- 虚拟滚动(几十轮不卡)
- "跳到底部"浮动按钮
- 新消息指示(当不在底部时)

### 上下文窗口
- 附件/文件/图片的缩略展示
- "基于以上 N 条对话"的上下文提示(可选)

### 分支对话
- 重新生成后显示"版本 1/2/3"切换
- 或折叠旧版本

### 对话历史
- 侧栏列表(标题 + 时间 + 摘要)
- 搜索历史
- 按时间分组(今天/昨天/上周)

---

## 十一、AI 界面的配色与氛围

### 当前主流做法
```
ChatGPT:  极简灰白/暗色双主题,几乎无色彩
Claude:   暖白底 + 轻微衬线 + 橙褐品牌色
Perplexity: 浅底 + 蓝绿强调 + 引用卡片
v0:       深色 + 代码感 + 紫蓝
```

### 气质选择
- **极简中性**(ChatGPT 式)— 界面消失,内容为主
- **温暖人文**(Claude 式)— 亲切,像在对话
- **科技数据**(Perplexity 式)— 引用/数据/精确
- **开发者**(v0/Cursor 式)— 深色,代码为主

### AI 产品常见装饰
- 极光渐变背景(品牌化)
- 流式输出的光标是品牌色
- 思考/加载用品牌动画(不是通用 spinner)
- 输入框 focus 时有品牌色光效

---

## 十二、Prompt 模板

```
【AI 原生界面】

输入区:
- 圆角输入框 16px,auto-grow 自动增高
- 发送按钮:圆形主色,有内容才激活
- 下方 suggested prompts:pill chips 横向可滚动
- 附件:输入框上方缩略图 + ✕
- focus: 边框主色 + 2px ring

等待态:
- <3s: 三点跳动指示器
- 3-10s: "正在思考"可折叠卡片,当前步骤 pulse
- 工具调用:每步 pending→running→done 三态

流式输出:
- 逐字出现 30ms/字
- 末尾品牌色光标闪烁
- Markdown 实时渲染(标题/列表/加粗/代码)
- 完成后光标消失
- "停止生成"按钮在底部

代码块:
- 深色底(即使浅色 UI)
- 顶部:语言标签 + [复制] 按钮
- 复制点击后变 [✓ 已复制] 2s 恢复
- 语法高亮,等宽字体 14px

引用:
- 内联上标数字 badge
- hover tooltip(来源标题)
- 底部引用卡片列表(横向可滚动)

消息:
- 用户:右侧,有背景色
- AI:左侧,无背景或极浅底 + 头像
- 同方连续间距 8px,切换方 20px

反馈:
- AI 回答底部:[👍] [👎] [复制] [重新生成]
- ghost 按钮,hover 显示或常驻
- 底部免责:"AI 生成内容请验证"

配色:
- {极简中性 / 温暖人文 / 科技数据 / 开发者深色}
- 品牌化:光标色/思考动画/输入 focus 用品牌色
```

---

## 十三、检验清单

- [ ] 输入框 auto-grow + 有内容才激活发送
- [ ] 有 suggested prompts
- [ ] 等待态按时长分级(指示器→思考→步骤)
- [ ] 流式逐字出现 + 光标闪烁
- [ ] Markdown 实时渲染(不是先纯文本后格式化)
- [ ] 代码块有语言标签 + 复制按钮
- [ ] 引用有内联数字 + 可展开来源
- [ ] 用户/AI 消息有明确视觉区分
- [ ] 有 👍👎 反馈 + 重新生成
- [ ] 有"停止生成"按钮
- [ ] 长对话有"跳到底部"
- [ ] 不确定内容有免责/标记
- [ ] 加载/思考动画品牌化
