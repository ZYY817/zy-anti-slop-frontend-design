# B48 · 视觉一致性审计

> "看起来不专业"的第一原因不是丑,是不一致。
> 这份是系统检查全站视觉一致的方法——像代码 lint 一样 lint 设计。

---

## 一、什么是视觉一致性

同一个产品里:
- **同类元素看起来一样**(所有主按钮相同)
- **同类操作行为一样**(删除都弹确认)
- **视觉规则贯穿全站**(圆角/间距/色彩不跳变)

**不一致的三大来源**:
1. 多人协作(每人一套"差不多")
2. 迭代积累(新功能用新样式,旧的没更新)
3. 没有系统(没 tokens,靠手敲值)

---

## 二、审计的 12 个维度

### 1. 色彩一致
```
检查:
□ 主色在全站是否同一色值?
□ 中性色阶是否统一(不混 Slate 和 Gray)?
□ 语义色是否统一(所有成功都是同一个绿)?
□ 同一元素在不同页面色值是否一致?
□ 有无随手写的色值(#333 vs #374151)?
```

### 2. 字号一致
```
□ 全站字号是否来自同一套阶梯?
□ 有无"差不多"的值(15px 和 16px 共存)?
□ 同级标题是否同字号?
□ 正文字号全站统一?
```

### 3. 字重一致
```
□ 只用了声明的 2-3 种字重?
□ 按钮/标签/标题字重一致?
□ 无"有时 500 有时 600"的情况?
```

### 4. 间距一致
```
□ 所有间距来自系统(4 的倍数)?
□ 无随手值(15px/22px/37px)?
□ 同类组件的 padding 相同?
□ 卡片间距全站统一?
□ section 间距统一?
```

### 5. 圆角一致
```
□ 全站圆角统一(如:卡片 12,按钮 8,输入 8)?
□ 无"有的 4 有的 6 有的 8"?
□ 嵌套圆角层级正确(外 > 内)?
```

### 6. 阴影一致
```
□ 同一层级的元素用同一套阴影?
□ 只有浮层有阴影(不是到处都有)?
□ 阴影值来自 tokens?
```

### 7. 图标一致
```
□ 全站用同一套图标库?
□ 线宽一致(全 1.5px 或全 2px)?
□ 尺寸一致(全 20px 或全 24px)?
□ 风格一致(不混线性和填充)?
□ 颜色一致(全 currentColor)?
```

### 8. 按钮一致
```
□ 同类按钮全站外观相同(primary 都一样)?
□ 尺寸阶梯统一?
□ 状态(hover/active/disabled)一致?
□ 图标位置一致(全左或全右)?
```

### 9. 表单一致
```
□ 输入框高度/padding/border 全站统一?
□ 标签位置统一(全在上方)?
□ 错误提示样式统一?
□ focus 样式统一?
```

### 10. 动效一致
```
□ 入场动画统一(方向/时长/缓动)?
□ hover 动效统一?
□ 过渡时长一致?
□ 缓动函数统一?
```

### 11. 文案一致
```
□ 人称统一(全"你"或全"您")?
□ 按钮动词风格一致?
□ 错误信息格式一致?
□ 日期/时间格式一致?
```

### 12. 布局一致
```
□ 栅格系统统一?
□ 容器宽度统一?
□ 同类页面布局模式统一?
□ 响应式策略统一?
```

---

## 三、审计方法

### 方法 1:视觉截图并排
把同类页面/组件截图并排放:
- 所有卡片并排 → 有没有不同的?
- 所有按钮并排 → 有没有色值/大小差异?
- 所有表单并排 → 有没有样式跳变?

### 方法 2:DevTools 采样
随机检查 10 个页面的:
- 色值(是否来自 tokens)
- 间距(是否 4 倍数)
- 字号(是否在阶梯内)
- 圆角(是否统一)

### 方法 3:Token 覆盖率
```
Token 覆盖率 = 使用 tokens 的样式 / 总样式

>95%  优秀(几乎无硬编码)
80-95% 良好
<80%  需要重构
```

### 方法 4:组件清单对账
列出所有在用的组件变体:
```
Button: primary/secondary/ghost/destructive × sm/md/lg
→ 是否每个都有明确定义?
→ 有无"野生变体"(非标准的)?
```

### 方法 5:跨页面对比
同一元素(如导航/footer)在不同页面:
- 位置是否一致?
- 间距是否一致?
- 响应式行为是否一致?

---

## 四、自动化审计工具

### 设计侧
- **Figma Lint 插件** — 检查 Figma 文件中的不一致
- **Figma Token Studio** — 检查 token 使用率

### 代码侧
- **Stylelint** — CSS 规则检查
- **ESLint plugin a11y** — 无障碍检查
- **axe DevTools** — 无障碍
- **css-stats.com** — 统计项目中的颜色/字号/间距数量(越多越乱)

### 视觉回归
- **Chromatic** — Storybook 视觉回归
- **Percy** — 截图对比
- **Playwright screenshot** — 自动截图对比

---

## 五、一致性的维护策略

### 1. Design Tokens(根本解决)
```
所有值来自变量,不允许硬编码。
改 token = 全站同步变。
```

### 2. 组件库(约束实现)
```
不允许在组件之外自己写按钮/表单。
用组件 → 自然一致。
```

### 3. 设计规范文档(约束协作)
```
每个组件:什么时候用/不用/变体/尺寸/状态。
有人疑问时查文档,不是各自发挥。
```

### 4. Review 流程(约束上线)
```
设计 Review:检查新页面是否遵守系统。
代码 Review:检查有无硬编码值。
视觉 QA:对比设计稿和实现。
```

### 5. 定期审计
```
每月/每季度跑一次审计。
像代码技术债一样,设计也有"设计债"。
```

---

## 六、一致性 vs 灵活性

**不是 100% 一致才好**——有些场景需要打破一致:
- 营销页 vs 产品内(营销可以更自由)
- 特殊活动页(节日/发布)
- 品牌传达 vs 功能操作(品牌部分可以特殊)

**规则**:
```
90% 一致(系统内)+ 10% 有意打破(有理由的特殊)
```
**"有意打破"和"不小心不一致"的区别**:能说出为什么打破 = 有意。

---

## 七、Prompt 模板

```
【一致性要求】

色彩:所有值来自 tokens,禁止硬编码色值
- 主色/中性色/语义色 每个只有一个来源

字号:严格阶梯(12/14/16/20/24/30/36/48),禁止中间值

字重:只用 400/500/600/700 四档

间距:全部 4 的倍数,来自 tokens

圆角:全站统一(按钮 8 / 卡片 12 / Modal 16 / full)

阴影:来自 tokens(shadow-sm/md/lg)

图标:统一库 + 统一尺寸 + 统一线宽

按钮:同变体全站外观/尺寸/状态一致

表单:输入框高度/padding/border/focus 全站统一

动效:入场方向/时长/缓动 全站统一(400ms easeOutExpo)

文案:人称/动词/错误格式 统一

检验:
- 无硬编码色值
- 无随手间距
- 无野生组件变体
- Token 覆盖率 >95%
```

---

## 八、检验清单(快速审计)

- [ ] 全站主色同一色值?
- [ ] 中性色只用一套(不混 Slate/Gray)?
- [ ] 字号只在阶梯内?
- [ ] 间距全部 4 的倍数?
- [ ] 圆角统一?
- [ ] 图标同库同线宽同尺寸?
- [ ] 同类按钮全站外观一致?
- [ ] 表单样式全站统一?
- [ ] 动效参数一致?
- [ ] 文案人称统一?
- [ ] 无"野生变体"?
- [ ] 能说出每处"打破一致"的理由?
