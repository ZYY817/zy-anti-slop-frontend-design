# M1 · 让 AI 生成顶级前端的方法论

> 这是整个库的"总纲"。术语是弹药,方法论是枪法。
> 综合 2026 年业界最佳实践(v0 / Lovable / Superdesign / OpenAI 设计 playbook / NN/g)提炼。

> **系统接入**：本文负责“如何分阶段设计与生成”；全库审计、知识升级、Prompt 检索和生成后质量闭环分别见 [M2-知识库审计与升级协议.md](M2-知识库审计与升级协议.md)、[Z2-Prompt检索与组合编排.md](Z2-Prompt检索与组合编排.md)、[Z3-前端质量审计与迭代闭环.md](Z3-前端质量审计与迭代闭环.md)。

## v2 工作边界

M1 不再承担“把整个知识库一次性塞进 Prompt”的职责。每次生成前，先由 Z2 根据产品上下文选择少量高相关知识；生成后由 Z3 审计并修复。若同一问题在多个项目反复出现，再回写对应专题或控制层。

---

## 核心洞察:为什么 AI 生成的界面"一看就是 AI 做的"

**根本原因**:一个 prompt 同时要求 AI 做三件事——**选品味 + 探索方案 + 写代码**——它就会退回到"统计平均值"。

> AI 输出的是它训练过的所有 dashboard 的平均值,所以永远是:圆角卡片 + Inter 字体 + 蓝色渐变。

**2026 年的共识**:通用感(generic)正在被市场惩罚。用户已经练出了"AI 味雷达"。把 Logo 去掉后你的页面和五个竞品一模一样,那你就没有品牌,只有一份默认配置。

**好消息**:这个问题是纯结构性的。**改约束,就能改输出。**

---

## 铁律一:拆分创意与实现(最重要)

不要让一个 prompt 干完所有事。拆成五步,每步单独做:

```
第 1 步｜文字规划创意   —— 先用文字想清楚方向,不写任何代码
第 2 步｜图像探索布局   —— 用图像模型(Nano Banana/Midjourney)出视觉参考,不用编码 AI
第 3 步｜定义设计系统   —— 生成任何页面前,先定好色彩/字体/间距/圆角
第 4 步｜给明确规格     —— 用具体参数,不说"做得酷一点"
第 5 步｜加入真实动效   —— GSAP(滚动)/ Motion.dev(应用内)
```

**如果你只改一件事,改第 1 步。** 其余四步都是同一思想在不同环节的应用。

---

## 铁律二:用参考,不用描述

> "从零描述布局,几乎总是导致混乱的输出。"—— Lovable 50+ 项目经验

**正确流程**(NN/g 验证):
1. 找一个你喜欢的设计(Dribbble / 竞品 / 真实产品截图)
2. 让 AI 分析它:"描述这个设计的风格、颜色、字体、布局模式"
3. 把分析结果用进你的 prompt:"把以下设计特征应用到我的 [页面类型]:[粘贴分析]"

**这一步把"视觉品味"翻译成了"可提示的具体属性"。**

---

## 铁律三:DESIGN.md —— 单点最高杠杆

> OpenAI GPT-5.4 设计 playbook 称这是production 级 AI UI 最有效的单一实践。
> awesome-design-md 项目(GitHub 54,000+ stars)提供 66+ 现成品牌参考。

**做法**:在项目根目录放一个 `DESIGN.md`,里面写死设计规则,**要求每个 prompt 都遵守它**。

### 最小可用 DESIGN.md(5 个决定搞定 80%)

```markdown
# DESIGN.md

## 1. 色彩(完全替换,不是扩展)
- Primary:  #1E293B(主色)
- Surface:  #FAFAFA(背景)
- Text:     #0F172A(文字)
- Accent:   #3B82F6(唯一强调色)
- 不使用 Tailwind 默认色,直接替换,让旧 token 无法编译

## 2. 字体
- 字体族:Geist / Inter(替换默认)
- 字号阶梯:14 / 16 / 20 / 28 / 40 / 56
- 字重:400 / 600 / 700

## 3. 间距
- 系统:4pt 基础(4/8/12/16/24/32/48/64/96)
- section 间距:96px(桌面)/ 48px(移动)

## 4. 圆角
- 统一:8px(卡片 12px)

## 5. 动效
- 缓动:cubic-bezier(0.16, 1, 0.3, 1)
- 时长:200ms(交互)/ 400ms(入场)
- 滚动动画用 GSAP,应用内用 Motion.dev
```

> **关键技巧**:色彩要"完全替换"而非"扩展"——让 AI 想用蓝色渐变都没有那个 token 可用。

### DESIGN.md 2026 进阶（Google Labs 开源规范）

- **规范**：DESIGN.md 是 Google Labs 的 Apache-2.0 开源文件格式规范（2026-04-21），Google Stitch 首创；`awesome-design-md` 已 **67k stars**（Stripe/Linear/Vercel/Notion/Apple/Airbnb/Tesla 等 60+ 品牌逆向工程文件）
- **结构**：产品与原则（3-5 句产品定位 + 3-5 条设计原则，如"clarity over density"）→ 色彩 tokens + **用法规则**（哪个场景用哪个色，防 AI 到处用 primary）→ 字体/间距/圆角 → 组件模式 → 动效/反模式
- **注意**（Adam Wathan/Tailwind 作者直言）：直接抄品牌 token 得到的是"山寨货"——参考可以，产品化必须写自己的 DESIGN.md
- **接入方式**：Claude Code 不会自动读 DESIGN.md，需在 CLAUDE.md 加一行 `Always read DESIGN.md at project root before generating any UI`；Cursor 可自动索引并 `@DESIGN.md` 引用
- **分文件**（Weft 实践）：`AGENTS.md`=怎么构建 / `DESIGN.md`=长什么样、什么感觉 / `README.md`=是什么；DESIGN.md 里要包含 tokens + 原则 + 组件行为 + 无障碍 + 反模式，而非只有色值
- **tokens 与 DTCG 打通**：DESIGN.md 里的值应来自 DTCG JSON 单源（见 D3 第七节），一处修改全端生效

---

## 铁律四:多趟生成(Multi-Pass)

复杂页面别一次生成。分层来:

```
第 1 趟｜布局   生成结构线框(先不管样式)
第 2 趟｜主题   套用颜色、字体、样式
第 3 趟｜内容   填入真实数据和文案(不要 Lorem Ipsum)
第 4 趟｜状态   生成 loading / empty / error 变体
第 5 趟｜响应式 适配不同屏幕尺寸
```

每一趟只让 AI 专注一件事,质量远高于"一次到位"。

---

## 铁律五:给规格,不给形容词

| ❌ 形容词(AI 只能猜) | ✅ 规格(AI 能执行) |
|---|---|
| "做得现代一点" | "无衬线字体,大量留白,弱阴影,8px 圆角" |
| "高级感" | "低版面率 55%+,字号跳跃率 4:1,哑光质感,单一强调色" |
| "有活力" | "强对比,饱和主色,微交互丰富,硬阴影" |
| "干净" | "高信噪比,去除装饰边框,分隔用留白" |
| "酷炫的动画" | "入场 fade+translateY 12px,stagger 50ms,ease-out 400ms" |

**这份术语库存在的意义,就是把形容词翻译成规格。**

---

## PROMPT 六要素框架

写 prompt 时至少包含 P + O + M:

- **P**latform 平台 —— Web / iOS / 移动端
- **R**ole/User 角色用户 —— 给谁用
- **O**utput 输出规格 —— 具体要什么(组件/页面/尺寸)
- **M**ood/Style 情绪风格 —— 气质三词 + 参考
- **P**atterns/Components 模式组件 —— 用什么组件(shadcn 等)
- **T**echnical 技术约束 —— React/Tailwind/框架

---

## 角色提示(Role Prompt)

在 prompt 开头加身份,能显著提升输出质量:
```
"作为一名资深产品设计师..."
"以顶级前端工程师的标准..."
"你是一位注重细节的品牌设计师..."
```

---

## 让 AI 自我批评

生成后追问:
```
"这个设计有什么可以改进的?"
"这看起来像不像通用的 SaaS 模板?如果是,怎么让它有独特性?"
"用 squint test 检查视觉层级,有问题吗?"
```
AI 往往能给出有用的自我修正。

---

## 三个验收测试(生成后必做)

### 1. 眯眼测试(Squint Test)
后退,眯眼看到页面模糊。应该看到:一个主视觉块 + 一个次视觉块 + 清晰的留白。
**如果所有东西都是同等重量(一堆同尺寸卡片),说明层级坏了。**

### 2. 标题扫读测试(Headline Scan)
只读标题。能看懂这个页面是干嘛的、为什么重要吗?
**如果标题读起来像通用 SaaS 模板,先重写文案再动视觉。**

### 3. 单一职责测试(One-Job Test)
每个 section 只该做一件事。
**如果一个 section 既想解释功能、又想放社会证明、又想推 CTA,拆成三个。**

---

## 工具选型(2026)

| 工具 | 最适合 | 特点 |
|---|---|---|
| **v0 (Vercel)** | React 组件脚手架 | shadcn/ui + Tailwind 原生,代码最干净 |
| **Lovable** | 落地页 / 营销页 | Framer Motion + 高端排版,美学最强,自带文案 |
| **Bolt.new** | 全栈应用从零搭 | 前后端 + 数据库一起生成 |
| **Cursor** | 在代码库里改 UI | 能看到你的代码库,配合 v0 用 |
| **Superdesign** | 探索多方案 | 有画布、变体对比 |

### 各工具技巧
- **v0**:直接点名 shadcn 组件("use Card, Button, Input");别跟它对抗 React+Tailwind;用 Design Mode 可视化迭代
- **Lovable**:先截图 → 生成 design.json → 让它"仅用于样式";先在 Coolors 定色再开工
- **Bolt/Lovable**:它们生成全栈,如果只要前端要说明;描述交互行为不只是布局

---

## Lovable 50+ 项目的完整工作流(值得照抄)

```
1. 从参考开始,不从描述开始
   截图 Dribbble → ChatGPT 生成 design.json(颜色/间距/字体/布局规则)
   → 告诉工具"仅用于样式"

2. 先锁定颜色再开建
   花 5 分钟在 Coolors 选色,导出确切色值,不要让 AI 边建边"修 UI"

3. 用 design.json 保证第一屏就一致

4. 逐屏生成,每屏遵守同一 design.json

5. 状态变体单独生成
```

---

## 人在回路(Human in the Loop)

- 永远不要把 AI 生成的东西直接推上生产
- 人工验证:对比度、无障碍、品牌语气
- 把 AI 输出当"草稿",不是"成品"
- 生成多个变体再挑

---

## 完整实战流程(整合版)

```
┌─ 阶段 1:定方向(纯文字,不写码)
│  - 目标 / 用户 / 核心任务(见"第0层思考")
│  - 气质三词 + 找 2-3 个参考(见 E3 经典产品)
│  - 让 AI 分析参考,提炼设计特征
│
├─ 阶段 2:建设计系统(DESIGN.md)
│  - Coolors 定色(完全替换默认)
│  - 定字号阶梯 / 间距 / 圆角 / 动效(见 D3 tokens)
│  - 写成 DESIGN.md,要求所有生成遵守
│
├─ 阶段 3:多趟生成
│  - 布局趟 → 主题趟 → 内容趟 → 状态趟 → 响应式趟
│  - 每趟给明确规格(用本库术语),不给形容词
│
├─ 阶段 4:加视觉效果与动效
│  - 从 V 板块挑效果,用 prompt 措辞
│  - 入场编排(见 B11)、微交互
│
├─ 阶段 5:验收与迭代
│  - 三个测试(眯眼/标题/单一职责)
│  - 让 AI 自我批评
│  - 对照 B7 廉价感/高级感清单
│  - 边界情况检查(见 B14)
│
└─ 阶段 6:人工把关
   - 对比度 / 无障碍 / 品牌语气
   - 挑变体、微调、上线
```

---

## 必读资源(业界公认)

- **Refactoring UI**(Adam Wathan & Steve Schoger)—— 教非设计师做出好判断的经典
- **Anthony Hobday 的视觉规则** —— 几十条无需判断力的规则,像 lint 一样跑一遍 AI 输出
- **awesome-design-md**(GitHub)—— 66+ 现成 DESIGN.md 品牌参考
- **shadcn/ui** —— AI 工具原生支持,主题化是获得独特外观最便宜的路径
- **NN/g 原型研究** —— "分析参考 → 提炼属性 → 应用"方法

---

## 一句话总结

> **通用感,是一个 prompt 被迫同时选品味、做探索、写代码时的产物。**
> **把这三件事拆开,分别用对工具和明确规格,普通人也能生成顶级前端。**

而"明确规格"需要的所有词汇,就在这个库的其余文档里。**方法论(本文) + 术语库(其余)= 从 0 到 1 做出世界级前端的完整武器。**

---

## 检验:你的 AI 工作流是否专业

- [ ] 有没有先用文字规划,而不是直接让 AI 出成品
- [ ] 有没有用参考(截图/竞品),而不是纯描述
- [ ] 有没有 DESIGN.md 或等价的设计系统约束
- [ ] 色彩是不是完全替换默认(而非扩展)
- [ ] 是不是多趟生成(布局/主题/内容/状态/响应式)
- [ ] prompt 里是规格还是形容词
- [ ] 生成后跑没跑三个测试
- [ ] 有没有让 AI 自我批评
- [ ] 有没有人工把关再上线
