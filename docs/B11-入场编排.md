# B11 · 入场编排

> 用户进入页面看到的不该是"淡入",而是一场精心编排的开幕。
> 这是最容易做出"高级感"的单点突破。

---

## 一、入场编排的三层结构

好的入场不是所有元素一起 fade,而是分层依次登场:

```
第 1 层｜结构层 (Structure)   0-200ms
  背景 / 大区块 / 导航骨架就位
  作用:建立舞台,让用户知道"这是个什么形状的页面"

第 2 层｜内容层 (Content)     200-600ms
  标题 → 副标 → 正文 → 图片
  作用:传递信息,按视线路径依次浮现

第 3 层｜装饰层 (Accent)      600-1000ms
  微动效 / 光效 / 装饰图形 / 次要元素
  作用:点睛,增加"活着"的感觉
```

**关键**:三层不重叠,依次推进,总时长控制在 800-1200ms 内。

---

## 二、编排原则（Choreography Principles）

### 1. 锚点先入（Anchor First）
主标题/主视觉最先出现,先给眼睛一个落点。
> ❌ 所有元素同时淡入 → 眼睛不知道看哪
> ✅ H1 先出 → 副标跟进 → CTA 最后 → 视线自然被引导

### 2. 视线路径入场（Guided Entrance）
元素按用户该看的顺序出现,动画本身就是引导。
```
Z 型页面:左上 Logo → 右上导航 → 中央标题 → 右下 CTA
F 型页面:顶部标题 → 左侧列表依次向下
```

### 3. 错峰入场（Stagger）★最实用
兄弟元素错开出现,不要齐刷刷。
```
间隔 30ms  — 极紧凑,大量元素
间隔 50ms  — 标准推荐
间隔 80ms  — 从容,少量元素
间隔 120ms+ — 显得故意、拖沓
```

### 4. 由主到次（Priority Cascade）
视觉权重高的先出,但主 CTA 可以**故意最后**(押韵收尾,吸引注意)。

### 5. 背景先安（Set the Stage）
大面积色块/渐变/图片先就位,细节后填充。避免"内容先出来但背景还在加载"。

### 6. 三段式动画（Anticipation → Action → Follow-through）
借自迪士尼动画原则:
```
预备 (Anticipation)  — 轻微反向蓄力(可选,增加生命感)
主动 (Action)        — 主要位移/淡入
收尾 (Follow-through)— 略微过冲后回弹(spring)
```

### 7. 方向一致（Directional Consistency）
一屏之内所有元素同方向入场(全部从下,或全部从左)。混方向会乱。

### 8. 距离克制（Restrained Distance）
```
4-8px    极克制、精密感(Linear 风格)
8-16px   标准、自然
16-32px  明显、有戏剧感
32px+    夸张,慎用
```
**规律**:位移越小越显高级。

---

## 三、入场动效类型

| 类型 | 效果 | 气质 |
|---|---|---|
| **Fade in** | 纯透明度 0→1 | 最安全、克制 |
| **Fade + Rise** | 淡入 + 从下浮起 | 最常用、自然 |
| **Fade + Scale** | 淡入 + 0.96→1 | 有存在感 |
| **Slide in** | 从边缘滑入 | 有方向感 |
| **Blur in** | 模糊 → 清晰 | 梦幻、高级 |
| **Mask reveal** | 遮罩擦除揭示 | 精致、有设计感 |
| **Clip reveal** | 裁切逐步展开 | 现代、锐利 |
| **Line draw** | 线条绘制 | 精密、技术感 |
| **Text by word** | 逐词出现 | 叙事感 |
| **Text by char** | 逐字出现 | 打字机感、AI 感 |
| **Counter tween** | 数字滚动增长 | 数据感、有力 |
| **Stagger grid** | 网格依次亮起 | 系统感、科技 |
| **Curtain** | 幕布拉开 | 仪式感 |
| **Zoom out** | 从大缩到位 | 电影感 |

---

## 四、缓动与时长

### 入场用 ease-out（快→慢）
"冲出来然后稳稳落定",符合物理直觉。
```css
/* 标准入场 */
cubic-bezier(0, 0, 0.2, 1)

/* 更利落 */
cubic-bezier(0.16, 1, 0.3, 1)   /* easeOutExpo,推荐 */

/* 有弹性 */
spring(stiffness: 120, damping: 14)
```

### 时长建议
```
单个元素入场    300-500ms
一屏完整编排    800-1200ms
品牌开幕动画    1500-2500ms(仅首访)
```

**警告**:超过 1.5s 的入场用户会觉得"卡"。

---

## 五、加载 → 内容的衔接

这一步做不好,前面全废。

### 骨架屏交接
```
骨架屏 (shimmer)
  ↓ crossfade 200ms(不是硬切!)
真实内容
```

### 图片衔接
```
LQIP 模糊小图
  ↓ 真图加载完成
blur(20px) → blur(0) 400ms
```

### 防跳动（CLS）
提前用 `aspect-ratio` 占好位置,图片加载不推挤布局。

---

## 六、首访 vs 回访

| | 首次访问 | 回访 |
|---|---|---|
| 品牌开幕动画 | ✅ 播放 | ❌ 跳过 |
| 入场编排 | 完整(1000ms) | 简化(300ms) |
| 引导提示 | 显示 | 隐藏 |
| 内容 | 从头讲 | 直入主题 |

**实现思路**:localStorage 记录访问次数。

---

## 七、滚动入场（Scroll Reveal）

首屏之后的 section 靠滚动触发。

### 触发时机
```
元素进入视口 20%   → 太早,还没看到就动完了
元素进入视口 40%   → 推荐 ✅
元素进入视口 60%   → 略晚
```

### 触发一次 vs 反复
- **只触发一次(推荐)** — 滚回去不重播,不烦
- 反复触发 — 只用于装饰性微动

### 现代实现(无 JS)
```css
@keyframes reveal {
  from { opacity: 0; transform: translateY(16px); }
  to   { opacity: 1; transform: translateY(0); }
}
.section {
  animation: reveal both;
  animation-timeline: view();
  animation-range: entry 20% cover 40%;
}
```

---

## 八、光标与后续引导

入场结束不该是"死"的,要留个引子:

- **滚动暗示** — 底部箭头微弹 / 下一 section 露出 10%
- **CTA 呼吸** — 主按钮极轻微的 pulse(2s 一次,幅度 2%)
- **光标跟随** — 高级页面的 spotlight 跟随鼠标
- **微光扫过** — CTA 按钮的 shimmer 光带(3s 一次)

**注意**:这些要极其克制,否则变成廉价闪烁。

---

## 九、减弱动效适配

```css
@media (prefers-reduced-motion: reduce) {
  /* 去掉位移,保留淡入 */
  .element {
    animation: fade-only 200ms ease-out both;
    transform: none !important;
  }
}
```

**原则**:不完全去掉动画(会失去反馈),而是"只保留 fade,去掉位移和缩放"。

---

## 十、完整入场编排 Prompt 模板

### 标准克制型（推荐大多数场景）
```
【入场编排】
三层结构,总时长 900ms:

第 1 层 结构层 (0-200ms):
- 背景/渐变立即就位(无动画)
- 导航栏 fade-in 200ms

第 2 层 内容层 (200-700ms):
- H1 主标题:fade + translateY(12px→0),400ms,easeOutExpo,delay 200ms
- 副标题:同上,delay 300ms
- 主 CTA:同上,delay 450ms(最后出现,吸引注意)
- Hero 视觉:fade + scale(0.98→1),500ms,delay 250ms

第 3 层 装饰层 (700-900ms):
- 背景光效/装饰图形:fade-in 400ms,delay 700ms
- 滚动暗示箭头:fade + 微弹,delay 800ms

【参数】
- 缓动:cubic-bezier(0.16, 1, 0.3, 1)
- 位移距离:12px(克制,显精密)
- 错峰间隔:50ms
- 方向:统一从下往上

【滚动入场】
- 触发:元素进入视口 40%
- 只触发一次,不反复
- 效果:fade + translateY(16px→0),400ms
- 卡片组:stagger 60ms
- 实现:CSS scroll-driven animations

【加载衔接】
- 骨架屏 shimmer → 内容 crossfade 200ms
- 图片 blur-up:blur(20px)→blur(0) 400ms
- 用 aspect-ratio 预占位防 CLS

【首访/回访】
- 首访播完整编排;回访简化到 300ms

【无障碍】
- prefers-reduced-motion 时只保留 fade,去掉所有 transform
```

### 品牌大片型
```
【入场编排】品牌级开幕,总时长 2200ms(仅首访)

0-600ms:   Logo 从中央 blur-in + scale(1.1→1)
600-900ms: Logo 上移到导航位,同时缩小
900-1400ms:Hero 巨型标题逐词出现(每词 stagger 80ms)
1400-1800ms:Hero 视觉 mask reveal 从下往上揭示
1800-2200ms:CTA + 装饰光效浮现

【缓动】全程 easeOutExpo,Logo 用 spring 带轻微过冲
【位移】较大(24-32px),制造戏剧感
【回访】完全跳过,直接显示最终状态
```

---

## 十一、常见错误

- ❌ 所有元素同时 fade(没有编排,平庸)
- ❌ 位移太大(>32px 显廉价夸张)
- ❌ 时长太长(>1.5s 觉得卡)
- ❌ stagger 间隔太大(>100ms 显故意)
- ❌ 混方向入场(有的从左有的从下)
- ❌ 用 ease-in-out 做入场(应该 ease-out)
- ❌ 骨架屏硬切到内容(应该 crossfade)
- ❌ 回访还播完整开幕动画(烦)
- ❌ 滚动入场反复触发
- ❌ 忽略 prefers-reduced-motion
- ❌ 装饰动效抢过内容(闪烁廉价)

---

## 十二、检验清单

- [ ] 分了三层(结构/内容/装饰)依次入场
- [ ] 有明确的锚点先入
- [ ] 入场顺序符合视线路径
- [ ] stagger 间隔 50ms 左右
- [ ] 位移距离克制(8-16px)
- [ ] 缓动用 ease-out 系
- [ ] 总时长 ≤1200ms
- [ ] 方向统一
- [ ] 骨架屏 crossfade 衔接
- [ ] 图片 blur-up + aspect-ratio 防跳
- [ ] 首访/回访有区分
- [ ] 结束后有后续引导(滚动暗示)
- [ ] 支持 reduced-motion
