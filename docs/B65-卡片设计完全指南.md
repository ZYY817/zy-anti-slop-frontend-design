# B65 · 卡片设计完全指南

> 卡片是现代界面的**原子单位**。一个产品有 80% 的内容都装在卡片里。
> 卡片做好了,整个产品就精致了;卡片做糙了,再好的配色也救不回来。
> 但大部分人只知道"加个圆角加个阴影",不知道**卡片内部的 12 个间距决策**、**hover 的 5 层反馈**、**群组时的对齐工程**。
> 这份文档拆的是:卡片的解剖学、内功和进阶技巧。

---

## 一、卡片术语表

| 中文术语 | 英文术语 | 一句话含义 |
|---|---|---|
| 卡片 | Card | 承载一组相关内容的矩形容器 |
| 卡片解剖 | Card Anatomy | 卡片的组成部分结构 |
| 媒体区 | Media Area | 卡片顶部的图片/视频区 |
| 内容区 | Content Area | 卡片的文字内容区 |
| 操作区 | Action Area | 卡片的按钮/链接区 |
| 元信息 | Metadata | 时间/作者/标签等辅助信息 |
| 卡片头部 | Card Header | 标题和图标所在区域 |
| 卡片脚部 | Card Footer | 底部操作或状态区 |
| 内边距 | Padding | 卡片边缘到内容的距离 |
| 内部间距 | Internal Gap | 卡片内元素之间的距离 |
| 卡片间距 | Card Gap | 相邻卡片之间的距离 |
| 卡片宽高比 | Aspect Ratio | 卡片或其媒体区的宽高比例 |
| 卡片网格 | Card Grid | 卡片的多列排布方式 |
| 瀑布流 | Masonry | 不等高卡片的错落排布 |
| 等高卡片 | Equal Height Cards | 强制同行卡片等高 |
| 可点击卡片 | Clickable Card | 整卡可点击的交互卡 |
| 卡片链接区 | Card Link Region | 卡片内的可点击范围 |
| 卡片抬升 | Card Elevation | 卡片的悬浮层级 |
| 悬停反馈 | Hover Feedback | 鼠标悬停时的视觉变化 |
| 卡片选中态 | Selected State | 被选中的卡片视觉 |
| 卡片骨架 | Card Skeleton | 卡片的加载占位视觉 |
| 可展开卡片 | Expandable Card | 可展开显示更多内容的卡 |
| 卡片堆叠 | Card Stack | 多张卡片重叠的视觉 |
| 卡片轮播 | Card Carousel | 横向滑动的卡片序列 |
| 卡片拖拽 | Card Drag | 拖拽重排卡片 |
| 卡片密度 | Card Density | 卡片的紧凑/宽松程度 |

---

## 二、卡片解剖学

### 2.1 完整结构

```
┌─────────────────────────────────┐
│  ┌───────────────────────────┐  │ ← 外 padding
│  │                           │  │
│  │       媒体区(16:9)        │  │ ← 图片/视频
│  │                           │  │
│  └───────────────────────────┘  │
│         ↕ 16px                  │ ← 媒体到内容
│  [标签]  [标签]                 │ ← 分类标签
│         ↕ 8px                   │
│  卡片标题(18px/600)             │ ← 标题
│         ↕ 6px                   │
│  描述文字,可能两行,超出         │ ← 描述
│  用省略号处理...                │
│         ↕ 12px                  │
│  ─────────────────────────      │ ← 可选分隔线
│         ↕ 12px                  │
│  👤 作者  ·  2 天前   [→]       │ ← 元信息 + 操作
│                                 │
└─────────────────────────────────┘
```

### 2.2 12 个间距决策

```css
.card {
  /* 1. 卡片外 padding */
  --card-padding: 20px;

  /* 2. 媒体区到内容区 */
  --media-to-content: 16px;

  /* 3. 标签到标题 */
  --tag-to-title: 10px;

  /* 4. 标题到描述 */
  --title-to-desc: 6px;

  /* 5. 描述到元信息 */
  --desc-to-meta: 16px;

  /* 6. 元信息内部元素间距 */
  --meta-gap: 8px;

  /* 7. 操作按钮之间 */
  --action-gap: 12px;

  /* 8. 卡片之间(网格 gap) */
  --card-gap: 20px;

  /* 9. 图标到文字 */
  --icon-to-text: 8px;

  /* 10. 头像到名字 */
  --avatar-to-name: 10px;

  /* 11. 分隔线上下 */
  --divider-space: 12px;

  /* 12. 卡片圆角(与 padding 相关) */
  --card-radius: 12px;
}
```

### 2.3 精工规格(标准卡片)

```css
.card {
  padding: 20px;
  border-radius: 12px;
  background: var(--surface);
  border: 1px solid oklch(92% 0.005 265);
  box-shadow: 0 1px 2px oklch(0% 0 0 / 0.03);
  display: flex;
  flex-direction: column;
  gap: 0;  /* 用具体 margin 精细控制,不用统一 gap */
}

/* 媒体区:负 margin 让图片顶到卡片边缘 */
.card-media {
  margin: -20px -20px 16px;   /* 抵消 padding */
  aspect-ratio: 16 / 9;
  overflow: hidden;
  /* 上圆角与卡片一致,下圆角为 0 */
  border-radius: 12px 12px 0 0;
}

.card-media img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
  transition: transform 500ms cubic-bezier(0.2, 0, 0, 1);
}

.card-tags {
  display: flex;
  gap: 6px;
  margin-bottom: 10px;
}

.card-title {
  font-size: 17px;
  font-weight: 600;
  line-height: 1.35;
  color: var(--text-primary);
  margin: 0 0 6px;

  /* 两行截断 */
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
  text-wrap: pretty;
}

.card-desc {
  font-size: 14px;
  line-height: 1.55;
  color: var(--text-secondary);
  margin: 0 0 16px;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.card-meta {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 12px;
  color: var(--text-tertiary);
  margin-top: auto;   /* 关键:推到底部,保证卡片等高时元信息对齐 */
}
```

---

## 三、圆角的嵌套数学

### 3.1 嵌套圆角公式

```
外圆角 = 内圆角 + padding
```

```css
.card {
  --inner-radius: 8px;
  --padding: 16px;

  padding: var(--padding);
  border-radius: calc(var(--inner-radius) + var(--padding));  /* = 24px */
}

.card > .inner-element {
  border-radius: var(--inner-radius);   /* 8px */
}
```

**为什么**:如果内外圆角相同,内部元素会显得"角太尖";按公式算才视觉同心。

### 3.2 媒体区的圆角处理

```css
/* 图片顶到边缘:上圆角 = 卡片圆角,下圆角 = 0 */
.card-media {
  border-radius: var(--card-radius) var(--card-radius) 0 0;
}

/* 图片内嵌(有 padding):四角都要小一点 */
.card-media-inset {
  border-radius: calc(var(--card-radius) - var(--card-padding) / 2);
}
```

---

## 四、Hover 的五层反馈

### 4.1 完整 hover 系统

```css
.card {
  position: relative;
  transition:
    transform 300ms cubic-bezier(0.2, 0, 0, 1),
    box-shadow 300ms cubic-bezier(0.2, 0, 0, 1),
    border-color 300ms;
  cursor: pointer;
}

.card:hover {
  /* 层1:位移(浮起) */
  transform: translateY(-4px);

  /* 层2:阴影加深 */
  box-shadow:
    0 2px 4px oklch(0% 0 0 / 0.04),
    0 12px 32px oklch(0% 0 0 / 0.10);

  /* 层3:边框变色 */
  border-color: oklch(85% 0.010 265);
}

/* 层4:图片微放大(Ken Burns) */
.card:hover .card-media img {
  transform: scale(1.05);
}

/* 层5:标题变色 */
.card:hover .card-title {
  color: var(--brand);
}

/* 层6(可选):箭头前移 */
.card:hover .card-arrow {
  transform: translateX(4px);
}
```

### 4.2 克制版(专业产品)

```css
/* 只有背景微变,不位移不放大 */
.card-subtle {
  transition: background 150ms, border-color 150ms;
}
.card-subtle:hover {
  background: oklch(99% 0.003 265);
  border-color: oklch(88% 0.008 265);
}
```

### 4.3 Active/按下态

```css
.card:active {
  transform: translateY(-1px) scale(0.995);
  transition-duration: 100ms;   /* 按下反馈要更快 */
}
```

---

## 五、可点击卡片的正确做法

### 5.1 问题:整卡可点击 + 内部有链接

```html
<!-- ❌ 错误:嵌套 <a> 是无效 HTML -->
<a href="/post/1">
  <div class="card">
    <h3>标题</h3>
    <a href="/author/1">作者</a>  <!-- 嵌套链接! -->
  </div>
</a>
```

### 5.2 解法:伪元素覆盖法(推荐)

```html
<article class="card">
  <img src="..." alt="">
  <h3><a href="/post/1" class="card-link">文章标题</a></h3>
  <p>描述...</p>
  <div class="card-meta">
    <a href="/author/1" class="meta-link">作者名</a>
    <time>2 天前</time>
  </div>
</article>
```

```css
.card {
  position: relative;
}

/* 主链接的伪元素覆盖整卡 */
.card-link::after {
  content: "";
  position: absolute;
  inset: 0;
  z-index: 1;
}

/* 内部次级链接提升 z-index,可正常点击 */
.card .meta-link {
  position: relative;
  z-index: 2;
}

/* 保证文字仍可选中 */
.card-link::after {
  /* 不阻挡文字选择 */
}
.card p {
  position: relative;
  z-index: 2;
  /* 允许选中描述文字 */
  pointer-events: none;  /* 但不拦截点击 */
}
.card p::selection { }
```

### 5.3 键盘可访问性

```css
/* focus 时整卡显示 focus ring */
.card:focus-within {
  outline: 2px solid var(--brand);
  outline-offset: 2px;
}

/* 移除内部链接的默认 outline(已经由卡片显示) */
.card-link:focus-visible {
  outline: none;
}
```

---

## 六、卡片群组布局

### 6.1 响应式网格(最常用)

```css
.card-grid {
  display: grid;
  /* 自动列数:最小 280px,自动填充 */
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 20px;
}

/* 更精确:auto-fit 让最后一行也拉满 */
.card-grid-fit {
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
}
```

### 6.2 等高卡片(元信息底部对齐)

```css
.card-grid { display: grid; /* grid 自带等高 */ }

.card {
  display: flex;
  flex-direction: column;
  height: 100%;
}

.card-meta {
  margin-top: auto;   /* 关键:推到底部 */
}
```

### 6.3 CSS Masonry(瀑布流,2025+)

```css
/* 原生 Masonry(Chrome 实验中) */
.masonry {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  grid-template-rows: masonry;
  gap: 20px;
}

/* 降级方案:columns */
@supports not (grid-template-rows: masonry) {
  .masonry {
    display: block;
    columns: 280px;
    column-gap: 20px;
  }
  .masonry .card {
    break-inside: avoid;
    margin-bottom: 20px;
  }
}
```

### 6.4 横向轮播

```css
.card-carousel {
  display: flex;
  gap: 16px;
  overflow-x: auto;
  scroll-snap-type: x mandatory;
  scroll-padding-inline: 20px;
  padding-inline: 20px;
  scrollbar-width: none;
  -ms-overflow-style: none;
}
.card-carousel::-webkit-scrollbar { display: none; }

.card-carousel .card {
  flex: 0 0 280px;
  scroll-snap-align: start;
}

/* 移动端优化:第一张和最后一张有边距 */
.card-carousel::after {
  content: "";
  flex: 0 0 1px;
}
```

### 6.5 特色卡片(占两格)

```css
.card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 20px;
}

.card.featured {
  grid-column: span 2;
  grid-row: span 2;
}

/* 移动端取消跨列 */
@media (width < 640px) {
  .card.featured {
    grid-column: span 1;
    grid-row: span 1;
  }
}
```

---

## 七、卡片状态

### 7.1 选中态

```css
.card.selected {
  border-color: var(--brand);
  border-width: 2px;
  /* 关键:padding 减 1px 抵消边框变粗,防止内容跳动 */
  padding: 19px;
  background: var(--brand-alpha-5);
}

/* 或用 outline 不影响布局 */
.card.selected {
  outline: 2px solid var(--brand);
  outline-offset: -2px;   /* 向内,不占空间 */
  background: var(--brand-alpha-5);
}

/* 选中标记 */
.card.selected::before {
  content: "✓";
  position: absolute;
  top: 12px;
  right: 12px;
  width: 20px;
  height: 20px;
  background: var(--brand);
  color: white;
  border-radius: 50%;
  display: grid;
  place-items: center;
  font-size: 12px;
  z-index: 3;
}
```

### 7.2 禁用态

```css
.card.disabled {
  opacity: 0.5;
  pointer-events: none;
  filter: grayscale(0.5);
  cursor: not-allowed;
}
```

### 7.3 加载骨架

```css
.card-skeleton {
  padding: 20px;
  border-radius: 12px;
  background: var(--surface);
  border: 1px solid oklch(92% 0.005 265);
}

.skeleton-media {
  margin: -20px -20px 16px;
  aspect-ratio: 16/9;
  background: oklch(94% 0.005 265);
  border-radius: 12px 12px 0 0;
}

.skeleton-line {
  height: 12px;
  border-radius: 4px;
  background: linear-gradient(
    90deg,
    oklch(94% 0.005 265) 0%,
    oklch(97% 0.003 265) 50%,
    oklch(94% 0.005 265) 100%
  );
  background-size: 200% 100%;
  animation: shimmer 1.5s linear infinite;
  margin-bottom: 8px;
}

.skeleton-line:nth-child(1) { width: 40%; }
.skeleton-line:nth-child(2) { width: 90%; }
.skeleton-line:nth-child(3) { width: 70%; }

@keyframes shimmer {
  to { background-position: -200% 0; }
}
```

**关键**:骨架的**每一条线的宽度**要模拟真实内容的长度分布,不要都一样长。

---

## 八、可展开卡片

```html
<article class="card expandable">
  <header class="card-header">
    <h3>卡片标题</h3>
    <button class="expand-btn" aria-expanded="false" aria-controls="detail-1">
      <svg class="chevron"></svg>
    </button>
  </header>
  <div class="card-detail" id="detail-1">
    <p>展开后显示的详细内容...</p>
  </div>
</article>
```

```css
.card-detail {
  /* 用 grid 实现 height auto 动画(2025 技巧) */
  display: grid;
  grid-template-rows: 0fr;
  transition: grid-template-rows 300ms cubic-bezier(0.2, 0, 0, 1);
  overflow: hidden;
}

.card.expanded .card-detail {
  grid-template-rows: 1fr;
}

.card-detail > * {
  overflow: hidden;
  min-height: 0;
}

/* chevron 旋转 */
.chevron {
  transition: transform 300ms cubic-bezier(0.2, 0, 0, 1);
}
.card.expanded .chevron {
  transform: rotate(180deg);
}
```

**或者用 CSS `interpolate-size`(2025+)**:
```css
.card-detail {
  interpolate-size: allow-keywords;
  height: 0;
  overflow: hidden;
  transition: height 300ms;
}
.card.expanded .card-detail {
  height: auto;   /* 现在可以过渡到 auto 了! */
}
```

---

## 九、卡片堆叠与拖拽

### 9.1 卡片堆叠

```css
.card-stack {
  position: relative;
  height: 200px;
}

.card-stack .card {
  position: absolute;
  inset: 0;
  transition: transform 300ms, opacity 300ms;
}

.card-stack .card:nth-child(1) {
  transform: translateY(0) scale(1);
  z-index: 3;
}
.card-stack .card:nth-child(2) {
  transform: translateY(8px) scale(0.96);
  z-index: 2;
  opacity: 0.7;
}
.card-stack .card:nth-child(3) {
  transform: translateY(16px) scale(0.92);
  z-index: 1;
  opacity: 0.4;
}
```

### 9.2 拖拽状态

```css
.card[draggable="true"] {
  cursor: grab;
}

.card.dragging {
  cursor: grabbing;
  opacity: 0.9;
  transform: scale(1.03) rotate(-1.5deg);
  box-shadow: 0 20px 48px oklch(0% 0 0 / 0.2);
  z-index: 1000;
}

/* 放置目标提示 */
.drop-zone.active {
  background: var(--brand-alpha-5);
  border: 2px dashed var(--brand);
  border-radius: 12px;
}

/* 占位空隙 */
.card-placeholder {
  background: oklch(0% 0 0 / 0.03);
  border: 2px dashed oklch(85% 0.010 265);
  border-radius: 12px;
}
```

---

## 十、移动端卡片

### 10.1 移动端调整

```css
@media (width < 640px) {
  .card {
    padding: 16px;
    border-radius: 12px;
  }

  .card-media {
    margin: -16px -16px 12px;
    aspect-ratio: 16 / 10;   /* 移动端稍高 */
  }

  .card-title {
    font-size: 16px;
    -webkit-line-clamp: 2;
  }

  .card-desc {
    -webkit-line-clamp: 2;   /* 移动端少显示 */
  }

  .card-grid {
    grid-template-columns: 1fr;
    gap: 12px;
  }

  /* 移动端不要 hover 效果 */
  .card:hover {
    transform: none;
    box-shadow: 0 1px 2px oklch(0% 0 0 / 0.03);
  }

  /* 改用 active 反馈 */
  .card:active {
    background: oklch(97% 0.005 265);
    transform: scale(0.985);
  }
}

/* 只在支持 hover 的设备上启用 hover */
@media (hover: hover) and (pointer: fine) {
  .card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 32px oklch(0% 0 0 / 0.10);
  }
}
```

### 10.2 滑动操作(Swipe Actions)

```css
.card-swipeable {
  position: relative;
  overflow: hidden;
  touch-action: pan-y;
}

.card-swipeable .card-content {
  transition: transform 250ms cubic-bezier(0.2, 0, 0, 1);
}

.card-swipeable.swiped .card-content {
  transform: translateX(-80px);
}

.swipe-actions {
  position: absolute;
  right: 0;
  top: 0;
  bottom: 0;
  width: 80px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: var(--error);
  color: white;
}
```

---

## 十一、Prompt 模板

```
【卡片设计】

═══ 卡片基础规格 ═══
padding: 20px(移动端 16px)
border-radius: 12px
background: var(--surface)
border: 1px solid oklch(92% 0.005 265)
box-shadow: 0 1px 2px oklch(0% 0 0 / 0.03)
display: flex; flex-direction: column

═══ 内部间距(12 个决策) ═══
媒体区 → 内容: 16px
标签 → 标题: 10px
标题 → 描述: 6px
描述 → 元信息: 16px
元信息内部 gap: 8px
图标 → 文字: 8px
头像 → 名字: 10px
卡片之间 gap: 20px

═══ 媒体区 ═══
aspect-ratio: 16/9(移动端 16/10)
margin: -20px -20px 16px(负 margin 顶到边缘)
border-radius: 12px 12px 0 0
img: object-fit: cover + transition transform 500ms

═══ 文字规格 ═══
标题: 17px / 600 / line-height 1.35 / -webkit-line-clamp 2
描述: 14px / 400 / line-height 1.55 / text-secondary / clamp 2
元信息: 12px / text-tertiary / margin-top auto(推到底部)

═══ 嵌套圆角公式 ═══
外圆角 = 内圆角 + padding
内部元素 border-radius = 卡片圆角 - padding

═══ Hover 五层反馈 ═══
1. transform: translateY(-4px)
2. box-shadow 加深到 0 12px 32px alpha 0.10
3. border-color 变深一档
4. 图片 scale(1.05) 500ms
5. 标题变主色
transition: 300ms cubic-bezier(0.2, 0, 0, 1)
只在 @media (hover: hover) and (pointer: fine) 内启用

═══ Active 态 ═══
transform: translateY(-1px) scale(0.995), 100ms(更快)
移动端: background 变浅灰 + scale(0.985)

═══ 可点击卡片 ═══
用伪元素覆盖法:
.card { position: relative }
.card-link::after { content:""; position:absolute; inset:0; z-index:1 }
内部次级链接 { position: relative; z-index: 2 }
键盘: .card:focus-within { outline: 2px solid brand; outline-offset: 2px }

═══ 群组布局 ═══
grid-template-columns: repeat(auto-fill, minmax(280px, 1fr))
gap: 20px
等高: .card { height: 100% } + .card-meta { margin-top: auto }
瀑布流: grid-template-rows: masonry(降级用 columns)
特色卡: grid-column: span 2

═══ 状态 ═══
选中: outline 2px solid brand + outline-offset -2px + 背景 brand-alpha-5 + 右上 ✓
禁用: opacity 0.5 + grayscale(0.5) + pointer-events none
骨架: shimmer 动画,每行宽度不同(40%/90%/70%)模拟真实内容

═══ 可展开 ═══
grid-template-rows: 0fr → 1fr 过渡(或 interpolate-size + height auto)
chevron rotate(180deg) 300ms

═══ 拖拽 ═══
dragging: scale(1.03) rotate(-1.5deg) + shadow 0 20px 48px + z-index 1000
占位: 虚线边框 + 极浅背景
```

---

## 十二、踩坑指南

### 坑1:卡片内外圆角相同
**症状**:内部图片的角看起来"太尖",不同心。
**解法**:外圆角 = 内圆角 + padding。

### 坑2:嵌套 `<a>` 标签
**症状**:整卡链接 + 内部作者链接 = 无效 HTML。
**解法**:伪元素覆盖法 + 内部链接 z-index 提升。

### 坑3:等高卡片元信息不对齐
**症状**:标题长度不同,底部时间戳错乱。
**解法**:`.card-meta { margin-top: auto }` + 卡片 `height: 100%`。

### 坑4:选中态边框加粗导致内容跳动
**症状**:选中时 border 从 1px 变 2px,内容位移 1px。
**解法**:用 `outline` + `outline-offset: -2px`,或 padding 减 1px 补偿。

### 坑5:移动端保留 hover 效果
**症状**:手机点了卡片后一直停留在 hover 态。
**解法**:hover 效果包在 `@media (hover: hover) and (pointer: fine)` 内。

### 坑6:骨架屏所有行等长
**症状**:骨架屏看起来假,像表格。
**解法**:每行宽度不同(40%/90%/70%),模拟真实内容分布。

### 坑7:图片未设 aspect-ratio 导致 CLS
**症状**:图片加载后卡片高度跳变。
**解法**:媒体区必须设 `aspect-ratio`。

### 坑8:文字截断没有 text-wrap
**症状**:标题最后一行只有一个字。
**解法**:`text-wrap: pretty` + `-webkit-line-clamp`。

### 坑9:卡片阴影太重
**症状**:每个卡片都强阴影,像"漂浮的塑料片"。
**解法**:静态阴影极弱(0 1px 2px alpha 0.03),hover 才加深。

### 坑10:高度动画用 max-height
**症状**:`max-height: 0 → 1000px` 过渡不自然,速度不均。
**解法**:用 `grid-template-rows: 0fr → 1fr` 或 `interpolate-size` + `height: auto`。

### 坑11:卡片网格 minmax 太小
**症状**:窄屏时卡片被压到 200px,内容挤爆。
**解法**:minmax 最小值不低于 260-280px。

### 坑12:轮播卡片没有 scroll-snap
**症状**:横滑后卡片停在半截位置。
**解法**:`scroll-snap-type: x mandatory` + `scroll-snap-align: start`。

---

## 十三、谁在用(卡片典范)

| 产品 | 卡片手法 | 值得学的 |
|---|---|---|
| **Airbnb** | 房源卡 | 图片轮播 + 收藏按钮 + 完美的信息层级 |
| **Pinterest** | 瀑布流卡片 | Masonry 布局的鼻祖,hover 操作层 |
| **Linear** | 极简卡片 | 静态几乎无阴影,hover 微妙背景变化 |
| **Notion** | 数据库卡片视图 | 可自定义显示字段,拖拽重排 |
| **Stripe Dashboard** | 数据卡片 | 数字为主体,图表为辅,信息密度高 |
| **Dribbble** | 作品卡片 | 图片为绝对主体,信息极简 |
| **Vercel** | 项目卡片 | 边框而非阴影,极致克制 |
| **Trello** | 看板卡片 | 拖拽体验的标杆,占位提示清晰 |
| **App Store** | 特色卡片 | 大圆角 + 全出血图片 + 玻璃文字层 |
| **YouTube** | 视频卡片 | 缩略图 hover 预览 + 完美的元信息布局 |

---

## 检验清单

- [ ] 卡片 12 个内部间距都有明确规则
- [ ] 圆角遵守嵌套公式(外 = 内 + padding)
- [ ] 媒体区有 aspect-ratio(防 CLS)
- [ ] 媒体区用负 margin 顶到卡片边缘
- [ ] 标题/描述有 line-clamp 截断
- [ ] 元信息 margin-top: auto(等高时底部对齐)
- [ ] hover 有多层反馈(位移+阴影+图片+标题)
- [ ] hover 只在 @media (hover: hover) 内启用
- [ ] 移动端用 active 态代替 hover
- [ ] 可点击卡片用伪元素覆盖法(无嵌套 a)
- [ ] focus-within 显示 focus ring(键盘可访问)
- [ ] 选中态用 outline 不影响布局
- [ ] 骨架屏每行宽度不同(模拟真实内容)
- [ ] 静态阴影极弱,hover 才加深
- [ ] 网格 minmax 最小值 ≥ 260px
- [ ] 轮播有 scroll-snap
- [ ] 展开动画用 grid-template-rows 或 interpolate-size
- [ ] 拖拽有明确的"抬起"视觉(scale+rotate+强阴影)
