# B32 · 图片与媒体的视觉处理

> 图片往往占页面视觉重量的 50%+。图片处理不好,再好的排版也白搭。
> 从选图、构图、调色到技术优化,全套。

---

## 一、图片的视觉角色

先想清楚图片在页面里是什么角色:

| 角色 | 作用 | 处理重点 |
|---|---|---|
| **主视觉(Hero)** | 定调、冲击 | 高质量、构图讲究、可加文字蒙层 |
| **氛围(Ambient)** | 烘托、背景 | 模糊/降饱和/蒙层,退到背景 |
| **说明(Explanatory)** | 展示产品/功能 | 清晰、真实、聚焦细节 |
| **装饰(Decorative)** | 填充、点缀 | 克制、统一风格 |
| **内容(Content)** | 用户/文章图片 | 统一容器、防止破版 |

---

## 二、图片构图法则(选图/裁图)

### 三分法
主体放在九宫格交叉点,不是正中央。

### 视觉引导线
利用图片中的线条(道路/栏杆/视线)引导视线到重点。

### 负空间
图片留白区域可以放文字(hero 常用)。
- 选图时预留文字区
- 或裁剪出留白

### 视线方向
- 人物图:模特看向哪,用户视线就跟向哪
- 让模特"看向"CTA 或标题(引导)
- 避免模特看向屏幕外(把注意力带走)

### 裁剪比例
```
1:1    正方形(头像/产品/社交)
4:3    传统(相机)
3:2    摄影(35mm)
16:9   宽屏(视频/封面/hero)
21:9   影院(超宽 banner)
9:16   竖版(手机/短视频/故事)
3:4    竖版海报
```

### 焦点裁剪
```css
object-fit: cover;
object-position: center;   /* 或 top / 30% 20% 精确控制 */
```
用 `object-position` 保证裁剪时主体不被切掉。

---

## 三、图片调色(Color Grading)★ 统一感关键

**核心问题**:一堆来源不同、色调各异的图片放一起 = 廉价、混乱。

### 统一调色方案

**方案 1:统一滤镜**
```css
img {
  filter: saturate(0.9) contrast(1.05) brightness(1.02);
}
```
所有图片过同一套滤镜 → 视觉统一。

**方案 2:双色调(Duotone)**
```css
.image {
  background: linear-gradient(#4F46E5, #EC4899);
}
.image img {
  mix-blend-mode: luminosity;
  opacity: 0.85;
}
```
所有图片统一映射到品牌双色 → 强品牌感(Spotify 风)。

**方案 3:主色蒙层(Color Overlay)**
```css
.image::after {
  content: '';
  position: absolute; inset: 0;
  background: rgba(79, 70, 229, 0.2);   /* 品牌色蒙层 */
  mix-blend-mode: multiply;
}
```

**方案 4:统一后期风格**
- 全部降饱和(高级感)
- 全部暖调(温暖感)
- 全部高对比(戏剧感)
- 全部褪色(复古感)

### 调色的气质
- **降饱和 + 略暖** = 高级、电影感
- **高饱和 + 高对比** = 活力、年轻
- **褪色 + 颗粒** = 复古、胶片
- **冷调 + 高对比** = 科技、冷峻

---

## 四、图片上的文字(Text over Image)★

**核心问题**:文字放图片上经常看不清。

### 解决方案

**1. 渐变蒙层(Scrim)** — 最常用
```css
.hero::after {
  background: linear-gradient(
    to top,
    rgba(0,0,0,0.7) 0%,
    rgba(0,0,0,0.3) 40%,
    transparent 100%
  );
}
```
文字区域下方加深色渐变,保证可读。

**2. 整体压暗**
```css
.hero img { filter: brightness(0.6); }
```

**3. 文字阴影**
```css
.hero-text {
  text-shadow: 0 2px 12px rgba(0,0,0,0.6);
}
```

**4. 毛玻璃文字板**
```css
.text-panel {
  background: rgba(0,0,0,0.4);
  backdrop-filter: blur(12px);
  padding: 24px;
}
```

**5. 选图时就选有留白的**
主体在一侧,另一侧留白放文字。

### 对比度要求
文字与图片(最亮/最暗处)对比度仍需 ≥4.5:1。用 scrim 保证。

---

## 五、图片加载体验

### Blur-up(模糊占位)★
```
1. 先加载极小模糊图(LQIP, 20px 宽,base64 内联)
2. 真图加载完成
3. blur(20px) → blur(0) 过渡 400ms
```
用户不会看到白屏或突然弹出。

### 主色占位(Dominant Color)
先显示图片主色纯色块,加载完淡入真图。

### 骨架占位
```css
.image-skeleton {
  background: linear-gradient(90deg, #f0f0f0 25%, #e0e0e0 50%, #f0f0f0 75%);
  animation: shimmer 1.5s infinite;
}
```

### 防止布局跳动(CLS)
```css
.image-container {
  aspect-ratio: 16 / 9;   /* 提前占位 */
}
img { width: 100%; height: 100%; object-fit: cover; }
```

### 懒加载
```html
<img loading="lazy" decoding="async" src="..." alt="...">
```
首屏图片用 `loading="eager"` + `fetchpriority="high"`。

---

## 六、图片格式与优化

### 格式选择
| 格式 | 用途 | 特点 |
|---|---|---|
| **AVIF** | 首选 | 最小(比 JPG 小 50%),兼容性渐好 |
| **WebP** | 通用 | 比 JPG 小 30%,兼容性好 |
| **JPG** | 兜底 | 照片,有损 |
| **PNG** | 透明/图形 | 无损,大 |
| **SVG** | 图标/插画 | 矢量,无限缩放 |
| **WebP 动图** | 替代 GIF | 比 GIF 小得多 |

### 渐进降级
```html
<picture>
  <source type="image/avif" srcset="photo.avif">
  <source type="image/webp" srcset="photo.webp">
  <img src="photo.jpg" alt="描述" loading="lazy">
</picture>
```

### 响应式尺寸
```html
<img
  srcset="img-400.webp 400w, img-800.webp 800w, img-1600.webp 1600w"
  sizes="(max-width: 768px) 100vw, 50vw"
  src="img-800.webp" alt="描述">
```
不给手机发桌面尺寸的大图。

### 压缩目标
- Hero 大图:< 200KB
- 内容图:< 100KB
- 缩略图:< 30KB
- 用 Squoosh / ImageOptim / sharp 压缩

---

## 七、视频处理

### 背景视频
```html
<video autoplay muted loop playsinline poster="fallback.jpg">
  <source src="bg.webm" type="video/webm">
  <source src="bg.mp4" type="video/mp4">
</video>
```
- **必须 muted**(否则不自动播放)
- **playsinline**(iOS 不全屏)
- **poster**(加载前显示封面,防白屏)
- **loop**(循环)
- 移动端考虑用静态图替代(省流量)

### 视频优化
- 背景视频压到 < 2-3MB
- 时长 10-30s 循环
- 分辨率:背景视频 720p 够了
- 提供 reduced-motion 降级(改静态图)

```css
@media (prefers-reduced-motion: reduce) {
  video { display: none; }
  .video-fallback { display: block; }
}
```

### 视频封面(Poster)
- 选一个有代表性、构图好的帧
- 与视频首帧一致(避免跳变)

---

## 八、动图与 Lottie

### GIF 的问题
- 文件巨大
- 色彩受限(256 色)
- 无法暂停

**替代方案**:
- **WebP 动图 / AVIF 动图** — 小得多
- **`<video>` 循环** — 更小,可控
- **Lottie** — 矢量动画,极小,可交互
- **CSS/SVG 动画** — 最优,可控

### Lottie(矢量动画)
```
从 After Effects 导出 JSON → lottie-web 播放
优点:文件极小、无限清晰、可控制播放
用途:加载动画、插画动效、图标动效、庆祝动画
```

---

## 九、图片的无障碍

### Alt 文本
```html
<!-- 有信息的图片 -->
<img src="chart.png" alt="2026 年销售额同比增长 42%">

<!-- 装饰图片 -->
<img src="decoration.svg" alt="" aria-hidden="true">

<!-- 复杂图片(图表) -->
<img src="chart.png" alt="销售趋势图" aria-describedby="chart-detail">
<div id="chart-detail" class="sr-only">详细数据描述...</div>
```

### Alt 写作原则
- 描述**内容和功能**,不是"图片"
- ❌ "一张图片"
- ❌ "image_2026.jpg"
- ✅ "团队在会议室讨论产品路线图"
- 装饰图用空 alt(`alt=""`)
- 不要以 "图片显示..." 开头(冗余)

---

## 十、图片的品牌一致性

### 建立图片规范
- **调色**:统一滤镜/LUT
- **裁剪**:统一比例(全部 16:9 或 4:3)
- **风格**:摄影 vs 插画 vs 3D,不混
- **主题**:人物/产品/抽象,保持一致
- **色调**:与品牌色协调

### 图片库管理
- 统一命名规范
- 统一后期风格
- 提供多尺寸版本
- 标注使用场景

---

## 十一、Prompt 模板

```
【图片与媒体处理】

图片角色:
- Hero:高质量主视觉,预留文字留白区
- 内容图:统一容器 + 圆角 12px + overflow hidden

构图:
- 三分法,主体在交叉点
- 人物视线引导向 CTA/标题
- object-fit: cover + object-position 保证主体不被切

调色统一:
- 所有图片过统一滤镜:saturate(0.9) contrast(1.05)
- 或双色调映射品牌色(高级/品牌感)
- 降饱和 + 略暖 = 电影级高级感

图片上文字:
- 渐变 scrim(底部深色渐变到透明)
- 文字与图片对比度仍 ≥4.5:1
- 或毛玻璃文字板

加载体验:
- blur-up 占位(LQIP 小图 → 真图)
- aspect-ratio 预占位防 CLS
- loading="lazy"(首屏外)
- 首屏图 fetchpriority="high"

格式:
- AVIF → WebP → JPG 渐进降级(picture)
- srcset + sizes 响应式尺寸
- Hero < 200KB,内容图 < 100KB

视频:
- autoplay muted loop playsinline + poster
- 背景视频 < 3MB,720p
- prefers-reduced-motion 降级为静态图

动画:
- 用 Lottie / WebP 动图 / video,不用 GIF

无障碍:
- 信息图有描述性 alt
- 装饰图 alt="" + aria-hidden
```

---

## 十二、检验清单

- [ ] 所有图片统一调色(不是五颜六色)
- [ ] 图片上文字有 scrim/阴影保证可读
- [ ] object-fit: cover + position 防主体被切
- [ ] blur-up 或主色占位加载
- [ ] aspect-ratio 防 CLS
- [ ] loading="lazy"(首屏外)
- [ ] AVIF/WebP 格式 + 兜底
- [ ] srcset 响应式尺寸
- [ ] 图片压缩达标
- [ ] 视频 muted + playsinline + poster
- [ ] reduced-motion 视频降级
- [ ] 不用 GIF(用 Lottie/WebP/video)
- [ ] 信息图有描述性 alt,装饰图空 alt
- [ ] 图片风格全站统一

---

## 十三、v2 · 媒体艺术指导决策

图片、视频、3D 或生成视觉必须先定义它在界面中的角色：

```yaml
media_role: focal-point | proof | explanation | atmosphere | navigation | decoration
subject:
subject_position:
text_safe_zone:
crop_strategy: fixed | responsive | art-directed
tonal_strategy:
overlay_strategy:
mobile_strategy: crop | move | replace | reduce | remove
fallback:
performance_budget:
semantic_status: informative | decorative
```

### 选择顺序

1. 先判断内容是否真的需要媒体；不需要就不用。
2. 再决定主体、视线、留白和文字安全区。
3. 再决定媒体如何与页面构图、品牌色和排版协同。
4. 最后决定格式、加载、动画、裁切和降级。

### 视觉决策规则

- 背景媒体不得和标题争夺同等视觉权重，除非页面目标是沉浸式叙事。
- 人物、产品和建筑的视线/方向应引导到标题、CTA 或下一段内容。
- 复杂图像上不直接叠白字；优先使用局部 scrim、安全区或重新裁切。
- 同一页面的图片需要共享光线、色温、景别、颗粒和后期逻辑。
- 移动端优先保证主体和文字，不强行保留桌面端横向构图。
- 任何视频、3D、粒子或滚动媒体必须有静态 fallback、poster 和 reduced-motion 方案。

### v2 Prompt

```text
为每个媒体元素先输出 Media Direction：角色、主体位置、文字安全区、裁切策略、
调色规则、移动端重组、无障碍语义、性能预算和静态降级。
如果无法说明它服务于内容、品牌或交互目的，删除该媒体。
不要把“放一张好看的背景图”当作设计决策。
```
