# B56 · 视觉焦点与注意力引导

> 用户的眼睛不会平均看你的界面——它们像探照灯,一次只照亮一处。
> 好设计不是"把所有东西放好看",而是**精确控制用户先看哪、再看哪、最后看哪**。
> 你不能对着用户喊"看这里",但可以用视觉手段让他们的眼睛**自己**走到你想让他去的地方。
> 这份文档拆的是:如何像导演调度镜头一样,调度用户的注意力。

---

## 一、注意力引导术语表

| 中文术语 | 英文术语 | 一句话含义 |
|---|---|---|
| 视觉焦点 | Focal Point | 画面中最先吸引视线的点 |
| 视觉层级 | Visual Hierarchy | 元素按重要性排列的视觉次序 |
| 视觉重心 | Visual Center | 画面中视觉分量的平衡中心(略高于几何中心) |
| 视觉动线 | Visual Flow / Eye Path | 视线在界面上移动的路径 |
| 扫描模式 | Scanning Pattern | 用户浏览页面的典型眼动路径 |
| F型阅读 | F-Pattern | 文本密集页面的 F 形扫描路径 |
| Z型阅读 | Z-Pattern | 简洁页面的 Z 形扫描路径 |
| 古腾堡图表 | Gutenberg Diagram | 页面四象限的视觉权重分布模型 |
| 焦点竞争 | Focal Competition | 多个元素争夺注意力的冲突 |
| 注意力预算 | Attention Budget | 用户愿意投入的有限注意力总量 |
| 视觉锚点 | Visual Anchor | 稳定视线的强识别元素 |
| 引导线 | Leading Line | 引导视线朝特定方向移动的线条 |
| 视线方向 | Gaze Direction | 人物/箭头视线所指的方向(观者会跟随) |
| 视觉入口 | Visual Entry Point | 视线进入画面的第一落点 |
| 隔离原则 | Isolation / Von Restorff | 孤立元素更易被记住(隔离效应) |
| 预注意加工 | Pre-attentive Processing | 无需集中注意就能瞬间感知的视觉属性 |
| 视觉爆点 | Hero Moment | 页面最强视觉冲击的核心区域 |
| 视觉呼吸点 | Resting Point | 让视线短暂停留休息的空白区域 |
| 视觉噪音 | Visual Noise | 分散注意力的无关视觉元素 |
| 注意力漏斗 | Attention Funnel | 从广泛到聚焦的注意力收窄路径 |
| 焦点衰减 | Focus Decay | 次要元素逐级弱化的视觉处理 |
| 突显 | Salience | 元素在环境中脱颖而出的程度 |
| 中央凹视觉 | Foveal Vision | 视野中心的高清晰区域(约 2°) |
| 周边视觉 | Peripheral Vision | 视野边缘的低分辨率感知区域 |
| 显著性图 | Saliency Map | 预测视线注意力分布的热力图 |

---

## 二、预注意加工:瞬间被感知的视觉属性

人脑在 **200ms 内**(无需集中注意)就能感知的视觉属性,这是引导注意力的底层武器:

| 属性 | 效果强度 | 使用场景 |
|---|---|---|
| **颜色(色相)** | ★★★★★ | 最强,一个红点在灰色里瞬间跳出 |
| **运动/动效** | ★★★★★ | 周边视觉都能捕捉,慎用(易烦) |
| **大小** | ★★★★ | 大的先被看到 |
| **方向/朝向** | ★★★★ | 一个倾斜的元素在整齐中突出 |
| **添加标记** | ★★★★ | 加个徽章/圆点/下划线 |
| **闪烁** | ★★★★★ | 极强但极烦,仅用于紧急 |
| **形状** | ★★★ | 圆形在方形群里突出 |
| **明度对比** | ★★★★ | 亮的先被看到 |
| **包围/框选** | ★★★ | 加边框圈起来 |
| **空间位置** | ★★★ | 孤立放置 = 隔离效应 |

```css
/* 单一维度突显:只用一个属性,别叠加太多 */
.highlight {
  /* ✅ 只用颜色 */
  color: var(--brand);
}

/* ❌ 反例:同时改色+加粗+放大+加框+动画 → 视觉过载 */
```

**核心法则**:一次只用 1-2 个预注意属性突显。用得越多,反而越乱。

---

## 三、视觉层级的建立

### 3.1 层级的六种手段

```
1. 大小    — 越大越重要(标题 > 正文 > 说明)
2. 颜色    — 高饱和/深色 > 低饱和/浅色
3. 对比    — 高对比元素跳出
4. 字重    — 粗 > 细
5. 位置    — 上方/左侧优先(阅读顺序)
6. 空间    — 周围留白越多越尊贵
```

### 3.2 三级层级系统(最实用)

```css
/* 主要:最想让用户看到的(1个) */
.level-primary {
  font-size: var(--text-3xl);
  font-weight: 800;
  color: var(--text-primary);
}

/* 次要:支撑信息(2-3个) */
.level-secondary {
  font-size: var(--text-lg);
  font-weight: 600;
  color: var(--text-primary);
}

/* 三级:辅助/元信息(其余) */
.level-tertiary {
  font-size: var(--text-sm);
  font-weight: 400;
  color: var(--text-secondary);
}
```

**铁律**:一屏内**只能有一个** primary。两个 primary = 没有 primary。

### 3.3 层级测试:眯眼法

眯起眼睛看界面(或调低亮度/加高斯模糊):
- 能看清的 = 高层级
- 模糊的 = 低层级
- **如果所有东西都一样清晰 = 层级失败**

```css
/* 开发时测试用 */
.hierarchy-test {
  filter: blur(4px);
}
/* 眯眼后应该只有 CTA 和主标题还能辨认 */
```

---

## 四、扫描模式与布局

### 4.1 F 型模式(文本密集页)

**适用**:博客、搜索结果、新闻、文档、列表页

```
████████████████████  ← 第一横:标题/首行,读得最全
████████████
██████
███████████████       ← 第二横:次级信息,略短
█████
███                   ← 竖线:左侧扫读,越往下越少
█
```

**设计对策**:
- 最重要信息放**左上**和**每段首行**
- 左侧对齐(左边缘是扫描锚点)
- 段落首句放关键词(用户只读首句)
- 重点词加粗(给扫读的眼睛"抓手")

### 4.2 Z 型模式(简洁页)

**适用**:落地页、海报式页面、内容少的营销页

```
①────────────────②    ①Logo    ②导航/CTA
 ╲
  ╲                    对角线:视线扫过中间内容
   ╲
    ╲
③────────────────④    ③次要信息  ④主 CTA(终点!)
```

**设计对策**:
- Logo 放左上(①,视觉入口)
- 主 CTA 放右下(④,视线终点)
- 沿对角线布置核心卖点

### 4.3 古腾堡图表(四象限权重)

```
┌──────────────┬──────────────┐
│  主视区       │  强休息区     │
│ (最先看)      │ (略过)        │
├──────────────┼──────────────┤
│  弱休息区     │  终视区       │
│ (最易忽略)    │ (行动落点)    │
└──────────────┴──────────────┘
```

- **左上(主视区)**:放品牌/主标题
- **右下(终视区)**:放最终 CTA
- **左下(弱休息区)**:最不受重视,别放重要东西

### 4.4 分层焦点(现代复杂页)

现代页面不是单一路径,而是**多个焦点区块**,每个区块内部再引导:

```
┌─────────────────────────────┐
│ [Hero 焦点1]  巨大标题+CTA    │  ← 第一屏一个强焦点
├─────────────────────────────┤
│ [区块焦点2]   特性介绍         │  ← 滚动后新焦点
├─────────────────────────────┤
│ [区块焦点3]   社会证明         │
├─────────────────────────────┤
│ [终焦点]      最终转化 CTA     │
└─────────────────────────────┘
```

---

## 五、引导线与视线方向

### 5.1 隐性引导线

```css
/* 用渐变/线条把视线导向 CTA */
.guide-line {
  background: linear-gradient(
    105deg,
    transparent 40%,
    var(--brand-subtle) 60%
  );
  /* 视觉暗示:从左下引导到右上的 CTA */
}
```

**常见引导线载体**:
- 道路/轨道/线条(摄影图片中)
- 人物手指/视线朝向
- 箭头(显性,但别太多)
- 光束/渐变方向
- 元素排列形成的隐形线

### 5.2 视线方向效应

**人像照片中人物看向哪,观者就跟着看哪。**

```
❌ 人物看向画面外 → 把用户注意力带走
✅ 人物看向 CTA/标题 → 引导注意力到关键处
```

```html
<!-- Hero 区人物图片朝向 CTA 一侧 -->
<div class="hero">
  <img src="person-looking-right.jpg" alt="" class="hero-image">
  <div class="hero-cta">  <!-- 放在人物视线方向 -->
    <button>立即开始</button>
  </div>
</div>
```

### 5.3 箭头与指向

```css
/* 微妙的方向暗示,不要粗暴大箭头 */
.subtle-arrow::after {
  content: "→";
  display: inline-block;
  margin-left: 6px;
  transition: transform 200ms;
}

.subtle-arrow:hover::after {
  transform: translateX(4px);  /* hover 时箭头前移,强化"点这里去下一步" */
}
```

---

## 六、焦点竞争与降噪

### 6.1 识别焦点竞争

**症状**:一屏内有 3 个以上"都很显眼"的元素,用户不知道先看哪。

```
❌ 焦点竞争:
- 3 个不同颜色的 CTA 按钮
- 标题、图片、弹窗提示同时抢眼
- 每个卡片都有高饱和色块

✅ 单一焦点:
- 1 个主 CTA(实色),其余次要动作(ghost/文字)
- 图片降饱和,标题突出
- 卡片统一低调,只有 1 个推荐卡高亮
```

### 6.2 焦点衰减(Focus Decay)

主焦点确定后,其余元素**逐级弱化**:

```css
:root {
  --text-primary:   oklch(16% 0.005 265);  /* 100% 权重 */
  --text-secondary: oklch(45% 0.011 265);  /* 60% 权重 */
  --text-tertiary:  oklch(62% 0.008 265);  /* 40% 权重 */
  --text-disabled:  oklch(75% 0.005 265);  /* 20% 权重 */
}

/* 次要按钮弱化 */
.btn-primary   { background: var(--brand); color: white; }
.btn-secondary { background: transparent; border: 1px solid var(--border); }
.btn-tertiary  { background: transparent; color: var(--text-secondary); }
```

### 6.3 视觉降噪清单

```
□ 减少颜色数量(主色+中性+1个强调色)
□ 统一圆角/间距/字重(减少无意义的变化)
□ 弱化边框(用间距或浅色分割代替粗线)
□ 移除装饰性元素(不承载信息的花边)
□ 合并重复元素(3个相似卡片 → 统一样式)
□ 降低次要元素饱和度
□ 增加留白(留白是最好的降噪)
```

### 6.4 聚焦手法:遮罩与虚化

```css
/* 引导用户注意某处时,虚化其余(如新手引导) */
.spotlight-overlay {
  position: fixed;
  inset: 0;
  background: oklch(0% 0 0 / 0.6);
  z-index: 1000;
}

/* 被聚焦元素挖空高亮 */
.spotlight-target {
  position: relative;
  z-index: 1001;
  box-shadow: 0 0 0 9999px oklch(0% 0 0 / 0.6);  /* 反向遮罩 */
  border-radius: 8px;
}
```

---

## 七、CTA 的注意力工程

### 7.1 CTA 突显的层层递进

```
基础:  实色填充 + 高对比
增强:  + 阴影(浮起感)
再增:  + 微动效(hover 放大/位移)
终极:  + 周围留白(隔离效应)+ 引导线指向
```

```css
.cta-hero {
  background: var(--brand);
  color: white;
  padding: 16px 40px;
  font-size: 17px;
  font-weight: 600;
  border-radius: 12px;

  /* 浮起 */
  box-shadow: 0 8px 24px oklch(55% 0.20 265 / 0.3);

  /* 隔离:周围大量留白 */
  margin: 48px auto;

  transition: transform 200ms cubic-bezier(0.34, 1.56, 0.64, 1),
              box-shadow 200ms;
}

.cta-hero:hover {
  transform: translateY(-2px) scale(1.02);
  box-shadow: 0 12px 32px oklch(55% 0.20 265 / 0.4);
}
```

### 7.2 呼吸引导(极克制)

```css
/* 极微妙的脉冲,吸引但不烦人 */
@keyframes cta-attention {
  0%, 100% { box-shadow: 0 8px 24px oklch(55% 0.20 265 / 0.3); }
  50%      { box-shadow: 0 8px 32px oklch(55% 0.20 265 / 0.45); }
}

.cta-hero.pulse {
  animation: cta-attention 3s ease-in-out infinite;
}

@media (prefers-reduced-motion: reduce) {
  .cta-hero.pulse { animation: none; }
}
```

---

## 八、Prompt 模板

```
【视觉焦点与注意力引导】

═══ 焦点策略 ═══
主焦点: {Hero 巨型标题 / 核心 CTA / 关键数据}
- 一屏只有 1 个 primary 焦点
- 其余元素逐级弱化(secondary 60% / tertiary 40% 视觉权重)
- 用"眯眼测试":模糊后应只剩焦点可辨认

═══ 扫描模式 ═══
页面类型: {文本密集用 F 型 / 简洁营销用 Z 型}
- F 型: 重点放左上和每段首行,左对齐,重点词加粗
- Z 型: Logo 左上, 主 CTA 右下(视线终点), 卖点沿对角线

═══ 层级手段 ═══
- 大小: 标题 text-3xl/800 > 正文 base/400 > 说明 sm/secondary色
- 颜色: 主色只给 CTA(≤10%面积)
- 留白: 焦点周围留白最多(隔离效应)

═══ 引导线 ═══
- 人像图片视线朝向 CTA 一侧
- hover 时箭头前移 4px 暗示"下一步"
- 用渐变/线条隐性导向关键区

═══ 降噪 ═══
- 颜色 ≤ 3 种(主+中性+1强调)
- 次要按钮用 ghost/文字样式,不与主 CTA 竞争
- 弱化边框,用间距和浅色分割

═══ 预注意突显 ═══
突显元素时只用 1-2 个属性(颜色 / 大小 / 标记)
禁止同时叠加 变色+加粗+放大+加框+动画

═══ CTA 工程 ═══
主 CTA: 实色 + 阴影浮起 + 周围 48px 留白 + hover translateY(-2px)scale(1.02)
可选极微脉冲 3s(respect reduced-motion)
```

---

## 九、踩坑指南

### 坑1:所有东西都想突出 = 没有突出
**症状**:每个元素都加粗/变色/加框。
**解法**:强制排序,只留一个 primary,其余降级。

### 坑2:多个高饱和 CTA 竞争
**症状**:页面 3 个红按钮,用户不知道点哪。
**解法**:1 个实色主 CTA,其余全部 ghost/文字样式。

### 坑3:人像视线朝外把注意力带走
**解法**:人物朝向要面向内容/CTA,不要看向画面外。

### 坑4:动效滥用造成注意力疲劳
**症状**:满屏东西都在动,用户无所适从。
**解法**:动效是最强的注意力武器,一屏最多 1-2 处持续动效。

### 坑5:焦点放在古腾堡"弱休息区"(左下)
**解法**:重要元素避开左下角,放主视区(左上)或终视区(右下)。

### 坑6:层级靠颜色但色盲用户看不到
**解法**:层级不能只靠颜色,叠加大小/字重/位置(见 B43 可访问性)。

### 坑7:留白不足,焦点被挤压
**症状**:CTA 塞在密集内容里,再显眼也没用。
**解法**:焦点周围留出呼吸空间,隔离才能突显。

---

## 十、谁在用(注意力设计典范)

| 产品/网站 | 手法 | 值得学的 |
|---|---|---|
| **Apple 产品页** | 单一焦点 + 极致留白 | 一屏一个信息,焦点无争议 |
| **Stripe** | Z 型 + 引导渐变 | 视线从标题流畅导向代码示例和 CTA |
| **Linear** | 焦点衰减系统 | 次要信息弱化到位,主操作永远最清晰 |
| **Notion 落地页** | 分层焦点 | 每屏一个新焦点,滚动叙事清晰 |
| **Superhuman** | CTA 隔离 | 主 CTA 周围大量留白,无可争议 |
| **Vercel** | 降噪极简 | 颜色极少,黑白+一个强调,焦点自然浮现 |
| **Duolingo** | 引导线 + 吉祥物视线 | Duo 的视线和手势引导用户下一步 |

---

## 检验清单

- [ ] 一屏内只有一个 primary 视觉焦点
- [ ] 眯眼/模糊测试:只有焦点和 CTA 可辨认
- [ ] 次要元素逐级弱化(色彩/大小/字重)
- [ ] 页面类型匹配扫描模式(F 型或 Z 型)
- [ ] 主 CTA 放在视线终点(Z 型右下)
- [ ] 突显元素只用 1-2 个预注意属性,不叠加
- [ ] 颜色数量 ≤ 3 种,主色只给 CTA
- [ ] 人像视线朝向内容/CTA,不朝画面外
- [ ] 焦点周围有足够留白(隔离效应)
- [ ] 持续动效一屏 ≤ 1-2 处
- [ ] 层级不只靠颜色(叠加大小/字重/位置)
