# G3 · 移动端场景 Prompt 库

> 大多数 AI 生成"移动端"就是把桌面布局压窄。真正的移动端有独立的设计语言：
> 拇指区、安全区、手势、平台惯例、原生感。
> 本文件给出移动端各类界面的完整 prompt + 2026 平台规范（iOS 26 Liquid Glass / Material 3 Expressive）。

---

## 一、移动端铁律（先记住这些）

| 铁律 | 具体值/做法 |
|---|---|
| **触控目标** | 最小 44×44pt（iOS）/ 48×48dp（Android）→ 统一用 **48px** |
| **拇指区** | 主要操作放屏幕**下 2/3**；顶栏只放标签和状态，不放高频点击 |
| **底 Tab 数量** | **3-5 个**，超过 5 个必须收进"更多" |
| **三次点击可达** | 任何主功能从首页 ≤3 tap |
| **一屏一任务** | 一屏若有两个同等权重的主操作，说明该拆成两屏 |
| **安全区** | 必须用 `env(safe-area-inset-*)`，刘海/Home 条/圆角 |
| **视口高度** | 用 `100dvh` 不用 `100vh`（地址栏会变） |
| **内容优先** | 滚动时隐藏工具栏，上滑恢复 |

### 安全区标准写法
```css
.bottom-nav {
  padding-bottom: calc(12px + env(safe-area-inset-bottom));
}
.header {
  padding-top: calc(12px + env(safe-area-inset-top));
}
.full-screen {
  min-height: 100dvh;
  padding-inline: max(16px, env(safe-area-inset-left));
}
```
```html
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
```
**`viewport-fit=cover` 必须加**，否则 `env()` 全是 0。

---

## 二、App 主框架（Tab Bar 型）

```
【产品类型】移动 App 主框架
【平台】{iOS 风格 / Android Material 3 / 跨平台中性}

【整体结构】
┌─────────────────────┐
│  状态栏（安全区）      │
│  顶栏（标题+操作）     │
├─────────────────────┤
│                     │
│  内容区（可滚动）      │
│                     │
├─────────────────────┤
│ 🏠  🔍  ➕  🔔  👤   │ ← 底 Tab（安全区）
└─────────────────────┘

【底部 Tab Bar】
- 4-5 个 Tab，图标 24px + 文字 10-11px（或纯图标）
- 当前 Tab：图标填充态 + 品牌色 + 文字加粗
- 未选中：线性图标 + 灰色
- 高度 56px + safe-area-inset-bottom
- 背景：半透明 + backdrop-filter: blur(20px)（iOS 感）
- 中间可用凸起 FAB（发布/创建类）
- 切换动效：图标 scale 弹跳（spring stiffness 400 damping 12）
- 徽标：未读数用红色圆点 badge（右上角）

【顶栏】
- 高度 44px + safe-area-inset-top
- 左：返回箭头 或 头像
- 中：页面标题（滚动时缩小/居中变化）
- 右：1-2 个图标操作（不超过 2 个）
- 滚动时：大标题收缩成小标题（iOS Large Title 效果）

【内容区】
- 横向 padding 16px
- 卡片间距 12px
- 支持下拉刷新（橡皮筋回弹 + spinner）
- 触底自动加载更多（骨架屏占位）

【手势】
- 左滑返回（iOS 边缘滑动）
- 列表项左滑显示删除/归档
- 下拉刷新
- 长按显示上下文菜单（haptic 反馈）

【平台差异】
- iOS：返回在左上 + 边缘滑动返回；毛玻璃工具栏；Large Title
- Android：系统返回键/手势；Material FAB；顶栏无返回时用汉堡

【安全区与视口】
- viewport meta 加 viewport-fit=cover
- 底部固定元素 padding-bottom: calc(12px + env(safe-area-inset-bottom))
- 全屏用 100dvh

【无障碍】
- 所有触控目标 ≥48px
- Tab 有 role="tab" + aria-selected
- 图标按钮有 aria-label
- 支持系统字号放大（用 rem，不用固定 px）
```

---

## 三、Feed 流 / 卡片流

```
【页面】内容 Feed（社交/资讯/短视频）

【卡片结构】
┌─────────────────────┐
│ 👤 用户名 · 2小时前   │
│ 正文文字...           │
│ ┌─────────────────┐ │
│ │   图片/视频       │ │
│ └─────────────────┘ │
│ ❤️ 128  💬 24  ↗️      │
└─────────────────────┘

【规格】
- 卡片：全宽无左右边距（沉浸）或 16px 边距 + 12px 圆角（卡片感）
- 图片：aspect-ratio 固定（4:3 或 1:1 或 16:9），防 CLS
- 图片懒加载 + blur-up 占位（LQIP）
- 头像 40px 圆形
- 正文最多 3 行 + "展开"

【交互】
- 双击图片点赞（心形迸发 + haptic）
- 点赞按钮：scale 0.8→1.3→1 弹跳 + 颜色变红
- 长按图片：保存/分享菜单
- 上下滑动：短视频型全屏切换（snap scroll）

【无限滚动】
- 触底 200px 时预加载下一页
- 加载中：底部骨架卡片（2 个）
- 到底：显示"没有更多了"
- 保持滚动位置（返回时不跳回顶部）

【性能】
- 虚拟列表（>50 项）
- 图片用 srcset 按 DPR 加载
- 视频仅可见时播放（IntersectionObserver）
- content-visibility: auto

【CSS 关键】
- 短视频全屏滑动：scroll-snap-type: y mandatory + scroll-snap-align: start
- 图片占位：aspect-ratio + background: var(--skeleton)
```

---

## 四、Bottom Sheet（底部抽屉）

```
【组件】Bottom Sheet - 移动端最重要的容器

【三种形态】
1. 半屏（Peek）：占 40% 高度，可上拉展开
2. 全屏（Expanded）：占 90%，顶部留出背景可见
3. 多档吸附（Snap Points）：25% / 50% / 90%

【结构】
┌─────────────────────┐
│  [半透明遮罩]         │
│                     │
├─────────────────────┤
│      ━━━            │ ← 拖拽把手
│  标题                │
│  内容...             │
│  [主操作]            │
└─────────────────────┘

【交互】
- 顶部拖拽把手（36×4px 圆角灰条）
- 拖拽跟手（1:1 跟随手指）
- 松手吸附最近档位（spring 动画）
- 下拉超过 40% 高度则关闭
- 点击遮罩关闭
- 遮罩：rgba(0,0,0,0.4) + 随高度渐变透明度

【实现要点】
- 用 vaul（React）或原生 <dialog> + touch 事件
- 内容可滚动，但滚到顶继续下拉 → 转为关闭手势
- 底部按钮固定在 sheet 底部（含 safe-area）
- 打开时禁止 body 滚动

【prompt 措辞】
"筛选/详情用 bottom sheet（vaul），三档吸附 25/50/90%，
 顶部拖拽把手，拖拽跟手，下拉超40%关闭，遮罩随高度渐变"
```

---

## 五、表单与输入（移动端特殊处理）

```
【移动端表单要点】

【输入类型（关键，AI 常漏）】
<input type="email" inputmode="email" autocomplete="email">
<input type="tel" inputmode="tel" autocomplete="tel">
<input type="number" inputmode="numeric" pattern="[0-9]*">
<input type="text" inputmode="decimal">      <!-- 金额 -->
<input type="search" enterkeyhint="search">  <!-- 回车键显示"搜索" -->
<input autocomplete="one-time-code">          <!-- 验证码自动填充 -->
→ 正确的 inputmode 让手机弹出对应键盘，这是移动端体验的分水岭

【键盘处理】
- 输入框聚焦时不被键盘遮挡：
  · viewport meta 加 interactive-widget=resizes-content（**仅 Chromium/Firefox，iOS Safari 不支持**）
  · iOS 兜底：JS 监听 visualViewport.resize + scrollIntoView({ block: 'center' })
- 底部固定按钮：键盘弹出时上移或隐藏
- 感知可用高度用 visualViewport.height（跨平台最可靠）
  · env(keyboard-inset-height) 仅 Chromium 94+，iOS/Firefox 不可用

【字段设计】
- 每屏字段 ≤5 个，多了分步
- 标签在上方（不用 placeholder 当标签）
- 输入框高度 ≥48px，字号 ≥16px（<16px iOS 会自动缩放页面！）
- 数字键盘类型正确（金额/电话/验证码）
- 一键清空按钮（右侧 ×）

【验证码输入 OTP】
- 6 个独立方格，自动跳格
- 支持粘贴自动分配
- autocomplete="one-time-code" 让 iOS 自动填充短信验证码

【提交】
- 底部固定主按钮（全宽，48-56px 高，含 safe-area padding）
- loading 态：按钮内 spinner + 禁用
- 成功：Toast 或跳转
```

---

## 六、列表与操作

```
【列表项】
- 高度 ≥56px（单行）/ 72px（双行带头像）
- 左：图标/头像，中：主文字+副文字，右：箭头/值/开关
- 分隔线：左侧从内容起始位置开始（不通到边）
- 点击态：整行背景变浅灰（不是只有文字）

【左滑操作（Swipe Actions）】
- 左滑显示 1-3 个操作按钮（删除红/归档灰/标记蓝）
- 滑动跟手，超过阈值松手执行
- 全滑到底 = 执行主操作（通常删除）
- 删除后：行收起 + 后续项上移 + Toast 带"撤销"

【长按上下文菜单】
- 长按 500ms 触发 + haptic 反馈
- 元素轻微放大（scale 1.02）+ 背景模糊
- 菜单从元素旁弹出（不是屏幕中央）

【下拉刷新】
- 下拉 60px 触发，橡皮筋阻尼
- 释放后 spinner 旋转
- 完成：spinner 收起 + 内容 crossfade
```

---

## 七、原生感的关键细节

| 细节 | 做法 |
|---|---|
| **滚动惯性** | iOS 13+ 已默认平滑滚动，无需 `-webkit-overflow-scrolling: touch`（2026 可省略） |
| **禁用长按选中** | `-webkit-touch-callout: none; user-select: none`（仅按钮） |
| **禁用点击高光** | `-webkit-tap-highlight-color: transparent` |
| **300ms 延迟** | `touch-action: manipulation` 消除双击缩放延迟 |
| **haptic 反馈** | `navigator.vibrate()` **iOS Safari 不支持**（仅 Android Chrome）；原生 haptic 只在 App/PWA+Native Bridge 可用，Web 端视为渐进增强 |
| **过度滚动** | `overscroll-behavior: contain` 防止滚动穿透 |
| **毛玻璃** | `backdrop-filter` + `-webkit-backdrop-filter`（iOS <15.4 需前缀） |
| **状态栏色** | `<meta name="theme-color" content="#fff">` |
| **PWA 全屏** | `<meta name="mobile-web-app-capable" content="yes">`（旧 iOS 兼容加 `apple-mobile-web-app-capable`） |
| **PWA 状态栏** | `<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">` |
| **字号防缩放** | 输入框字号 ≥16px |
| **hover 处理** | `@media (hover: hover)` 包裹 hover 样式，触屏不触发 |
| **will-change** | 移动端慎用，滥用会爆内存；只在动画前加、动画后移除 |

### 一段"移动端基础样式"
```css
* { -webkit-tap-highlight-color: transparent; }
button, a { touch-action: manipulation; }
body {
  overscroll-behavior-y: contain;
  -webkit-text-size-adjust: 100%;
}
@media (hover: hover) {
  .card:hover { transform: translateY(-2px); }
}
```

---

## 八、Prompt 模板（通用移动端）

```
生成一个移动端 {App 首页 / Feed / 详情页}，React + Tailwind。

【框架】
- viewport meta 加 viewport-fit=cover
- 全屏用 100dvh（不用 100vh）
- 底部固定元素 padding-bottom: calc(12px + env(safe-area-inset-bottom))
- 顶栏 padding-top: env(safe-area-inset-top)

【布局】
- 底部 Tab Bar 4-5 个，56px 高 + 安全区，半透明 backdrop-blur
- 当前 Tab 图标填充+品牌色，切换时 spring 弹跳
- 主要操作在屏幕下 2/3（拇指区）
- 横向 padding 16px

【触控】
- 所有可点元素 ≥48×48px
- touch-action: manipulation（消除 300ms 延迟）
- -webkit-tap-highlight-color: transparent
- hover 样式包在 @media (hover: hover) 里

【输入】
- 正确的 inputmode（email/tel/numeric/decimal）
- 输入框字号 ≥16px（防 iOS 缩放）、高度 ≥48px
- 验证码用 autocomplete="one-time-code"
- 底部按钮在键盘弹出时上移

【交互】
- 筛选/详情用 bottom sheet（拖拽把手 + 三档吸附 + 下拉关闭）
- 列表左滑操作（删除/归档），删除后 Toast 带撤销
- 下拉刷新（橡皮筋阻尼）
- 长按上下文菜单 + haptic

【性能】
- 图片 aspect-ratio 占位 + lazy + srcset
- 长列表虚拟滚动
- overscroll-behavior: contain 防滚动穿透

【平台】
- iOS：Large Title 收缩、边缘滑动返回、毛玻璃工具栏
- Android：Material FAB、系统返回

【无障碍】
- Tab 用 role="tab" + aria-selected
- 图标按钮有 aria-label
- 字号用 rem 支持系统放大
```

---

## 九、检验清单

- [ ] viewport meta 有 `viewport-fit=cover`
- [ ] 用 `100dvh` 不是 `100vh`
- [ ] 底部/顶部固定元素用了 `env(safe-area-inset-*)`
- [ ] 所有触控目标 ≥48×48px
- [ ] 底 Tab 数量 3-5 个
- [ ] 主要操作在屏幕下 2/3
- [ ] 输入框 `inputmode` 正确（弹对键盘）
- [ ] 输入框字号 ≥16px（防 iOS 自动缩放）
- [ ] hover 样式包在 `@media (hover: hover)`
- [ ] `touch-action: manipulation` 消除点击延迟
- [ ] `-webkit-tap-highlight-color: transparent`
- [ ] `overscroll-behavior: contain` 防滚动穿透
- [ ] 图片有 `aspect-ratio` 占位（防 CLS）
- [ ] 长列表虚拟滚动
- [ ] 有下拉刷新 + 触底加载
- [ ] 列表左滑操作 + 删除可撤销
- [ ] Bottom sheet 拖拽跟手 + 多档吸附
- [ ] 键盘弹出不遮挡输入框和提交按钮
- [ ] 字号用 rem 支持系统放大
