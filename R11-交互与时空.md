# R11 · 交互与时空（提示词库 F 组）

> 定位：静态好看只是及格——**"被操控时的手感"和"时间上的编排"才是网页视觉与图片的本质区别**，本文覆盖网页相对海报/渲染图的唯一优势，也是最容易被浪费的优势。
> 覆盖层：L2 气 + L5 髓（少量 L0 意）。
> 条目卡格式：每个条目标注 `名称 · L 层级 · 档位（基础/进阶/电影级）· 成本`，其下按需给出「视觉收益 / 强度档位 / 关键约束 / 同族拓展 / 提示词片段」等字段（按条目实际保留，参数值一字不改）。本文小节沿用源库 F1–F8 编号——此处 F 指本组"交互与时空"，与目标库 F 板块「前端资源与工具」无关，切勿混淆；文内跨组引用一律已改写为 `R{n}·{原引用}`。
> 来源：融合自外部「视觉提示词库 · F 组：交互与时空」（v1.x，2026-08-16）；技术术语（scroll-driven animations、View Transitions、pointer events、Web Audio、prefers-reduced-motion 等）与参数值为忠实迁移，跨库交叉引用已改写为目标库 R 编号（总索引指向 `R0`）。

---

## 使用说明

R3–R7（几何/体积/光照/后期/风格化）做的是"一帧画面"。本组做的是**"这一帧如何被唤起、如何回应你、如何过渡到下一帧"**。

这是网页相对于海报/渲染图的唯一优势，也是最容易被浪费的优势。大量作品静帧惊艳但一动就露馅——鼠标跟随生硬、滚动跳帧、转场硬切、加载黑屏。

铁律三条：
1. **所有输入都要经过阻尼**，绝不直接赋值（这是"廉价感"的第一来源）
2. **时间常数比数值更重要**：阻尼系数、缓动曲线、错峰间隔决定"手感"
3. **可中断优于可播放**：用户随时打断的动画必须能优雅接管，不能等播完

```
F1 指针驱动             F5 设备与环境输入
F2 滚动驱动             F6 加载与首屏
F3 转场与形变           F7 状态与反馈
F4 音频驱动             F8 时间编排与节奏
```

> 关于"反 AI 味"：本组多处提到的"廉价默认值"（如所有 hover 都 `scale(1.05)`、统一 `stagger fadeIn`、通用 `spinner`）在目标库 `B82`（反 AI 味）与 `B44`（视觉禁忌）中被列为**高风险默认值，建议主动偏离**；但这是**参考清单，不是硬性铁律，请按项目自行取舍**，并非硬性禁令。

---

## F1 指针驱动（Pointer-Driven）

鼠标/触摸是最直接的"连接感"来源。**这一节做好，作品立刻显得贵。**

- **指针阻尼跟随（Damped Pointer Lerp）** · L5 · 基础 · 成本:低
  - 视觉收益：视觉元素平滑追赶指针而非瞬移，产生"重量"与"惯性"
  - 强度档位：lerp 系数 0.2（重、迟滞）/ 0.1（标准，推荐）/ 0.03（极重、梦幻）
  - 关键约束：必须**帧率无关**（用 `1-pow(1-k, dt*60)`），否则高刷屏上手感全变
  - 同族拓展：exponential smoothing、critically damped spring、velocity carry-over
  - 提示词片段：`指针位置做帧率无关阻尼：p += (target-p) * (1-pow(0.001, dt))；禁止直接赋值`

- **指针速度驱动（Pointer Velocity Mapping）** · L5 · 进阶 · 成本:低
  - 视觉收益：把移动速度映射到视觉参数（拉伸、模糊、扰动强度、粒子发射率），让"快速甩动"和"缓慢移动"看起来不同
  - 同族拓展：velocity magnitude、squash & stretch、speed-based emission、motion trail 长度
  - 提示词片段：`把指针速度映射到 shader 的扰动强度（0–1，衰减时间 400ms），快速移动时扰动更强`

- **磁吸与吸附（Magnetic Hover）** · L5 · 进阶 · 成本:低
  - 视觉收益：元素在指针接近时被"吸"过去一点，产生活物感
  - 强度档位：偏移上限 4px（克制）/ 10px（明显）/ 24px（俏皮）
  - 同族拓展：magnetic button、cursor attraction、distance falloff、inverse-square pull
  - 提示词片段：`按钮在指针 120px 内被吸引，最大偏移 8px，衰减用 (1-d/120)²`

- **自定义光标（Custom Cursor）** · L5 · 进阶 · 成本:低
  - 视觉收益：品牌感与"这不是普通网页"的第一信号
  - 关键约束：**必须保留可用性**——可点击处必须有明确的状态变化；触屏设备要完全禁用
  - 同族拓展：cursor blend mode（difference/exclusion 混合）、cursor morph（hover 时变形/展开文字）、双层光标（快点+慢环）
  - 提示词片段：`自定义光标：内层小点即时跟随 + 外层环阻尼 0.12 跟随；hover 可交互元素时外环放大 2.5× 并显示动作词；触屏隐藏`

- **指针拖尾（Pointer Trail）** · L5 · 进阶 · 成本:中
  - 视觉收益：移动留下痕迹，画面从"点"变"线"
  - 同族拓展：见 `R4·B7` 拖尾累积、spline trail、ribbon geometry、衰减 RT
  - 提示词片段：`指针在累积 RT 上绘制笔刷，每帧乘 0.94 衰减；用该 RT 驱动主 shader 的局部扰动`

- **视差跟随（Pointer Parallax）** · L5 · 基础 · 成本:低
  - 视觉收益：多层按不同幅度响应指针，产生伪 3D 纵深
  - 强度档位：远层 2px / 中层 8px / 近层 20px
  - 关键约束：幅度要小。超过 30px 会晕
  - 同族拓展：layered parallax、tilt-based transform、见 `R1·G4` 空间感
  - 提示词片段：`3 层视差响应指针，幅度 2/8/20px，各层阻尼系数递减`

- **交互涟漪（Interaction Ripple）** · L5 · 基础 · 成本:低
  - 视觉收益：点击处发出扩散波，最直观的"我收到了"反馈
  - 同族拓展：见 `R3·A8` 屏幕空间扭曲、Material ripple、shockwave、多点并发管理
  - 提示词片段：`点击发出屏幕空间涟漪（半径 0–400px，600ms，ease-out），最多同时 4 个`

- **惯性滚动与拖拽（Inertial Drag）** · L5 · 进阶 · 成本:低
  - 视觉收益：松手后继续滑行并平滑停止，物理直觉正确
  - 同族拓展：momentum scroll、friction 0.92–0.96、rubber-band 边界回弹
  - 提示词片段：`拖拽释放后保留速度，摩擦 0.94；越界时用 rubber-band 阻力并回弹`

---

## F2 滚动驱动（Scroll-Driven）

滚动是网页最原生的"时间轴"。**把滚动当播放头，而不是当翻页。**

- **滚动进度映射（Scroll Progress Mapping）** · L2 · 基础 · 成本:低
  - 视觉收益：把滚动位置归一化为 0–1，驱动任意视觉参数（相机、shader uniform、色彩）
  - 同族拓展：CSS `animation-timeline: scroll()` / `view()`、IntersectionObserver、GSAP ScrollTrigger、`scroll-driven animations`
  - 提示词片段：`用原生 animation-timeline: view() 驱动区块动画；shader 参数用归一化滚动进度做 uniform`

- **平滑滚动（Smooth Scroll / Scroll Lerp）** · L5 · 进阶 · 成本:低
  - 视觉收益：滚动位置本身被阻尼，消除滚轮的离散跳跃感
  - 关键约束：**这是一项有争议的技术**——会破坏浏览器原生滚动、影响可访问性与搜索。用则必须：尊重 `prefers-reduced-motion`、保留键盘/锚点跳转、不劫持过度
  - 同族拓展：Lenis、locomotive-scroll、虚拟滚动、CSS `scroll-behavior: smooth`（更保守的选择）
  - 提示词片段：`平滑滚动 lerp 0.1；reduced-motion 时完全禁用；键盘 PageUp/Down 与锚点必须仍可用`

- **钉住与场景（Pinning / Sticky Scenes）** · L2 · 进阶 · 成本:中
  - 视觉收益：元素固定不动而内部内容随滚动演进——"一屏讲一件事"
  - 同族拓展：`position: sticky`、ScrollTrigger pin、scroll-jacking（慎用）
  - 提示词片段：`主视觉区 sticky 钉住 3 屏高度，内部按滚动进度分 3 段演进`

- **滚动叙事（Scrollytelling）** · L0 · 进阶 · 成本:中
  - 视觉收益：滚动即叙事推进，信息按节奏揭示
  - 同族拓展：step-based narrative、状态机（而非连续插值）、Pudding/NYT 式图文联动
  - 提示词片段：`滚动分 5 个离散步骤（非连续插值），每步一个明确的视觉状态与一句文案`

- **视差滚动（Scroll Parallax）** · L5 · 基础 · 成本:低
  - 视觉收益：多层不同速度，最经典的纵深手法
  - 关键约束：**幅度要小且方向一致**。反向视差与大幅度都会晕
  - 同族拓展：见 `R3·A4` 视差层、`R1·G4` 空间感
  - 提示词片段：`背景 0.3× / 中景 0.6× / 前景 1.0× 速度；禁止反向视差`

- **滚动速度反馈（Scroll Velocity Feedback）** · L5 · 电影级 · 成本:低
  - 视觉收益：快速滚动时元素被拉伸/倾斜/模糊，把"滚动力度"可视化
  - 强度档位：微（倾斜 1°）/ 中（倾斜 4° + 轻模糊）/ 强（明显拉伸，风格化）
  - 同族拓展：skew on scroll、velocity blur、squash & stretch
  - 提示词片段：`卡片按滚动速度做 skewY（上限 4°）与纵向拉伸（上限 1.06），停止后 spring 回弹`

- **区块入场（Reveal on Enter）** · L5 · 基础 · 成本:低
  - 视觉收益：内容进入视口时揭示，建立节奏感
  - 关键约束：**只做一次**（不要滚回去又播一遍）；阈值不要太苛刻（否则用户看到空白）
  - 同族拓展：IntersectionObserver、stagger 错峰、clip-path reveal、mask wipe
  - 提示词片段：`入场：clip-path 从下向上揭示 + 位移 24px，600ms cubic-bezier(0.16,1,0.3,1)，子元素错峰 60ms，只播一次`

---

## F3 转场与形变（Transition & Morph）

**页面之间、状态之间的那 400ms，是最被忽视也最能体现水准的地方。**

- **共享元素转场（Shared Element Transition）** · L5 · 电影级 · 成本:中
  - 视觉收益：同一元素在两个页面/状态间连续移动变形，产生"空间连续"的错觉
  - 同族拓展：View Transitions API（`view-transition-name`）、FLIP 技术（First-Last-Invert-Play）、Framer Motion layoutId
  - 提示词片段：`用 View Transitions API 做共享元素转场；给关键元素 view-transition-name；不支持时用 FLIP 回退`

- **遮罩擦除（Mask Wipe / Clip Reveal）** · L5 · 基础 · 成本:低
  - 视觉收益：形状化的揭示，比淡入淡出高级得多
  - 强度档位：直线擦除 / 斜线 / 圆形扩散 / 不规则形状 / 文字形状
  - 同族拓展：clip-path animation、mask-image gradient、SVG mask、圆形扩散（从点击点开始）
  - 提示词片段：`转场用 clip-path 圆形从触发点扩散；禁止简单 opacity 淡入淡出`

- **溶解与噪声转场（Noise Dissolve）** · L5 · 进阶 · 成本:低
  - 视觉收益：按噪声阈值逐渐消失/出现，比线性淡出有机得多
  - 同族拓展：dissolve shader、threshold on noise、burn edge（溶解边缘发光）
  - 提示词片段：`溶解转场：用 FBM 噪声做阈值，阈值推进时在边界 0.05 宽度内加发光边`

- **几何形变（Morph / Shape Interpolation）** · L5 · 电影级 · 成本:中
  - 视觉收益：一个形状连续变成另一个，强烈的"魔法感"
  - 实现路径：SDF 插值（最平滑）/ SVG path morph（需点数一致）/ 顶点位移
  - 同族拓展：SDF blend、flubber.js、vertex morph target、metaball 融合（见 `R3·A3` smooth union）
  - 提示词片段：`形状过渡用 SDF 线性插值（mix(d1,d2,t)）而非 path morph，得到真正的融合而非顶点滑动`

- **顶点位移动画（Vertex Displacement）** · L5 · 进阶 · 成本:低
  - 视觉收益：网格在 shader 里被噪声/波形推动，产生流动、爆散、呼吸
  - 同族拓展：noise displacement、explode/implode、wave deform、沿法线挤出
  - 提示词片段：`顶点沿法线按 FBM 位移（幅度 0.08），进出场时幅度从 1.0 收敛到 0.08`

- **文字逐字动画（Text Stagger / Kinetic Type）** · L5 · 基础 · 成本:低
  - 视觉收益：标题逐字/逐词/逐行揭示，节奏感与仪式感
  - 关键约束：**逐字只用于短标题**（≤8 字）。长句逐字会让人焦躁；正文用逐行或整块
  - 同族拓展：SplitType、逐词优于逐字（可读性更好）、mask 从下推入（比 opacity 高级）、可变字重过渡
  - 提示词片段：`标题按词拆分，用 overflow:hidden 遮罩从下推入，错峰 50ms，总时长 ≤600ms；正文整块淡入不拆字`

- **可变字体过渡（Variable Font Animation）** · L5 · 电影级 · 成本:低
  - 视觉收益：字重/字宽/倾斜连续变化，只有可变字体能做到，极高级
  - 同族拓展：`font-variation-settings` 动画、wght/wdth/slnt/opsz 轴、hover 加粗、滚动驱动字重
  - 提示词片段：`标题 hover 时 wght 从 400 平滑到 700（不是切换两个字重），过渡 300ms`

- **可中断与接管（Interruptible Animation）** · L5 · 进阶 · 成本:低
  - 视觉收益：用户中途操作时动画从当前状态平滑接管，而不是跳变或等播完
  - 关键约束：这是"手感高级"与"手感僵硬"的分界线
  - 同族拓展：spring physics（天然可中断）、velocity handoff、`Web Animations API` playbackRate、避免固定时长 tween
  - 提示词片段：`所有交互动画用 spring 而非固定时长 tween，保证任意时刻可被新输入接管并保留当前速度`

---

## F4 音频驱动（Audio-Reactive）

**成本极低、观感提升极大的一个维度**，而且几乎没人做对。

- **频谱分析（FFT / AnalyserNode）** · L2 · 进阶 · 成本:低
  - 视觉收益：把音频频谱作为 shader 输入，视觉与声音同步
  - 关键约束：`fftSize` 2048、`smoothingTimeConstant` 0.8（不平滑会剧烈闪烁）
  - 同族拓展：Web Audio AnalyserNode、getByteFrequencyData、频谱转纹理喂给 shader
  - 提示词片段：`用 AnalyserNode（fftSize 2048, smoothing 0.8）把频谱写进 DataTexture 作为 shader uniform`

- **频段分离（Frequency Bands）** · L2 · 进阶 · 成本:低
  - 视觉收益：低频驱动大结构（缩放/位移），高频驱动细节（噪点/闪烁）——**分频驱动是关键，整体音量驱动很廉价**
  - 强度档位：低频 20–250Hz / 中频 250–2kHz / 高频 2k–16kHz
  - 同族拓展：bass/mid/treble split、mel scale（感知均匀，比线性分频好）、log-frequency 分布
  - 提示词片段：`分 3 个频段（低/中/高，按对数分布）：低频驱动整体缩放、中频驱动色相、高频驱动颗粒与细节噪声`

- **节拍检测（Beat Detection / Onset）** · L2 · 电影级 · 成本:中
  - 视觉收益：在鼓点上触发离散事件（闪光、切镜、涟漪），比连续映射有力得多
  - 同族拓展：energy-based onset detection、spectral flux、running average + threshold、BPM 估计、防抖（最小间隔 200ms）
  - 提示词片段：`用能量突变 + 滑动均值阈值检测节拍，最小间隔 200ms；命中时触发一次闪光与相机微推`

- **包络跟随（Envelope Follower）** · L2 · 进阶 · 成本:低
  - 视觉收益：快速上升、缓慢下降的响应曲线，模拟真实的"冲击后余韵"
  - 强度档位：attack 20ms / release 400ms（标准）
  - 同族拓展：attack-release envelope、peak hold、asymmetric smoothing
  - 提示词片段：`音频响应用非对称平滑：上升 attack 30ms、下降 release 500ms，禁止对称平滑`

- **氛围音与静音优先（Ambient Audio / Muted by Default）** · L0 · 基础 · 成本:低
  - 视觉收益：声音能极大提升沉浸感，但**必须默认静音**
  - 关键约束：浏览器策略要求用户手势才能播放；必须有明显的开关；音量默认 ≤30%
  - 同族拓展：AudioContext resume on gesture、fade in/out、loop crossfade（避免接缝爆音）
  - 提示词片段：`氛围音默认关闭，提供明显的音频开关（右下角）；首次开启时 1.5s 淡入，音量 0.25；循环点做 crossfade`

- **程序化音频（Procedural Audio）** · L0 · 电影级 · 成本:中
  - 视觉收益：无需音频文件，用振荡器合成氛围音，且能与视觉参数联动
  - 同族拓展：OscillatorNode、噪声 + 滤波（做风声/引力嗡鸣）、交互触发音（click/hover 的合成音效）
  - 提示词片段：`用两个低频正弦 + 带通滤波白噪声合成低沉氛围音，频率随相机距离变化；无需音频文件`

---

## F5 设备与环境输入（Device & Environment）

免费的"魔法感"来源。**用了就显得懂行。**

- **陀螺仪与倾斜（Device Orientation）** · L5 · 进阶 · 成本:低
  - 视觉收益：手机倾斜时视差/光照跟着变，移动端的"真 3D"体验
  - 关键约束：iOS 需要 `requestPermission()` 且必须由用户手势触发；必须有降级
  - 同族拓展：DeviceOrientationEvent、gravity vector、permission gating、幅度限制（±15°）
  - 提示词片段：`移动端可选启用陀螺仪视差（需用户点击授权），倾斜映射上限 ±15°，不授权则回退指针/自动巡游`

- **系统偏好（prefers-* Media Queries）** · L5 · 基础 · 成本:低
  - 视觉收益：尊重用户系统设置，是"专业"而非"炫技"的标志
  - 完整清单：`prefers-reduced-motion` / `prefers-color-scheme` / `prefers-contrast` / `prefers-reduced-transparency` / `prefers-reduced-data` / `forced-colors`
  - 提示词片段：`响应全部 prefers-*：reduced-motion 关闭相机循环与视差、reduced-transparency 去掉毛玻璃、reduced-data 降到低质量档`

- **视口与容器查询（Container Queries）** · L5 · 基础 · 成本:低
  - 视觉收益：组件按自身容器尺寸响应而非视口，布局在任何位置都对
  - 同族拓展：`@container`、`cqw/cqh` 单位、`clamp()` 流式排版、`aspect-ratio`
  - 提示词片段：`组件用 @container 查询自身宽度；字号用 clamp() 流式缩放而非断点跳变`

- **页面可见性与省电（Visibility & Power）** · L5 · 基础 · 成本:低
  - 视觉收益：切走标签页时停止渲染，不烧电不发热（也避免回来时时间跳跃）
  - 同族拓展：`visibilitychange`、`requestAnimationFrame` 自动暂停、`Battery Status API`、`navigator.connection.saveData`
  - 提示词片段：`页面不可见时暂停 rAF 与音频；恢复时不要补偿累积时间（避免跳帧）`

- **摄像头与麦克风输入（Camera / Mic Input）** · L2 · 电影级 · 成本:中
  - 视觉收益：把用户自己变成视觉的一部分，极强的记忆点
  - 关键约束：**必须明确告知且默认关闭**；不上传任何数据（本地处理）
  - 同族拓展：getUserMedia、video texture、麦克风驱动（同 `R11·F4`）、边缘检测/剪影提取
  - 提示词片段：`可选开启摄像头，仅本地处理为剪影驱动 shader 扰动，明确标注"不上传任何数据"`

- **指针类型检测（Pointer Type Detection）** · L5 · 基础 · 成本:低
  - 视觉收益：触屏上不做 hover 效果、不显示自定义光标，避免"卡住"的 hover 状态
  - 同族拓展：`@media (hover: hover)` / `(pointer: fine)`、PointerEvent.pointerType
  - 提示词片段：`所有 hover 效果包在 @media (hover: hover) and (pointer: fine) 内；触屏改用 active 反馈`

---

## F6 加载与首屏（Loading & First Paint）

**用户对作品的第一印象发生在内容出现之前。** 这一节决定他会不会等下去。

- **零黑屏策略（No Black Screen）** · L5 · 基础 · 成本:低
  - 视觉收益：任何时刻都有内容。黑屏 1 秒就流失一批人
  - 同族拓展：见 `R6·D8` 首帧策略、inline critical CSS、SSR/静态首屏、poster 占位图
  - 提示词片段：`首屏用内联 CSS 立即绘制背景与标题（不等 JS）；canvas 就绪后交叉淡入 400ms；禁止任何时刻纯黑`

- **渐进式渲染（Progressive Rendering）** · L5 · 进阶 · 成本:中
  - 视觉收益：先出低质量画面再逐步精修，感知速度快得多
  - 同族拓展：先低步数后高步数、先低 DPR 后升、渐进式采样累积、LQIP/blur-up 图片
  - 提示词片段：`首帧用 Standard 档渲染并显示，随后 3 帧内逐步提升到目标档；图片用 blur-up 占位`

- **有意义的加载态（Meaningful Loader）** · L0 · 进阶 · 成本:低
  - 视觉收益：加载动画本身就是作品的一部分，而不是通用 spinner
  - 关键约束：**必须有真实进度**（不要假进度条）；超过 3s 要给文字说明
  - 同族拓展：主题化 loader（用作品自身的视觉语言，如粒子场作品用"粒子回流"、流体作品用"波纹平息"，而非通用 spinner）、真实资源进度、骨架屏
  - 提示词片段：`loader 是作品主题的极简版（不是通用 spinner），显示真实资源进度百分比；>3s 时加一行说明文字`

- **资源优先级（Resource Priority）** · L5 · 基础 · 成本:低
  - 视觉收益：首屏可见的东西先到
  - 同族拓展：`fetchpriority="high"`、`rel=preload`（字体/关键图）、`loading=lazy`（首屏外）、字体 `font-display: swap`、代码分割
  - 提示词片段：`首屏字体用 preload + font-display:swap；首屏外图片 loading=lazy；shader 与 vendor 按需 import()`

- **布局稳定（Layout Stability / 防跳动）** · L5 · 基础 · 成本:低
  - 视觉收益：内容加载时不发生位移，观感专业度的隐形指标
  - 同族拓展：CLS、图片 `width/height` 或 `aspect-ratio`、字体 fallback 度量匹配（`size-adjust`）、预留骨架高度
  - 提示词片段：`所有媒体声明 aspect-ratio；fallback 字体用 size-adjust 匹配度量，避免字体切换跳动`

- **骨架屏与真实布局一致（Skeleton Parity）** · L5 · 进阶 · 成本:低
  - 视觉收益：骨架屏不是"随便放几个灰条"——它必须与加载完成后的真实布局**同尺寸、同位置**，否则内容一到就跳一下，反而更糟
  - 关键约束：骨架的每个块的宽高/圆角/间距 = 真实元素的值｜用同一套令牌驱动骨架与真实组件｜骨架的微光扫过动画方向一致、速度慢（1.2–1.6s 一次）
  - ❌ 骨架是三条等宽灰条、真实内容是图+两行不等宽文字（一到就重排）
  - 同族拓展：见 `R17·Q1` 加载态、`R18·R1` 图片占位、`content-visibility`、shimmer 动画（`@media (prefers-reduced-motion)` 时改为静态）
  - 提示词片段：`骨架屏用与真实组件相同的尺寸令牌生成（同宽高/圆角/间距/位置），内容到达时零位移；微光扫过 1.4s 一次、reduced-motion 时静态`

- **乐观 UI（Optimistic UI）** · L5 · 进阶 · 成本:中
  - 视觉收益：交互后**立刻**呈现预期结果（不等服务器/计算返回），感知性能翻倍
  - 参考做法：点赞立刻变红、切换立刻生效、提交立刻插入列表｜失败时回滚并给非阻塞提示（见 `R11·F7`）｜回滚动画要温和不要猛地弹回
  - 关键约束：只对**大概率成功**的操作用乐观 UI；删除/支付这类不可逆操作不用
  - 同族拓展：见 `R11·F7` 反馈时序、`R17·Q3` 错误态、状态回滚、pending 态的轻微半透明
  - 提示词片段：`高成功率操作用乐观 UI：交互后立即更新界面并标记 pending（轻微半透明），成功则落定、失败则温和回滚 + 非阻塞提示；不可逆操作不用`

- **视口外内容跳过渲染（content-visibility）** · L5 · 进阶 · 成本:低
  - 视觉收益：长页面首屏渲染更快，滚动更流畅
  - 关键约束：**必须同时给 `contain-intrinsic-size`**，否则滚动条会乱跳（浏览器不知道未渲染块的高度）｜只用在首屏外的大区块，不要滥用到每个小元素
  - 同族拓展：`content-visibility: auto`、`contain-intrinsic-size`、`contain`、见 `R6·D8` 性能档位
  - 提示词片段：`首屏外的大区块用 content-visibility:auto + contain-intrinsic-size 预估高度（防滚动条跳）；不用在小元素上`

---

## F7 状态与反馈（State & Feedback）

**视觉不只是"好看的静态"，还包括"每种状态下都好看"。** 这是最容易漏的一块。

- **完整状态覆盖（Full State Coverage）** · L5 · 基础 · 成本:低
  - 视觉收益：每个交互元素都有 default / hover / active / focus-visible / disabled / loading / error 七态
  - 关键约束：`:focus-visible` 必须可见且美观（不是 `outline: none`）
  - 同族拓展：`:focus-visible`、`:has()`、`aria-busy`、`:disabled` 的降饱和而非降透明
  - 提示词片段：`每个可交互元素定义 7 态；focus-visible 用 2px 偏移描边（品牌色）；disabled 用降饱和不用降透明`

- **反馈时序（Feedback Timing）** · L5 · 基础 · 成本:低
  - 视觉收益：反馈必须"即时"，但完成动画可以慢
  - 参考数值：按下反馈 <50ms｜hover 过渡 150–200ms｜状态切换 250–350ms｜大转场 400–600ms｜绝不超过 800ms
  - 同族拓展：perceived performance、optimistic UI、`transition-delay` 用于 hover-out（防抖）
  - 提示词片段：`按下反馈 ≤50ms（用 transform scale 0.97）；hover 进入 180ms / 离开 260ms（离开略慢更从容）`

- **空态与边缘态（Empty / Edge States）** · L0 · 进阶 · 成本:低
  - 视觉收益：空列表、无搜索结果、加载失败——这些状态设计过的作品才完整
  - 同族拓展：empty state illustration、零结果引导、错误恢复路径、离线态
  - 提示词片段：`设计空态与错误态：各有一句人话说明 + 一个明确的下一步动作，不用通用插图`

- **微交互（Micro-Interactions）** · L5 · 进阶 · 成本:低
  - 视觉收益：开关的弹动、复选框的勾勒、复制成功的短暂确认——细节堆出的"精致"
  - 同族拓展：spring toggle、SVG path draw、checkmark stroke、toast、haptic（`navigator.vibrate`，移动端）
  - 提示词片段：`开关切换用 spring(stiffness 400, damping 25) 带轻微 overshoot；复制成功显示 1.5s 内联确认而非 toast`

- **进度与预期管理（Progress & Expectation）** · L5 · 基础 · 成本:低
  - 视觉收益：让用户知道"还要多久""还有多少"，减少焦虑
  - 同族拓展：滚动进度条、章节指示、剩余阅读时间、分步指示器
  - 提示词片段：`顶部 2px 滚动进度条 + 右侧章节点指示当前位置`

- **快捷键与可发现性（Shortcuts & Discoverability）** · L5 · 进阶 · 成本:低
  - 视觉收益：极客感与专业感；但**必须可发现**，否则等于不存在
  - 同族拓展：`?` 打开帮助面板、首次访问的一次性提示、HUD 常驻小字、命令面板（`Cmd+K`）
  - 提示词片段：`快捷键：数字键切调试视图、空格暂停、? 打开帮助；首次访问在 HUD 显示一次性提示 4s 后淡出`

- **可访问性即视觉（A11y as Visual Quality）** · L5 · 基础 · 成本:低
  - 视觉收益：对比度、焦点顺序、可读性——这些同时是"看起来专业"的指标
  - 同族拓展：对比度 ≥4.5:1（正文）/ 3:1（大字）、`prefers-reduced-motion`、语义标签、canvas 内容的文字替代
  - 提示词片段：`正文对比度 ≥4.5:1；键盘可完整遍历；canvas 提供 aria-label 与文字版说明；文字永不放进 canvas`

---

## F8 时间编排与节奏（Choreography & Rhythm）

**多个动画之间的关系**，比单个动画的曲线更决定质感。

- **错峰（Stagger）** · L5 · 基础 · 成本:低
  - 视觉收益：元素依次而非同时出现，产生流动与秩序
  - 强度档位：紧凑 30ms / 标准 60ms / 舒缓 100ms / 戏剧 150ms+
  - 关键约束：总时长要有上限。20 个元素 × 100ms = 2 秒，太久
  - 同族拓展：stagger from center/edges/random、按空间距离错峰（波纹式）、总时长封顶
  - 提示词片段：`列表错峰 60ms，但总入场时长封顶 800ms（元素多时自动压缩间隔）`

- **缓动语义（Easing Semantics）** · L5 · 基础 · 成本:低
  - 视觉收益：不同缓动传达不同性格。**用错缓动比没有动画更糟**
  - 语义映射：
    - 入场 → `ease-out`（快进慢停，有到达感）
    - 退场 → `ease-in`（慢起快走，果断离开）
    - 位置移动 → `spring`（有物理重量）
    - 循环/呼吸 → `ease-in-out` 或正弦
    - 强调/弹跳 → 带 overshoot 的 spring
    - `linear` → **仅用于匀速旋转、进度条、视频时间轴**
  - 同族拓展：cubic-bezier(0.16,1,0.3,1)（常用的"expo out"）、spring stiffness/damping、anticipation（反向预备）
  - 提示词片段：`入场 ease-out / 退场 ease-in / 位移 spring；禁止对入场用 ease-in-out 或 linear`

- **主次节奏（Primary-Secondary Motion）** · L5 · 电影级 · 成本:低
  - 视觉收益：主体动，附属物延迟跟随（follow-through），迪士尼动画原理
  - 同族拓展：follow-through & overlapping action、secondary animation、delay 递增
  - 提示词片段：`卡片移动时内部图标延迟 80ms 跟随并轻微过冲，产生 follow-through`

- **预备与余韵（Anticipation & Follow-Through）** · L5 · 电影级 · 成本:低
  - 视觉收益：动作前的轻微反向预备 + 动作后的过冲回弹，让运动"有生命"
  - 强度档位：预备 2–4% / 过冲 3–8%
  - 提示词片段：`展开动画：先反向压缩 3%（120ms）再展开并过冲 5% 回弹`

- **节奏对比（Tempo Contrast）** · L0 · 进阶 · 成本:低
  - 视觉收益：全程同速 = 平庸。快-慢-快的节奏才有起伏
  - 同族拓展：pacing、pause as punctuation（停顿即标点）、密集区与呼吸区交替
  - 提示词片段：`页面节奏：首屏慢（6s 呼吸）→ 中段快（连续 3 个 400ms 揭示）→ 结尾慢（静止收束）`

- **循环与呼吸（Idle Loop / Breathing）** · L5 · 进阶 · 成本:低
  - 视觉收益：无操作时画面仍缓慢演化，作品"活着"而不是"卡住"
  - 关键约束：周期要长（≥6s）且幅度要小，否则烦人
  - 同族拓展：见 `R6·D5` 镜头呼吸、ambient loop、无理数周期比（避免同步重复）
  - 提示词片段：`空闲呼吸：周期 8s 与 11s 两个不同相位叠加（避免明显重复），幅度 ≤2%`

- **时间常数一致性（Consistent Time Constants）** · L5 · 进阶 · 成本:低
  - 视觉收益：全站只用 3–4 个时长值（如 150/300/600ms），形成统一"手感指纹"
  - 同族拓展：design token for duration、`--dur-fast/base/slow`、缓动也建 token
  - 提示词片段：`时长只允许 4 个 token：120/240/480/720ms；缓动只允许 3 个；全站复用`

---

## F 组 · 组合速查

| 想要的效果 | 组合 |
|---|---|
| 高级手感（最低成本） | F1 阻尼跟随 + F8 缓动语义 + F7 反馈时序 + F3 可中断 |
| 沉浸交互作品 | F1 全套 + F1 自定义光标 + F4 音频驱动 + F5 陀螺仪 + F6 主题化 loader |
| 滚动叙事长页 | F2 进度映射 + F2 钉住 + F2 区块入场 + F8 错峰 + F8 节奏对比 |
| 音乐可视化 | F4 分频驱动 + F4 节拍检测 + F4 包络 + `R4·B7` GPGPU 粒子 + `R6·D2` Bloom |
| 页面间连续感 | F3 共享元素转场 + F3 遮罩擦除 + F6 布局稳定 |
| 有生命的静态页 | F8 空闲呼吸 + `R6·D5` 镜头呼吸 + F1 视差跟随 + F7 微交互 |
| 完整交付质量 | F5 prefers-* + F6 零黑屏 + F7 七态覆盖 + F7 a11y + F5 可见性暂停 |

**常见坑**：
- 鼠标跟随很生硬 → F1 缺阻尼，或阻尼不是帧率无关的
- 动画很僵硬 → F3 用了固定时长 tween，改 spring
- 入场很别扭 → F8 缓动语义错了（入场用了 ease-in 或 linear）
- 滚动一卡一卡 → F2 在 scroll 事件里做重计算，改 rAF + 缓存
- 音频可视化很廉价 → F4 用了整体音量驱动，必须分频
- 触屏上 hover 卡住 → F5 缺指针类型检测
- 加载很久很焦虑 → F6 缺真实进度与主题化 loader
- 高刷屏上手感全变 → F1/F8 动画没做帧率无关
- 平滑滚动被吐槽 → F2 劫持过度，或没尊重 reduced-motion
- 焦点框很丑就删掉了 → F7 错误做法，应设计好看的 focus-visible

> **F 组的检验方法**：录一段 10 秒操作视频，逐帧看。所有"跳变"的地方都是问题；所有"等你"的地方都是问题。

---

## 与库内衔接

| 本文小节 | 目标库相关文档 | 关系（地基·扩展·验证） |
|---|---|---|
| F1 指针驱动 / F8 缓动语义 | B5 动效系统 | 地基：B5 给出动效体系框架，本文补充指针阻尼与时间常数的具体配方 |
| F8 时间编排与节奏 | B71 动效编排进阶 | 扩展：错峰/主次节奏/预备余韵/节奏对比是编排进阶的落地配方 |
| F3 转场与形变 | B58 视觉过渡与连续性 | 扩展：共享元素转场/遮罩擦除/几何形变是连续性的实现手段 |
| F2 滚动驱动 | V10 滚动视觉效果 | 扩展：滚动进度映射/钉住/滚动叙事/视差的提示词化 |
| F1 指针驱动（磁吸/自定义光标/拖尾/视差） | V9 悬浮与光标效果 | 扩展：光标与 hover 的具体配方与强度档位 |
| F7 微交互 | B74 微交互配方大全 | 扩展：spring toggle / checkmark stroke / toast 等配方补充 |
| F7 状态与反馈 / F6 加载与首屏 | B73 状态体验与感知性能工艺 | 扩展 + 验证：七态覆盖/乐观 UI/骨架屏一致性的工艺落地 |
| F5 系统偏好 / F7 a11y | C4 无障碍（reduced-motion） | 验证：`prefers-reduced-motion` 降级、对比度、焦点顺序是验收项 |

---

## 参考资料

本文融合自外部「视觉提示词库 · F 组：交互与时空」（v1.x，2026-08-16），为忠实迁移 + 规范适配：保留全部小节、L 层级、档位、成本、强度值域、同族拓展与提示词片段，仅改写跨库交叉引用（统一为 `R{n}·{原引用}`）并移除项目专属内容。

技术术语（scroll-driven animations、View Transitions API、pointer events、Web Audio AnalyserNode、prefers-reduced-motion、content-visibility、Container Queries 等）与所有参数值均来自该库原始调研，未新增未经验证的数据，亦未编造论文或网址来源。
