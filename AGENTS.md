# AGENTS.md — 本库使用协议（AI 入口）

> 本文件是本知识库的"说明书"，面向 AI 编码助手（Claude Code / Cursor / Copilot 等）。
> 目标是让任何 AI 都能用 140+ 篇文档，产出**有主见、不像 AI 做的**顶级前端。

## 1. 库结构速览

所有文档位于本仓库根目录，命名规则 `板块字母-编号-标题.md`：

| 板块 | 内容 |
|---|---|
| 00-* | 索引与规划（`00-总索引.md` 是入口，先读它） |
| A | 视觉基础原理（格式塔/认知/无障碍） |
| B | 视觉设计专题（排版/色彩/动效/布局/质感/品牌/差异化，**内容最多**） |
| C | 内容与交互模式 |
| D | 设计与工程系统（令牌/结构蓝图） |
| E | 趋势与竞品参考 |
| F | 前端资源与工具 |
| G | 场景与行业 |
| H | 行为与转化 |
| M | 方法论与治理（M1 是生成方法论核心） |
| P | 排版与布局 |
| R | 可粘贴视觉提示词库（`R0`–`R18`，`R0` 是入口）+ 视觉方法论与维度清单（`R19`–`R22`） |
| T | 度量与验收 |
| V | 视觉特效 |
| Z | Prompt 与控制（Z1/Z2/Z3 是执行入口） |

## 2. 生成任务工作流（必须遵守）

1. **先读 `00-总索引.md`**，确定要检索的板块
2. 按 `Z2` 的检索协议：根据产品上下文选 5-12 个相关文档，**不要把所有专题塞进一个 prompt**
3. 按 `M1` 的方法论规划并生成（铁律：用参考不用描述；写代码前先陈述字体/色彩/布局方向）
4. 参考 `B82`/`B44` 的反 AI 味清单：**当作参考清单，不是硬性铁律**——逐条看一遍，主动偏离 AI 共同分母，但具体取舍由项目自己决定
5. 有参考素材时按 `B85` 演化法：拆解 → 杂交 → 约束变异 → 三问验证
6. 仅当视觉效果是页面主表达时动用 R 板块：先用 `R20` 想清楚、`R21` 定一个主方向，按需以 `R19`/`R22` 查漏，再进 `R0` 从 `R1`–`R18` 挑少量可执行条目；普通页面不必强行使用 R。**不要整组塞进 prompt**
7. 生成后按 `Z3` 五轮审计（视觉/UX/内容/工程/原创性），**P0/P1 必须实际修复**，不得以"下次注意"搪塞

## 3. 质量自检（参考清单，按项目取舍）

以下是高风险默认值提醒，不是必须全部满足的验收门槛：

- [ ] 字体不是 Inter/Roboto/系统默认（B82）
- [ ] 配色不是紫蓝渐变 + 白底（B82）
- [ ] 布局不是三张圆角卡片（B82）
- [ ] 有 1-2 个英雄元素 + 足够留白（B18）
- [ ] 通过原创性三问：遮 logo 能认出品牌？是新组合不是熟悉组合？有只有你能给的约束？（B85）
- [ ] 内容真实，无 lorem ipsum（C6）

以下是**安全项，不建议省**：

- [ ] 无障碍 AA 达标、动效有 reduced-motion 降级（C4/C7）
- [ ] 状态完整：默认/hover/loading/空（D4/B73）
- [ ] 闪烁安全与多通道冗余（B43/R16）

## 4. 审计/学习任务

- 审计已有页面：读 `Z3` 五轮审计清单，逐项打分并实际修复
- 更新知识库：按 `M2` 协议扫描 → 升级 → 决定是否新增；新增遵循 CONTRIBUTING.md 规范
- 学习新知识：新增/深化文档必须带"参考资料（第 N 轮深挖）"，数据要有来源

## 5. 反模式（禁止）

- ❌ 不读索引直接生成
- ❌ 把 B1-B85 或 R0-R18 全部塞进 prompt（上下文爆炸 + 平均化）
- ❌ 审计发现问题却只承诺不改
- ❌ 用 lorem ipsum 占位
- ❌ 抄袭参考的"指纹"（专属配色/标志性布局）而非学习其逻辑（B85 反模式）
- ❌ 把 R 文档里的 `R5·C5`、`R4·B3` 当成本库 B/C 板块编号来读（那是 R 文档的内部小节号）
