# B53 · 导航设计完全指南

> 导航是空间的地图、是用户的方向盘。
> 一个不会迷路的人不是因为路简单,而是因为路标好。
> 导航决定了用户**能否找到**内容、**愿不愿意探索**更多,以及**是否信任**这个产品。
> 从顶部栏到侧边栏,从汉堡菜单到面包屑,从滚动行为到响应式策略——全部拆干净。

---

## 一、导航术语表

| 中文术语 | 英文术语 | 一句话含义 |
|---|---|---|
| 全局导航 | Global Navigation | 贯穿整站、每页可见的主导航系统 |
| 局部导航 | Local Navigation | 某个板块/页面内部的子级导航 |
| 导航栏 | Navigation Bar (Navbar) | 页面顶部/侧边的主导航容器 |
| 汉堡菜单 | Hamburger Menu | 三横线图标,点击展开隐藏导航(移动端常见) |
| 面包屑 | Breadcrumb | 显示当前页面在层级结构中的位置路径 |
| 标签栏 | Tab Bar | 底部固定的 3-5 个主入口(移动端核心导航) |
| 侧边栏 | Sidebar | 页面左/右侧的垂直导航区域 |
| 巨型菜单 | Mega Menu | 下拉展开的大面积多列菜单面板 |
| 分段控件 | Segmented Control | 2-5 个互斥选项的切换控件(类 Tab 的紧凑形式) |
| 页面标签 | Tab Navigation | 同一页内多个内容区域的水平切换 |
| 抽屉导航 | Drawer / Off-canvas | 从屏幕边缘滑出的导航面板 |
| 固定头部 | Sticky Header | 滚动时保持在视口顶部的导航栏 |
| 自动隐藏头部 | Auto-hide Header | 下滚隐藏、上滚显示的智能头部 |
| 滚动锚定 | Scroll Anchoring | 页面内容长时利用锚点定位各区段 |
| 目录导航 | Table of Contents (TOC) | 长内容页的章节目录(常见于文档/博客) |
| 分页 | Pagination | 内容过多时的逐页浏览控制 |
| 无限滚动 | Infinite Scroll | 滚动到底自动加载更多内容 |
| 步骤条 | Stepper / Progress Steps | 多步流程的进度指示与跳转 |
| 命令面板 | Command Palette (⌘K) | 键盘快捷键唤出的搜索+导航面板 |
| 快速跳转 | Quick Jump / Spotlight | 模糊搜索直达任意页面/功能 |
| 返回顶部 | Back to Top | 长页面的一键回到顶部按钮 |
| 上下文导航 | Contextual Navigation | 根据当前内容动态出现的相关链接 |
| 关联推荐 | Related / See Also | 内容底部的"相关阅读"推荐区 |
| 导航层级 | Navigation Hierarchy | 一级→二级→三级的信息层次结构 |
| 导航密度 | Navigation Density | 可见导航项的数量与间距关系 |
| 导航可发现性 | Navigation Discoverability | 用户能否自然发现导航入口 |
| 导航可预测性 | Navigation Predictability | 用户能否预判点击后会到达哪里 |
| 路标效应 | Signpost Effect | 导航项像路标一样指引方向的作用 |
| 导航盲区 | Navigation Blindness | 用户因习惯忽略某些导航区域 |
| 深链接 | Deep Linking | 直接链接到应用深层页面的能力 |
| 面包屑溢出 | Breadcrumb Overflow | 层级过深时面包屑的折叠/截断处理 |
| 导航权重 | Navigation Weight | 各导航项的重要程度排序依据 |

---

## 二、导航层级模型

### 三层导航架构

```
┌─────────────────────────────────────────────────┐
│  L1 全局导航(Global)                            │
│  ≤ 7 项 · 始终可见 · 代表产品核心功能板块          │
├─────────────────────────────────────────────────┤
│  L2 板块导航(Section)                           │
│  ≤ 12 项 · 进入板块后可见 · 侧栏/子Tab/Mega      │
├─────────────────────────────────────────────────┤
│  L3 页面导航(Page)                              │
│  面包屑/TOC/锚点/步骤条 · 在当前页内定位            │
└─────────────────────────────────────────────────┘
```

### 层级数量的铁律

| 层级深度 | 用户体验 | 对策 |
|---|---|---|
| 1 层 | 极好,一步到达 | 简单产品首选 |
| 2 层 | 好,用户尚可理解 | 中等复杂产品标配 |
| 3 层 | 可接受,需面包屑 | 大型产品/电商/文档 |
| 4 层+ | 危险,用户易迷失 | 必须有搜索兜底+面包屑 |

**Miller定律**:人短期记忆容量 7±2。全局导航超过 7 项就需要分组或 Mega Menu。

---

## 三、导航模式详解

### 3.1 顶部水平导航(Top Navbar)

**适用**:品牌站/落地页/内容少于 7 个一级板块的站点

```
┌─ Logo ─── Nav1 ─── Nav2 ─── Nav3 ─── Nav4 ─── [CTA] ─┐
│                                                        │
└────────────────────────────────────────────────────────┘
```

**视觉规格**:
```css
.navbar {
  height: 64px;                          /* 桌面 56-80px */
  padding-inline: clamp(16px, 4vw, 80px);
  display: flex;
  align-items: center;
  gap: 32px;
  backdrop-filter: blur(12px);           /* 半透明玻璃效果 */
  background: oklch(100% 0 0 / 0.8);
  border-bottom: 1px solid oklch(0% 0 0 / 0.06);
  position: sticky;
  top: 0;
  z-index: 100;
}

.nav-link {
  font-size: 14px;
  font-weight: 500;
  color: var(--text-secondary);
  text-decoration: none;
  padding: 8px 12px;
  border-radius: 6px;
  transition: color 200ms, background 200ms;
}

.nav-link:hover {
  color: var(--text-primary);
  background: oklch(0% 0 0 / 0.04);
}

.nav-link[aria-current="page"] {
  color: var(--primary);
  font-weight: 600;
}
```

**精工细节**:
- Logo 高度 28-36px,与导航项视觉对齐
- 导航项间距 24-40px(太密看着廉价,太疏像没做完)
- CTA 按钮与文字导航项高度差 ≤ 4px(视觉平衡)
- 固定头部加投影:`box-shadow: 0 1px 3px oklch(0% 0 0 / 0.06)` — 仅滚动后出现
- 首屏导航透明,滚动后渐变到白底(landing page 经典处理)

### 3.2 侧边导航(Sidebar)

**适用**:后台系统/文档站/SaaS 工具/层级多于 2 层的产品

```
┌──────────┬──────────────────────────────┐
│ Logo     │                              │
│ ──────── │                              │
│ 📊 仪表盘 │                              │
│ 👥 用户   │        主内容区               │
│ 📋 订单   │                              │
│ ⚙️ 设置   │                              │
│          │                              │
│ [折叠]   │                              │
└──────────┴──────────────────────────────┘
```

**视觉规格**:
```css
.sidebar {
  width: 260px;                          /* 展开状态 */
  /* width: 64px;                        /* 折叠状态 */
  height: 100dvh;
  position: fixed;
  left: 0;
  top: 0;
  padding: 16px 12px;
  background: var(--surface-secondary);
  border-right: 1px solid var(--border);
  overflow-y: auto;
  overscroll-behavior: contain;
  transition: width 250ms cubic-bezier(0.4, 0, 0.2, 1);
}

.sidebar-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 10px 12px;
  border-radius: 8px;
  font-size: 14px;
  color: var(--text-secondary);
  transition: background 150ms, color 150ms;
}

.sidebar-item:hover {
  background: oklch(0% 0 0 / 0.04);
  color: var(--text-primary);
}

.sidebar-item.active {
  background: var(--primary-alpha-10);
  color: var(--primary);
  font-weight: 600;
}

/* 折叠时图标居中 */
.sidebar.collapsed .sidebar-item {
  justify-content: center;
  padding: 10px;
}
.sidebar.collapsed .sidebar-item span {
  display: none;
}
```

**精工细节**:
- 展开宽度 240-280px(太窄放不下中文,太宽浪费空间)
- 折叠宽度 56-72px(只显示图标 + tooltip)
- 分组标题用 12px 大写灰字(UPPERCASE 的英文更适合做分隔)
- 活跃项左侧可加 3px 色条:`border-left: 3px solid var(--primary)`
- 滚动时侧栏头部(Logo区)固定,仅导航项可滚动
- 子级折叠用 chevron 旋转动画 180ms

### 3.3 汉堡菜单(Hamburger Menu)

**适用**:移动端 / 导航项超过 5 个时的响应式降级方案

```
打开前:  [≡] Logo            [搜索] [头像]
打开后:  ┌──────────────────────────────┐
         │ [✕]                          │
         │                              │
         │  首页                         │
         │  产品                         │
         │  方案                         │
         │  定价                         │
         │  博客                         │
         │                              │
         │  ─────────────────           │
         │  登录    注册                  │
         └──────────────────────────────┘
```

**视觉规格**:
```css
/* 汉堡图标动画 → X */
.hamburger {
  width: 44px;
  height: 44px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  gap: 5px;
  cursor: pointer;
}

.hamburger span {
  display: block;
  width: 22px;
  height: 2px;
  background: var(--text-primary);
  border-radius: 1px;
  transition: transform 300ms cubic-bezier(0.4, 0, 0.2, 1),
              opacity 200ms;
}

/* 打开状态 → X */
.hamburger.open span:nth-child(1) {
  transform: translateY(7px) rotate(45deg);
}
.hamburger.open span:nth-child(2) {
  opacity: 0;
}
.hamburger.open span:nth-child(3) {
  transform: translateY(-7px) rotate(-45deg);
}

/* 全屏覆盖面板 */
.mobile-menu {
  position: fixed;
  inset: 0;
  background: var(--surface);
  padding: 80px 24px 24px;
  display: flex;
  flex-direction: column;
  gap: 4px;
  opacity: 0;
  visibility: hidden;
  transition: opacity 300ms, visibility 0s 300ms;
}

.mobile-menu.open {
  opacity: 1;
  visibility: visible;
  transition: opacity 300ms;
}

.mobile-menu a {
  font-size: 24px;
  font-weight: 600;
  padding: 16px 0;
  border-bottom: 1px solid var(--border);
  color: var(--text-primary);
}
```

**精工细节**:
- 汉堡按钮最小触控区 44×44px(iOS HIG 标准)
- 三横线间距 5-6px,线宽 2px,线长 20-24px
- 打开面板时 body overflow: hidden 禁止背景滚动
- 面板内导航项 stagger 入场(每项 delay 50ms,从上到下)
- 关闭时整体 fade(不要 stagger 退场,会慢)
- 打开状态下 focus-trap:Tab 键不能跳出面板

### 3.4 底部标签栏(Tab Bar)

**适用**:移动端 App / PWA — 3-5 个最核心入口

```
┌────────────────────────────────────────┐
│              主内容区                    │
│                                        │
├────┬────┬────┬────┬────────────────────┤
│ 🏠 │ 🔍 │ ➕ │ 💬 │ 👤                  │
│ 首页 │ 发现 │ 发布 │ 消息 │ 我的            │
└────┴────┴────┴────┴────────────────────┘
```

**视觉规格**:
```css
.tab-bar {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  height: 56px;                        /* + safe-area-inset-bottom */
  padding-bottom: env(safe-area-inset-bottom);
  display: flex;
  align-items: center;
  justify-content: space-around;
  background: var(--surface);
  border-top: 1px solid var(--border);
  z-index: 100;
}

.tab-item {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2px;
  padding: 6px 0;
  min-width: 64px;
  font-size: 10px;
  color: var(--text-tertiary);
  transition: color 200ms;
}

.tab-item.active {
  color: var(--primary);
}

.tab-item .icon {
  width: 24px;
  height: 24px;
  transition: transform 200ms cubic-bezier(0.4, 0, 0.2, 1);
}

.tab-item.active .icon {
  transform: scale(1.1);
}
```

**精工细节**:
- 数量铁律:3-5 个(≤ 2 太少不需 Tab Bar,≥ 6 太挤)
- 图标 24px + 文字 10px,总高 48-56px(不含安全区)
- 活跃项:图标填充态 + 色彩 + 微 scale(1.05-1.1)
- 未读红点:直径 8px,偏移到图标右上角(-2px, -2px)
- iOS 需要加 `padding-bottom: env(safe-area-inset-bottom)` 避开 Home Indicator
- 切换时给内容区加 crossfade 或 slide 过渡

### 3.5 面包屑(Breadcrumb)

**适用**:层级 ≥ 3 的产品(电商/文档/后台/企业站)

```
首页  ›  男装  ›  外套  ›  风衣      ← 当前页不可点击
```

**视觉规格**:
```css
.breadcrumb {
  display: flex;
  align-items: center;
  gap: 4px;
  font-size: 13px;
  color: var(--text-tertiary);
  padding: 12px 0;
}

.breadcrumb a {
  color: var(--text-secondary);
  text-decoration: none;
  padding: 4px 6px;
  border-radius: 4px;
  transition: color 150ms, background 150ms;
}

.breadcrumb a:hover {
  color: var(--primary);
  background: var(--primary-alpha-5);
}

.breadcrumb .separator {
  color: var(--text-tertiary);
  font-size: 12px;
  user-select: none;
}

.breadcrumb .current {
  color: var(--text-primary);
  font-weight: 500;
}
```

**精工细节**:
- 分隔符用 › 或 / 或 chevron 图标(不要用 >)
- 当前页不加链接,显示为深色
- 层级 > 4 时中间折叠为 `...`,hover 展开
- 移动端面包屑显示 「← 上一级」即可(空间不够展示全部)
- 结构化数据:`<nav aria-label="breadcrumb">` + Schema.org BreadcrumbList

**溢出处理**:
```css
/* 层级过深时折叠中间项 */
.breadcrumb.overflow .middle-items {
  display: none;
}
.breadcrumb.overflow .ellipsis {
  display: inline-flex;
  cursor: pointer;
}
.breadcrumb.overflow .ellipsis:hover + .middle-items {
  display: flex;
  position: absolute;
  background: var(--surface);
  box-shadow: var(--shadow-md);
  border-radius: 8px;
  padding: 8px;
  flex-direction: column;
}
```

### 3.6 Mega Menu(巨型菜单)

**适用**:电商/企业站/导航项内有大量子分类的场景

```
┌──────────────────────────────────────────────────────┐
│  产品 ▾                                              │
├──────────────────────────────────────────────────────┤
│ ┌─────────┬──────────┬──────────┬─────────────────┐ │
│ │ 设计工具  │ 开发工具   │ 协作平台  │  🖼️ 精选推荐   │ │
│ │ ──────── │ ──────── │ ──────── │                 │ │
│ │ Figma    │ VS Code  │ Slack    │  [推荐卡片]     │ │
│ │ Sketch   │ GitHub   │ Notion   │                 │ │
│ │ Adobe XD │ Vercel   │ Linear   │                 │ │
│ │ Framer   │ Netlify  │ Discord  │                 │ │
│ └─────────┴──────────┴──────────┴─────────────────┘ │
└──────────────────────────────────────────────────────┘
```

**视觉规格**:
```css
.mega-menu {
  position: absolute;
  top: 100%;
  left: 50%;
  transform: translateX(-50%);
  width: min(1200px, 90vw);
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 12px;
  box-shadow: 0 20px 60px oklch(0% 0 0 / 0.1),
              0 4px 12px oklch(0% 0 0 / 0.05);
  padding: 32px;
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 32px;
  
  /* 入场动画 */
  opacity: 0;
  transform: translateX(-50%) translateY(-8px);
  pointer-events: none;
  transition: opacity 200ms, transform 200ms cubic-bezier(0.4, 0, 0.2, 1);
}

.nav-item:hover .mega-menu,
.nav-item:focus-within .mega-menu {
  opacity: 1;
  transform: translateX(-50%) translateY(0);
  pointer-events: auto;
}

.mega-column h3 {
  font-size: 12px;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  color: var(--text-tertiary);
  margin-bottom: 12px;
}

.mega-link {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 8px 10px;
  border-radius: 8px;
  font-size: 14px;
  transition: background 150ms;
}

.mega-link:hover {
  background: oklch(0% 0 0 / 0.04);
}
```

**精工细节**:
- 显示延迟 100-150ms(防止鼠标路过就弹出)
- 消失延迟 300-400ms(允许光标移向面板)
- 面板与触发项之间留"安全三角"(diagonal movement safe zone)
- 最右列放视觉推荐(图片/卡片),打破纯文字的枯燥
- 分列标题全大写 + letter-spacing 加宽(起分隔线作用)
- 键盘可访问:Enter 打开,Escape 关闭,方向键导航

### 3.7 命令面板(Command Palette / ⌘K)

**适用**:工具类产品/开发者产品/内容多搜索比导航高效的场景

```
┌────────────────────────────────────────┐
│ 🔍 搜索页面、命令或设置...              │
├────────────────────────────────────────┤
│ 最近访问                               │
│   📄 用户管理                           │
│   📄 数据看板                           │
│                                        │
│ 建议                                   │
│   ⚡ 创建新项目                         │
│   ⚙️ 打开设置                          │
│   👤 切换账号                           │
└────────────────────────────────────────┘
```

```css
.command-palette {
  position: fixed;
  top: 20%;
  left: 50%;
  transform: translateX(-50%);
  width: min(640px, 90vw);
  max-height: 60vh;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 16px;
  box-shadow: 0 40px 100px oklch(0% 0 0 / 0.2);
  overflow: hidden;
  
  /* 入场 */
  animation: palette-in 200ms cubic-bezier(0.2, 0, 0, 1);
}

@keyframes palette-in {
  from {
    opacity: 0;
    transform: translateX(-50%) scale(0.95);
  }
}

.command-input {
  width: 100%;
  padding: 16px 20px;
  font-size: 16px;
  border: none;
  border-bottom: 1px solid var(--border);
  outline: none;
  background: transparent;
}

.command-item {
  display: flex;
  align-items: center;
  gap: 12px;
  padding: 10px 20px;
  font-size: 14px;
  cursor: pointer;
  transition: background 100ms;
}

.command-item[aria-selected="true"],
.command-item:hover {
  background: oklch(0% 0 0 / 0.04);
}

.command-item kbd {
  margin-left: auto;
  font-size: 11px;
  padding: 2px 6px;
  background: oklch(0% 0 0 / 0.06);
  border-radius: 4px;
  font-family: inherit;
}
```

**精工细节**:
- ⌘K / Ctrl+K 全局快捷键唤出
- 输入框 autofocus,无需点击直接输入
- 实时模糊匹配 + 高亮匹配字符
- 键盘 ↑↓ 选中,Enter 执行,Escape 关闭
- 分组显示:最近 / 页面 / 命令 / 设置
- 无结果时显示"没有匹配" + 建议操作

---

## 四、滚动行为

### 4.1 固定头部(Sticky Header)

```css
.header {
  position: sticky;
  top: 0;
  z-index: 100;
  transition: box-shadow 300ms, background 300ms;
}

/* 滚动后出现投影 */
.header.scrolled {
  box-shadow: 0 1px 3px oklch(0% 0 0 / 0.08);
  background: oklch(100% 0 0 / 0.95);
  backdrop-filter: blur(12px);
}
```

### 4.2 自动隐藏头部(Scroll Direction Aware)

```css
.header {
  position: sticky;
  top: 0;
  transform: translateY(0);
  transition: transform 300ms cubic-bezier(0.4, 0, 0.2, 1);
}

.header.hidden {
  transform: translateY(-100%);
}
```

```javascript
// 滚动方向检测
let lastScroll = 0;
const header = document.querySelector('.header');

window.addEventListener('scroll', () => {
  const currentScroll = window.scrollY;
  
  if (currentScroll > lastScroll && currentScroll > 80) {
    header.classList.add('hidden');    // 下滚 → 隐藏
  } else {
    header.classList.remove('hidden'); // 上滚 → 显示
  }
  
  lastScroll = currentScroll;
}, { passive: true });
```

**精工细节**:
- 死区阈值:滚动 < 80px 不触发(防页面顶部抖动)
- 上滚立即显示(不要等到顶部才显示)
- 隐藏时不要影响页面布局(用 transform 不用 display)
- 移动端尤其重要:64px 头部在小屏上很奢侈

### 4.3 头部尺寸渐变(Shrink on Scroll)

```css
.header {
  --header-height: 80px;
  height: var(--header-height);
  transition: height 300ms;
}

.header.compact {
  --header-height: 56px;
}

.header .logo {
  height: calc(var(--header-height) * 0.45);
  transition: height 300ms;
}
```

**适用**:品牌站首屏 Logo 大、滚动后缩小让出空间。

### 4.4 锚点导航 + 滚动监听

```css
/* 平滑滚动 */
html {
  scroll-behavior: smooth;
  scroll-padding-top: 80px;  /* 留出固定头部高度 */
}

/* TOC 当前项高亮 */
.toc-link.active {
  color: var(--primary);
  font-weight: 600;
  border-left: 2px solid var(--primary);
  padding-left: 14px;
}
```

```javascript
// IntersectionObserver 监听各 section
const observer = new IntersectionObserver(
  (entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        document.querySelectorAll('.toc-link').forEach(link => {
          link.classList.toggle('active', 
            link.getAttribute('href') === `#${entry.target.id}`
          );
        });
      }
    });
  },
  { rootMargin: '-80px 0px -60% 0px' }
);

document.querySelectorAll('section[id]').forEach(s => observer.observe(s));
```

---

## 五、响应式导航策略

### 断点与导航形态变换

| 视口 | 宽度 | 导航形态 |
|---|---|---|
| 移动端 | < 768px | 汉堡菜单 + 底部标签栏 |
| 平板 | 768-1024px | 精简顶栏(收起次要项) |
| 桌面 | 1024-1440px | 完整顶栏 或 侧栏+顶栏 |
| 超宽 | > 1440px | 同桌面,max-width 限制内容区 |

### 响应式降级逻辑

```css
/* 移动端:隐藏桌面导航,显示汉堡 */
@media (width < 768px) {
  .desktop-nav { display: none; }
  .hamburger { display: flex; }
  .tab-bar { display: flex; }
}

/* 平板:精简但保留水平导航 */
@media (768px <= width < 1024px) {
  .desktop-nav .secondary-items { display: none; }
  .desktop-nav .more-btn { display: flex; }
}

/* 桌面 */
@media (width >= 1024px) {
  .hamburger { display: none; }
  .tab-bar { display: none; }
  .desktop-nav { display: flex; }
}
```

### 侧栏的响应式

```css
/* 移动端:侧栏变为抽屉 */
@media (width < 1024px) {
  .sidebar {
    transform: translateX(-100%);
    position: fixed;
    z-index: 200;
    transition: transform 300ms cubic-bezier(0.4, 0, 0.2, 1);
  }
  
  .sidebar.open {
    transform: translateX(0);
  }
  
  .sidebar-overlay {
    position: fixed;
    inset: 0;
    background: oklch(0% 0 0 / 0.5);
    opacity: 0;
    transition: opacity 300ms;
    pointer-events: none;
  }
  
  .sidebar.open ~ .sidebar-overlay {
    opacity: 1;
    pointer-events: auto;
  }
}
```

---

## 六、导航无障碍

### 必做项

```html
<!-- 语义结构 -->
<nav aria-label="主导航">
  <ul role="menubar">
    <li role="none"><a role="menuitem" href="/">首页</a></li>
    <li role="none"><a role="menuitem" aria-current="page">产品</a></li>
  </ul>
</nav>

<!-- 跳过导航(屏幕阅读器用户的快捷方式) -->
<a href="#main-content" class="skip-link">跳到主要内容</a>
```

```css
.skip-link {
  position: absolute;
  top: -100%;
  left: 16px;
  padding: 8px 16px;
  background: var(--primary);
  color: white;
  border-radius: 0 0 8px 8px;
  z-index: 1000;
  font-weight: 600;
  transition: top 200ms;
}

.skip-link:focus {
  top: 0;
}
```

### 键盘导航

| 按键 | 行为 |
|---|---|
| Tab | 在导航项之间移动焦点 |
| Enter / Space | 激活当前项 / 展开子菜单 |
| Escape | 关闭子菜单 / Mega Menu / 命令面板 |
| ← → | 水平导航项之间移动 |
| ↑ ↓ | 垂直子菜单项之间移动 |
| Home / End | 跳到第一项 / 最后一项 |

### Focus 样式

```css
:focus-visible {
  outline: 2px solid var(--primary);
  outline-offset: 2px;
  border-radius: 4px;
}

/* 移除鼠标 focus 样式(保留键盘的) */
:focus:not(:focus-visible) {
  outline: none;
}
```

---

## 七、导航动效

### 活跃指示器动画(Sliding Indicator)

```css
.nav-indicator {
  position: absolute;
  bottom: 0;
  height: 2px;
  background: var(--primary);
  border-radius: 1px;
  transition: left 300ms cubic-bezier(0.4, 0, 0.2, 1),
              width 300ms cubic-bezier(0.4, 0, 0.2, 1);
}
```

用 JS 计算当前活跃项的 offsetLeft 和 offsetWidth,赋给 indicator 的 left 和 width。

### 下拉展开动画

```css
.dropdown {
  transform-origin: top center;
  opacity: 0;
  transform: scaleY(0.95) translateY(-4px);
  transition: opacity 200ms, transform 200ms cubic-bezier(0.4, 0, 0.2, 1);
}

.dropdown.open {
  opacity: 1;
  transform: scaleY(1) translateY(0);
}
```

### 移动端菜单 Stagger 入场

```css
.mobile-menu a {
  opacity: 0;
  transform: translateY(12px);
  transition: opacity 300ms, transform 300ms cubic-bezier(0.4, 0, 0.2, 1);
}

.mobile-menu.open a:nth-child(1) { transition-delay: 50ms; }
.mobile-menu.open a:nth-child(2) { transition-delay: 100ms; }
.mobile-menu.open a:nth-child(3) { transition-delay: 150ms; }
.mobile-menu.open a:nth-child(4) { transition-delay: 200ms; }
.mobile-menu.open a:nth-child(5) { transition-delay: 250ms; }

.mobile-menu.open a {
  opacity: 1;
  transform: translateY(0);
}
```

---

## 八、Prompt 模板

```
【导航系统设计】

全局结构:
- 顶部导航栏 height {64px}
- 位置: sticky top
- 玻璃效果: backdrop-filter blur(12px), 背景 rgba(255,255,255,0.85)
- 滚动后出现 1px 底边框 + 微投影
- Logo 左侧 28px 高
- 导航项: 居中 / 右侧, font-size 14px, weight 500
- 当前页: 主色高亮 + weight 600
- CTA: 右侧实色按钮

响应式:
- ≥ 1024px: 完整水平导航
- 768-1023px: 收起次要项到 "更多" 下拉
- < 768px: 汉堡菜单(全屏覆盖)
  - 汉堡图标 44×44 触控区, 三横线 → X 动画
  - 全屏白底面板, 导航项 24px font-size
  - stagger 50ms 依次入场

滚动行为: {选一个}
- [固定] 始终可见 + 滚动后加投影
- [自动隐藏] 下滚隐藏 + 上滚立即显示 + 80px 死区
- [缩小] 首屏 80px 大导航 → 滚动后 56px 紧凑版

子导航(如果需要):
- Mega Menu: 宽度 1200px, 3-4 列 + 推荐卡片列
- 面包屑: ≥ 3 层级时显示, 分隔符 ›
- 侧栏: 260px 展开 / 64px 折叠, 左侧色条标记活跃项

无障碍:
- <nav aria-label="主导航">
- 跳过导航链接
- 键盘 Tab/Enter/Escape/方向键
- focus-visible 2px primary outline
- aria-current="page" 标记当前页

动效:
- 活跃指示器滑动 300ms ease-out
- 下拉 fade + translateY 200ms
- 移动端菜单 stagger 入场 50ms/项
```

---

## 九、实现代码 · 完整响应式导航

```html
<header class="header" id="header">
  <nav class="nav" aria-label="主导航">
    <a href="/" class="nav-logo">
      <img src="/logo.svg" alt="Brand" width="120" height="32">
    </a>
    
    <!-- 桌面导航 -->
    <ul class="nav-menu" role="menubar">
      <li><a href="/products" role="menuitem">产品</a></li>
      <li><a href="/solutions" role="menuitem">解决方案</a></li>
      <li><a href="/pricing" role="menuitem" aria-current="page">定价</a></li>
      <li><a href="/docs" role="menuitem">文档</a></li>
      <li><a href="/blog" role="menuitem">博客</a></li>
    </ul>
    
    <div class="nav-actions">
      <a href="/login" class="nav-link">登录</a>
      <a href="/signup" class="nav-cta">免费开始</a>
    </div>
    
    <!-- 移动端汉堡 -->
    <button class="hamburger" aria-label="打开菜单" aria-expanded="false">
      <span></span><span></span><span></span>
    </button>
  </nav>
</header>

<!-- 移动端面板 -->
<div class="mobile-menu" id="mobile-menu" aria-hidden="true">
  <a href="/products">产品</a>
  <a href="/solutions">解决方案</a>
  <a href="/pricing">定价</a>
  <a href="/docs">文档</a>
  <a href="/blog">博客</a>
  <hr>
  <a href="/login">登录</a>
  <a href="/signup" class="mobile-cta">免费开始</a>
</div>
```

```css
/* ===== 完整导航样式 ===== */
.header {
  position: sticky;
  top: 0;
  z-index: 100;
  background: oklch(100% 0 0 / 0.85);
  backdrop-filter: blur(12px);
  border-bottom: 1px solid transparent;
  transition: border-color 300ms, box-shadow 300ms;
}

.header.scrolled {
  border-color: oklch(0% 0 0 / 0.06);
  box-shadow: 0 1px 4px oklch(0% 0 0 / 0.06);
}

.nav {
  display: flex;
  align-items: center;
  height: 64px;
  max-width: 1280px;
  margin: 0 auto;
  padding-inline: clamp(16px, 4vw, 40px);
}

.nav-logo img {
  height: 28px;
  width: auto;
}

.nav-menu {
  display: flex;
  gap: 4px;
  margin-left: 48px;
  list-style: none;
}

.nav-menu a {
  padding: 8px 14px;
  font-size: 14px;
  font-weight: 500;
  color: oklch(30% 0 0);
  border-radius: 6px;
  text-decoration: none;
  transition: color 150ms, background 150ms;
}

.nav-menu a:hover {
  color: oklch(15% 0 0);
  background: oklch(0% 0 0 / 0.04);
}

.nav-menu a[aria-current="page"] {
  color: var(--primary, oklch(55% 0.2 260));
  font-weight: 600;
}

.nav-actions {
  display: flex;
  align-items: center;
  gap: 16px;
  margin-left: auto;
}

.nav-cta {
  padding: 8px 20px;
  font-size: 14px;
  font-weight: 600;
  color: white;
  background: var(--primary, oklch(55% 0.2 260));
  border-radius: 8px;
  text-decoration: none;
  transition: opacity 150ms;
}

.nav-cta:hover { opacity: 0.9; }

/* ===== 响应式 ===== */
.hamburger { display: none; }

@media (width < 768px) {
  .nav-menu,
  .nav-actions { display: none; }
  .hamburger { display: flex; }
}
```

---

## 十、踩坑指南

### 坑1:Mega Menu 的"对角线问题"
鼠标从导航项移向菜单面板时路径是对角线,如果面板检测到 mouseleave 就关了。
**解法**:用 300ms 关闭延迟,或实现 Amazon 式"安全三角"算法。

### 坑2:iOS Safari 底部安全区
底部标签栏没加 `env(safe-area-inset-bottom)` 会被 Home Indicator 遮挡。

### 坑3:固定头部遮挡锚点
页面内跳转到 `#section` 时,内容被固定头部盖住。
**解法**:`html { scroll-padding-top: 80px; }` 或 section 加 `scroll-margin-top`。

### 坑4:汉堡菜单打开时背景滚动
**解法**:打开时 `document.body.style.overflow = 'hidden'`,关闭时恢复。注意 iOS 需要额外处理 `-webkit-overflow-scrolling`。

### 坑5:Tab Bar 和虚拟键盘冲突
移动端弹出键盘时底部 Tab Bar 会被推上来。
**解法**:检测 `visualViewport.resize` 事件,键盘出现时隐藏 Tab Bar。

### 坑6:导航项过多时的溢出
**错误做法**:允许换行(导航变成两行很丑)。
**正确做法**:隐藏溢出项到"更多"下拉,或用 `ResizeObserver` 动态计算能显示几个。

### 坑7:面包屑 SEO 但显示截断
搜索引擎需要完整面包屑(结构化数据),但 UI 可以折叠中间层。
**解法**:HTML 保留完整结构,CSS 隐藏中间项,结构化数据保持完整。

### 坑8:侧栏折叠后 Tooltip 定位
折叠侧栏只显示图标,hover 时需要 tooltip 显示文字。
**坑**:tooltip 被 sidebar 的 `overflow: hidden` 裁切。
**解法**:tooltip 用 `position: fixed` 或挂载到 body。

---

## 十一、谁在用(优秀案例)

| 产品/网站 | 导航手法 | 值得学的 |
|---|---|---|
| **Linear** | 侧栏 + ⌘K | 折叠态动画丝滑,键盘体验极致 |
| **Stripe** | 顶栏 + Mega Menu | 安全三角 + 面板内有代码示例 + 动画精致 |
| **Notion** | 侧栏(树形) | 无限嵌套可折叠,拖拽重排,宽度可调 |
| **Apple** | 顶栏 + 固定 | 首屏透明→滚动后毛玻璃,极简项数 |
| **Vercel** | 顶栏 + ⌘K | 下拉菜单 popover 动画,命令面板功能全 |
| **Shopify** | 侧栏 + 面包屑 | 三级深度管理完善,移动端抽屉 |
| **Airbnb** | 底部标签栏(移动) | 图标动效,切换时微 bounce |
| **GitHub** | 顶栏 + 侧栏 + ⌘K | 三套并存无冲突,响应式降级完美 |
| **Framer** | 首屏透明头 + 滚动后固定 | 头部缩放 + 毛玻璃过渡 + Mega 面板光影 |

---

## 检验清单

- [ ] 全局导航项 ≤ 7 个,超过有分组/Mega 方案
- [ ] 当前页有明确的活跃状态指示
- [ ] 移动端有汉堡菜单或底部标签栏(不是把桌面导航直接缩小)
- [ ] 固定头部不遮挡锚点跳转(scroll-padding-top)
- [ ] 汉堡菜单有 ≡ → × 过渡动画
- [ ] 打开移动菜单时背景不可滚动
- [ ] 有 `<nav aria-label>` + 跳过导航链接
- [ ] 键盘 Tab/Enter/Escape 可完成所有导航操作
- [ ] 层级 ≥ 3 时有面包屑
- [ ] 面包屑溢出时有折叠处理(不会撑爆一行)
- [ ] 下拉/Mega 有入场动画(不是瞬间出现)
- [ ] 底部标签栏加了 safe-area-inset-bottom
- [ ] 侧栏有折叠态,折叠后有 tooltip 显示文字
- [ ] 导航在 375px 宽度下不溢出/不换行
