# B37 · 空间设计(AR/VR/visionOS)

> 界面从"平面"走向"空间"。2025+ Apple Vision Pro / Meta Quest / 空间计算成为新战场。
> 空间 UI 有一整套独立的设计规则——平面设计经验只能用一半。

---

## 一、空间设计 vs 平面设计

| 维度 | 平面 UI | 空间 UI |
|---|---|---|
| **维度** | 2D(x, y) | 3D(x, y, **z**) |
| **深度** | 模拟(阴影/层叠) | 真实(物体在 z 轴不同位置) |
| **视角** | 固定(屏幕) | 动态(头部移动改变视角) |
| **输入** | 鼠标/触控/键盘 | **注视 + 手势 + 语音** |
| **距离** | 固定(30-60cm) | 可变(0.5m - 20m) |
| **光照** | 模拟 | 真实环境光参与 |
| **材质** | CSS 模拟 | 真正的材质(玻璃/金属) |
| **尺寸** | 像素 | 物理尺寸(点/米) |

---

## 二、空间 UI 的核心原则

### 1. 舒适区域(Comfort Zone)
人的视野有舒适范围:
```
水平视角:  中心 ±30° 舒适,±60° 可见,超过需转头
垂直视角:  中心 ±20° 舒适,向下比向上舒适
深度距离:  0.5m - 3m 舒适阅读,<0.5m 太近(不适),>10m 太远(看不清)
```

**规则**:
- 核心 UI 放在视线正前方 1-2m 处
- 不要把内容放在极端角度(需要转头)
- 避免太近的内容(引发不适)

### 2. 窗口(Window)而非屏幕
空间中没有"屏幕边缘"——UI 是浮在空间中的**玻璃窗口(Window)**。

**visionOS 窗口特征**:
- 有圆角(大圆角,通常 32-48pt)
- 有玻璃质感(半透明 + 模糊)
- 有深度阴影(落在真实桌面/地板上)
- 可以移动/缩放/堆叠
- 窗口之间有 z 间距

### 3. 注视交互(Gaze Interaction)
用户**看到**元素 → 元素响应(高亮)→ **手指捏合**确认。

**设计要点**:
- hover 态 = 注视态(gaze highlight)
- 注视 hover 要明显(因为没有鼠标指针显示"我在这")
- 元素必须足够大(注视精度比鼠标低)
- 注视态不能太 subtle(空间环境干扰多)

### 4. 手势交互(Gesture)
- **捏合(Pinch)** = 点击/确认
- **拖拽(Pinch + Move)** = 移动元素
- **旋转(Two-hand Rotate)** = 旋转 3D 对象
- **缩放(Two-hand Pinch)** = 放大缩小
- **轻拍(Tap)** = 快速交互(近距离)

### 5. 深度层级(Z-Depth)
空间中不同内容的 z 轴位置:
```
最远层(-2m):  背景环境/天幕(Skybox)
远层(-1m):    背景装饰/粒子
基准层(0):    主要窗口/内容
近层(+0.2m):  浮层/Popover/Toast
最近层(+0.5m):Modal/Alert(紧急)
```

**规则**:重要/紧急的更近;背景/装饰的更远。

---

## 三、visionOS 设计语言

### 材质系统

**玻璃(Glass)**— 主要材质:
```
- 半透明(看到后面的真实环境)
- 背景模糊(系统级 blur)
- 亮面/暗面自动跟随环境光
- specular 高光随视角变化
- 厚度感(边缘有光折射)
```

**应用层 Glass 特征**:
- 窗口背景:系统玻璃(自动适配光线)
- 按钮:圆角矩形,hover 时亮起
- 导航栏:底部 tab bar,圆角,半透明
- 侧边栏:独立玻璃面板

### 尺寸系统(Points)
visionOS 使用 **点(pt)** 不是像素:
```
最小触控目标:  60pt × 60pt(比手机 44pt 大!)
按钮高度:      44-60pt
文字最小:      17pt(比屏幕上的 16px 大)
窗口最小:      360pt 宽
按钮间距:      ≥16pt(手指精度低)
```

### 焦点态(Hover/Focus)
注视时的视觉反馈:
```
默认态:   正常渲染
注视态:   元素轻微"浮起" + 高光边缘 + 微弱发光
  (用户知道"这个可以交互")
按下态:   缩小(scale 0.95) + 亮度降低
```

### 光照与阴影
- 阴影是**真实的**(跟随环境光方向)
- 窗口在"下方表面"投影
- 不需要手动加阴影——系统自动处理
- 窗口越高(z 轴越前),阴影越远越柔

---

## 四、空间中的排版

- **字号必须更大**(通常 17pt 起,标题 28-36pt)
- **行高更大**(1.4 在空间中太紧)
- **对比度要更强**(环境光干扰,文字要清晰)
- **避免细字重**(200/300 在空间中难读)
- **深色文字 + 浅色玻璃底**(最佳可读性)
- **或浅色文字 + 深色底**(暗环境)

---

## 五、空间中的动效

### 原则
- 动效要**物理化**(有重量/惯性/弹性)
- 窗口移动有**惯性衰减**(松手后滑行减速)
- 避免突变(没有 `display: none` 突然消失)
- 进入/退出有**深度方向的动画**(从远到近淡入)

### 进入动效
```
窗口出现:从 z-100pt 处 → 到位(z: 0)
         同时 opacity 0 → 1 + scale 0.9 → 1
         时长:400-600ms
         缓动:spring(柔和弹性)
```

### 退出动效
```
窗口消失:从 z: 0 → z: -100pt
         同时 opacity 1 → 0 + scale 1 → 0.95
         时长:300ms(比进入快)
```

### 内容过渡
- 标签页切换:内容 crossfade(不是滑动)
- 导航深入:新页从右/从远处滑入
- 返回:反向

---

## 六、空间 UI 的独特模式

### Ornaments(装饰件)
浮在窗口外的小控件(播放按钮浮在视频窗口下方)。

### Volumes(体积)
3D 对象占据真实空间(地球仪/产品 3D 模型)。
- 可以从各个角度看
- 可以旋转/缩放
- 有真实光影

### Immersive Spaces(沉浸空间)
完全替换或扩展真实环境:
- **Passthrough**(默认):看到真实世界 + UI 叠加
- **半沉浸**:部分环境被虚拟内容覆盖
- **全沉浸**:完全在虚拟世界中

### SharePlay 多人
多人看到同一个虚拟对象,各自视角不同。

---

## 七、Web 上模拟空间 UI

### CSS 3D
```css
.spatial-card {
  transform: perspective(1000px) translateZ(40px);
  backdrop-filter: blur(20px);
  background: rgba(255,255,255,0.08);
  border: 1px solid rgba(255,255,255,0.12);
  border-radius: 24px;
  box-shadow: 0 20px 60px rgba(0,0,0,0.3);
}
```

### 视差响应鼠标
```js
// 鼠标位置 → 各层不同偏移
document.addEventListener('mousemove', (e) => {
  const x = (e.clientX / innerWidth - 0.5) * 20;
  const y = (e.clientY / innerHeight - 0.5) * 20;
  layer1.style.transform = `translate(${x*0.5}px, ${y*0.5}px)`;
  layer2.style.transform = `translate(${x*1}px, ${y*1}px)`;
  layer3.style.transform = `translate(${x*2}px, ${y*2}px)`;
});
```

### WebXR(真正的 AR/VR Web)
```js
const session = await navigator.xr.requestSession('immersive-ar');
// Three.js + WebXR 渲染
```

---

## 八、空间设计的可访问性

- **注视交互 + 手势替代**:语音控制 / 头部追踪 / 开关控制
- **字号足够大**(空间中比屏幕更大)
- **对比度高**(环境光干扰)
- **不完全依赖空间深度传达信息**(部分用户深度感知弱)
- **动效可关闭**(空间中动效更容易引发眩晕)
- **音频描述**(为视障用户描述空间布局)

---

## 九、Prompt 模板(空间风格 Web)

```
【空间 UI 风格(Web 模拟)】

视觉气质:visionOS / Apple Vision Pro 感觉

材质:
- 所有面板用玻璃拟态:
  - background: rgba(255,255,255,0.06)
  - backdrop-filter: blur(20px) saturate(150%)
  - border: 1px solid rgba(255,255,255,0.1)
  - border-radius: 24px
- 顶部 1px 内高光(rim light)

深度:
- 多层 translateZ 分层(背景/内容/浮层)
- 鼠标移动时层间视差偏移(最大 10px)
- perspective: 1200px
- 浮层有更强的阴影 + 更亮的背景

光效:
- 卡片 hover 时"浮起"感(translateZ + 阴影加深 + 亮度微增)
- CTA 有柔和外发光
- 环境光球在背景层(极慢漂浮)

动效:
- 入场:从 z 远处淡入近处(translateZ + opacity + scale)
- spring 缓动(有质感)
- 退场:反向(近→远 + 淡出)

排版:
- 字号偏大(body 18px)
- 高对比(玻璃底 + 深色文字)
- 不用极细字重

触控:
- 大触控目标(60×60pt)
- hover 态明显(发光+浮起)
```

---

## 十、检验清单

- [ ] 元素有真实 z 轴分层(不只是阴影模拟)
- [ ] 玻璃材质:半透明 + blur + 白边 + 内高光
- [ ] hover/focus 有"浮起"反馈
- [ ] 入场/退场有深度方向的动画
- [ ] 字号比平面 UI 大
- [ ] 触控目标 ≥60pt(空间中手势精度低)
- [ ] 鼠标/头部移动有视差响应
- [ ] 不完全依赖深度传达关键信息
- [ ] 有 reduced-motion 降级
