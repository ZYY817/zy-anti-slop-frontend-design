# D5 · 界面结构蓝图与视觉规格系统

> **定位**：把产品需求转译成可讨论、可检索、可生成、可审计的界面设计蓝图。
>
> **原则**：先决定内容和结构，再决定视觉；先完成视觉规格，再编译成 Prompt 和代码。

---

## 一、完整输出链

```text
Product Intent
→ Information Architecture
→ Screen Map
→ Page Anatomy
→ Content Hierarchy
→ Layout Blueprint
→ Typography System
→ Color / Surface System
→ Background / Media Direction
→ UI Component Map
→ State Matrix
→ Responsive Recomposition
→ Engineering Constraints
→ Validation Plan
```

任何一层没有明确答案时，不要用“现代、漂亮、高级、科技感”填空。

---

## 二、Interface Blueprint

### 1. Product Intent

```yaml
product:
primary_user:
primary_job:
business_goal:
page_goal:
emotional_goal:
content_density: low | medium | high | extreme
brand_personality:
device_priority: desktop | mobile | responsive
```

### 2. Screen Map

先确定页面在产品中的位置：

```text
产品
├── 全局导航
├── 当前区域
│   ├── 列表 / 概览
│   ├── 详情 / 工作区
│   └── 设置 / 帮助
└── 全局反馈 / 账户 / 权限
```

回答：

- 用户从哪里进入？
- 进入前已经知道什么？
- 离开后要去哪里？
- 哪些内容全局稳定，哪些内容局部变化？

### 3. Page Anatomy

不要只罗列组件，要说明每个区域的职责：

```text
Page
├── Orientation      用户确认“我在哪里”
├── Value            用户理解“这里最重要的结果是什么”
├── Evidence         用户判断“为什么可信”
├── Action           用户知道“下一步做什么”
├── Status           用户知道“当前发生什么”
└── Detail           用户需要深入时继续探索
```

### 4. Content Hierarchy

每个区域必须标注：

| 内容角色 | 说明 | 视觉责任 |
|---|---|---|
| Primary | 核心标题、核心结果、核心任务 | 最大视觉权重 |
| Supporting | 摘要、解释、上下文 | 降低理解成本 |
| Evidence | 数据、来源、案例、评价 | 建立信任 |
| Action | 主操作、次操作 | 明确下一步 |
| Metadata | 时间、作者、状态、单位 | 支持比较和追溯 |
| Decoration | 背景、纹理、图形 | 只能辅助，不抢内容 |

---

## 三、页面构造图的表达方式

每个重要页面至少输出 4 张图或等价文本：

### 1. 区域结构图

```text
┌──────────────────────────────────────────┐
│ Global Nav                               │
├───────────────────┬──────────────────────┤
│ Context / Title   │ Primary Visual       │
│ Summary           │ Image / Chart / UI   │
│ Primary Action    │                      │
├───────────────────┴──────────────────────┤
│ Evidence / Metrics / Supporting Content   │
├──────────────────────────────────────────┤
│ Next Action / Related Content             │
└──────────────────────────────────────────┘
```

### 2. 组件树

```text
Page
├── Header
│   ├── Brand
│   ├── Navigation
│   └── AccountMenu
├── Hero
│   ├── Eyebrow
│   ├── Heading
│   ├── Body
│   ├── Actions
│   └── Media
└── ContentSections
```

### 3. 视觉重量图

标出：

- 最大面积
- 最强对比
- 最醒目文字
- 最密集区域
- 最大留白
- 视觉动线
- 可能互相竞争的焦点

### 4. 响应式重组图

```text
Desktop:  左文字 + 右媒体 + 下方证据
Tablet:   上文字 + 下媒体 + 横向证据
Mobile:   标题 → CTA → 媒体 → 证据，次要内容折叠
```

---

## 四、排版规格必须先于 UI

排版是结构约束，不是最后加上的装饰。

### Typography Contract

```yaml
display_font:
body_font:
data_font:
fallback_stack:
type_scale: 1.2 | 1.25 | 1.333 | 1.5
display_size:
heading_sizes:
body_size:
caption_size:
weights: [400, 500, 600, 700]
heading_line_height:
body_line_height:
measure: 45-75ch
chinese_measure: 25-40 characters
tracking:
numeric_style: proportional | tabular
wrapping: balance | pretty | normal
alignment:
```

### 排版决策顺序

1. 先定义内容层级，再分配字号。
2. 先确定阅读长度，再确定容器宽度。
3. 先确定品牌气质，再选择字体组合。
4. 先确定语言和数据形态，再决定字重、字宽和数字样式。
5. 最后才决定特殊效果：裁切、描边、重叠、渐变字或动态文字。

### 排版验收

- [ ] 页面只有一个主标题语义焦点
- [ ] 标题层级与内容层级一致
- [ ] 正文行宽适合阅读
- [ ] 中文和英文分别使用合适字体
- [ ] 长标题换行后仍保持构图
- [ ] 数字、金额、日期和表格对齐
- [ ] 200% 缩放后文字仍可读
- [ ] 去掉颜色后层级仍然成立

排版基础见 [P1-排版基础.md](P1-排版基础.md)，进阶规则见 [B1-字体排版.md](B1-字体排版.md)、[B33-排版完整技术.md](B33-排版完整技术.md)、[B60-数字排版极致细节.md](B60-数字排版极致细节.md)。

---

## 五、背景图与媒体规格

背景不是填充空间的装饰，而是页面构图的一部分。

```yaml
media_type: none | photo | illustration | video | 3d | generative | chart | ui-preview
purpose: atmosphere | explanation | proof | navigation | focal-point
subject_position:
safe_text_zone:
crop_strategy: cover | contain | art-directed
overlay:
contrast_requirement:
mobile_behavior: crop | move-above | reduce | replace | remove
fallback:
performance_budget:
alt_or_semantic_role:
```

决策规则：

- 不能说明媒体服务的内容或品牌目的时，删除媒体。
- 背景图必须先定义主体位置，再定义文字位置。
- 文字不能压在高细节区域；必要时使用局部遮罩，不要整页黑蒙层。
- 移动端重新做裁切和顺序，不强行保留桌面构图。
- 视频、3D、粒子必须有静态替代和 reduced-motion 方案。
- 背景层的视觉权重必须低于内容层，除非页面目标就是沉浸式视觉叙事。

---

## 六、UI 组合规格

组件选择由内容和任务决定：

| 内容/任务 | 优先考虑 | 不要默认使用 |
|---|---|---|
| 高密度比较 | Table、List、Filter、Summary | 一堆等尺寸 Card |
| 长文阅读 | Prose、TOC、Quote、Progress | 宽屏卡片容器 |
| 单一主任务 | Focused Form、Primary CTA | 多个同权重按钮 |
| 复杂工作区 | Sidebar、Toolbar、Panel | 全屏装饰 Hero |
| 视觉叙事 | Media、Editorial Block、Scroll Scene | 机械三列 Feature |
| 空状态 | Explanation、Example、CTA | 只有“暂无数据” |
| AI Agent | Plan、Progress、Approval、Trace | 只有 Chat Bubble |

每个组件都要定义：

- 语义角色
- 内容输入
- 视觉权重
- 变体
- 状态
- 交互反馈
- 移动端形态
- 无障碍要求
- 何时不用

---

## 七、生成前的 Blueprint Prompt

```markdown
在写任何前端代码之前，先输出 Interface Blueprint。

请依次输出：

1. Product Intent：产品目标、用户、核心任务、内容密度和品牌人格。
2. Screen Map：页面在产品中的位置、入口、出口和导航关系。
3. Page Anatomy：页面区域、每个区域的职责和叙事顺序。
4. Content Hierarchy：Primary / Supporting / Evidence / Action / Metadata / Decoration。
5. Structure Diagram：页面区域结构图、组件树、视觉重量图。
6. Layout Blueprint：容器、栅格、对齐轴、版面率、留白、视觉动线和破格位置。
7. Typography Contract：字体、字号阶梯、字重、行高、行宽、字距、换行和数字规则。
8. Background / Media Direction：媒介类型、焦点、文字安全区、裁切、遮罩、移动端降级和性能预算。
9. UI Composition：每个区域使用什么组件、为什么使用、何时不用、有哪些状态。
10. Responsive Recomposition：桌面、平板、移动端的结构变化，而不是简单缩小。
11. State Matrix：loading、empty、error、success、disabled、permission、long-content。
12. Validation Plan：眯眼、灰度、缩略图、内容压力、移动端、无障碍和 AI 味检查。

每个重要决策必须包含：

- Decision：决定什么
- Reason：为什么适合当前产品
- Spec：用什么参数或结构实现
- Check：生成后如何验证

Blueprint 未完成前，不要写代码，不要先挑装饰效果。
```

---

## 八、完成标准

一个界面蓝图只有满足以下条件，才可以进入 Prompt 和代码阶段：

- [ ] 页面结构不是组件名称堆叠
- [ ] 内容层级已经确定
- [ ] 排版规格已经确定
- [ ] 背景和媒体有明确目的
- [ ] UI 组件有选择理由和禁用条件
- [ ] 视觉焦点和视觉重量可解释
- [ ] 移动端有重组方案
- [ ] 状态和边界已经列出
- [ ] 每个高风险视觉决策都有验证方法
