# B83 · AI Agent 界面模式

> B50 是"AI 原生界面视觉"（输入区/流式/代码块长什么样），G6 是"六种 AI 产品场景 prompt"。
> 本文件补的是 **Agent 时代的交互模式与信任设计**：工具调用、流式输出、引用、不确定性、多智能体协作。
> 核心问题：**用户怎么知道 AI 在干什么、可信不可信、失败了怎么恢复。**

---

## 一、Agent 交互核心模式

| 模式 | 设计要点 |
|---|---|
| 流式输出（Streaming） | 逐 token 呈现；保持光标稳定；展示进度而非空白等待（见 B50/G6） |
| 工具调用（Tool Calling） | 透明展示"正在调哪个工具/读什么数据"，可取消、可查看中间结果 |
| 引用/引文（Citations） | 结论锚定来源；悬停即见出处；来源可点开验证 |
| 思考态（Thinking） | 不隐藏过程但别剧透错的方向；用可折叠"正在考虑"状态 |
| 生成物（Artifacts） | 复杂产出独立画布，非阻塞聊天流；可编辑、可版本 |
| 上下文管理 | 展示模型"记得什么/不知道什么"，避免误解上下文 |
| 不确定性表达 | 说"我不确定"、"置信度低"、"可能出错"——比假自信可信 |

---

## 二、信任设计（AI 界面最缺的）

| 维度 | 做法 |
|---|---|
| 透明度 | 显示数据来源、推理依据、使用的工具/模型 |
| 可验证性 | 每个断言可点开核对（引用链） |
| 权限边界 | 明确 AI 能做什么不能做什么（读/写/执行） |
| 失败恢复 | 出错给出可理解的错误 + 下一步建议，不白屏 |
| 免责与边界 | 关键决策给"验证建议"（"请人工复核"） |
| 可中断 | 长任务随时可停、可改、可回退 |
| 人机交接 | AI 做不了时平滑交给用户，不假装能行 |

> 信任不是"看起来专业"，是**可验证 + 可控制 + 诚实**。

---

## 三、AI 功能的 UX 度量（设计是否有效）

| 指标 | 含义 | 警戒 |
|---|---|---|
| 采纳率 | AI 功能是否真的被用 | 低=入口/价值不清 |
| 建议采纳率 | 生成结果被接受比例 | 衡量输出质量 |
| 丢弃率 | 生成后放弃的比例 | 上升=设计警报（质量或展示问题） |
| 纠错率 | 用户需要改多少 | 高=上下文理解差 |
| 取消/重试率 | 中途放弃重来 | 高=流式体验或延迟问题 |

---

## 四、多智能体 / Agent 协作界面

- 角色可见：谁在做什么（"搜索→总结→起草"分步展示）
- 并行透明：多个子任务并行时显示进度条/阶段，不黑盒
- 结果合并：子结果合并后给出"综合结论 + 各自来源"
- 冲突处理：多个 agent 意见冲突时，向用户呈现分歧而非静默覆盖

---

## 五、AI 界面视觉注意点（视觉层）

- 流式输出区与用户输入区视觉区分明确
- "AI 在思考/工作"的动效克制（避免伪装速度，见 B73 感知性能）
- 引用/来源用小而清晰的内联锚点，不喧宾夺主
- 不确定性用中性色 + 措辞，不用夸张警告色
- 工具调用轨迹可折叠展开（默认收起，需要才看）
- 长响应给"跳到结论"或摘要，避免信息瀑布

---

## 六、Agent 高级模式（2026 前线）

### 6.1 五大核心模式（模式库共识，aiuxplayground 等 150+ 模式提炼）

1. **渐进能力披露**：不一次性亮所有能力；按任务逐步展示"我能做 X/Y/Z"，降认知负荷
2. **不确定性状态**：始终可见"AI 当前不确定什么"（置信度、可能出错点、备选方案）
3. **人类覆盖（Human Override）**：任何时刻用户可打断、接管、否决（键盘/语音/按钮三通道）
4. **一致输出结构**：同类型任务输出结构稳定可预期（用户学会"读"它）
5. **可解释性信号**：结论旁给依据（工具名、文件、引用序号），而非纯断言

### 6.2 安全失败与恢复（Safe Failure & Recovery）

- 风险随任务"爆炸半径"升高时，**从 agentic 自动降级为确定性流程**（分步确认式向导）
- **"I'm stuck" 状态**：Agent 卡住时明确说"我需要帮助"，触发人工接管 + 给出已完成的子任务与可恢复计划
- 恢复路径三件套：**冻结状态 → 解释卡在哪 → 提供选择**（重试/换方式/交给人）

### 6.3 记忆控制（Memory as UX）

- 记忆**可见、可解释、可编辑**："我记得这些"面板 → 每条可看来源、可删除、可关总开关
- 敏感数据（位置/健康/支付）写前**显式请求同意**（consent 模式），且允许"这次不要记住"
- 记忆变更即时反馈：AI 显示"我刚把 X 存进了长期记忆"，用户可撤

### 6.4 信任不对称与幻觉代价（数据）

- NN/g 实测：**一次幻觉 → 功能参与度两周内 -40%**；一次坏输出 > 十次好输出
- 结论：**宁可多展示"我不确定"，不可表演自信**；引用的质量（≤5 个来源、按相关性排序）比数量重要

### 6.5 可见规划与意图预览

- 执行前展示**计划步骤 + 将调用的工具 + 将触碰的文件**；步骤可编辑、可重排
- **意图预览**：点"发送"前先预览"AI 将执行什么"，长任务必备
- **权限门**：破坏性/花钱/不可逆操作必须显式确认，且每次独立确认（不记住"总是允许"）

### 6.6 流式即信任（2026 转变）

- 流式从"性能技巧"变成**信任机制**：进程可见 = 可预期 = 可信
- 实测原则：**30 秒可见的流式 > 5 秒隐藏的等待**；工具调用别埋在 debug 面板
- 引用面板规格：TTFT ≤ 800ms；Abort 100ms 内真正停止

### 6.7 自主度旋钮（Autonomy Dial）

| 档位 | 行为 | 适用 |
|---|---|---|
| Advisor（建议） | 只给建议不执行 | 高破坏风险场景 |
| Co-Pilot（副驾） | 执行但每步可中断 | 日常创作 |
| Autopilot（自驾） | 自主跑完、结果汇报 | 低风险批处理 |

### 6.8 共同错误（反面清单）

- 隐藏不确定性、假自信
- 把工具调用/推理埋进 debug 面板
- 引用不做质量把关（死链、海量来源）
- 把权限门做成"一次允许永远允许"
- 失败时白屏/空输出，不给恢复路径

---

## 检查清单（AI 界面生成前/后）

- [ ] 长任务有进度/阶段展示，非空白等待
- [ ] 引用可验证：结论 → 来源可点开
- [ ] 权限/能力边界对用户可见
- [ ] 出错有解释 + 恢复路径
- [ ] 可中断/可回退（Undo）
- [ ] 不确定性被诚实表达（不假自信）
- [ ] AI 工作态动效不伪装速度、有 reduced-motion 降级
- [ ] 采纳/丢弃率有埋点（见 T2）
- [ ] 人工复核建议放在高风险决策处
- [ ] 高风险任务有"stuck"状态 + 人工接管路径
- [ ] 记忆面板可见可编辑（如涉及）
- [ ] 意图预览：执行前展示计划/将触达的文件

---

## 参考资料（第二轮深挖）

- aiuxplayground.com：150+ AI UX 模式库（Streaming / Citations / Confidence / Human Review / Cost Signals / Memory）
- hatches + Agent UX Patterns 汇编：Safe Failure & Recovery（Pattern 10）、Memory Controls（Pattern 11）
- NN/g AI UX 研究：幻觉对参与度的影响（-40% / 两周）
- Generative UI Design Patterns for AI Apps（desinance）
- 结合本库：B50(视觉)、G6(场景 prompt)、B73(状态/感知性能)、T2(度量)、C7(无障碍)