# V5 · 遮罩、裁切与形状

> 遮罩和裁切是"高级构图"的底层技术。用好了能做出设计师级的视觉效果。

---

## 一、clip-path 裁切路径

### 基本形状
```css
clip-path: circle(50%);                     /* 圆形 */
clip-path: ellipse(40% 60%);                /* 椭圆 */
clip-path: inset(10% 20% 30% 40%);          /* 矩形内缩 */
clip-path: inset(0 round 16px);             /* 圆角矩形 */
clip-path: polygon(50% 0%, 0% 100%, 100% 100%); /* 三角形 */
```

### 经典多边形
```css
/* 斜切 */
clip-path: polygon(0 0, 100% 0, 100% 80%, 0 100%);

/* 箭头 */
clip-path: polygon(0 0, 80% 0, 100% 50%, 80% 100%, 0 100%);

/* 六边形 */
clip-path: polygon(25% 0%, 75% 0%, 100% 50%, 75% 100%, 25% 100%, 0% 50%);
```

- **感觉**:几何、锐利、现代
- **场景**:图片裁切、section 分隔、装饰形状
- **prompt 措辞**:"section 底部用斜切分隔(clip-path polygon)而非直线"

### SVG 路径裁切
```css
clip-path: url(#custom-shape);
```
可以做任何形状(曲线、有机形态)。

### 裁切动画 ★高级
```css
.reveal {
  clip-path: inset(0 100% 0 0);   /* 从右完全裁切 */
  transition: clip-path 600ms cubic-bezier(0.16, 1, 0.3, 1);
}
.reveal.visible {
  clip-path: inset(0 0 0 0);       /* 完全展开 */
}
```
- **感觉**:揭示、电影感、仪式感
- **场景**:图片入场、section 揭示、品牌动画

---

## 二、mask 遮罩

### 渐变遮罩（最常用）
```css
mask-image: linear-gradient(to bottom, black 60%, transparent);
```
- **感觉**:优雅渐隐
- **场景**:内容底部淡出、图片边缘渐隐、滚动暗示
- **prompt 措辞**:"列表底部渐变遮罩淡出,暗示下方还有内容"

### 图片遮罩
```css
mask-image: url('shape.svg');
mask-size: cover;
```
用任意图形作为遮罩形状。

### 文字遮罩(文字内显示图片)
```css
background-image: url('photo.jpg');
background-clip: text;
color: transparent;
```
见 V7 图片填充文字。

### 遮罩揭示动画(Mask Reveal)★
- **感觉**:精致、有设计感、电影
- **场景**:hero 图片入场、作品集展示
- **实现**:mask-position 或 mask-size 动画
```css
.image {
  mask-image: linear-gradient(to right, black, black);
  mask-size: 0% 100%;
  transition: mask-size 800ms;
}
.image.visible {
  mask-size: 100% 100%;
}
```

---

## 三、形状分隔器（Shape Divider）

section 之间用形状代替直线分隔:

### 类型
- **波浪 Wave** — 柔和、有机
- **斜切 Angled** — 现代、动感
- **曲线 Curve** — 优雅
- **锯齿 Zigzag** — 活泼
- **三角 Triangle** — 几何

### 实现
```css
.divider {
  position: absolute; bottom: 0; width: 100%;
  height: 60px;
  background: url("data:image/svg+xml,...");
}
```
或用 `clip-path` 直接在 section 上裁切。

- **prompt 措辞**:"section 之间用 SVG 波浪形分隔器过渡,颜色跟随下一 section 背景"
- **工具**:shapedivider.app 可视化生成

---

## 四、形状变形（Shape Morphing）

一个形状平滑过渡成另一个形状。

- **感觉**:流动、有机、高级
- **场景**:Loading、hover 变形、品牌动画
- **实现**:
  - CSS `clip-path` 在两个 polygon 之间 transition(顶点数必须一致)
  - SVG `<animate>` / GSAP MorphSVG
  - Flubber.js(自动匹配顶点)

---

## 五、布尔运算视觉（Boolean Operations）

- **联合 Union** — 形状合并
- **减去 Subtract** — 一个形状从另一个挖去
- **相交 Intersect** — 只保留重叠部分
- **排除 Exclude** — 去掉重叠部分

Web 上用 SVG path 或 clip-path 组合实现。

---

## 六、边缘羽化（Feathered Edge）

- **感觉**:柔和过渡,不硬切
- **实现**:mask-image 渐变,边缘从 opaque 到 transparent
```css
mask-image: linear-gradient(to right, transparent, black 10%, black 90%, transparent);
```
- **场景**:跑马灯两端渐隐、图片拼接

---

## 七、Prompt 模板

```
【遮罩与裁切】
- section 分隔:波浪形 SVG divider(不用直线)
- hero 图片入场:clip-path inset 从右揭示,600ms easeOutExpo
- 列表底部:渐变 mask 淡出,暗示可滚动
- 跑马灯两端:mask 渐变透明渐隐
- 作品集图片:hover 时 clip-path 从圆形展开到矩形
```

---

## 八、检验清单

- [ ] section 分隔不是只有直线(有形状变化)
- [ ] 有内容渐隐(列表/文章底部)暗示可滚动
- [ ] 裁切动画缓动用 easeOut(快→慢)
- [ ] SVG 路径裁切有降级(老浏览器看到方形)
- [ ] 形状变形顶点数匹配(否则不能 transition)
