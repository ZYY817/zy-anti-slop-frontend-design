# C4 · 无障碍

> 无障碍不是"额外工作"，是基本质量。全球 16% 人口有某种残障。
> 且无障碍界面对所有人都更好用。

---

## 一、WCAG 标准框架

### 四大原则（POUR）
- **可感知 (Perceivable)** — 信息能被感知（视觉/听觉/触觉）
- **可操作 (Operable)** — 界面能被操作（鼠标/键盘/语音/开关）
- **可理解 (Understandable)** — 信息和操作能被理解
- **健壮 (Robust)** — 能被各种辅助技术解析

### 等级
- **A** — 最低要求
- **AA** — 行业标准（法律通常要求这个）
- **AAA** — 最高标准（部分场景难达到）

### 法规
- **ADA** (美国) — 商业网站需 WCAG AA
- **Section 508** (美国政府)
- **EAA / EN 301 549** (欧盟) — 2025 年 6 月起强制
- **信息无障碍国标 GB/T 37668** (中国)

---

## 二、对比度

| 内容 | AA | AAA |
|---|---|---|
| 正文（<18pt） | 4.5:1 | 7:1 |
| 大字（≥18pt 或 14pt 粗） | 3:1 | 4.5:1 |
| 界面元素/图形 | 3:1 | — |
| 禁用元素 | 无要求 | — |
| Logo | 无要求 | — |

**工具**：
- Chrome DevTools > Elements > 拾色器自带对比度
- `contrast-ratio.com`
- Figma 插件 Stark / A11y

---

## 三、语义化 HTML

### 用对标签
```html
<!-- ✅ 正确 -->
<button onclick="...">提交</button>
<a href="/about">关于</a>
<nav><ul><li>...</li></ul></nav>
<main>...</main>
<h1>页面主标题</h1>

<!-- ❌ 错误 -->
<div onclick="...">提交</div>
<span class="link">关于</span>
<div class="nav">...</div>
```

### 地标（Landmarks）
```html
<header>       网站头部
<nav>          导航
<main>         主内容（每页仅一个）
<aside>        侧边内容
<article>      独立文章
<section>      章节（需配 heading）
<footer>       页脚
<search>       搜索区（新标签）
```

### 标题层级
- 每页一个 `<h1>`
- 不跳级（h1 → h2 → h3，不要 h1 → h3）
- 用于结构而非字号（字号用 CSS 控制）

---

## 四、ARIA 属性

### 常用
| 属性 | 用途 |
|---|---|
| `aria-label` | 无可见文字时提供名称 |
| `aria-labelledby` | 引用其他元素作为名称 |
| `aria-describedby` | 引用描述文字（如错误提示） |
| `aria-hidden="true"` | 对屏幕阅读器隐藏（装饰元素） |
| `aria-expanded` | 展开/折叠状态 |
| `aria-selected` | 选中状态（Tab） |
| `aria-checked` | 勾选状态（Checkbox） |
| `aria-disabled` | 禁用状态 |
| `aria-current="page"` | 当前页 |
| `aria-live="polite"` | 动态内容通知 |
| `aria-live="assertive"` | 紧急通知（打断） |
| `aria-busy` | 加载中 |
| `aria-invalid` | 输入无效 |
| `aria-required` | 必填 |
| `aria-modal="true"` | 模态框 |
| `aria-controls` | 控制哪个元素 |
| `role` | 角色（button/dialog/alert/tab...） |

### 第一原则
**能用语义化 HTML 就不要用 ARIA。** ARIA 是补充，不是替代。

```html
<!-- ❌ 不必要 -->
<div role="button" tabindex="0" aria-label="提交">提交</div>

<!-- ✅ 更好 -->
<button>提交</button>
```

---

## 五、键盘可访问

### 必须支持
| 键 | 行为 |
|---|---|
| `Tab` | 下一个可聚焦元素 |
| `Shift+Tab` | 上一个 |
| `Enter` | 激活按钮/链接 |
| `Space` | 激活按钮、勾选复选框、滚动 |
| `Escape` | 关闭 Modal/Dropdown |
| `方向键` | 在组内移动（Radio/Tab/Menu） |
| `Home/End` | 跳到首/尾 |

### Tab 顺序
- 遵循视觉顺序（DOM 顺序 = 视觉顺序）
- 不要用 `tabindex` 正数（打乱顺序）
- `tabindex="0"` 让非交互元素可聚焦
- `tabindex="-1"` 可编程聚焦但不在 Tab 序列

### Focus 管理
- **Focus Trap** — Modal 打开时焦点锁在内部
- **Focus Return** — Modal 关闭时焦点返回触发元素
- **Focus Visible** — 键盘操作时显示 focus ring
```css
:focus-visible {
  outline: 2px solid var(--focus-color);
  outline-offset: 2px;
}
```

### Skip Link
```html
<a href="#main" class="sr-only focus:not-sr-only">
  跳到主内容
</a>
```

---

## 六、屏幕阅读器

### 主流工具
- **VoiceOver** — macOS / iOS（内置）
- **NVDA** — Windows（免费）
- **JAWS** — Windows（商业，最普及）
- **TalkBack** — Android
- **Narrator** — Windows（内置）

### 视觉隐藏但可读
```css
.sr-only {
  position: absolute;
  width: 1px;
  height: 1px;
  padding: 0;
  margin: -1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
  border-width: 0;
}
```

### 图片替代文字
```html
<!-- 有意义的图片 -->
<img src="chart.png" alt="2025 年销售额增长 45%">

<!-- 装饰图片 -->
<img src="decoration.svg" alt="" aria-hidden="true">

<!-- 复杂图表 -->
<img src="chart.png" alt="销售趋势图" aria-describedby="chart-desc">
<div id="chart-desc" class="sr-only">详细数据描述...</div>
```

---

## 七、表单无障碍

```html
<!-- ✅ 完整无障碍表单字段 -->
<div>
  <label for="email">邮箱地址 <span aria-hidden="true">*</span></label>
  <input
    id="email"
    type="email"
    required
    aria-required="true"
    aria-invalid="true"
    aria-describedby="email-hint email-error"
  />
  <p id="email-hint">我们不会分享你的邮箱</p>
  <p id="email-error" role="alert">请输入有效的邮箱地址</p>
</div>
```

**要点**：
- `label` 必须与 `input` 关联（`for` + `id`）
- 错误提示用 `aria-describedby` 关联
- 错误提示用 `role="alert"` 立即播报
- 必填用 `required` + `aria-required`
- 不要只用 placeholder 当标签（focus 后消失）

---

## 八、触控与目标尺寸

| 标准 | 最小尺寸 |
|---|---|
| WCAG 2.1 AA | 24x24px |
| WCAG 2.2 AAA | 44x44px |
| Apple HIG | 44x44pt |
| Material Design | 48x48dp |

**实践**：图标可以小（16-20px），但点击区域要撑到 44px。
```css
.icon-button {
  width: 44px;
  height: 44px;
  display: grid;
  place-items: center;
}
```

---

## 九、色彩无障碍

### 不能只用颜色
```html
<!-- ❌ 只有颜色 -->
<span class="text-red-500">失败</span>

<!-- ✅ 颜色 + 图标 + 文字 -->
<span class="text-red-500">
  <XCircleIcon aria-hidden="true" /> 失败
</span>
```

### 色觉障碍类型
- **红色盲 (Protanopia)** — 1% 男性
- **绿色盲 (Deuteranopia)** — 最常见，6% 男性
- **蓝色盲 (Tritanopia)** — 极少
- **全色盲 (Achromatopsia)** — 极少

### 安全配色
- 用蓝-橙代替红-绿区分
- 加图案/纹理区分图表系列
- 加数据标签

---

## 十、动效无障碍

```css
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}
```

**要避免**：
- 大幅视差（引发前庭障碍/眩晕）
- 闪烁 >3 次/秒（诱发癫痫）
- 自动播放视频/轮播（无法暂停）

---

## 十一、缩放与响应

- **200% 缩放** 不破版（WCAG AA）
- **400% 缩放** 内容可用（WCAG AAA）
- 用相对单位（rem/em）而非 px 定义字号
- 不禁止用户缩放：
```html
<!-- ❌ 禁止缩放 -->
<meta name="viewport" content="width=device-width, user-scalable=no">

<!-- ✅ 允许缩放 -->
<meta name="viewport" content="width=device-width, initial-scale=1">
```

---

## 十二、动态内容通知

```html
<!-- 温和通知（等当前朗读完） -->
<div aria-live="polite" aria-atomic="true">
  已保存
</div>

<!-- 紧急通知（立即打断） -->
<div role="alert">
  网络连接失败
</div>

<!-- 加载状态 -->
<div aria-busy="true" aria-label="加载中">
  <Spinner />
</div>
```

---

## 十三、检测工具

### 自动化
- **axe DevTools** — Chrome 插件，最准
- **Lighthouse** — 内置 Accessibility 评分
- **WAVE** — 可视化标注问题
- **Pa11y** — CI 集成
- **eslint-plugin-jsx-a11y** — 代码层检查

### 手动
- 只用键盘走一遍完整流程
- 关掉 CSS 看内容顺序是否合理
- 用屏幕阅读器听一遍
- 200% 缩放检查
- 色盲模拟器检查

**注意**：自动化工具只能发现 30-40% 问题，手动测试必须做。

---

## 十四、常见错误

- ❌ `<div onclick>` 代替 `<button>`
- ❌ 只用 placeholder 当标签
- ❌ 图片无 alt 或 alt 无意义（"image1.png"）
- ❌ `outline: none` 去掉 focus ring
- ❌ 对比度不足（灰色文字）
- ❌ 只用颜色区分状态
- ❌ Modal 无 focus trap
- ❌ 无 skip link
- ❌ 标题层级跳级
- ❌ 无 `lang` 属性
- ❌ 图标按钮无 aria-label
- ❌ 自动播放无法停止

---

## 十五、Prompt 表达模板

```
【无障碍要求】：
- WCAG 2.2 AA 合规
- 语义化 HTML：header/nav/main/section/footer，标题层级不跳级
- 所有交互元素键盘可达（Tab / Enter / Space / Escape）
- focus-visible 样式：2px outline + 2px offset
- Modal 有 focus trap + Escape 关闭 + 焦点返回
- 表单：label 关联 input，错误用 aria-describedby + role="alert"
- 对比度：正文 ≥4.5:1，大字 ≥3:1，界面元素 ≥3:1
- 图标按钮有 aria-label；装饰图标 aria-hidden
- 触控目标 ≥44x44px
- 不只用颜色传达状态（配图标+文字）
- 支持 prefers-reduced-motion
- 页面有 skip-to-main link
- 200% 缩放不破版，用 rem 定义字号
```

---

## 检验清单（快速版）

- [ ] 只用键盘能完成所有操作
- [ ] focus ring 全程可见
- [ ] 所有图片有恰当 alt
- [ ] 所有输入有关联 label
- [ ] 对比度全部达标
- [ ] 状态不只靠颜色
- [ ] 语义化标签正确
- [ ] Modal 有 focus trap
- [ ] 支持 reduced-motion
- [ ] axe DevTools 零错误
