# B30 · 响应式设计完整系统

> 响应式不是"加几个断点让它不崩",是**同一内容在不同环境下重新组织**的设计学问。
> 从手表到超宽屏到折叠屏,全套维度。

---

## 一、响应式的四种范式

| 范式 | 定义 | 特点 |
|---|---|---|
| **Responsive 响应式** | 流体布局 + 断点适配 | 同一套代码自适应 |
| **Adaptive 自适应** | 为不同尺寸提供不同布局 | 每档独立设计 |
| **Fluid 流体** | 完全无断点,连续缩放 | clamp() / vw 驱动 |
| **Intrinsic 内在响应** | 元素根据自身内容和容器决定 | Container Queries |

**2026 主流做法**:Fluid(排版/间距) + Intrinsic(组件) + 少量断点(布局重组)

---

## 二、断点系统

### Tailwind(事实标准)
```
sm:  640px    大手机 / 手机横屏
md:  768px    平板竖屏
lg:  1024px   平板横屏 / 小笔记本
xl:  1280px   桌面
2xl: 1536px   大屏
```

### Material Design 3(按窗口类)
```
Compact:   <600px      手机
Medium:    600-840px   平板竖 / 折叠展开
Expanded:  840-1200px  平板横 / 小桌面
Large:     1200-1600px 桌面
Extra:     >1600px     大屏 / 电视
```

### 断点设计原则
1. **不为设备设断点,为内容设** — 布局"崩了"的地方才加断点
2. **断点越少越好** — 3-4 个通常够(768 / 1024 / 1280)
3. **移动优先** — 默认写移动样式,用 `min-width` 递进覆盖

```css
/* ✅ 移动优先 */
.card { padding: 16px; }
@media (min-width: 768px) { .card { padding: 24px; } }
@media (min-width: 1280px) { .card { padding: 32px; } }
```

---

## 三、流体排版(Fluid Typography)

### clamp() 三段式
```css
font-size: clamp(最小值, 首选值, 最大值);

/* 例:标题在 32px 到 72px 之间流体变化 */
h1 { font-size: clamp(2rem, 5vw + 1rem, 4.5rem); }
```

### 计算公式
```
首选值 = 斜率 × vw + 截距

斜率 = (最大字号 - 最小字号) / (最大视口 - 最小视口)
```

### Utopia 方法(业界推荐)
基于视口的模块化阶梯,自动生成整套流体字号+间距。
- 定义两端(320px 视口的字号阶梯 + 1440px 视口的字号阶梯)
- 中间自动插值
- 工具:utopia.fyi

### 流体间距
```css
padding: clamp(1rem, 4vw, 3rem);
gap: clamp(0.5rem, 2vw, 1.5rem);
```

**优势**:无断点跳变,任何尺寸都刚好。

---

## 四、容器查询(Container Queries)★ 2025+ 核心

### 问题
媒体查询看**视口**,但组件应该看**自己所在的容器**。
同一个卡片放在侧栏(窄)和主区(宽),应该有不同布局——媒体查询做不到。

### 解决
```css
/* 声明容器 */
.card-wrapper {
  container-type: inline-size;
  container-name: card;
}

/* 容器查询 */
@container card (min-width: 400px) {
  .card { display: flex; gap: 16px; }
}
@container card (max-width: 399px) {
  .card { display: block; }
}
```

### 容器查询单位
```
cqw   容器宽度 1%
cqh   容器高度 1%
cqi   容器 inline 尺寸 1%
cqb   容器 block 尺寸 1%
cqmin / cqmax
```

**意义**:真正的组件驱动响应式——**组件到哪都自适应,无需知道页面上下文**。

---

## 五、逻辑属性(Logical Properties)★ i18n 必备

物理属性(left/right)在 RTL 语言下会错。逻辑属性自动适配:

| 物理属性 | 逻辑属性 |
|---|---|
| `margin-left` | `margin-inline-start` |
| `margin-right` | `margin-inline-end` |
| `margin-top` | `margin-block-start` |
| `margin-bottom` | `margin-block-end` |
| `padding-left/right` | `padding-inline` |
| `padding-top/bottom` | `padding-block` |
| `width` | `inline-size` |
| `height` | `block-size` |
| `text-align: left` | `text-align: start` |
| `border-left` | `border-inline-start` |
| `left: 0; right: 0` | `inset-inline: 0` |
| `top/right/bottom/left` | `inset` |

**规则**:做多语言项目,**全部用逻辑属性**。

---

## 六、视口单位完整表

| 单位 | 含义 | 用途 |
|---|---|---|
| `vw` / `vh` | 视口宽/高 1% | 传统全屏 |
| `vmin` / `vmax` | 视口较小/较大边 1% | 正方形元素 |
| `svh` / `svw` | **Small** 视口(地址栏展开时) | 保守全屏 |
| `lvh` / `lvw` | **Large** 视口(地址栏收起时) | 最大全屏 |
| `dvh` / `dvw` | **Dynamic** 视口(实时变化) | **移动端全屏首选** |
| `svi` / `svb` | 逻辑方向版本 | i18n |

**关键**:移动端全屏用 `dvh` 而非 `vh`
```css
/* ❌ 移动端地址栏会遮挡 */
.hero { height: 100vh; }

/* ✅ 动态适配 */
.hero { height: 100dvh; }
```

---

## 七、响应式布局技巧(无媒体查询)

### 自动换行网格
```css
grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
```
装得下几列就几列,自动换行。

### auto-fit vs auto-fill
- `auto-fit` — 空列折叠,内容撑满
- `auto-fill` — 保留空列位置

### Flexbox 自动换行
```css
display: flex;
flex-wrap: wrap;
gap: 16px;
/* 子元素 */
flex: 1 1 280px;   /* 最小 280px,可伸缩 */
```

### 汉堡菜单式布局(RAM 技巧)
```css
/* Repeat, Auto, Minmax */
grid-template-columns: repeat(auto-fit, minmax(min(100%, 280px), 1fr));
```
`min(100%, 280px)` 保证小屏时不溢出。

### 圣杯布局(现代版)
```css
display: grid;
grid-template-columns: minmax(200px, 1fr) 3fr minmax(200px, 1fr);
/* 移动端 */
@media (max-width: 768px) {
  grid-template-columns: 1fr;
}
```

---

## 八、触控与指针适配

### 检测指针类型
```css
/* 精确指针(鼠标) */
@media (pointer: fine) {
  .button { padding: 8px 16px; }
}

/* 粗糙指针(手指) */
@media (pointer: coarse) {
  .button { padding: 12px 24px; min-height: 44px; }
}

/* 支持 hover 的设备 */
@media (hover: hover) {
  .card:hover { transform: translateY(-4px); }
}
```

**重要**:hover 效果必须包在 `@media (hover: hover)` 里,否则移动端 tap 后会"卡住 hover 态"。

### 触控目标尺寸
```
WCAG 2.1 AA:  24×24px 最低
WCAG 2.2 AAA: 44×44px
Apple HIG:    44×44pt
Material:     48×48dp
```

**实践**:图标可以 20px,但点击区域撑到 44px。

### 拇指区(Thumb Zone)
手机单手操作时:
```
┌─────────────┐
│  难以触及   │  ← 顶部:放不常用/展示信息
│             │
│   舒适区    │  ← 中部:主要内容
│             │
│  最易触及   │  ← 底部:主要操作/导航
└─────────────┘
```
**主 CTA 和导航放底部**(移动端)。

---

## 九、特殊设备适配

### 安全区(Safe Area)— 刘海屏/圆角屏
```css
padding-top: env(safe-area-inset-top);
padding-bottom: env(safe-area-inset-bottom);
padding-left: env(safe-area-inset-left);
padding-right: env(safe-area-inset-right);

/* 常见用法:底部固定栏 */
.bottom-bar {
  padding-bottom: max(16px, env(safe-area-inset-bottom));
}
```
需配合 viewport meta:
```html
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">
```

### 折叠屏(Foldable)
```css
/* 检测折叠 */
@media (horizontal-viewport-segments: 2) {
  /* 双屏横向布局 */
  .layout { display: grid; grid-template-columns: 1fr 1fr; }
}
@media (vertical-viewport-segments: 2) {
  /* 双屏纵向 */
}
```
CSS 环境变量:`env(viewport-segment-width 0 0)` 等。

### 超宽屏(Ultra-wide)
```css
/* 内容不要铺满 3440px */
.container {
  max-width: 1440px;
  margin-inline: auto;
}

/* 或多列利用空间 */
@media (min-width: 1920px) {
  .content { columns: 2; column-gap: 48px; }
}
```

### 手表(Wearable)
- 极小屏(<300px)
- 只显示单一核心信息
- 大字号 + 高对比
- 触控目标极大

### 电视(TV / 10-foot UI)
- 观看距离 3m,字号要**2-3 倍**
- 焦点导航(遥控器方向键),focus 态必须极明显
- 安全边距(overscan)5% 内不放内容
- 不依赖 hover

### 键盘顶起(移动端)
```css
/* 输入框被键盘遮挡的处理 */
.form {
  height: 100dvh;   /* 动态视口自动收缩 */
}
```
或用 `VisualViewport API` 监听键盘。

---

## 十、响应式图片

### srcset + sizes
```html
<img
  src="photo-800.jpg"
  srcset="photo-400.jpg 400w,
          photo-800.jpg 800w,
          photo-1600.jpg 1600w"
  sizes="(max-width: 768px) 100vw,
         (max-width: 1280px) 50vw,
         33vw"
  alt="描述"
  loading="lazy"
  decoding="async"
/>
```

### picture(艺术裁剪)
```html
<picture>
  <source media="(max-width: 768px)" srcset="mobile-crop.jpg">
  <source media="(min-width: 769px)" srcset="desktop-wide.jpg">
  <img src="fallback.jpg" alt="描述">
</picture>
```
**不只是尺寸变化,是不同裁剪**(手机竖版,桌面横版)。

### 格式渐进
```html
<picture>
  <source type="image/avif" srcset="photo.avif">
  <source type="image/webp" srcset="photo.webp">
  <img src="photo.jpg" alt="描述">
</picture>
```

---

## 十一、响应式的设计决策(不只是技术)

### 移动端不是"缩小版"
| 元素 | 桌面 | 移动 |
|---|---|---|
| 导航 | 横向菜单 | 底部 tab / 汉堡 |
| 卡片 | 3-4 列 | 1 列 |
| 表格 | 完整表格 | 卡片列表 / 横向滚动 |
| 侧栏筛选 | 常驻侧栏 | 底部弹层 |
| 多步表单 | 一屏多字段 | 一屏一字段 |
| Modal | 居中弹窗 | 全屏 / 底部 sheet |
| Tooltip | hover 显示 | tap 显示或改为内联文字 |
| 数据图表 | 完整图例 | 简化 + 可展开 |

### 内容优先级重排
桌面并列的内容,移动端要决定谁在上:
```css
/* 桌面:图左文右 */
.section { display: grid; grid-template-columns: 1fr 1fr; }

/* 移动:文在上图在下(内容优先) */
@media (max-width: 768px) {
  .section { grid-template-columns: 1fr; }
  .text { order: 1; }
  .image { order: 2; }
}
```

### 交互降级
- hover → tap 或长按
- 拖拽 → 按钮操作
- 右键菜单 → 长按菜单 / 更多按钮
- 复杂手势 → 明确按钮

---

## 十二、测试矩阵

必测尺寸:
```
320px   最小手机(iPhone SE)
375px   标准手机
390px   iPhone 14/15
414px   大手机
768px   iPad 竖
1024px  iPad 横
1280px  笔记本
1440px  桌面
1920px  大屏
2560px+ 超宽
```

必测场景:
- [ ] 横屏(landscape)
- [ ] 键盘弹出时
- [ ] 200% 缩放
- [ ] 深色模式 × 各尺寸
- [ ] 超长文本(名字 50 字)
- [ ] 折叠屏展开/折叠
- [ ] 刘海屏安全区

---

## 十三、Prompt 模板

```
【响应式系统】

范式:Fluid 排版 + Intrinsic 组件 + 少量布局断点

断点:768 / 1024 / 1280(移动优先,min-width 递进)

流体:
- 字号:clamp(1rem, 2vw + 0.5rem, 1.25rem)
- 标题:clamp(2rem, 5vw + 1rem, 4.5rem)
- 间距:clamp(1rem, 4vw, 3rem)
- section padding:clamp(48px, 8vw, 128px)

容器查询:
- 卡片组件用 container-type: inline-size
- 组件根据容器宽度切换布局,不依赖视口

逻辑属性:
- 全部使用 margin-inline / padding-block / inset-inline
- text-align: start 而非 left(为 RTL 准备)

视口单位:
- 移动端全屏用 100dvh(不用 vh)
- 全屏 hero:min-height: 100dvh

触控:
- hover 效果包在 @media (hover: hover)
- 触控目标 ≥44×44px
- 移动端主 CTA 放底部(拇指区)

特殊设备:
- 安全区:padding-bottom: max(16px, env(safe-area-inset-bottom))
- viewport-fit=cover
- 超宽屏 max-width 1440px 居中

布局重组(不是缩小):
- 导航:桌面横向 → 移动底部 tab
- 卡片:桌面 3 列 → 移动 1 列
- 表格:桌面完整 → 移动卡片列表
- 筛选:桌面侧栏 → 移动底部 sheet
- Modal:桌面居中 → 移动全屏

图片:
- srcset + sizes 响应式
- picture 做艺术裁剪(手机竖版/桌面横版)
- AVIF → WebP → JPG 渐进降级
- loading="lazy"(首屏外)

必测:320 / 375 / 768 / 1024 / 1280 / 1920 + 横屏 + 键盘 + 200%缩放
```

---

## 十四、检验清单

- [ ] 移动优先(min-width 递进)
- [ ] 断点 ≤4 个
- [ ] 字号/间距用 clamp() 流体
- [ ] 组件用容器查询而非媒体查询
- [ ] 全部逻辑属性(为 i18n)
- [ ] 移动全屏用 dvh
- [ ] hover 包在 @media (hover: hover)
- [ ] 触控目标 ≥44px
- [ ] 安全区适配(env safe-area)
- [ ] 超宽屏有 max-width
- [ ] 移动端是重排不是缩小
- [ ] 图片 srcset + picture 艺术裁剪
- [ ] 320px 不横向滚动
- [ ] 200% 缩放不破版
- [ ] 键盘弹出不遮挡输入框

---

## 十五、v2 · 响应式艺术指导

响应式设计不是把桌面页面压小，而是重新定义每个视口的内容优先级、视觉焦点和操作路径。

### 每个关键区域输出重组表

| 区域 | Desktop | Tablet | Mobile | 不变的核心 |
|---|---|---|---|---|
| Navigation | 横向主导航 | 精简导航 | Bottom Bar / Sheet | 核心入口顺序 |
| Hero | 文字 + 媒体并置 | 上下分区 | 文字先于媒体 | 核心价值 |
| Data | 多列表格 | 减少列 | 摘要卡片 + 展开详情 | 比较关系 |
| Sidebar | 固定侧栏 | 可收缩 | Bottom Sheet | 筛选能力 |
| CTA | 右侧或顶部 | 浮动/下移 | 拇指区固定 | 主操作 |

### 响应式视觉规则

- 每个视口都重新确认主焦点，不默认保留桌面构图。
- 媒体可以换裁切、换顺序、换比例或被静态替代。
- 高密度内容优先保留任务和状态，删除装饰而不是删除关键数据。
- 长标题、中文、英文、RTL 和超长标签必须参与断点测试。
- 移动端动效应减少位移、层数和并行动作，不只降低时长。

### v2 Prompt

```text
请输出 Desktop / Tablet / Mobile 三份界面重组方案。
不要只写“3 列变 1 列”。明确每个区域的内容顺序、视觉焦点、媒体裁切、组件替代、
导航变化、CTA 位置、密度变化和交互降级。说明哪些内容永远不能被隐藏或删除。
```
