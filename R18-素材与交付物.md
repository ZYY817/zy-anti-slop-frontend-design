# R18 · 素材与交付物（提示词库 R 组）

> **定位**：前面各组讲"生成出来的视觉"，这一组讲"素材来的视觉"，以及"作品最后被别人看到的那一眼"。
> **覆盖层**：L3 肉 + L5 髓（L 层级沿用提示词库的分层：L1 形 / L2 气 / L3 骨 / L5 髓）。
> **条目卡格式**：每条为 `**中文名（English Term）** · L层级 · 档位（基础/进阶/电影级） · 成本:低/中/高`，下挂「视觉收益 / 参考做法·参考值 / 同族拓展 / 提示词片段」。提示词片段可直接粘进生成 prompt。
> **关键判断一**：**任何外来素材先过 `R18·R8` 的统一处理链**——一张色调不对的照片能毁掉整页的调色，这一步的收益远高于给它单独调色。
> **关键判断二**：**3D 资产先过 `R18·R5`，贴图占 80% 体积**——PNG 贴图是最大的浪费，纹理压缩是 3D 瘦身的第一优先级。
> **来源**：融合自外部「视觉提示词库 R 组：素材与交付物」（v1.0，2026-08-16）。原库入口为其「总索引与用法」（对应本库 `R0`）。原库的组内交叉引用（A/B/C…R 组）已改写为本库 R 编号，避免与本库 A/B/C/D 板块含义冲突。
> **与反 AI 味清单的关系**：本文参数与 `B82`（反 AI 味）、`B44`（视觉禁忌）并不冲突；那两份是**参考清单，不是硬性铁律，按项目自行取舍**。风格谱系见 `B16`、`E1`，全维度清单见 `B15`；素材版权与商用许可见 `F6`。

---

## 使用说明

这一组补的是全库最容易被跳过、却每次都躲不开的两件事：

1. **素材层**：只要页面里有一张照片、一段视频、一个 3D 模型，它就是画面的一部分。素材没统一，前面各组的精修全部作废——**一张色调不对的照片能毁掉整页的调色**。
2. **交付层**：别人看到作品的第一眼，往往不是网站本身，而是**社交卡片 / README 首图 / 一段录屏**。这一层做不好，前面全部白做——对**作品集与个人展示类页面**尤其致命。

三条铁律：

1. **素材必须被"收编"**——所有外来图片/视频都要经过统一的色调、裁切、格式处理，不能原样放进去
2. **图上放字必须有保障层**——不能靠"这张图这里正好比较暗"
3. **交付物是作品的一部分**——OG 图、README 首图、录屏和网站本体同等重要

```
R18·R1 图片作为视觉素材        R18·R5 3D 资产流水线
R18·R2 图文叠压与可读性        R18·R6 截图与录屏规格
R18·R3 视频与动态素材          R18·R7 分享卡与 README 呈现
R18·R4 图像格式与加载          R18·R8 素材一致性与母带
```

> `R18·R8` 是这一组的收口，**做完所有素材处理必须回来过一遍 `R18·R8`**。

---

## R18·R1 图片作为视觉素材（Image as Material）

- **裁切即构图（Crop as Composition）** · L1 · 基础 · 成本:低
  - 视觉收益：`object-fit: cover` 只解决"不变形"，不解决"裁得对"。裁切点决定这张图好不好看
  - 关键：`object-position` 必须手调，默认 `center` 会切掉主体
  - 同族拓展：`object-fit: cover/contain`、`object-position`、`aspect-ratio` 锁比例、人像上移（`object-position: 50% 30%`）、见 `R1·G2` 构图与视线引导
  - 提示词片段：`图片一律 aspect-ratio 锁比例 + object-fit:cover；object-position 按主体手调（人像用 50% 30%，不用默认 center）；同一列表内所有图用同一比例`

- **比例体系（Aspect Ratio System）** · L1 · 基础 · 成本:低
  - 视觉收益：全站图片比例收敛到 2–3 种，页面立刻显得被设计过
  - 参考值：主视觉 `16/9` 或 `21/9`｜卡片 `4/3` 或 `3/2`｜头像 `1/1`｜竖版 `4/5`
  - 同族拓展：见 `R1·G7` 设计令牌（比例也是令牌）、`R1·G5` 形状语言
  - 提示词片段：`全站图片比例只用 3 种：主视觉 16/9、卡片 3/2、头像 1/1；禁止出现第四种`

- **焦点与安全区（Focal Point & Safe Area）** · L1 · 进阶 · 成本:低
  - 视觉收益：同一张图在不同断点被裁成不同尺寸时，主体不能被切掉
  - 同族拓展：艺术指导（`<picture>` + 不同断点给不同裁切）、焦点坐标存进数据、`object-position` 响应式覆盖
  - 提示词片段：`每张图记录焦点坐标；窄屏时用 <picture> 换成竖版裁切而非等比压扁；主体永远在安全区内`

- **图片的边界处理（Image Framing）** · L3 · 进阶 · 成本:低
  - 视觉收益：图片直接贴在背景上很"贴图感"，需要边界语言
  - 参考做法：1px 半透明内描边（`inset 0 0 0 1px rgba(255,255,255,.08)`）｜圆角与容器同心（见 `R1·G5`）｜底部渐隐融入背景｜浅色投影
  - 同族拓展：见 `R9·H6` 边框与 1px 的讲究、`R9·H5` 遮罩与裁切、`mask-image` 渐隐
  - 提示词片段：`图片加 1px 半透明内描边 + 与容器同心的圆角；深色底上的图片底部用 mask-image 渐隐 20% 融入背景，不要硬边`

- **占位与加载态（Placeholder）** · L5 · 基础 · 成本:低
  - 视觉收益：图片加载前的空洞是最廉价的观感，占位必须与最终图同色调
  - 参考做法：主色块占位（从图中提取主色）｜LQIP / blurhash（20px 缩略图放大模糊）｜`aspect-ratio` 预留高度防跳动
  - 同族拓展：见 `R11·F6` 渐进式渲染、`R17·Q1` 加载态即体验、`decoding="async"`
  - 提示词片段：`每张图预生成主色 + 20px 模糊占位（LQIP），加载完成时 300ms 交叉淡入；容器用 aspect-ratio 预留高度，禁止加载后跳动`

- **图片的降级与缺失（Missing Image）** · L5 · 基础 · 成本:低
  - 视觉收益：图挂了不能露出浏览器的破图图标
  - 同族拓展：`onerror` 换占位、`alt` 文案设计（见 `R16·P6`）、见 `R17·Q3` 空态与引导
  - 提示词片段：`图片加载失败时替换为设计过的占位块（主色 + 图标 + 说明文字），不允许出现浏览器默认破图`

---

## R18·R2 图文叠压与可读性（Text over Image）

**这一节是硬性要求，不是审美偏好。** 图上放字失败是作品里最常见的低级错误。

- **保障层而非侥幸（Scrim / Overlay）** · L1 · 基础 · 成本:低
  - 视觉收益：不能依赖"这张图这块正好比较暗"，必须加一层可控的保障
  - 参考做法：渐变蒙版（从文字侧起 `rgba(0,0,0,.55)` → 透明）优于整幅压暗｜局部 `backdrop-filter: blur(8px) brightness(.7)`｜文字块自带极淡底
  - ❌ 整幅 `opacity: .5` 压暗（图片也废了）｜✅ 只在文字覆盖区做梯度蒙版
  - 同族拓展：见 `R9·H2` 混合模式、`R9·H5` 遮罩、`R2·K3` 对比度算法
  - 提示词片段：`图上文字必须有保障层：文字侧加线性渐变蒙版 rgba(0,0,0,.55)→透明（覆盖文字区 +20%）；禁止整幅压暗；蒙版方向跟随文字位置`

- **对比度实测（Measured Contrast）** · L5 · 基础 · 成本:低
  - 视觉收益：图片是变化的背景，对比度必须按**最亮的那块**算
  - 参考值：正文 APCA Lc ≥ 75｜大字 ≥ 60（见 `R2·K3`）｜取文字区域最亮像素做基准
  - 提示词片段：`图上文字的对比度按文字区域内最亮像素计算，APCA Lc ≥ 60（大字）/ ≥75（正文）；不达标就加深蒙版，不要改文字颜色`

- **文字描边的正确做法（Text Legibility）** · L5 · 进阶 · 成本:低
  - 视觉收益：`text-shadow` 生硬且廉价，有更好的手段
  - 参考顺序：① 加蒙版（首选）② 加极淡的多层柔和阴影（`0 1px 2px rgba(0,0,0,.4), 0 2px 8px rgba(0,0,0,.25)`）③ 加 `paint-order: stroke` 的细描边（仅 SVG/大标题）
  - ❌ `text-shadow: 2px 2px 0 #000`（PPT 感）
  - 提示词片段：`图上文字优先靠蒙版保障；必要时用两层柔和阴影（0 1px 2px .4 / 0 2px 8px .25），禁止硬边 text-shadow 与 1px 黑描边`

- **图文分区而非叠压（Separation）** · L1 · 基础 · 成本:低
  - 视觉收益：**最省事的方案是不叠**——文字放在图外的留白区，观感往往更高级
  - 同族拓展：见 `R1·G3` 负空间、图文并置（split layout）、图片作为背景但文字在独立卡片内
  - 提示词片段：`优先让文字与图片分区（左图右文或图下留白），只在主视觉这一处使用叠压`

---

## R18·R3 视频与动态素材（Video & Motion Assets）

- **背景视频规范（Background Video）** · L3 · 进阶 · 成本:中
  - 视觉收益：一段对的循环视频比任何静图都有生命力，但违规使用会直接毁掉性能与体验
  - 硬性要求：`muted playsinline loop` 三件套（否则移动端不自动播）｜`poster` 必填（首帧前显示）｜文件 ≤ 3MB｜时长 3–8s 无缝循环｜`preload="metadata"`
  - 同族拓展：`prefers-reduced-motion` 时改用 poster 静图（见 `R11·F5`）、窄屏不加载视频、`IntersectionObserver` 离屏暂停
  - 提示词片段：`背景视频必须 muted playsinline loop + poster；≤3MB、3-8s 无缝循环；prefers-reduced-motion 或窄屏时只显示 poster 不加载视频；离屏自动暂停`

- **视频作为纹理（Video Texture）** · L3 · 电影级 · 成本:中
  - 视觉收益：把视频喂进 shader/canvas，可以做扭曲、置换、色彩重映射——比直接播放高级得多
  - 同族拓展：`THREE.VideoTexture`、`drawImage(video)` 到 canvas、视频作为置换贴图、视频作为遮罩（亮度当 alpha）
  - 提示词片段：`视频作为 VideoTexture 输入 shader，做轻微域变形与色彩重映射后再上屏，不直接原样播放；needsUpdate 跟随 requestVideoFrameCallback`

- **视频当遮罩与转场（Video as Mask）** · L3 · 电影级 · 成本:中
  - 视觉收益：用视频的亮度通道做遮罩，可做出"液体擦除""墨迹转场"这类高成本效果
  - 同族拓展：亮度键（luma key）、`mask` + `webkitMaskImage`、alpha 通道视频（WebM VP9 / HEVC）、见 `R11·F3` 转场与形变
  - 提示词片段：`转场用一段墨迹/液体视频的亮度做遮罩驱动，而非 CSS 曲线；提供 alpha 通道版本（WebM VP9）作首选`

- **GIF 的替代（Never GIF）** · L5 · 基础 · 成本:低
  - 视觉收益：GIF 体积是同画质 MP4 的 5–10 倍且只有 256 色，网页内一律不用
  - 参考做法：改用 `<video autoplay muted loop playsinline>`（体积降 90%）｜或 APNG/WebP 动图（小图标类）｜GIF 只在**不支持视频的场景**（如 GitHub README 的部分位置）保留
  - 提示词片段：`网页内禁止使用 GIF，改用 muted loop 的 MP4/WebM；仅 README 等不支持视频的位置可用 GIF，且限制在 5MB 内`

- **循环的无缝性（Seamless Loop）** · L5 · 进阶 · 成本:低
  - 视觉收益：循环接缝的跳帧是最容易被看出的廉价点
  - 同族拓展：首尾同帧、交叉淡化两个错峰播放的实例、镜像回放（ping-pong，仅适合流体类）
  - 提示词片段：`循环素材首尾必须同帧；做不到时用两个实例交叉淡化（重叠 0.6s）掩盖接缝`

---

## R18·R4 图像格式与加载（Formats & Delivery）

- **格式决策（Format Decision）** · L5 · 基础 · 成本:低
  - 视觉收益：同画质下体积差 2–5 倍，直接影响首屏观感
  - 参考决策：
    - 照片/复杂图 → **AVIF**（最小，压缩强）→ 回退 **WebP** → 回退 JPEG
    - 需要透明 → WebP / AVIF（PNG 只作最终回退）
    - 图标/线条/logo → **SVG**（并做 SVGO 压缩）
    - 动图 → MP4/WebM（不是 GIF，见 `R18·R3`）
    - 3D 贴图 → KTX2 / Basis（见 `R18·R5`，不是 PNG）
  - 同族拓展：`<picture>` + `type="image/avif"` 多源、`srcset` + `sizes`、CDN 按 `Accept` 头自动协商
  - 提示词片段：`照片用 <picture> 提供 AVIF → WebP → JPEG 三层；图标一律 SVG 并 SVGO 压缩；动图用 MP4 不用 GIF`

- **尺寸与 DPR（Responsive Images）** · L5 · 基础 · 成本:低
  - 视觉收益：给手机送 3000px 的图既慢又没意义；给 2x 屏送 1x 的图会糊
  - 参考值：生成 `1x / 2x` 两档即可（3x 收益极低体积翻倍）｜最大边不超过实际显示宽度 × 2
  - 同族拓展：`srcset`/`sizes`、`image-set()`（CSS 背景）、`devicePixelRatio` 上限截断为 2（与 `R6·D8` 的 DPR 策略一致）
  - 提示词片段：`每张图出 1x/2x 两档 srcset 并写准 sizes；不生成 3x；DPR 计算上限截断为 2`

- **加载优先级（Loading Priority）** · L5 · 基础 · 成本:低
  - 视觉收益：首屏主视觉必须最先到，首屏外的图不许抢带宽
  - 参考做法：首屏主图 `fetchpriority="high"` + `preload`，**不要** `loading="lazy"`｜首屏外全部 `loading="lazy" decoding="async"`
  - ❌ 给首屏 LCP 图加 `lazy`（最常见的性能自伤）
  - 同族拓展：见 `R11·F6` 资源优先级、`link rel=preload as=image`、`content-visibility`（见 `R11·F6`）
  - 提示词片段：`首屏 LCP 图用 fetchpriority=high + preload 且不加 lazy；首屏外图片全部 loading=lazy decoding=async`

- **体积预算（Asset Budget）** · L5 · 进阶 · 成本:低
  - 视觉收益：预算写在前面，才不会到最后砍不动
  - 参考值：单张主视觉 ≤ 300KB｜列表缩略图 ≤ 60KB｜首屏图片总量 ≤ 800KB｜字体总量 ≤ 200KB（见 `R10·I1`）｜背景视频 ≤ 3MB
  - 提示词片段：`写明体积预算：主视觉 ≤300KB、缩略图 ≤60KB、首屏图片合计 ≤800KB、视频 ≤3MB；超预算先降尺寸再降质量`

- **SVG 的正确使用（SVG Hygiene）** · L5 · 进阶 · 成本:低
  - 视觉收益：SVG 常被塞进未清理的编辑器垃圾，且描边会随缩放变粗
  - 同族拓展：SVGO 清理、`vector-effect: non-scaling-stroke`、`currentColor` 让图标继承文字色、内联 vs sprite vs `<img>` 的取舍、`viewBox` 必填
  - 提示词片段：`SVG 经 SVGO 清理；描边图标用 currentColor 继承文字色 + vector-effect:non-scaling-stroke；图标集用 sprite 或内联，不用逐个 <img>`

---

## R18·R5 3D 资产流水线（3D Asset Pipeline）

**`R13·M8` 只给了一句预算（KTX2 + draco + instancing），这一节把整条链路展开。** 没有这一节，3D 场景大概率卡在"模型 20MB 加载 30 秒"。

- **glTF 作为唯一格式（glTF/GLB Only）** · L1 · 基础 · 成本:低
  - 视觉收益：glTF 2.0 是 Web 3D 的事实标准，其他格式一律转过来
  - 参考做法：交付 `.glb`（单文件二进制）而非 `.gltf` + 一堆散文件｜Y-up、米为单位、原点在物体底部中心｜命名有意义（不是 `Mesh_001`）
  - 同族拓展：`GLTFLoader`、Blender 导出设置、`gltf-transform` CLI、`gltfpack`
  - 提示词片段：`3D 资产一律 .glb（glTF 2.0），Y-up、单位米、原点在底部中心；用 gltf-transform 做后处理流水线`

- **几何压缩（Draco / Meshopt）** · L1 · 进阶 · 成本:低
  - 视觉收益：几何体积降 70–90%，是最立竿见影的一步
  - 参考取舍：**Draco** 压缩率更高但解码慢且需 WASM 解码器｜**Meshopt** 解码极快、体积略大，**交互式场景优先 Meshopt**｜量化位数 `position 14 / normal 10 / uv 12` 是安全起点
  - ❌ 对已经很低模的物体做 Draco（收益小、解码开销白付）
  - 同族拓展：`DRACOLoader`、`MeshoptDecoder`、`simplify`（减面）、法线重算
  - 提示词片段：`几何用 meshopt 压缩（交互场景优先，解码快）；量化 position 14/normal 10/uv 12；超 100k 面先 simplify 减面再压缩`

- **纹理压缩（KTX2 / Basis）** · L3 · 进阶 · 成本:中
  - 视觉收益：**纹理通常占 3D 资产体积的 80%**，PNG 贴图是最大的浪费；KTX2 还能常驻 GPU 压缩格式，显存也省
  - 参考做法：`toktx` / `gltf-transform etc1s|uastc`｜颜色贴图用 **ETC1S**（体积极小）｜法线/粗糙度用 **UASTC**（质量优先）｜贴图尺寸 2 的幂且 ≤ 2048（移动端 ≤ 1024）
  - 同族拓展：`KTX2Loader` + transcoder、mipmap 自动生成、贴图通道打包（AO/Rough/Metal 塞进 RGB 三通道）
  - 提示词片段：`所有贴图转 KTX2：颜色贴图 ETC1S、法线与粗糙度 UASTC；尺寸 2 的幂 ≤2048（移动端 ≤1024）；AO/Roughness/Metallic 打包进单张 RGB 三通道`

- **烘焙（Baking AO & Lightmaps）** · L2 · 电影级 · 成本:中
  - 视觉收益：**静态场景最高性价比的一步**——把间接光与接触阴影烘进贴图，实时只需一个方向光，观感却接近离线渲染
  - 参考做法：AO 贴图（烘接触阴影）｜光照贴图（烘全局光，需第二套 UV `uv2`）｜阴影贴图烘进地面纹理
  - 关键约束：只适用于**不动的物体与不动的光**；动态物体仍需实时阴影
  - 同族拓展：Blender Cycles 烘焙、`aoMap` + `aoMapIntensity`、`lightMap` + `lightMapIntensity`、见 `R5·C2` 阴影与遮蔽
  - 提示词片段：`静态场景烘 AO 贴图与光照贴图（需 uv2 通道），实时只保留 1 个方向光；aoMapIntensity 0.8-1.0、lightMapIntensity 1.0；动态物体单独用实时软阴影`

- **Matcap 与假光照（Matcap / Fake Lighting）** · L2 · 进阶 · 成本:低
  - 视觉收益：**低成本出高级感的头号手段**——一张球面预渲染贴图直接给出金属/陶瓷/玉石质感，零光照计算
  - 适用：产品展示、图标级 3D、风格化物件（不适用需要真实环境反射的场景）
  - 同族拓展：`MeshMatcapMaterial`、matcap + 描边（见 `R7·E1` NPR）、多 matcap 按法线混合、gradient map（卡通分层）
  - 提示词片段：`风格化物件用 MeshMatcapMaterial + 一张高质量 matcap，不做实时光照；配合 1px 外描边形成图标级质感`

- **HDRI 环境贴图的选择（HDRI Selection）** · L4 · 进阶 · 成本:低
  - 视觉收益：**PBR 材质的观感 80% 由环境贴图决定**，选错 HDRI 比调材质参数影响大得多
  - 选择标准：要有**明确的大面积亮源**（窗/天光）才有漂亮高光形状｜室内 studio 类适合产品｜避免默认的"蓝天白云"（属于反 AI 味的参考禁忌，见 `B82`、`B44`，是参考清单不是硬性铁律，按项目自行取舍）｜分辨率 1k–2k 足够（只作反射）
  - 参考做法：`PMREMGenerator` 预过滤｜`.hdr` 转 `.exr`/KTX2 压缩｜只作 `environment` 不作 `background`（背景另做）
  - 同族拓展：见 `R5·C1` 图像照明（IBL）、程序化渐变环境（无 HDRI 时的替代）、`environmentIntensity`、`envMapRotation` 手调高光位置
  - 提示词片段：`HDRI 选有明确大面积亮源的室内 studio 类，1k-2k 分辨率，经 PMREMGenerator 预过滤；只作 environment 不作 background；旋转环境贴图把主高光调到造型最好看的位置；禁用默认蓝天白云`

- **实例化与合并（Instancing & Merging）** · L1 · 进阶 · 成本:中
  - 视觉收益：draw call 是 3D 性能的第一瓶颈，重复物体必须实例化
  - 参考值：同一模型出现 > 10 次 → `InstancedMesh`｜大量静态小物件 → 合并为单个几何体｜见 `R13·M8` LOD
  - 同族拓展：`InstancedMesh`、`BatchedMesh`、`mergeGeometries`、实例属性（每实例颜色/缩放）
  - 提示词片段：`同一模型出现 >10 次一律 InstancedMesh 并给每实例随机颜色/缩放；静态小物件 mergeGeometries 合并；目标 draw call <150`

- **加载编排（Loading Orchestration）** · L5 · 进阶 · 成本:低
  - 视觉收益：3D 资产大，加载过程必须被设计，不能白屏等待
  - 参考做法：先加载低模/低清贴图上屏 → 再替换高清｜`LoadingManager` 真实进度（不是假进度条）｜首屏只加载视野内资产
  - 同族拓展：见 `R17·Q1` 加载态即体验、`R11·F6` 渐进式渲染、`DRACOLoader` worker 池、资产分包按场景
  - 提示词片段：`3D 资产分两级：先上低模 + 512 贴图让画面立刻成立，再后台替换高清；用 LoadingManager 真实进度驱动加载动画，禁止假进度条`

---

## R18·R6 截图与录屏规格（Capture Specs）

**这一节写的是交付物成品的采集规格。** 规格写死了，AI 也能帮你生成对应的截图接口（见 `R17·Q6`）。

- **截图规格（Screenshot Specs）** · L5 · 基础 · 成本:低
  - 参考值：
    - 视口 **1440×900**（最通用的作品展示尺寸）｜宽幅用 1920×1080
    - DPR **2**（导出 2880×1800，缩放后才锐）
    - **不带浏览器框**（带框显得像教程截图）；需要"网页感"时用统一的极简 mock 框
    - 深浅两套各出一张
  - 同族拓展：`R17·Q6` 的 `?shot=1` 截图模式（隐藏光标/滚动条/调试 UI）、Playwright `page.screenshot({ deviceScaleFactor: 2 })`
  - 提示词片段：`提供 ?shot=1 截图模式：隐藏自定义光标、滚动条、调试面板与所有动效初始态，固定视口 1440×900，供 DPR2 截图`

- **截图的构图（Screenshot Composition）** · L1 · 进阶 · 成本:低
  - 视觉收益：截图是**独立的一张平面设计作品**，不是"随手截的页面状态"
  - 参考做法：选动效的**峰值帧**而不是静止态｜主体不居中（见 `R1·G2`）｜首图必须能通过缩略图测试（见 `R2·K5`，200px 下仍可识别）
  - 提示词片段：`截图选动效峰值帧；构图按三分法不居中；截完缩到 200px 检查是否仍可识别（缩略图测试）`

- **录屏规格（Screen Recording）** · L5 · 基础 · 成本:低
  - 参考值：
    - 分辨率 **1920×1080**（横版）/ **1080×1920**（竖版，投 X/小红书）
    - **60fps**（视觉作品必须 60，30fps 会毁掉动效评价）
    - 码率 8–12 Mbps，H.264（兼容性）
    - 时长 **30–45s**（超过没人看完）
  - 关键：录之前关掉所有通知；鼠标轨迹要慢且有目的；不要来回乱晃
  - 提示词片段：`录屏 1920×1080@60fps，H.264 8-12Mbps，时长 30-45s；鼠标移动慢而有目的；录制前关闭所有通知与开发者工具`

- **录屏的叙事结构（Recording Narrative）** · L0 · 进阶 · 成本:低
  - 视觉收益：录屏也要有情绪弧线（见 `R1·G8`），不是功能演示
  - 参考结构：**前 3 秒必须给最强画面**（决定看不看完）→ 一次交互展示"活的" → 一次滚动/转场展示编排 → 结尾停在最好看的一帧
  - 提示词片段：`录屏结构：前 3s 给最强画面 → 一次交互 → 一次转场 → 停在最好看的一帧；不做功能逐项演示`

- **动图（GIF/WebP）的取舍** · L5 · 基础 · 成本:低
  - 参考值：README 内动图 ≤ 5MB，宽度 ≤ 900px，帧率 15–20fps，时长 ≤ 8s，只展示一个动作
  - 参考做法：优先 MP4（GitHub README 支持视频上传）；必须用动图时优先 **动态 WebP**（体积约 GIF 的 1/3）
  - 提示词片段：`README 优先上传 MP4；必须用动图时用动态 WebP，≤5MB、≤900px 宽、15-20fps、≤8s、只展示一个动作`

---

## R18·R7 分享卡与 README 呈现（Social & README）

**这是"第一眼"真正发生的地方。** 别人点开链接之前，先看到的是分享卡；进 GitHub 之后，先看到的是 README 首图。作品集与个人展示类页面尤其吃这一层。

- **OG 图规格（Open Graph Image）** · L5 · 基础 · 成本:低
  - 参考值：**1200×630**（比例 1.91:1）｜安全区留边 ≥ 60px｜文字不小于 32px（缩略图下要可读）｜≤ 300KB
  - 参考做法：不要直接用页面截图（信息太密）；单独设计一张：**大标题 + 一个视觉锚点 + 域名**
  - 同族拓展：见 `R17·Q6` 社交分享卡、`twitter:card = summary_large_image`、`og:image:alt`、动态生成（Satori / `@vercel/og`）
  - 提示词片段：`OG 图 1200×630 单独设计（不用页面截图）：大标题 32px+ / 一个视觉锚点 / 域名；留边 60px；≤300KB；同时写 twitter:card=summary_large_image 与 og:image:alt`

- **分享卡的自检（Card Preview）** · L5 · 基础 · 成本:低
  - 视觉收益：OG 图写错是最常见的低级错误，且缓存后很难改
  - 参考做法：`og:image` 必须是**绝对 URL**｜发布前用各平台调试器验证（不要靠"应该没问题"）｜改图要换文件名破缓存
  - 提示词片段：`og:image 使用绝对 URL；发布前逐个平台验证预览；更新图片时更换文件名以破缓存`

- **README 首图（Hero Shot）** · L5 · 基础 · 成本:低
  - 视觉收益：README 的第一屏决定对方是否往下读
  - 参考结构：**一张宽幅首图（1440px 宽，深色底）→ 一行项目定位 → 在线预览按钮 → 一段 30s 动图/视频 → 技术要点 → 本地运行**
  - ❌ 开头就是一大段文字介绍｜❌ 开头就是安装步骤
  - 同族拓展：徽章不要超过 3 个（多了很业余）、`<div align="center">` 首屏居中、深色底图在 GitHub 两套主题下都要能看
  - 提示词片段：`README 结构：宽幅首图（1440px、深色底）→ 一行定位 → 在线预览链接 → 30s 动图 → 技术要点 → 本地运行；徽章 ≤3 个；首图在 GitHub 亮/暗两套主题下都要成立`

- **图片在 README 的显示控制** · L5 · 进阶 · 成本:低
  - 同族拓展：`<img width="800">` 控制宽度、`<picture>` + `prefers-color-scheme` 给亮暗两套图、并排图用表格、相对路径而非外链（外链会挂）
  - 提示词片段：`README 图片用 <img width> 控宽；亮暗主题用 <picture> media=prefers-color-scheme 提供两套；图片放仓库内相对路径，不用外链图床`

- **favicon 与浏览器身份（Favicon Set）** · L5 · 基础 · 成本:低
  - 视觉收益：标签页那 16px 是作品被记住的一部分（见 `R17·Q6`）
  - 参考做法：SVG favicon（自动适配亮暗）+ 180×180 apple-touch-icon + `theme-color`｜16px 下必须是**一个形状**，不能是缩小的 logo 全文
  - 提示词片段：`favicon 用 SVG（含 prefers-color-scheme 双色）+ 180×180 apple-touch-icon + theme-color；图形在 16px 下必须仍是可辨识的单一形状`

---

## R18·R8 素材一致性与母带（Material Consistency）

**这一节是收口。** 所有素材处理完，回来过这一遍。

- **母带滤镜（Master Filter）** · L4 · 进阶 · 成本:低
  - 视觉收益：外来素材色调各异，最省事的统一手段是在**最外层加一道极轻的全局滤镜**，图片自动继承
  - 参考值：`html { filter: contrast(1.02) saturate(.98) }`（幅度必须极小）｜深色主题可加 `brightness(.98)`
  - 关键：**禁止逐图调色**——一张一张调必然调不一致
  - 同族拓展：见 `R5·C7` 调色、`R6` 组后期、CSS `filter` 的层叠代价（会创建新层）
  - 提示词片段：`在 html 层加母带滤镜 contrast(1.02) saturate(0.98) 统一所有素材色调；禁止对单张图片单独调色`

- **素材的统一处理链（Uniform Pipeline）** · L3 · 进阶 · 成本:低
  - 视觉收益：所有图走同一条链，才会像"同一个作品里的图"
  - 参考链条：统一裁切比例 → 统一色温/饱和（降 5–10% 更耐看）→ 统一颗粒（见 `R7·E5`，极轻）→ 统一边界处理（`R18·R1`）→ 统一占位色
  - 提示词片段：`所有图片走同一处理链：统一比例 → 统一降饱和 8% → 统一叠加极轻颗粒（opacity 0.03）→ 统一 1px 内描边；参数写成一组变量复用`

- **单一色调锚（Tonal Anchor）** · L4 · 进阶 · 成本:低
  - 视觉收益：给所有素材注入一点主色，画面立刻整体
  - 参考做法：在图片上叠一层主色（`mix-blend-mode: soft-light`，`opacity .06–.12`）｜或用 duotone 处理次要图片
  - 同族拓展：见 `R9·H2` 混合模式、`R7·E2` 印刷媒介的双色调、`R5·C7` 色彩心理
  - 提示词片段：`所有次要图片叠一层主色 soft-light（opacity 0.08）注入色调锚；关键主视觉不叠，保留其原始冲击力`

- **禁止事项（Material Red Flags）** · L5 · 基础 · 成本:低
  - ❌ 混用不同风格的图库图（一张摄影 + 一张 3D 渲染 + 一张插画 = 立刻散）
  - ❌ 一眼能认出的免费图库热门图（比 AI 味更致命）
  - ❌ 带水印/带其他品牌元素的素材
  - ❌ 分辨率不足被拉伸的图（宁可裁小）
  - ❌ 素材比例与容器不匹配导致的白边
  - 说明：素材来源与商用许可、可否二次处理，先过 `F6`（素材版权与商用许可速查）
  - 提示词片段：`素材风格必须单一（全摄影或全 3D 或全插画，不混）；不使用图库热门图；无水印；分辨率不足宁可裁小不拉伸`

- **交付前素材清单（Asset Checklist）** · L5 · 基础 · 成本:低
  - [ ] 所有图有 `aspect-ratio` 与 `object-position`，无加载后跳动
  - [ ] 所有图有 AVIF/WebP 与 1x/2x
  - [ ] 首屏 LCP 图**没有** `lazy`，且有 `preload`
  - [ ] 图上所有文字有保障层且 APCA 达标
  - [ ] 视频有 `poster`、`muted playsinline loop`，reduced-motion 下不加载
  - [ ] 无 GIF
  - [ ] 3D 资产为 glb + meshopt/draco + KTX2，draw call < 150
  - [ ] OG 图 1200×630 已验证预览
  - [ ] README 首图与 30s 动图就位
  - [ ] favicon 在 16px 下可辨识
  - [ ] 全站素材通过"混搭测试"：随机挑 3 张放一起，看得出是同一个作品

---

## R 组 · 组合速查

| 想要的效果 | 组合 |
|---|---|
| 有照片的页面不掉档 | `R18·R1` 比例体系 + `R18·R8` 母带滤镜 + `R18·R8` 统一处理链 + `R18·R8` 色调锚 |
| 图上放字不出事 | `R18·R2` 渐变蒙版 + `R18·R2` 对比度实测 + `R18·R2` 柔和双层阴影（或干脆 `R18·R2` 分区） |
| 首屏图片又快又好 | `R18·R4` AVIF+1x/2x + `R18·R4` fetchpriority=high + `R18·R1` LQIP 占位 + `R18·R4` 体积预算 |
| 有生命力的背景 | `R18·R3` 背景视频三件套 + `R18·R3` 无缝循环 + `R18·R3` 视频作纹理（进阶） |
| 3D 场景能在 3 秒内成立 | `R18·R5` glb+meshopt + `R18·R5` KTX2 + `R18·R5` 加载编排两级 + `R13·M8` LOD |
| 低成本高级 3D 质感 | `R18·R5` Matcap + `R18·R5` 烘焙 AO + `R7·E1` 描边（跳过 PBR 全套） |
| PBR 场景观感翻倍 | `R18·R5` HDRI 选择 + `R18·R5` envMapRotation 手调高光 + `R5·C8` 曝光 |
| 交付用的一套物料 | `R18·R6` 截图 1440×900@2x + `R18·R6` 录屏 1080p60 30-45s + `R18·R7` OG 图 + `R18·R7` README 首图 |

**常见坑**：
- 图片加载后页面跳动 → `R18·R1` 缺 `aspect-ratio`
- 主体被裁掉 → `R18·R1` 用了默认 `object-position: center`
- 图上文字看不清 → `R18·R2` 靠侥幸没加保障层
- 首屏图很慢 → `R18·R4` 给 LCP 图加了 `lazy`（自伤）
- 首屏图很糊 → `R18·R4` 缺 2x
- 页面很卡且流量很大 → `R18·R3` 用了 GIF 或视频没压
- 3D 加载 30 秒 → `R18·R5` 贴图还是 PNG（占 80% 体积）
- 3D 材质怎么调都不对 → `R18·R5` HDRI 选错（不是材质参数问题）
- 3D 帧率低 → `R18·R5` 缺实例化，draw call 过高
- 分享出去没有图 → `R18·R7` `og:image` 用了相对路径
- 分享图上的字看不见 → `R18·R7` 文字小于 32px
- README 没人往下看 → `R18·R7` 开头是文字或安装步骤，不是首图
- 一堆图放在一起很散 → `R18·R8` 混用了不同风格素材（最难救，只能换素材）

> **R 组心法**：**素材是"收编"进来的，不是"放"进来的。** 任何一张外来图片进入页面前，都要过 `R18·R8` 的统一处理链——这一步的收益远高于给它单独调色。至于交付物（`R18·R6`/`R18·R7`），它决定了你前面所有努力**有没有机会被看到**，性价比与 `R17` 组同级。

---

## 与库内衔接

| 本文小节 | 目标库相关文档 | 关系：地基 · 扩展 · 验证 |
|---|---|---|
| `R18·R1` 图片作为素材、`R18·R8` 母带与一致性 | `B32` 图片与媒体的视觉处理 | 地基 — B32 讲图片在版面中的视觉处理原则，本文把它落到裁切/占位/格式与统一处理链的具体参数 |
| `R18·R8` 禁止事项、素材来源 | `F6` 素材版权与商用许可速查 | 地基 — 素材能否用、能否二次处理，先过 F6 的许可判断，再进本文的处理链 |
| `R18·R4` 格式与加载、`R18·R5` 3D 流水线 | `F5` 前端设计资源与工具速查 | 扩展 — F5 提供 AVIF/SVGO/gltf-transform/toktx 等具体工具入口，本文给使用规格 |
| `R18·R4` 加载优先级与体积预算、`R18·R3` 背景视频 | `F3` 性能与 Core Web Vitals | 验证 — 首屏 LCP、体积预算、视频加载策略最终以 F3 的 Core Web Vitals 指标验收 |
| `R18·R1` 裁切构图、`R18·R2` 图上放字、`R18·R8` 单一色调锚 | `B78` 摄影与影像艺术指导 | 扩展 — B78 给影像的艺术指导方向，本文负责把它工程化为可交付的素材规格 |
| `R18·R8` 素材来源、缺图占位 | `F8` AI 图片生成工作流速查 | 扩展 — 需要自制素材时走 F8 的 AI 生成工作流，产物再回到本文的统一处理链收编 |

---

## 参考资料

- 本文融合自外部「视觉提示词库 R 组：素材与交付物」（v1.x，2026-08-16），原库入口为其「总索引与用法」（对应本库 `R0`）。
- 迁移时保留全部小节、L 层级、档位、成本、强度值域、同族拓展、提示词片段，以及全部技术术语与参数值（AVIF/WebP、glTF/GLB、KTX2/Basis、Draco/Meshopt、matcap、HDRI、`fetchpriority`、OG image 等）。
- 组内交叉引用已按目标库板块编号改写（A→R3、B→R4、C→R5、D→R6、E→R7、F→R11、G→R1、H→R9、I→R10、J→R8、K→R2、L→R14、M→R13、N→R12、O→R15、P→R16、Q→R17、R→R18、总索引→R0），本组自身小节 `R1`–`R8` 统一写作 `R18·R1`–`R18·R8`，以避免与本库 `R1` 等文件冲突。
- **技术术语与参数值来自该库原始调研，未新增未经验证的数据；未编造论文或网址来源。**

_版本：v1.0（R 组 · 素材与交付物，迁移适配为本库 R18）_
_日期：2026-08-16（原库） / 迁移适配 2026-08-17_
_铁律：素材必须被收编 / 图上放字必须有保障层 / 交付物是作品的一部分_
