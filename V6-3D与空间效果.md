# V6 · 3D 与空间效果

> 从"扁平屏幕"到"有深度的空间"。2025-2026 空间 UI 趋势的基础。

---

## 一、CSS 3D 基础

### perspective（透视）
```css
.parent { perspective: 1000px; }  /* 观看距离,越小透视越强 */
```
- 数值小(400-600px)= 透视感强,夸张
- 数值大(1000-2000px)= 透视感弱,平缓

### transform-style
```css
.parent { transform-style: preserve-3d; }  /* 子元素保持 3D 空间 */
```

### 旋转
```css
transform: rotateX(10deg);   /* 沿 X 轴(前后倾斜) */
transform: rotateY(10deg);   /* 沿 Y 轴(左右旋转) */
transform: rotateZ(10deg);   /* 沿 Z 轴(平面旋转) */
transform: rotate3d(1, 1, 0, 15deg); /* 自定义轴 */
```

---

## 二、常见 3D 效果

### 倾斜卡片（Tilt Card）★最常用
- **感觉**:互动、立体、高端
- **场景**:产品卡片、作品集、定价卡
- **prompt 措辞**:"卡片跟随鼠标 3D 倾斜,最大 8 度,perspective 1000px"
- **实现**:鼠标位置 → CSS 变量 → rotateX/rotateY
- **进阶**:加高光跟随(内部一层 radial-gradient 跟着鼠标)
- **库**:vanilla-tilt.js

### 翻转卡片（Flip Card）
- **感觉**:交互惊喜
- **场景**:hover 翻转显示背面信息
- **实现**:
```css
.card { transform-style: preserve-3d; transition: transform 600ms; }
.card:hover { transform: rotateY(180deg); }
.back { transform: rotateY(180deg); backface-visibility: hidden; }
```

### 视差层（Parallax Layers）
- **感觉**:有深度、电影感
- **场景**:hero 背景多层
- **实现**:不同层 translateZ 不同值,配合 perspective
```css
.bg   { transform: translateZ(-200px) scale(2); }
.mid  { transform: translateZ(-100px) scale(1.5); }
.front { transform: translateZ(0); }
```

### 浮空元素（Floating Elements）
- **感觉**:失重、梦幻、科技
- **场景**:hero 装饰、产品展示
- **实现**:`translateZ` + 缓慢上下浮动动画
```css
@keyframes float {
  0%, 100% { transform: translateY(0) translateZ(20px); }
  50%      { transform: translateY(-10px) translateZ(20px); }
}
```

### 等距视图（Isometric）
- **感觉**:技术图表、游戏、2.5D
- **实现**:`rotateX(60deg) rotateZ(-45deg)`(经典等距角度)
- **场景**:产品功能图解、架构图

### 3D 文字（3D Extruded Text）
- 见 V7 3D 挤出文字

---

## 三、WebGL / Three.js 效果

### 常见 WebGL 场景
- **3D 产品旋转** — 用户可 360° 看产品
- **粒子系统** — 星空、烟雾、光点
- **液态/流体** — 有机形变
- **地球/球体** — 全球化展示
- **噪声地形** — 抽象背景
- **shader 渐变** — 比 CSS 更复杂的动态渐变

### React Three Fiber（React 生态首选）
```jsx
<Canvas>
  <mesh rotation={[0, 0.5, 0]}>
    <boxGeometry />
    <meshStandardMaterial color="blue" />
  </mesh>
</Canvas>
```

### Spline（无代码 3D）
导出到网页的 3D 场景,可交互。
- **场景**:落地页 hero 3D 视觉、产品展示
- **prompt 措辞**:"hero 区用 Spline 3D 场景,可鼠标拖拽旋转"

---

## 四、空间 UI（Spatial UI / visionOS 风格）

### 特征
- 元素有真实 Z 轴(不是装饰阴影,是 translateZ)
- 多层玻璃面板浮在空间
- 视差随鼠标/头部移动
- 无硬边框,浮在空中
- 背景模糊表达层级

### Web 模拟
```css
.spatial-card {
  transform: translateZ(40px);
  backdrop-filter: blur(20px);
  background: rgba(255,255,255,0.1);
  border: 1px solid rgba(255,255,255,0.15);
  border-radius: 24px;
  box-shadow: 0 20px 60px rgba(0,0,0,0.3);
}
```

- **prompt 措辞**:"visionOS 空间 UI 风格:多层玻璃面板浮在空间,有 Z 轴深度,鼠标移动时层间有微妙视差"

---

## 五、性能注意

- `transform-style: preserve-3d` 创建新层叠上下文
- 3D 变换触发 GPU 合成(好)但内存占用大
- WebGL 场景需要考虑 60fps
- 移动端 GPU 弱,3D 效果减少或简化
- `will-change: transform` 提前触发 GPU 层

---

## 六、Prompt 模板

### 空间感 hero
```
【3D 与空间】
- Hero 区域:三层视差(背景/中景/前景)
  - 背景:极光渐变,微慢移动
  - 中景:玻璃卡片,translateZ(40px)
  - 前景:文字+CTA,translateZ(80px)
- 鼠标移动时三层有微妙视差偏移(最大 10px)
- perspective: 1200px(柔和)
- 卡片:玻璃拟态 + border white/10 + blur-16
```

### 产品展示
```
【3D 效果】
- 产品卡片:hover 时 3D tilt 跟随鼠标,最大 6 度
- perspective: 1000px
- 内部有一层 specular 高光跟随
- transition: transform 200ms
- 移动端:禁用 tilt,改为 hover lift(translateY -4px)
```

---

## 七、检验清单

- [ ] perspective 值合理(800-1500px)
- [ ] 3D tilt 角度克制(≤10 度)
- [ ] 移动端有降级方案
- [ ] WebGL 场景有 fallback(不支持时显示静态图)
- [ ] 60fps 无掉帧
- [ ] preserve-3d 只在需要时加
- [ ] 空间层级清晰(Z 轴有规律)
