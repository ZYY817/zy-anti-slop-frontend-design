# R12 · 物理与拟真交互（提示词库 N 组）

> **定位**：让界面有重量。物理不是为了模拟真实，而是为了让操作符合身体直觉——手感高级的底层原因几乎都是物理。
> **覆盖层**：L2 气 + L5 髓（L 层级沿用提示词库的分层：L1 形 / L2 气 / L3 骨 / L5 髓）。
> **条目卡格式**：每条为 `**中文名（English Term）** · L层级 · 档位（基础/进阶/电影级） · 成本:低/中/高`，下挂「视觉收益 / 参数含义 / 参考档位 / 同族拓展 / 提示词片段」。提示词片段可直接粘进生成 prompt。
> **关键判断**：**80% 的"手感高级"只需要 `R12·N1` 弹簧与阻尼这一节**——物理引擎是重武器，只在元素需要真正互相碰撞时才上。
> **来源**：融合自外部「视觉提示词库 N 组：物理与拟真交互」（v1.0，2026-08-16）。原库的组内交叉引用（A/B/C…R 组）已改写为本库 R 编号，避免与本库 A/B/C/D 板块含义冲突。
> **与反 AI 味清单的关系**：本文参数与 `B82`（反 AI 味）、`B44`（视觉禁忌）并不冲突；那两份是**参考清单，不是硬性铁律，按项目自行取舍**。风格方向定位见 `B16`、`E1`，全维度清单见 `B15`。

---

## 使用说明

`R11·F1`/`R11·F3`/`R11·F8` 讲的是"用 spring 而不是 tween"，本文（N 组）讲的是**真正的物理系统**——碰撞、重力、软体、绳索、流体交互。

一个判断：

- 只是想让动画有弹性 → 用 `R11·F3` 的 spring 就够，**不要引入物理引擎**
- 需要元素互相碰撞、堆叠、被抛掷 → 需要本文
- 需要布料、绳索、软体的形变 → 需要本文

铁律：**物理必须可预测**。真实物理里的随机性在 UI 里是 bug——用户点两次应该得到相似结果。为可控性牺牲真实性是对的。

```
N1 弹簧与阻尼（最常用）   N4 流体与颗粒交互
N2 刚体与碰撞            N5 UI 中的物理隐喻
N3 软体·布料·绳索        N6 手势物理与惯性
```

---

## N1 弹簧与阻尼（Springs & Damping）

**本文里唯一"所有项目都该用"的一节。** 不需要引擎，几行代码。

- **临界阻尼弹簧（Critically Damped Spring）** · L5 · 基础 · 成本:低
  - 视觉收益：最快到达目标且不过冲，是"顺滑但不软"的标准解
  - 参数含义：`stiffness`（刚度，越大越快）、`damping`（阻尼，越大越不弹）、`mass`（质量，越大越沉）
  - 参考档位：
    - 干脆响应（按钮/开关）：stiffness 400, damping 30
    - 标准过渡（卡片/面板）：stiffness 200, damping 24
    - 柔软从容（大面板/页面）：stiffness 120, damping 18
    - 明显弹跳（俏皮反馈）：stiffness 300, damping 12
  - 同族拓展：critically damped、underdamped（过冲）、overdamped（迟滞）、`spring()` CSS 提案、Framer Motion / react-spring
  - 提示词片段：`所有位移过渡用 spring；按钮 (400,30)、卡片 (200,24)、大面板 (120,18)；禁止固定时长 tween`

- **速度接管（Velocity Handoff）** · L5 · 进阶 · 成本:低
  - 视觉收益：动画被打断时保留当前速度继续，而不是重新从 0 加速——**"手感连贯"的核心**
  - 同族拓展：见 `R11·F3` 可中断动画、当前速度作为新动画初速
  - 提示词片段：`动画被新输入打断时，把当前速度作为新 spring 的初速度传入，保证连贯`

- **帧率无关积分（Frame-Rate Independent）** · L5 · 基础 · 成本:低
  - 视觉收益：60Hz 与 144Hz 屏上手感一致
  - 关键做法：固定时间步进（accumulator）或半隐式欧拉带 dt 修正；**不要直接 `p += (t-p)*0.1`**
  - 同族拓展：fixed timestep + accumulator、`1-pow(1-k, dt*60)`、Verlet 积分
  - 提示词片段：`物理用固定时间步（1/120s）累加器推进，渲染插值；或用 1-pow(1-k, dt*60) 的帧率无关 lerp`

- **多弹簧链（Spring Chains）** · L5 · 电影级 · 成本:低
  - 视觉收益：一串元素依次跟随（尾巴、拖尾、菜单展开），自然的 follow-through
  - 同族拓展：见 `R11·F8` 主次运动、chain of springs、递减刚度
  - 提示词片段：`菜单项用弹簧链依次跟随（每项刚度递减 10%），产生自然的甩尾感`

- **软约束（Soft Constraints）** · L5 · 进阶 · 成本:低
  - 视觉收益：橡皮筋边界——可以越界但有阻力并回弹（iOS 滚动手感）
  - 同族拓展：rubber-band（越界位移取 log 或除以系数）、soft clamp、见 `R11·F1` 惯性拖拽
  - 提示词片段：`越界时位移按 log 衰减（rubber-band），松手 spring 回弹；不要硬性 clamp`

---

## N2 刚体与碰撞（Rigid Bodies & Collision）

- **引擎选型（Engine Choice）** · L2 · 进阶 · 成本:中
  - 选型对比：
    - `matter.js` — 2D，轻量，API 友好，适合 UI 里的物理玩具
    - `planck.js / box2d-wasm` — 2D，更准更快
    - `rapier` (Rust/WASM) — 2D+3D，现代、快、确定性好，**首选**
    - `cannon-es` — 3D，纯 JS，简单但性能有限
    - `physx-js / jolt` — 重型 3D，一般 web 用不到
  - 提示词片段：`2D UI 物理用 matter.js（轻量）；需要 3D 或确定性用 rapier（WASM）；避免 cannon-es 做大规模场景`

- **碰撞形状（Collision Shapes）** · L2 · 基础 · 成本:低
  - 视觉收益：用简单形状近似复杂外形，性能与稳定性都好
  - 同族拓展：box/sphere/capsule/convex hull/compound、trimesh（慢，只用于静态）、碰撞层与掩码
  - 提示词片段：`碰撞体用简单形状近似（box/capsule）；复杂静态用 trimesh，动态物体绝不用 trimesh`

- **确定性与可重现（Determinism）** · L5 · 进阶 · 成本:低
  - 视觉收益：同样操作得到同样结果，可调试可演示
  - 同族拓展：固定时间步、固定迭代次数、避免依赖帧率、rapier 的确定性模式
  - 提示词片段：`物理用固定时间步与固定求解迭代次数，保证可重现；不依赖实际帧率`

- **休眠与优化（Sleeping & Budget）** · L5 · 基础 · 成本:低
  - 视觉收益：静止的物体休眠不再计算，大量物体也能跑
  - 同族拓展：sleep threshold、broad-phase、islands、限制活跃物体数
  - 提示词片段：`开启物体休眠；活跃刚体数上限 200（移动端 60）；超出则移除最老的`

- **抛掷与投掷手感（Throwing）** · L5 · 进阶 · 成本:低
  - 视觉收益：拖拽后甩出去，速度来自最近几帧的位移平均（不是最后一帧）
  - 同族拓展：velocity from recent samples、clamp 最大速度、见 `R12·N6`
  - 提示词片段：`投掷速度取最近 3-5 帧位移的加权平均（不是最后一帧，会有噪声）；限制最大速度`

---

## N3 软体 · 布料 · 绳索（Soft Bodies, Cloth, Ropes）

- **Verlet 质点系统（Verlet Integration）** · L2 · 进阶 · 成本:中
  - 视觉收益：**最简单又最好用的软体方案**。质点 + 距离约束，能做绳索、布料、软体、头发
  - 同族拓展：position-based dynamics (PBD)、distance constraint、迭代求解次数（越多越硬）、pin 固定点
  - 提示词片段：`绳索/布料用 Verlet 质点 + 距离约束，求解迭代 8 次；固定点用 pin；无需物理引擎`

- **绳索与连线（Ropes & Cables）** · L2 · 进阶 · 成本:低
  - 视觉收益：可拖拽的线缆、悬挂的元素、连接线，极强的"活物感"
  - 同族拓展：Verlet chain、Catmull-Rom 平滑渲染（见 `R8·J7`）、SVG path 渲染、张力可视化（拉紧变直变色）
  - 提示词片段：`导航/装饰用可拖拽绳索（Verlet 链 12 节点），SVG path 用 Catmull-Rom 平滑；拉紧时略变直`

- **布料（Cloth）** · L2 · 电影级 · 成本:高
  - 视觉收益：旗帜、幕布、衣物的飘动
  - 同族拓展：Verlet 网格 + 结构/剪切/弯曲约束、风力（见 `R13·M5`）、自碰撞（很贵，通常省略）
  - 提示词片段：`布料用 Verlet 网格（20×20）+ 结构与剪切约束；风力用噪声驱动；跳过自碰撞`

- **软体与果冻（Soft Body / Jelly）** · L3 · 电影级 · 成本:中
  - 视觉收益：物体被挤压变形又弹回，Q 弹的质感
  - 同族拓展：shape matching、pressure model（充气）、顶点弹簧、squash & stretch（见 `R11·F8`）
  - 提示词片段：`软体用 shape-matching 保持整体形状 + 局部顶点弹簧；碰撞时挤压变形 15% 后弹回`

- **头发与毛发（Hair & Fur）** · L3 · 电影级 · 成本:高
  - 视觉收益：发丝随动，极强的生命感
  - 同族拓展：Verlet 发链、hair cards、shell/fin 毛发、各向异性高光（见 `R5·C4`）
  - 提示词片段：`发丝用 Verlet 链（每根 6 节点）+ 惯性；渲染用各向异性高光（见 R5·C4）`

---

## N4 流体与颗粒交互（Fluids & Granular）

- **交互式流体（Interactive Fluid）** · L2 · 电影级 · 成本:高
  - 视觉收益：鼠标搅动烟雾/水/颜料，最直接的"我在影响它"
  - 同族拓展：见 `R4·B5` stable fluids、SPH、FLIP、GPU 求解（见 `R14·L2`）、指针速度作为力注入（见 `R11·F1`）
  - 提示词片段：`流体用 stable fluids（GPU）；指针速度注入力与颜料；力的大小由 R11·F1 的指针速度驱动`

- **颗粒堆积（Granular / Sand）** · L2 · 电影级 · 成本:高
  - 视觉收益：沙、米、球堆积与流动，满足感极强
  - 同族拓展：DEM（离散元）、position-based granular、大量小刚体（性能要靠 GPU，见 `R14·L3`）
  - 提示词片段：`颗粒用 position-based 方法（不用完整刚体）；数量 >1 万时上 GPU（见 R14·L3）`

- **表面张力与液滴（Surface Tension）** · L3 · 电影级 · 成本:中
  - 视觉收益：液滴融合分裂（metaball 效果），有机而奇妙
  - 同族拓展：见 `R3·A3` smooth union / metaball、`R9·H3` blur+contrast 的 CSS 版本、SPH 表面张力
  - 提示词片段：`液滴融合用 metaball（SDF smooth union）或 CSS blur+contrast（见 R9·H3）；接近时自然粘连`

---

## N5 UI 中的物理隐喻（Physics Metaphors in UI）

**不是真的物理引擎，而是"借用物理直觉"让界面可理解。**

- **重量与惯性（Weight & Inertia）** · L5 · 基础 · 成本:低
  - 视觉收益：大面板动得慢而沉、小元素动得快而轻——**尺寸与速度反相关**符合直觉
  - 同族拓展：mass proportional to size、见 `R12·N1` spring 参数档位
  - 提示词片段：`元素越大动画越慢（质量感）：小按钮 180ms、卡片 280ms、全屏面板 420ms`

- **磁性与吸附（Magnetism & Snapping）** · L5 · 进阶 · 成本:低
  - 视觉收益：拖拽时吸附到网格/对齐线/最近位置，操作精确又省力
  - 同族拓展：见 `R11·F1` 磁吸、snap threshold、`scroll-snap`、对齐辅助线
  - 提示词片段：`拖拽时在 12px 内吸附到对齐位置，吸附瞬间有轻微 spring；列表滚动用 scroll-snap`

- **弹性边界（Elastic Boundaries）** · L5 · 进阶 · 成本:低
  - 视觉收益：见 `R12·N1` 软约束。到边界时不是硬停，而是有阻力
  - 提示词片段：`所有边界用弹性阻力而非硬停；最大越界 15% 后 spring 回弹`

- **层级即高度（Elevation as Z）** · L1 · 基础 · 成本:低
  - 视觉收益：用阴影与模糊表达"离用户多近"，建立可理解的层级
  - 同族拓展：见 `R1·G4` 投影接地、`R9·H6` 多层阴影、Material elevation、深色模式用变亮表达抬升（见 `R1·G7`）
  - 提示词片段：`层级用 elevation 表达（阴影+轻微缩放）；建立 4 级 elevation token；深色模式用变亮表达`

- **连续性与守恒（Continuity & Conservation）** · L5 · 进阶 · 成本:低
  - 视觉收益：元素不凭空出现消失，而是从某处来、到某处去（物质守恒直觉）
  - 同族拓展：见 `R11·F3` 共享元素转场、FLIP、origin-aware 动画（从点击点展开）
  - 提示词片段：`弹层从触发元素位置展开（不是屏幕中心淡入）；关闭时收回原处；元素不凭空生灭`

- **摩擦与阻尼的语义（Friction Semantics）** · L5 · 进阶 · 成本:低
  - 视觉收益：不同阻尼传达不同"材质"——低阻尼像冰、高阻尼像泥
  - 提示词片段：`滚动阻尼 0.95（顺滑）；拖拽阻尼 0.88（有控制感）；不同区域可用不同阻尼暗示不同性质`

---

## N6 手势物理与惯性（Gesture Physics）

- **惯性滚动（Momentum Scrolling）** · L5 · 进阶 · 成本:低
  - 视觉收益：见 `R11·F1` 惯性拖拽。松手后按摩擦减速
  - 参考值：摩擦系数 0.92（快停）/ 0.95（标准）/ 0.98（长滑）
  - 提示词片段：`惯性滚动摩擦 0.95，速度 <0.1px/frame 时停止；越界用 rubber-band`

- **速度采样（Velocity Sampling）** · L5 · 进阶 · 成本:低
  - 视觉收益：见 `R12·N2` 抛掷手感。用多帧平均而非最后一帧
  - 提示词片段：`手势速度取最近 100ms 内位移平均；忽略静止后的最后几帧`

- **意图预测（Intent Prediction）** · L5 · 电影级 · 成本:低
  - 视觉收益：根据当前速度预测终点，提前吸附到最近的合理位置（轮播、抽屉的"翻页"判定）
  - 同族拓展：projected end position、velocity threshold 决定翻页而非距离阈值
  - 提示词片段：`轮播翻页用速度预测终点（不是纯位移阈值）：快速轻扫也能翻页`

- **多指与缩放（Pinch & Multi-Touch）** · L5 · 进阶 · 成本:低
  - 视觉收益：双指缩放旋转，围绕手指中点而非元素中心
  - 同族拓展：pinch-zoom、transform origin at gesture center、`touch-action`
  - 提示词片段：`双指缩放围绕手势中点变换（不是元素中心）；设 touch-action 避免浏览器抢手势`

- **手势冲突与阈值（Gesture Disambiguation）** · L5 · 基础 · 成本:低
  - 视觉收益：区分点击 vs 拖拽 vs 滚动，不误触发
  - 同族拓展：movement threshold（>8px 才算拖拽）、方向锁定（水平滑动锁定不触发垂直滚动）、`touch-action: pan-y`
  - 提示词片段：`位移 >8px 才判为拖拽；主方向确定后锁定该轴，避免与页面滚动冲突`

---

## R12 · 组合速查

| 想要的效果 | 组合 |
|---|---|
| 高级手感（零成本） | N1 spring 档位 + N1 速度接管 + N1 帧率无关 + N5 重量感 |
| 可玩的物理玩具 | N2 matter.js/rapier + N2 抛掷手感 + N2 休眠优化 |
| 可拖拽的连线导航 | N3 Verlet 绳索 + R8·J7 Catmull-Rom 渲染 |
| 飘动的旗帜/幕布 | N3 布料 + R13·M5 风力 |
| Q 弹的按钮/卡片 | N3 软体挤压 + N1 spring(300,12) 弹跳 |
| 搅动的流体背景 | N4 交互式流体 + R11·F1 指针速度 + R14·L2 GPU 求解 |
| 移动端原生手感 | N6 全套（惯性/速度采样/意图预测/手势冲突） |
| 融合的液态元素 | N4 表面张力 + R9·H3 blur+contrast 或 R3·A3 metaball |

**常见坑**：

- 高刷屏上手感全变 → `R12·N1` 缺帧率无关积分
- 动画打断后跳变 → `R12·N1` 缺速度接管
- 抛掷方向不对/抖 → `R12·N2`/`R12·N6` 用了最后一帧速度，应多帧平均
- 物理表现每次不同 → `R12·N2` 缺固定时间步与固定迭代
- 大量物体卡死 → `R12·N2` 缺休眠与数量上限
- 布料像塑料板 → `R12·N3` 约束迭代太多（太硬），降到 6-8 次
- 绳索有折点 → `R12·N3` 渲染缺平滑（用 Catmull-Rom）
- 手势与页面滚动打架 → `R12·N6` 缺方向锁定与 touch-action
- 引入了物理引擎但只是想要弹性 → 应该只用 `R12·N1`，删掉引擎

> **本组心法**：**80% 的"手感高级"只需要 `R12·N1` 一节**（spring 参数 + 速度接管 + 帧率无关）。物理引擎是重武器，只在元素需要真正互相碰撞时才上。为了炫技引入引擎会拖慢加载并增加 bug 面。

---

## 与库内衔接

| 本文小节 | 目标库相关文档 | 关系 |
|---|---|---|
| `R12·N1` 弹簧与阻尼（stiffness/damping/mass 档位） | `B71` 动效编排进阶（Spring 参数） | 地基——B71 给出 spring 参数体系，本节补齐临界阻尼/速度接管/帧率无关积分的实现细节 |
| `R12·N1` 多弹簧链、`R12·N3` 软体挤压（squash & stretch） | `B27` 动效物理学与迪士尼 12 原则 | 地基——follow-through、主次运动、挤压拉伸的原理出处；本文提供可落地参数 |
| `R12·N5` UI 中的物理隐喻（重量/磁吸/弹性边界/连续性） | `B45` 手感与操作反馈 | 扩展——把"手感"拆成可实现的物理隐喻与阈值（12px 吸附、15% 越界、阻尼 0.88/0.95） |
| `R12·N6` 手势物理与惯性（速度采样/意图预测/方向锁定） | `B45` 手感与操作反馈、`B5` 动效系统 | 扩展——补充触摸端手势层的物理规则与 touch-action 治理 |
| 全文档位/成本标注与 spring token 化 | `B5` 动效系统 | 地基——动效 token、时长与曲线体系；本文的档位应落进 B5 的 token 表而非散落各处 |
| `R12·N4` 流体与颗粒交互、`R12·N3` 布料/绳索 | `V12` 粒子与动态 | 扩展——粒子与流体的视觉表现层；本文补物理求解与性能预算（休眠、数量上限、GPU 阈值） |
| `R12·N2` 休眠与优化、确定性 | `T` 板块度量与验收 | 验证——活跃刚体上限、固定时间步可作为性能与可重现性的验收项 |
| 全文动效与物理 | `C4` 无障碍（reduced-motion 降级） | 验证——所有弹簧、惯性、布料、流体在 `prefers-reduced-motion` 下必须降级为瞬时或极短过渡；这是发布前的硬性检查 |
| 风格取舍与禁忌核对 | `B82` 反 AI 味、`B44` 视觉禁忌、`B16`/`E1` 风格方向、`B15` 全维度清单 | 验证——参考清单，不是硬性铁律，按项目自行取舍 |

---

## 参考资料

本文融合自外部「视觉提示词库 N 组：物理与拟真交互」（v1.x，2026-08-16），按本库命名与引用规范做了适配：条目结构、L 层级、档位、成本、参数值域、同族拓展与提示词片段均为忠实迁移；原库的组内交叉引用（A–R 组）已统一改写为本库 `R{n}·{原引用}` 形式，本组自引用写作 `R12·N{n}`。

技术术语与参数值（spring stiffness/damping/mass 档位、critically damped、velocity handoff、fixed timestep、Verlet / position-based dynamics、rigid body、cloth、SPH/FLIP/DEM、matter.js / planck.js / box2d-wasm / rapier / cannon-es / physx-js / jolt、摩擦系数 0.92–0.98 等）来自该库原始调研，**未新增未经验证的数据**。

_铁律：物理必须可预测——为可控性牺牲真实性是对的_
