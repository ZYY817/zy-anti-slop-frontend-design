# V10 · 滚动视觉效果

> 滚动是用户与长页面交互的唯一方式。把滚动变成"体验"而非"翻页"。

---

## 一、滚动揭示（Scroll Reveal）★最基本

### 基础入场
- **效果**:元素滚入视口时淡入浮起
- **prompt 措辞**:"section 内容滚动入场:fade + 从下浮起 16px,视口 40% 触发"
- **实现(2026 原生 CSS)**:
```css
.item {
  animation: reveal both;
  animation-timeline: view();
  animation-range: entry 20% cover 40%;
}
@keyframes reveal {
  from { opacity: 0; transform: translateY(16px); }
  to   { opacity: 1; transform: translateY(0); }
}
```
- **JS 备选**:IntersectionObserver(更广兼容)/ GSAP ScrollTrigger(最强大)

### Stagger 入场
卡片组依次出现:
```css
.item:nth-child(1) { animation-range: entry 20% cover 35%; }
.item:nth-child(2) { animation-range: entry 25% cover 40%; }
.item:nth-child(3) { animation-range: entry 30% cover 45%; }
```

### 只触发一次
- **推荐**:滚回去不重播,不烦人
- **实现**:`animation-fill-mode: both` + JS 标记

---

## 二、视差（Parallax）

### 简单视差
不同层滚动速度不同,制造深度感。
- **近层**:正常速度
- **远层**:慢速(0.3-0.7x)

### CSS 原生视差(性能最好)
```css
.parallax-container {
  perspective: 1000px;
  overflow-y: auto;
  height: 100vh;
}
.bg-layer {
  transform: translateZ(-200px) scale(2);  /* 远=慢=缩放补偿 */
}
```

### scroll-driven 视差(2026)
```css
.bg {
  animation: parallax-shift linear;
  animation-timeline: scroll();
}
@keyframes parallax-shift {
  from { transform: translateY(0); }
  to   { transform: translateY(-100px); }
}
```

- **坑**:
  - 大幅视差引发前庭障碍(眩晕)→ 尊重 `prefers-reduced-motion`
  - 移动端性能差 → 移动端关闭或减弱

---

## 三、固定/钉住（Pin / Sticky Scroll）

元素固定在视口内,内容从旁边滚过。

- **感觉**:电影感、叙事、聚焦
- **场景**:产品 feature 展示、手机 mockup 固定 + 内容切换
- **prompt 措辞**:"左侧手机 mockup 固定,右侧功能描述依次滚入,当前功能高亮"
- **实现**:
  - CSS `position: sticky`
  - GSAP ScrollTrigger `pin: true`
```css
.phone-mockup {
  position: sticky;
  top: 50%;
  transform: translateY(-50%);
}
```

---

## 四、横向滚动（Horizontal Scroll）

纵向滚动转换为横向内容滚动。

- **感觉**:时间线、电影胶卷、作品集
- **场景**:Portfolio 画廊、时间线、全屏横向叙事
- **prompt 措辞**:"纵向滚动驱动横向内容移动,画廊展示"
- **实现**:GSAP `x: () => -(scrollWidth - window.innerWidth)` + pin
- **坑**:手机上横向滚动体验差,需要做适配

---

## 五、滚动进度（Scroll Progress）

### 阅读进度条
页面顶部一条细线,宽度跟随滚动 0→100%。
```css
.progress {
  position: fixed; top: 0; left: 0; height: 3px;
  background: var(--primary);
  animation: grow-width linear;
  animation-timeline: scroll();
}
@keyframes grow-width { from { width: 0; } to { width: 100%; } }
```

### 圆形进度
章节进度、目录当前位置指示。

---

## 六、滚动变色（Scroll Color Change）

背景颜色随滚动渐变。

- **场景**:品牌页从一个色调过渡到另一个
- **实现**:
```css
body {
  animation: color-shift linear;
  animation-timeline: scroll();
}
@keyframes color-shift {
  0%   { background: #0A0A0A; }
  50%  { background: #1E293B; }
  100% { background: #0F172A; }
}
```

---

## 七、图像序列（Image Sequence / Frame Animation）

滚动驱动一组连续帧,像"翻手翻书":

- **感觉**:电影级、产品旋转、3D 展示
- **场景**:Apple 产品页(iPhone/MacBook 滚动旋转)
- **prompt 措辞**:"产品 3D 旋转,滚动驱动帧序列(60 帧)"
- **实现**:预渲染 60-120 帧图片,滚动切换当前帧
- **坑**:图片总量大(需压缩 + 预加载)

---

## 八、文字滚动效果

### 逐段高亮(Reading Highlight)
长文本滚动时,当前段落高亮(其余灰化)。
- **场景**:scrollytelling、长文阅读引导

### 文字放大/缩小
标题随滚动从小变大(或反向)。

### 文字透明度
远离中心的文字淡化,中心的最清晰。

---

## 九、平滑滚动（Smooth Scroll / Lenis）

### 原生
```css
html { scroll-behavior: smooth; }
```

### Lenis(2024-2026 主流库)
更丝滑的惯性滚动,像 native app 感觉。
- **坑**:影响浏览器原生行为(锚点跳转、搜索定位)

---

## 十、Scrollytelling（滚动叙事）★品牌页核心

完整的滚动驱动叙事:

```
用户滚动
  ↓
第一幕:Hero 文字浮现
  ↓ 继续滚
第二幕:背景变色,3D 模型旋转
  ↓ 继续滚
第三幕:数据飞入,图表绘制
  ↓ 继续滚
第四幕:CTA 出现,故事结束
```

- **技术**:GSAP ScrollTrigger(最成熟)+ Lenis(平滑)
- **坑**:
  - 页面高度会很长(用户不知还要滚多久)→ 加进度指示
  - 移动端触控体验不同 → 简化
  - 可访问性:内容不能只靠滚动触发(键盘/阅读器也要能获取)

---

## 十一、性能与无障碍

### 性能
- 优先用 CSS scroll-driven animations(原生,GPU)
- GSAP 是 JS 但优化极好
- 避免在 scroll 事件中直接改 DOM
- 大量视差层 → `will-change: transform`
- 图像序列 → 预加载关键帧

### 无障碍
```css
@media (prefers-reduced-motion: reduce) {
  * { animation: none !important; scroll-behavior: auto !important; }
  .parallax { transform: none !important; }
}
```
- 视差大幅移动 → reduced-motion 时去掉
- pin/横向滚动 → 确保键盘仍可导航
- scrollytelling → 无滚动也能获取所有内容

---

## 十二、Prompt 模板

### SaaS 落地页
```
【滚动效果】
- 入场揭示:每个 section 进入视口 40% 时 fade + translateY(16→0),400ms
- 卡片组:stagger 60ms 依次入场
- 实现:CSS scroll-driven animations(不用 JS)
- 阅读进度条:顶部 3px 主色线,跟随滚动
- reduced-motion:保留 fade,去掉 translate
```

### 品牌叙事页
```
【Scrollytelling】
- 工具:GSAP ScrollTrigger + Lenis 平滑滚动
- 第一幕:hero 文字逐词浮现,背景 parallax
- 第二幕:左侧手机 pin 固定,右侧 3 段功能依次滚入切换
- 第三幕:数字 counter tween 从 0 增长到目标值
- 第四幕:背景渐变色 shift,CTA 最终浮现
- 进度指示:右侧 dot 导航显示当前幕
- reduced-motion:去掉视差和 pin,直接显示所有内容
```

### 产品展示(Apple 风)
```
【滚动 3D】
- 产品图像序列:60 帧,滚动驱动旋转 360°
- 图片预加载,WebP 格式压缩
- 下方文字随滚动 stagger 淡入
- 背景:纯黑 → section 变化随滚动渐变
```

---

## 十三、检验清单

- [ ] 滚动入场只触发一次
- [ ] 视差幅度克制(移动端关闭)
- [ ] 尊重 prefers-reduced-motion
- [ ] 有进度指示(长 scrollytelling 时)
- [ ] pin 元素键盘仍可导航
- [ ] 横向滚动移动端有适配
- [ ] 60fps 不卡顿
- [ ] 图像序列已压缩预加载
- [ ] 内容不滚动也能访问(无障碍)
